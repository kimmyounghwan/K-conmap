# CLAUDE.md — K-건설맵 v2

이 파일은 이 저장소에서 작업하는 Claude 를 위한 안내입니다.
사용자는 본인을 초보라고 표현합니다. **설계와 의견을 먼저 이야기하고,
"코드 줘"라고 할 때만 코드를 제시하세요.** 코드를 줄 때는 부분 수정보다
전체 파일을 통째로 주는 방식을 선호합니다(파일이 1,000행을 넘으면 부분 수정 동의).

---

## 1. 이 프로젝트가 뭔가

조달청 나라장터 공공 입찰 데이터를 분석해 무료로 보여주는 사이트입니다.
기존 Streamlit 판(`나노_건설시스템/app.py`, 3,074행)을 **React + Firebase Hosting +
정적 JSON** 으로 다시 지은 것이 이 저장소입니다.

- 회원제 없음, 로그인 없음, 대문(랜딩) 없음
- 수익은 애드센스 광고 (유료 구독 아님)
- 하단 탭 5개: 1순위 / 공고 / 계산 / 분석 / 구인구직

---

## 2. 절대 지켜야 할 원칙

### 비용 (이게 이 구조의 존재 이유입니다)

1. **모든 분석 데이터는 정적 JSON.** Firebase 읽기 과금 0.
   DB 를 새로 끌어들이자는 제안은 하지 마세요.
2. **큰 파일을 브라우저로 보내지 않는다.**
   기관·업체는 이름 첫 글자로 색인(`idx`)을 나누고, 집계는 200개씩
   묶음(`dat`)으로 쪼갭니다. 사용자는 검색한 것이 든 묶음 하나만 받습니다.
3. **Realtime DB 는 `onValue` 금지.** 화면 열 때 `get()` 한 번만.
   (사라사에서 24시간 폴링이 돌아 요금이 샌 전례가 있습니다)
4. **항상 `limitToLast`.** 노드 전체를 읽는 코드는 리뷰에서 막으세요.
5. `first.json` / `live.json` 은 **300건 상한**. 첫 화면에서 받는 파일입니다.
6. `/assets/**` 1년 캐시, `index.html` 캐시 금지 — `web/firebase.json`.

### 보안

1. **`web/database.rules.json` 은 기존 Streamlit 규칙과의 병합본입니다.**
   `stats` / `sarasa` / `archive_1st` / `archive_live` / `service_1st` /
   `service_live` 를 지우면 **돌아가는 Streamlit 사이트가 깨집니다.**
   규칙은 교체가 아니라 병합. (2026-08 사라사에서 규칙 교체로 소실 사고 있었음)
2. **루트 규칙에서 익명을 반드시 제외**해야 합니다.
   원래 루트가 `".read": "auth != null"` 이라, 익명 로그인을 켜면
   **누구나 DB 전체를 읽고 쓸 수 있게 됩니다.** Firebase 설정값이 공개
   저장소에 있어 실제로 악용 가능합니다.
   현재 규칙: `auth != null && auth.token.firebase.sign_in_provider != 'anonymous'`
3. **순서: ① 규칙 배포 → ② 익명 로그인 켜기.** 절대 반대로 하지 마세요.
4. **API 키·비밀번호를 코드에 넣지 마세요.** 전부 `.env` (gitignore 됨).
5. 원래 규칙 백업: `_임시/rtdb_rules_백업_20260830.json`

---

## 3. 구조

```
[내 PC 파이썬]                                  [Firebase Hosting]
 collect.py    조달청 수집   ─┐
 inbox.py      받은 자료     ─┼→ web/public/data/*.json ─→ npm run build ─→ deploy
 build_json.py 3년치 집계    ─┤                                              │
 seed_first.py 수집 전 임시   ─┤                                              ▼
 sitemap.py    주소 목록     ─┘                          방문자는 정적 파일만 받음
```

핵심: **사용자 접속 시 계산하지 않습니다.** 미리 구운 JSON 을 그냥 내려받습니다.

---

## 4. 명령

파이썬은 **루트**에서, `npm`·`firebase` 는 **`web`** 안에서 돕니다.
`.vscode/tasks.json` 의 작업 메뉴를 쓰면 폴더를 알아서 맞춰줍니다.

```
python run_all.py              전체 (수집+집계+빌드+배포)
python run_all.py --quick      집계 생략 (평소 이것)
python run_all.py --no-deploy  배포 없이
python collect.py --days 3     수집만
python build_json.py           집계만 (3~5분)
python inbox.py                inbox 폴더 자료 반영
python seed_first.py           수집 전 1순위 화면 임시 채우기

cd web && npm run dev          개발 서버
cd web && npm run build        빌드
cd web && firebase deploy --only hosting
cd web && firebase deploy --only database   # 규칙
```

---

## 5. 데이터 구조 (JSON 스키마)

`web/public/data/` — `build_json.py` 산출물. **gitignore 됨** (71MB, 2,220 파일).

| 경로 | 내용 |
|---|---|
| `overview.json` | 전체 요약 (건수·기간·평균 투찰률) |
| `agency/idx/{첫글자코드}.json` | `{기관명: [건수, 묶음번호]}` — 검색용 |
| `agency/dat/{n}.json` | `{기관명: 집계}` — 200곳씩 |
| `agency/top.json` | 건수 상위 300곳 (추천·사이트맵용) |
| `corp/idx/*` · `corp/dat/*` | 업체도 같은 구조. 키는 정규화된 업체명 |
| `kw/{첫글자코드}.json` | `{키워드: [건수, 최빈구간, 평균, 비율]}` — 유사공고용 |
| `first.json` / `live.json` | `{built, con:[...], serv:[...]}` — 수집 결과 |

기관 집계 한 건:
```
{ n, kind, s:{avg,std,min,max,med}, h1:[[구간,건수]], h01:[...],
  corps:[[업체,건수]], mono, m:[12개], y:{연도:건수}, amt:{...}, cases:[...] }
```

**업체명 정규화 규칙은 `build_json.py`의 `norm_corp` 과
`web/src/lib/fmt.js`의 `normCorp` 이 반드시 같아야 합니다.**
한쪽만 고치면 검색이 조용히 안 맞습니다.

---

## 6. 파일 지도

| 고칠 것 | 파일 |
|---|---|
| 색·간격·다크모드 | `web/src/styles.css` |
| 하단 탭 | `web/src/App.jsx` 의 `TABS` |
| 1순위 / 공고 화면 | `web/src/pages/FirstBoard.jsx` · `LiveBoard.jsx` |
| 계산기·낙찰스코어 화면 | `web/src/pages/Calc.jsx` |
| **점수 배점·등급 기준** | `web/src/lib/engines.js` |
| 분석 화면 | `web/src/pages/Analysis.jsx` · `AgencyReport.jsx` |
| 구인구직 | `web/src/pages/Jobs.jsx` |
| 정적 JSON 로더 | `web/src/lib/data.js` |
| 면허 키워드·표시 형식 | `web/src/lib/fmt.js` |

`engines.js` 의 점수 기준은 **기존 `app.py`의 `engine_bid_score` 를 그대로
옮긴 것입니다.** 숫자를 임의로 바꾸지 마세요 — 사용자가 쌓아온 감각과 어긋납니다.
(핫존 30 / 경쟁 20 / 유사공고 20 / 안정성 15 / 데이터량 15)

---

## 7. 함정 모음 (실제로 겪은 것)

- **한글 입력 깨짐(`ㄱ가강`)** — 입력창 `value` 를 URL·외부 상태에 직접 묶으면
  매 키 입력마다 리렌더가 나서 조합이 끊깁니다. 로컬 `useState` + 디바운스로
  짜여 있으니 그대로 두세요.
- **SPA 뒤로가기** — `←` 를 `navigate("/경로")` 로 만들면 «뒤로»가 아니라
  히스토리를 쌓는 앞으로 이동입니다. 무한 왕복이 생깁니다.
- **soft 404** — `path="*"` 를 홈으로 폴백시키면 없는 URL 이 전부 색인됩니다.
  `NotFound.jsx` 가 `noindex` 를 걸고, **언마운트 시 반드시 제거**합니다.
  (안 지우면 다음 정상 페이지까지 noindex)
- **사이트맵 과다 제출** — 신생 사이트에 URL 수천 개를 던지면
  «발견됨-색인 안 됨»만 쌓입니다. `SITEMAP_AGENCIES` 를 천천히 올리세요.
- **`build_json.py` 성능** — `groupby` 후 `get_group()` 을 8만 번 부르면
  매번 전체를 재스캔해 멈춥니다. 단일 순회로 집계 후 정렬하도록 고쳐져 있습니다.
- **PowerShell 폴더 혼동** — 루트와 `web` 를 헷갈리기 쉽습니다. 항상 절대경로로 안내.
- **긴 코드 붙여넣기** — 채팅에서 메모장으로 옮길 때 320행 부근에서 잘린 적 있습니다.
  파일은 250행 이내로 쪼개 주고, 저장 후 `python -c "import ast;ast.parse(...)"` 로 검증시키세요.

### 원격 작업 환경 제약 (Claude 가 직접 돌릴 때)

- `apis.data.go.kr` 은 **네트워크 allowlist 에 막혀 있습니다.** 수집은 사용자 PC 에서만 가능.
- 마운트된 폴더는 **삭제(unlink) 불가** → 그 안에서 `git commit`·`npm install` 이 실패합니다.
  스크래치(`$HOME`)에서 작업 후 결과만 복사하세요.
- **백그라운드 프로세스는 호출이 끝나면 죽습니다.** 45초 넘는 작업은 분할하거나
  클라우드에서 만들어 tar 로 옮기세요.
- `npm install` 을 리눅스에서 돌려 윈도우 폴더에 넣으면 **플랫폼 바이너리가 어긋납니다.**
  반드시 사용자가 윈도우에서 실행해야 합니다.

---

## 8. 진행 상황 (2026-08-31)

**완료**
- https://k-conmap.com 운영 중 (Firebase Hosting · Cloudflare DNS A 199.36.158.100 DNS only)
- 서치콘솔 소유권 확인 · 사이트맵 308주소 제출 · 색인 요청 완료
- GA4 `G-YFGFLYKBCQ` 작동 확인
- 기초금액 수집 성공 (BSIS 엔드포인트 + 필드 대소문자 흡수 + 실패원인 출력)
- 공고/1순위 카드 펼치면 기초금액·낙찰가·예가범위 표
- **용역 제외, 공사 전용** — 3년치 482,630건 중 75%가 용역이었음. 되돌리려면
  collect.py 의 KINDS 와 build_json.py 의 SOURCES 주석만 풀면 됨 (원본 zip 보존)
- **7주치 목록** — 500건씩 묶어 나눠 담고, 검색·지역선택 시 전체를 뒤에서 로드
  (collect.py export_board / web/src/lib/useBoard.js / RangeBar)
- 누적 자료를 **달별 CSV**(data/extra_YYYY-MM.csv)로 분리 — 자동화 커밋을 가볍게
- **GitHub Actions 자동화 작성 완료** (.github/workflows/update.yml)

**고친 버그 (둘 다 계산 결과를 틀어지게 하던 것)**
1. 3년치와 매일 수집분이 8,330건 겹치는데 중복 제거가 없었음 → 공고번호로 제거
2. 날짜 형식이 두 가지('2022-11-14 15:00:00' / '2026-08-31')로 섞여
   pandas 가 첫 형식으로 고정, 수집분 78,064건의 날짜가 전부 NaT 였음
   → 앞 10자리 파싱 + 숫자만 남긴 재시도로 교체. 기간 2026-04-24 → 2026-08-31 로 정상화
3. firebase.json 헤더는 겹치면 **뒤엣것이 이김**(실측). 맨 아래 `*.json` 규칙이
   `/data/**` 를 덮어써 캐시가 죽어 있었음 → 넓은 규칙 위, 좁은 규칙 아래로 재정렬

**남은 것**
1. `자동화_설정하기.md` 대로 새 비공개 저장소 만들고 push (사용자)
2. GitHub Secrets 2개 등록 (사용자만 가능): `G2B_API_KEY`, `FIREBASE_SERVICE_ACCOUNT`
3. 옛 Streamlit(나노_건설시스템)의 배너·비용절감 커밋 push — .git/index.lock 먼저 삭제
4. 9월 말: 색인 확인 후 옛 사이트 종료
5. 조달청 키 재발급 · GitHub Secrets `GMAIL_APP_PASSWORD` 갱신
6. 다음 주제: **입찰가격 집중** (기초금액이 모이기 시작했으므로 재료 확보됨)

**⚠️ 마운트된 폴더에서 하지 말 것**
- `git status` 등 git 명령 — .git/index.lock 을 만들고 지우지 못해 윈도우에서 git 이 막힘
- 파이썬으로 파일 재작성 후 CRLF 복원 안 하기 — diff 가 «전체 변경»으로 보임

---

## 🚨 반드시 지킬 것 — 실제로 사고가 났던 것들

이건 «좋은 습관» 목록이 아니라 **틀렸던 기록**입니다. 사용자가 직접 잡아낸 것들입니다.

### 1. 조달청이 주는 값이 있으면 그걸 그대로 쓴다 — 손으로 만들지 않는다

주소든 금액이든 마찬가지입니다. 만들어 쓰면 반드시 어디선가 틀립니다.

**실제 사고 (2026-09-01)**
- 「나라장터 공고」 버튼 주소를 손으로 조립하면서 차수를 `000` 으로 박아버렸다.
  조달청은 `bidNtceDtlUrl` 로 정확한 주소를 이미 주고 있었다.
  실제 자료로 세어보니 **500건 중 29건**(차수 001 이 24건, 002 가 5건)이
  엉뚱한 공고로 가고 있었다.
- A값을 «조달청 API 로는 못 가져온다» 고 단정했다. **틀렸다.**
  `getBidPblancListInfoCnstwkBsisAmount` 응답에 항목별로 전부 들어 있었다
  (`sftyMngcst`, `mrfnHealthInsrprm`, `npnInsrprm`, `odsnLngtrmrcprInsrprm`,
  `rtrfundNon`, `qltyMngcst` … 그리고 적용 여부까지 `bidPrceCalclAYn`).
  응답 필드를 한 번도 찍어보지 않고 «없다» 고 말한 것이 원인.
- 참가업체수도 마찬가지. `prtcptCnum` 이 개찰결과에 그냥 들어 있었는데
  «조달청이 안 준다» 고 말했다.

**그래서 만든 장치**: `collect.py` 가 오퍼레이션마다 응답 필드 이름을 한 번씩
찍어 `web/public/data/diag.json` 에 남긴다. 없다고 말하기 전에 이 파일부터 본다.

### 2. 붙이기 전에 실제로 열어서 확인한다

만든 주소는 **브라우저로 직접 열어보고**, 계산 결과는 **실제 자료로 대조**한다.
«형식이 맞으니 되겠지» 는 확인이 아니다.

### 3. 버튼 이름이 실제 동작과 다르지 않게 한다

「나라장터 투찰」이라고 써놓고 실제로는 공고 상세 화면이 열렸다.
진짜 투찰은 나라장터 로그인 + 보안토큰이 필요해 링크로 갈 수 없다.
→ 「나라장터 공고」로 바로잡음.

### 4. 없다고 단정하지 않는다

«못 한다 / 없다 / 안 된다» 고 말하기 전에 한 번은 확인한다.
확인 못 했으면 «확인해 보겠다» 고 말한다. 단정과 추측을 섞지 않는다.

**실제 사고**: 조달청 응답이 ConnectTimeout 나는 걸 보고 «조달청 API 가 먹통»
이라고 단정했다. 실제로는 한 시간쯤의 일시 장애였고, 같은 날 저녁 배치는
정상으로 돌아 자료가 11,821 → 11,972 건으로 늘었다. 로그 한 장으로 원인을
단정한 것이 잘못이었다.

### 5. 이름이 겹치지 않게 한다 (CSS)

`.chips` / `.chip` 을 지역 칩과 투찰률 칩이 같이 쓰는 바람에,
나중에 쓴 규칙이 이겨서 선택된 「전국」이 **흰 글씨에 연한 바탕**이 되어
아예 안 보였다. → 투찰률 칩은 `.ratepick` 으로 분리.

---

## 📅 내일 할 일 — 팀원을 만들어 작업을 나눈다

혼자 순서대로 하면 느리다. **파일이 겹치지 않는 일**끼리 갈라서 팀원에게 맡긴다.

### 팀원을 쓰는 기준
- **된다**: 읽기 전용 분석, 자료 검증, 문서 조사 — 파일을 안 건드리므로 충돌 없음
- **안 된다**: 같은 소스 파일을 동시에 고치는 일 — 충돌나고 오히려 느려진다
- **절대 금지**: 마운트된 폴더에서 팀원이 git 명령을 돌리는 것

(2026-09-01 에 낙찰스코어 역검증을 팀원에게 맡겨 성공했다.
 106,534건 백테스트를 12분에 끝냈고, 그 결과로 «핫존 −0.20%p» 를 찾아냈다.
 반면 코드 수정은 내가 혼자 했다 — 그게 맞았다.)

### 나눌 만한 일
| 일 | 팀원에게 | 이유 |
|---|---|---|
| 사정률이 어떻게 정해지는지 (복수예가 15개 중 4개 추첨) 규칙 규명 | ✅ | 읽기 전용 분석 |
| 참가업체수와 낙찰률의 관계 분석 | ✅ | 자료 확보되면 바로 |
| 발주기관 분석 화면 재설계안 | ✅ | 조사·설계만 |
| 건설 뉴스 RSS 후보 조사 | ✅ | 문서 조사 |
| collect.py / build_json.py 수정 | ❌ | 내가 혼자 |
| 화면(jsx/css) 수정 | ❌ | 내가 혼자 |

### 그 밖에 밀려 있는 것
- `www.k-conmap.com` DNS 레코드 추가 (Cloudflare, 사용자 승인 필요)
- 건설 뉴스란 (제목+링크만, 저작권 때문에 본문은 안 가져옴)
- 발주기관 분석 화면 손보기 — 사용자가 «이상하다» 고 했음
- 2·3순위 투찰 자료: 조달청 공개 API 에는 없음이 확인됨. 다른 경로 필요

## ⏰ GitHub 예약 실행(cron)은 «정시»가 아니다 — 2026-09-02 확인

「1순위가 어제 날짜에 멈춰 있다」의 진짜 원인이었다.
예약해 둔 시각과 실제로 돈 시각 (Actions 기록에서 직접 확인):

| 예약 | 실제 | 지연 |
|---|---|---|
| 08:00 | 09:42 | +1h42m |
| 14:00 | 18:47 | +4h47m |
| 20:00 | 00:23 | +4h23m |
| 20:00 | 02:54 | +6h54m |
| 08:00 | 10:31 | +2h31m |

GitHub 문서에도 «정각(:00)에는 요청이 몰려 지연되거나 건너뛸 수 있다»고 적혀 있다.
→ 대응 ① cron 의 «분»을 정각에서 비켜 놓는다(:07, :37)
   ② 횟수를 늘려 한 번이 밀려도 다음이 받게 한다 (하루 6번)
   ③ 급하면 Actions → Run workflow (핸드폰에서도 됨)

**교훈: 「사이트가 안 바뀐다」를 자료 문제로 단정하기 전에
Actions 의 «실제 실행 시각»을 먼저 본다.** 이번에도 나는
「오늘 개찰이 아직 없어서 정상」이라고 답했다가 틀렸다.
소장님이 「빠르면 7시부터 올라와」라고 다시 확인시켜 준 덕에 찾았다.

## 🔓 저장소 공개 전환 — 2026-09-02

저장소를 Public 으로 바꿨다. 이유는 GitHub Actions 시간이 무제한 무료가 되기 때문.
(비공개는 월 2,000분 한도. 1회 7.6분 × 하루 5번 = 월 1,155분이라 아슬아슬했다)

**공개하자마자 비밀키 검사에서 2건이 걸렸다. 둘 다 옛날 커밋 이력에 있던 것.**

| 경보 | 내용 | 처리 |
|---|---|---|
| #2 | k-map-key.json 의 서비스 계정 비밀키 (kmap-bot@k-map-493003) | 구글이 이미 «노출됨»으로 Disabled. Revoked 로 닫음 |
| #1 | app.py 의 Firebase 웹 API 키 | **오탐**. 지우면 사이트가 죽는다. Won't fix 로 닫음 |

### 여기서 크게 배운 것

**1. Firebase 웹 API 키는 «비밀»이 아니다.**
브라우저 번들에 그대로 실려 나가도록 설계된 공개 식별자다.
보호는 database.rules.json(보안 규칙)과 HTTP 리퍼러 제한이 한다.
GitHub 이 «비밀키 유출»이라고 띄워도 이건 지우면 안 된다.
→ 나는 처음에 «삭제하세요»라고 말했다. 틀렸다. **파일을 먼저 열어봤어야 했다.**
   web/src/firebase.js 18번째 줄에 그대로 쓰이고 있었다.

**2. «현재 파일이 깨끗함» ≠ «저장소가 깨끗함».**
작업트리와 git index 만 훑고 «깨끗합니다»라고 말했는데,
정작 유출은 **과거 커밋**에 있었다.
→ 공개 전환 전에는 GitHub 의 Security 탭(비밀키 검사)을 먼저 본다.

**3. 다행히 피해는 0원.**
2026년 결제 전액(₩144,203)이 Gemini API(Default Gemini Project, sarasa-kr) 였고
k-map-493003 은 청구 목록에 아예 없었다.

### 지금 걸어둔 보호
k-conmap 프로젝트의 Browser key 에 HTTP 리퍼러 제한:
k-conmap.com / www.k-conmap.com / k-conmap.web.app / k-conmap.firebaseapp.com / localhost:5173
→ 구인구직 익명 로그인 정상 동작 확인함 (2026-09-02)

## ⏱ 30분 갱신 + 바로투찰 보강 — 2026-09-02 오후

### 갱신 주기
한국시간 **08:07 ~ 18:07, 30분마다** (하루 21회). 저장소를 공개로 바꿔
Actions 시간이 무제한 무료가 됐기 때문에 가능해졌습니다.

- 분은 정각을 피합니다(:07 · :37). 정각은 GitHub 이 몰려서 몇 시간씩 밀립니다.
- 08시·13시 회차만 3일치를 훑고, 나머지는 2일치만 가볍게 받습니다.
  (30분마다 3일치를 훑으면 조달청 호출이 21배가 됩니다)
- 진단(--probe)은 아침 첫 회차에만 돕니다.

**⚠️ 공짜가 아닌 쪽이 있습니다 — Firebase Hosting 저장용량.**
배포할 때마다 사이트 한 벌(약 54MB)이 «이전 출시 버전»으로 쌓입니다.
무료 한도가 10GB 라, 하루 21번이면 9일 만에 막힙니다.
→ Firebase 콘솔 > Hosting > 출시 저장용량 설정 = **보관 10개** 로 잠가 뒀습니다 (2026-09-02).
   이걸 풀면 며칠 안에 배포가 막히거나 요금이 붙습니다.

### 바로투찰에 붙인 것 4가지
1. **입찰 조건** — 지역·지역제한 기준·계약방법·공동수급·재입찰·낙찰방법.
   ⚠️ 조달청 `prtcptPsblRgnNm`(참가가능지역)은 **비워서 옵니다(실측 0/500)**.
   대신 `cnstrtsiteRgnNm`(공사 현장 지역, 500/500)과 `rgnLmtBidLocplcJdgmBssNm`을 씁니다.
2. **경쟁 강도** — 참가업체수(prtcptCnum). 10억 미만 중앙 78개사, 가운데 절반 17~322개사.
3. **개찰 후 채점** — bidresult.json(최근 7일)을 «필요할 때만» 받아, 우리 권장 투찰률과
   실제 1순위 투찰률을 견줍니다. 순위는 투찰률로 갈리므로 사정률과 무관하게 비교됩니다.
   1순위 화면의 「📊 바로투찰에서 채점하기」로 이어집니다.
4. **A값 추정** — bandstat.json 의 규모별 A값/기초금액 중앙 비율. **추정이라고 화면에 명시**합니다.

### 전송량 원칙
bidindex.json 은 «마감 전 공고»만 담습니다(1,726건 · 529KB → brotli 95KB).
개찰 결과를 여기에 합치면 첫 화면 전송량이 두 배가 됩니다. 그래서 따로 뒀습니다.

## 🚨 가장 중요한 수정 — 투찰금액 공식이 틀렸었다 (2026-09-02)

### 무엇이 틀렸나
조달청이 개찰결과에 주는 «투찰률»은 **투찰금액 ÷ 예정가격** 이다. A값을 빼지 않는다.
그런데 바로투찰은 우리 권장 투찰률을 «A값을 뺀 식»에 넣고 있었다.

    (틀림) 투찰금액 = (예정가격 − A) × 권장률 + A
    (맞음) 투찰금액 =  예정가격 × 권장률

A값이 기초금액의 6%인 6.3억 공고에서:
  권장 90.10% 로 알고 넣었는데 → 실제로는 **90.706%** 로 들어갔다 (+0.6%p).
  전국 최빈 낙찰률이 90.3% 인데 그보다 위다. 사실상 못 딴다.

### 어떻게 확인했나 (추측 금지 — 실측)
A값이 실린 개찰 65건으로 두 가설을 대봤다.

| | 되짚은 사정률 중앙 | 낙찰하한과의 여유(참가 많은 공고) |
|---|---|---|
| 투찰금액÷예정가격 | **99.88** (기존 실측 기준선 99.852와 일치) | 0.002 ~ 0.05% — 하한에 딱 붙음 ✅ |
| (투찰−A)÷(예정−A) | 99.55 (0.3%p 어긋남) | 0.13 ~ 0.73% — 하한에서 뜸 ❌ |

결정적 증거: **두 번째 가설의 오차가 A값 비율에 정확히 비례**했다.
A값 1.1% 공고는 0.13%, A값 6.8% 공고는 0.73%. 구조적 편향이다.
첫 번째 가설은 A값 비율과 무관하게 참가업체수만 따라 0으로 수렴했다
(참가 157개사 → 여유 0.009%). 1순위는 하한에 붙는다는 상식과 일치한다.

### 함께 바로잡은 것
낙찰하한은 규정대로 A값을 뺀 식이 맞다:
    낙찰하한금액 = (예정가격 − A) × 낙찰하한율 + A
두 식이 서로 다르므로, **A값이 있으면 «실효 하한 투찰률»이 명목 하한율보다 높다.**
위 예에서 명목 89.745% → 실효 **90.372%**.
즉 전국 권장 90.10% 는 이 공고에서 **실격**이다. 이제 화면이 그렇게 경고한다.

**교훈: 핵심 공식은 표본 30건으로 바꾸지 않는다.
오차가 «어떤 변수에 비례하는가»를 봐야 진짜 원인이 보인다.**


## ★ 개찰 순위(1위~꼴찌)는 조달청이 «준다» — 2026-09-02 정정

**내가 두 번 틀렸다.** 「조달청은 1순위만 준다」고 단정했고, 그 근거로
개찰 10,913건 전부 corps 가 1곳이라는 실측까지 들이댔다. 실측은 맞았지만 결론이 틀렸다.

### 진짜 원인 — 진단을 «날짜로만» 두드렸다

    getOpengResultListInfoOpengCompt
      · 날짜(inqryBgnDt~inqryEndDt)로 부르면 → 아무것도 안 온다
      · **공고번호(bidNtceNo + bidNtceOrd)로 부르면 → 온다**

빈 응답을 보고 「그런 오퍼레이션이 없다」로 읽었다.
«응답 없음» 과 «필수값이 다름» 과 «없는 서비스(HTTP 400)» 는 완전히 다른 이야기다.

### 실제 응답 항목

    opengRank(개찰순위) · prcbdrNm(투찰업체) · prcbdrBizno · prcbdrCeoNm
    bidprcAmt(투찰금액) · bidprcrt(투찰률) · bidprcDt(투찰일시)
    drwtNo1 · drwtNo2   ← 그 업체가 뽑은 예비가격 추첨번호

### 함께 찾은 것 — 예비가격 상세 (아직 안 씀)

    getOpengResultListInfoCnstwkPreparPcDetail   ← 날짜로 부르면 999건씩 온다
      bssamt(기초금액) · plnprc(예정가격) · bsisPlnprc · totRsrvtnPrceNum(15)
      · drwtNum · drwtYn · compnoRsrvtnPrceSno · bssamtBssUpNum

이걸 쓰면 **확정 예정가격을 직접** 받는다. 지금은 낙찰금액÷투찰률로 되짚고 있다.
기초금액 커버리지 문제(마감 전 공고의 21%가 결측)도 이걸로 메울 수 있다. **다음 과제.**

### HTTP 400 = 없는 오퍼레이션

    getBidPblancListInfoCnstwkBidPrceList · getOpengResultListInfoBidPrceList
    getOpengResultListInfoCnstwkDetail · getOpengResultListInfoCnstwkBidPrceDetail
    getBidPblancListInfoBidPrceCalclA

### 교훈 (CLAUDE.md 4번 「없다고 단정하지 않는다」의 재발)

같은 실수를 세 번째 했다. 이번엔 «확인했다»는 말까지 붙여서.
**확인의 «방법»이 틀렸을 가능성을 먼저 의심할 것.**
진단은 이제 날짜·공고번호 두 방식으로 다 두드리고, 실패 이유(resultCode/resultMsg)를
`data/diag.json` 에 남긴다.

### 지금 도는 방식

최근 개찰부터, 아직 1곳뿐인 것만 채운다 (`--ranks N`).
**건수는 `collect.py` 가 한국시간 시각을 보고 스스로 정한다** — 08·13시 250건, 그 밖 60건.
하루 2×250 + 19×60 = 1,640건. 하루 개찰이 약 570건이라 밀린 물량도 11일이면 따라잡는다.

⚠️ **`.github/workflows/*.yml` 은 원격 도구로 못 고친다**(보호 파일).
   그래서 회차별 배분을 워크플로가 아니라 collect.py 안에 두었다.
   워크플로를 고쳐야 하는 설계를 피할 것 — 사용자가 손으로 고쳐야 한다.

⚠️ 조달청 호출 한도가 있으니 함부로 올리지 말 것.

**같은 함정을 두 곳에서 잡았다**: 「앞에서 N건만 집기」.
목록 순서가 매번 같으면 **같은 N건을 하루 21번 다시 묻고** 뒤쪽은 영영 차례가 안 온다.
실제로 기초금액 개별조회가 그랬다 — 빈 건 1,931건 중 앞 250건만 21번 반복.
→ 「마지막으로 물어본 시각」(`bask` / `rask`)을 적어 두고 **오래된 것부터** 돌아가며 묻는다.
참가업체가 정말 한 곳인 공고는 `nrank=1` 로 표시해 다시 묻지 않는다.

## 📊 채점을 «성립하는 자리»로 좁혔다 — 2026-09-03

소장님: 「1순위에서도 바로투찰 채점이 다 있을 필요가 없잖아.
낙찰가와 바로투찰 차이가 너무나. 바로투찰 신뢰도만 하락하잖아.」

### 한 것
공고 쪽의 «완비» 기준에 **등급 기준을 하나 더** 얹었다.
C·D 등급 개찰에는 「📊 바로투찰에서 채점하기」 버튼을 달지 않는다.
대신 «왜 안 하는지»를 적는다. `web/src/NoticeDetail.jsx` 의 `scoreLink()`.

근거: 실측 958건에서 C·D 156건은 **한 건도 못 땄다.**
1순위가 낙찰하한 위 0.005% 안에 붙는 자리라 누가 계산해도 결과가 같다.
그 자리에 우리 금액을 대보고 「1,387만원 비쌌다」고 적는 건
우리 성적이 아니라 그 공고의 성질을 우리 탓으로 적는 것이다.

### ⚠️ 솔직히 적어 둘 것 — 이걸로 «차이»가 줄지는 않는다

실측으로 확인했다 (958건, K=0.674 · 여유 1.003):

| 대상 | 건수 | 실격률 | 1순위율 | 낙찰가 차이(중앙) |
|---|---|---|---|---|
| 전체 | 958 | 11.48% | 4.07% | 0.753% |
| A·B 만 | 802 | **10.60%** | **4.86%** | 0.789% |

**1순위율은 4.07% → 4.86% 로 오르지만, 차이는 오히려 0.04%p 커진다.**
게이팅의 값어치는 «오차 축소»가 아니라 «못 딸 게 뻔한 156건을 성적에서 빼는 것» 이다.
사용자에게 이걸 «오차가 줄었다»고 말하면 안 된다. 거짓말이 된다.

### 차이 0.79% 는 «줄일 수 있는 오차»가 아니다 — K 훑기로 확인

A·B 802건에 K 와 여유를 바꿔 가며 대봤다:

| K | 여유 | 실격% | 1순위% | 차이중앙% | 딴 금액(억) |
|---|---|---|---|---|---|
| 0.253 | 1.003 | 24.44 | 2.99 | **0.676** | 16.4 |
| 0.524 | 1.003 | 15.84 | 3.62 | 0.820 | 25.8 |
| **0.674** | **1.003** | **10.60** | **4.86** | 0.883 | **98.1** |
| 0.842 | 1.003 | 7.73 | 2.99 | 0.986 | 82.4 |
| 0.674 | 1.000 | 23.57 | 2.99 | 0.692 | 26.5 |
| 0.674 | 1.010 | 1.37 | 1.00 | 1.513 | 17.4 |

**차이를 줄이는 방향(K↓ · 여유↓)은 전부 실격률을 올리고 딴 금액을 깎는다.**
0.676% 까지 줄이면 딴 금액이 98.1억 → 16.4억 이다. 현재 설정이 두 지표 모두 꼭대기다.
→ **차이 0.79% 는 «실격당하지 않기 위해 내는 값»이다. 없앨 수 있는 것이 아니다.**
   화면에서도 이걸 «오차»라고 부르면 안 된다.
   신뢰의 근거는 «차이가 작다»가 아니라 **«무작위보다 몇 배냐»** 여야 한다
   (참가 중앙 62곳이면 아무렇게나 넣어 1순위 될 확률 1.61%. 우리 4.86% 는 3.0배).
   그래서 sim.json 에 `rnd`(무작위 기대 1순위율)와 `np`(참가업체수 중앙)를 넣었다.

### 함께 찾은 버그 — 시뮬레이션이 모든 공고를 ±3% 로 보고 있었다

`build_sim` 은 누적 CSV 만 읽는데 **CSV 에 예가범위 칸이 없었다.**
실측 11.1% 가 ±2% 인데 전부 ±3% 로 계산하고 있었다.
±2% 는 σ 가 0.7676 → 0.5118 로 달라지고, 등급도 +1 이 아니라 −2 를 받아야 한다.
→ `ARCH_COLS` 에 **예가하한 · 예가상한 · 참가업체수** 를 넣었다. 오늘부터 쌓인다.
   옛 줄은 비어 있으므로 그때는 ±3% 로 본다 (그리고 A값처럼 뒤늦게 채운다).

### 규칙을 두 언어로 두 번 적어 놓은 것에 대한 방어

등급 규칙이 `web/src/lib/winodds.js`(화면) 와 `build_json.py`(시뮬레이션) 두 곳에 있다.
한쪽만 고치면 «공고는 A등급인데 시뮬레이션은 C로 빼는» 어긋남이 **조용히** 생긴다.
화면 숫자가 틀리는 것보다 훨씬 찾기 어렵다.
→ `tools/checkgrade.mjs` + `selfcheck.py` 의 `check_grades()` 가
   32가지 조합(금액대4 × 예가범위2 × 기관4)을 두 쪽에 똑같이 물어본다.
   일부러 한 줄을 틀리게 해서 6가지가 잡히는 것까지 확인했다.

    python tools/selfcheck.py
    → 계산 12가지 + 등급 32가지

**교훈: 같은 규칙을 두 번 적었으면 «두 벌이 같은지»를 검사에 넣는다.
   주석으로 «같이 고치세요»라고 적어 두는 것은 방어가 아니다.**

### 곁들여 잡은 것 — 한글 폴더에서 검사가 안 돌았다

    Cannot find module 'C:\Users\OS\Desktop\%EB%82%98%EB%85%B8_...\bidmath.js'

`tools/checkmath.mjs` 가 `new URL(import.meta.url).pathname` 을 «파일 경로»로 썼다.
**.pathname 은 이미 퍼센트 인코딩된 문자열이다.** 거기에 `pathToFileURL` 을 또 걸어
`%EB` → `%25EB` 로 두 번 인코딩됐다.

→ 경로를 조립하지 말고 그냥 `import * as M from '../web/src/lib/bidmath.js'`.

**왜 내가 못 잡았나: 내 쪽 폴더는 `/home/claude/work/...` 라 한글도 공백도 없었다.**
소장님 폴더는 「나노_건설맵 코드 등」 — 한글 + 공백 둘 다다.
→ 이제는 고친 뒤 `/tmp/한글 폴더 검사/` 에 통째로 복사해 한 번 돌려 보고 넘긴다.
   옛 코드가 거기서 같은 오류를 내는 것까지 확인했다.

**교훈: 「내 환경에서 됐다」는 확인이 아니다. 소장님 환경의 «다른 점»
(한글 경로·공백·윈도우)을 흉내 내서 돌려 봐야 확인이다.**
이건 CLAUDE.md 1번 「조달청이 주는 값을 그대로 쓴다 — 손으로 만들지 않는다」와
같은 잘못이다. 이번엔 주소가 아니라 파일 경로였을 뿐이다.

## 💰 비용 실측 — 2026-09-03 (추측 아님, 전부 재봄)

소장님: 「파이어베이스 저장은 충분해? 비용은. 계속 누적이 되면 안되잖아.」

### 쌓이는 것 4가지와 각각의 천장

| 쌓이는 것 | 지금 | 한도 | 여유 |
|---|---|---|---|
| Firebase Hosting **저장용량** | 배포 1벌 57MB × 보관 10개 | 10GB | 0.8~5.7% |
| Firebase Hosting **전송량** | 첫 방문 372KB | **360MB/일** | **하루 약 990명** |
| git 저장소 | 46MB (+연 60MB) | GitHub 권장 1GB | 10년 이상 |
| `data/store/*.json` | 70일치로 잘라냄 | — | **안 쌓임** (`KEEP_DAYS=70`) |

**진짜 천장은 저장용량이 아니라 전송량입니다.** 실측(gzip 후):

    첫 방문 · 1순위 탭만    168 KB   (html 1.2 + js/css 94 + overview 0.4 + first.json 72)
    공고 탭까지             372 KB   (+ live.json 50.6 + bidindex.json 153.7)
    채점까지                496 KB   (+ bidresult.json 123.4)
    재방문(assets 1년 캐시)  277 KB   → 하루 1,330명

⚠️ **가장 위험한 것: Firebase 콘솔의 「출시 저장용량 = 보관 10개」.**
이게 풀리면 하루 21번 × 57MB 라 **9일 만에 10GB 를 채웁니다.**
자동화를 늘리기 전에 이 설정부터 확인할 것.

### bidindex.json 다이어트 — 재보고 나서 답이 바뀐 사례

927KB 중 `aparts`(A값 내역)가 244KB. 두 가지를 실제로 만들어 재봤다:

| 안 | raw | gzip | 판정 |
|---|---|---|---|
| 지금 | 927 KB | 153.7 KB | |
| ① 이름표(산업안전보건관리비…)를 번호로 + 범례 | 766 KB | 148.1 KB | **−5.6KB. 헛수고** |
| ② `aparts` 를 별도 파일로 분리 | 683 KB | **109.2 KB** | **−29%** (별도파일 37KB) |

**①이 헛수고인 이유: gzip 이 이미 반복 문자열을 지웁니다.**
raw 로는 161KB 를 줄였는데 전송량은 5.6KB 밖에 안 줄었다.
→ **전송량을 논할 때 raw 크기를 보면 틀린다. 반드시 gzip 후로 잰다.**
   ②가 이기는 이유도 «작아서»가 아니라 «첫 화면에서 아예 안 받아서» 다.

②를 하면 첫 화면 372 → 327KB, 하루 990명 → 1,130명.

### 안 건드리기로 한 것
- `url`(172KB raw / 12.4KB gzip) — 공고번호로 다시 조립하면 차수 버그가 재발한다
  (CLAUDE.md 1번, 500건 중 29건 사고). 조달청이 준 `bidNtceDtlUrl` 그대로 둔다.
- `swin`(49KB raw / **2.6KB gzip**) — raw 만 보면 커 보이지만 전송량은 무의미하다.

## 🔴 전송량의 진짜 범인은 목록 «미리 다 받기» 였다 — 2026-09-03

앞의 「비용 실측」에서 **하루 990명**이라고 적었다. **틀렸다. 실제로는 112명이었다.**
두 가지를 잘못 셌다:

1. `first.json`(72KB)을 첫 화면 전송량에 넣었는데, **`getFirst` 는 아무 데서도 호출되지 않는다.**
   실제 목록은 `board/` 묶음에서 온다.
2. 그 board 묶음을 **첫 화면에서 전부 받고 있었다.** `useBoard` 가
   「첫 묶음을 그리고 나머지는 뒤에서 조용히 다 받는다」로 되어 있었다.

실측 (gzip):

| | 전 | 후 |
|---|---|---|
| 1순위 탭 열고 가만히 | **23묶음 1.49MB** | **1묶음 95KB** |
| 공고 탭까지 | +25묶음 3.22MB | +1묶음 190KB |
| 하루 방문자 천장 | **약 112명** | **약 730~1,880명** |

주석에는 「브로틀리라 실제 전송량은 크지 않습니다」라고 적혀 있었다. **재보지 않고 쓴 문장이었다.**
목록을 500건씩 쪼개 놓고 결국 다 받고 있었으니 쪼갠 의미가 없었다.

### 곁들여 잡은 두 번째 버그
`page` 는 1에서 시작하는데 자료가 오기 전에는 `pages` 도 1이라
「page(1) >= pages-1(0)」 이 **화면 뜨자마자 참**이 되어, 아무도 넘겨보지 않았는데
다섯 묶음(약 400KB)을 더 받고 있었다. → `!loading && page > 1` 을 조건에 넣었다.

### 확인 방법 (다음에도 이렇게 잰다)
Playwright 로 실제 브라우저를 띄워 `/data/` 요청을 센다. 세 가지를 본다:

    가만히 5초    → board 2개 (목록표 + 첫 묶음)      ← 대부분의 방문자
    검색어 입력   → board 24개 (전부)                 ← 안 그러면 검색이 틀린다
    3쪽 넘김      → board 2개                          ← 500건 안이라 더 안 받아도 됨

⚠️ **시험 서버가 없는 파일에 index.html 을 200 으로 돌려주면 404 가 숨는다.**
   실제로 `npm run build` 가 dist 를 비워 시험용 board 파일이 지워졌는데,
   서버가 HTML 을 200 으로 주는 바람에 **「검색해도 전체를 안 받는다」는 가짜 버그**를
   한참 쫓았다. → 시험 서버는 `/data/`·`/assets/` 에 대해 반드시 404 를 내야 한다.
   → 그리고 **빌드 뒤에는 항상 시험용 파일을 다시 만든다.**

## 🌲 「투찰 업체가 1곳이라는 게 말이 돼?」 — 된다 (2026-09-03)

소장님이 홍성군 「가곡지구 지구단위종합복구사업(산지)」 29.1억 개찰을 보고 물었다.

자료를 열어 보니 **우리 자료 구멍이 아니라 사실이었다**:

    "np": 1                       ← 조달청 prtcptCnum. 우리가 센 게 아니다
    "nrank": 1, "rask": "2026...", "rq": [[1, 2912500000]]
                                  ← 순위 API 를 실제로 물어봤고 1곳이 왔다
    "lic": ["산림조합(지역조합)/4119"]   ← 참가 자격이 지역 산림조합

**결정적 대조가 같은 사업 안에 있었다.** 같은 홍성군, 같은 「가곡지구」:

| 공고 | 면허 제한 | 참가 |
|---|---|---|
| 가곡소하천 (긴급) | 없음 | **323곳** |
| 가곡천 및 세천 (긴급) | 없음 | **377곳** |
| 산지 | 산림조합(지역조합) | **1곳** |

발주기관도 사업도 시기도 같다. 다른 건 면허 제한뿐이다.
참가 1곳 개찰은 저장소에 **799건**이고, 그 중 123건에 면허 제한이 걸려 있다.
단독 공고의 투찰률 중앙은 **90.573%** — 경쟁이 없으면 하한까지 내릴 이유가 없다.

### 그런데 화면은 틀렸다
「이 공고는 투찰업체가 한 곳이었습니다」라고만 하고 **왜인지를 말하지 않았다.**
`lic` 는 이미 화면까지 와 있었는데 쓰지 않고 있었다.
→ 이제 「참가 자격이 **산림조합(지역조합)**으로 묶여 있어…」와
   「경쟁이 없으면 하한까지 내릴 이유가 없어 투찰률이 높게 나옵니다」를 함께 적는다.
→ `bidresult.json` 에도 `lic` 를 넣었다(채점 화면에서도 같은 설명이 되도록).

**교훈: 사용자가 «말이 돼?» 라고 물으면 자료가 틀렸을 수도, 화면이 불친절할 수도 있다.
이번엔 후자였다. 값이 맞다고 끝이 아니라, «왜»를 같이 보여줘야 믿는다.**

## 🔎 검색 색인 — 2026-09-03

검색은 7주치를 다 뒤져야 해서 묶음을 **전부** 받고 있었다
(1순위 1,528KB · 공고 1,767KB). 검색 한 번이 안 하는 방문 16명분이었다.

### 설계 — 「전체를 받는다」가 아니라 「보는 쪽만 받는다」

실측이 방향을 정했다. 검색어별로 «몇 묶음이 필요한가»:

| 검색어 | 맞는 건수 | 전체를 그리려면 | **첫 쪽 20건만** |
|---|---|---|---|
| 도로 | 597 | 23묶음 | **1묶음** |
| 포장 | 255 | 23묶음 | **3묶음** |
| 경주시 | 65 | 19묶음 | **5묶음** |
| 전기 | 1,280 | 23묶음 | **1묶음** |

→ 걸러내기에 필요한 칸만 담은 **색인**을 따로 굽고,
   화면은 색인으로 «몇 건·몇 쪽»을 정확히 세고 **그 쪽에 나올 20건이 든 묶음만** 받는다.

    board/first-con-idx.json   [공고명, 기관, 낙찰업체]              358KB
    board/live-con-idx.json    [공고명, 기관, 기초, 예가lo, hi]      352KB

live 에 base/lo/hi 를 넣은 건 「해볼 만한 공고만(A·B)」 등급이 이 셋을 쓰기 때문이다.

### 묶음을 «번호별로» 보관한다
전에는 배열에 이어붙였다. 5번을 먼저 받으면 날짜 순서가 깨진다.
→ `chunks = {번호: 줄들}` 로 두고 번호 순으로 이어붙인다. 어떤 순서로 받아도 정렬이 유지된다.

### 브라우저로 확인한 것 (Playwright)
| | 결과 |
|---|---|
| 가만히 | 묶음 1개 |
| 「도로」 검색 | 색인 + 묶음 1개 · **2,255건**(= 11,271÷5, 파이썬과 일치) |
| 검색 후 3쪽 | 200·205·210호 — 1쪽과 안 겹침, 전부 «도로» |
| 검색 후 28쪽 | **2700호** = 매칭 #540 = 묶음 5 의 200번째 ✅ |
| 「경주시」 공고 | **2,499건** (파이썬으로 센 것과 일치) |

⚠️ 28쪽까지 «한 쪽씩 넘기면» 묶음 6개가 쌓인다. 정상이다 —
   건너뛰어 갔으면 묶음 1개만 받는다.

### 색인 칸 순서는 두 곳에 적혀 있다 → 검사에 넣었다
`collect.py` 의 `fields` 와 화면의 `const [name, inst, win] = a` 가 어긋나면
**검색이 엉뚱한 칸을 뒤진다 — 에러 없이, 결과도 그럴듯하게.**
→ `selfcheck.py` 의 `check_boardidx()` 가 대조한다. 일부러 두 칸을 바꿔서 잡히는 것까지 확인했다.

### 효과 (실측 기준 추정)
검색 1회: 1,528KB → **색인 358KB + 1~5묶음(95~425KB) = 450~780KB**. 약 2~3.4배.
1,000명·두 탭·검색률 20% 기준 하루 568MB → **368MB**.

**⚠️ 과장하지 말 것.** 종량제($0.15/GB)이므로 이건 «멈춤»이 아니라 돈 문제고,
1,000명 규모에서는 월 1,300원 → 100원 수준이다. 값어치는 **10배 커졌을 때** 나온다.

### 실제 자료로 검증함 — 2026-09-03 (합성 자료 아님)

소장님: 「되도록 클로드가 해줘. 나말고.」
→ 소장님 PC 의 **진짜 개찰 자료 11,271건**을 클라우드로 가져와 빌드된 화면에 걸고
   Playwright 로 직접 눌러 봤다. 합성 자료로 한 시험은 «내가 만든 규칙»만 확인해 준다.

| 한 일 | 받은 것 | 결과 |
|---|---|---|
| 1순위 탭 열자마자 | 목록표 + 묶음0 | **95 KB** (전 1,490KB) |
| 「도로」 검색 | + 색인 | **597건** — 파이썬으로 센 것과 일치 ✅ |
| 10쪽까지 넘김 | + 묶음 1~6 | 464KB · **20줄 전부** ✅ |

1쪽·10쪽에 그려진 공고명을 파이썬이 색인에서 직접 뽑은 정답과 대조 — 한 글자까지 같았다.

    1쪽  대동 예안 251-2번지 일원 도로재포장공사 …
    10쪽 국도19호선 유동지구 위험도로 개선공사 …

### 이 검증에서 새로 잡은 버그 — «조용히 짧은 쪽»
시험 중 묶음 6번이 404 났는데 **화면은 아무 말도 안 하고 17줄만 그렸다.**
사용자는 «원래 그만큼인가 보다» 라고 읽는다. 알 방법이 없다.
→ `pageReady` 를 두어, 그 쪽에 필요한 묶음이 다 오기 전에는 «불러오는 중»을 보여준다.

**교훈: 없는 자료를 «없는 채로» 그리면 안 된다. 모자라면 모자란다고 해야 한다.**
값이 틀린 화면보다 «조용히 덜 보여주는» 화면이 더 위험하다 — 아무도 못 알아챈다.

## 🧾 2026-09-03 밤 — 세 가지 더

### ① 1순위 목록에 「채점 가능 / 안 함 / 불가」 뱃지
소장님: 「1순위에서도 채점 가능·불가능 표시하기로 했잖아.」 — 펼쳐야만 보였다.
판단은 `NoticeDetail.jsx` 의 `scoreState(r)` **하나만** 쓴다(목록·상세 둘 다).
실측 11,360건: 채점 가능 55.2% · 값 부족 27.3% · C등급 7.9% · D등급 9.7%.

⚠️ «값 부족» 이 «아직 안 온 것» 인 줄 알고 「받는 중」 이라 쓰려다 재봤다:
   개찰 0~1일 80.9% · 15일+ 68.0% — **오래된 게 더 비어 있다.** 기초금액 수집 전의 것이다.
   → 「받는 중」 은 거짓말이 된다. 「값 부족」 그대로. **재보고 나서 말을 정한다.**

### ② 「최소 2위」 를 없앴다
소장님: 「바로투찰이라면 최소 2위라는 게 말이 돼?」 — 안 된다.
순위 자료가 1순위 한 곳뿐일 때 «낮게 쓴 곳 1곳 이상 → 최소 2위» 는 산술적으로 맞아도
92곳이 붙은 공고에서 **2위 한 것처럼 읽힌다.** 「유효 12곳 중 13위」 와 같은 잘못.
→ 등수를 말하지 않는다: 「1순위가 아니었습니다 · 등수는 알 수 없습니다」.
   실측 하나만 붙인다: 사다리 54건에서 **1순위보다 0.5%만 높아도 등수 중앙 20위**(4분위 5~50).
   표본이 적다고 같이 적는다. +2.28% 지점은 표본이 없어 **말하지 않는다.**
→ selfcheck 가 «등수 주장» 꼴(최소 N위 · 넣었으면 N위 · N위~M위)을 잡는다. 되살려서 잡히는 것 확인.

### ③ 검사 도구 자신이 «네 번째 사본» 이었다
`--browser` 로 전체를 돌리니 공고 5가지가 «화면 금액 0» 으로 빨갛게. 코드는 멀쩡했다.
**selfcheck.py 가 자기만의 IDX_F(24칸, aparts 포함)** 로 시험 자료를 만들고 있었다.
내 대조 검사는 collect.py ↔ 화면만 봤지 검사 도구 자신은 안 봤다.
→ IDX_F / RES_F 를 **collect.py 에서 읽어온다.** 시험 자료는 이름→값 사전 → 그 순서로 배열화.
   사본이 없으면 어긋날 수도 없다.

**교훈: «같은 규칙을 두 번 적지 않는다» 는 검사 도구에도 적용된다.
검사가 틀리면 코드가 틀린 것보다 나쁘다 — 검사를 못 믿게 된다.**

### 전체 검증 (python tools/selfcheck.py --browser)
계산 12 · 등급 32 · bidindex 23칸 · 검색색인 3+5칸 · 화면 12 · 등수주장 0 — **전부 통과.**
한글 폴더에서도 (playwright 는 저장소 밖 ../node_modules 라 링크 걸고 확인).

## ⚡ 원클릭 — 공고 카드에서 바로 금액 (2026-09-03)

소장님: 「아이건설넷 원클릭처럼 업그레이드해줘. 입찰가를 원클릭으로 구해서 입찰할 때.
바로투찰이 살아야 내가 살거든.」 그리고 「원클릭을 모방해서 바로투찰을 만든 거잖아.」

### 진단
바로투찰 화면 «자체»는 이미 한 화면에 금액·복사·나라장터가 있었다.
문제는 **거기까지 가는 길**이었다: 공고 탭 → 알약 → 화면 이동 → 그제야 금액.
원클릭이 되려면 **공고 카드에 금액이 이미 떠 있어야** 한다.

### 한 것
- `bidmath.js` 에 `quickBid(r, p50)` — 공고 한 줄로 권장 금액. **바로투찰 화면과 같은 길**
  (금액 → 투찰률 소수3자리 올림 → 다시 금액). 완비(isReady) 아니면 null.
- `LiveBoard.jsx` 카드 아래 `.oneclick` 줄: 권장 투찰금액 · 투찰률 · [금액 복사] · [나라장터 →]
  나라장터 주소는 조달청이 준 `url` 그대로 (손으로 만들면 차수 틀림 — 실제 사고).
- 색은 전부 토큰 — 다크 모드 실측 bg rgb(23,35,60) / fg rgb(232,234,237).

### selfcheck 가 잡은 것 — 카드와 화면이 2,354원 달랐다
처음엔 `recommend().amt` 를 그대로 냈다. 화면은 그 금액을 투찰률(소수 3자리 **올림**)로 바꿨다가
다시 금액으로 만든다. 4억 공고에서 0.001% = 4,090원이라 올림 한 번이 2,354원이었다.
→ quickBid 도 같은 길을 가게 고쳤다. 이제 5개 시나리오 전부 ±1원 안.
**같은 공고를 두 화면이 다른 숫자로 보여주면 사용자는 둘 다 안 믿는다.**

### 곁들여 잡은 것 — p50 대체값이 화면 안에 세 가지였다
`ov?.sjq?.p50 ?? 100` · `?? 99.9` · `?? 99.894` 가 BaroBid 에 6곳 흩어져 있었다.
sjq 가 빠지면 같은 공고 금액이 자리마다 달라지는 구조. → `P50_FALLBACK = 99.896` 하나로.
(라이브는 Actions 가 build_json 을 돌려 sjq 가 있다. 소장님 로컬 public/data 만 9/1 판이라 없다.)

### 실제 자료로 확인
공고 첫 묶음 500건 중 완비 332건. 첫 20장 중 9장에 원클릭 줄.
화면 금액 vs 파이썬 독립 계산: **5/5 일치** (186,758,456 · 817,178,069 · 59,927,381 …).
복사 버튼 → 「✓ 복사했습니다」. 라이트·다크 둘 다 스크린샷으로 봤다.

### 다음에 원클릭을 더 원클릭답게 하려면 (아직 안 함)
- 「내 면허」 + 「해볼 만한 공고만」 걸러 놓으면 아침에 스크롤만 하면서 복사 → 투찰. 이미 된다.
- 마감 임박 순 정렬 / 관심 공고 저장(localStorage) — 로그인 없이 가능.
- 아이건설넷 «원클릭 1순위 내역» 같은 공개 실적 페이지 — sim.json 의 win 사례로 만들 수 있다.

## 🏛 워크넷 건설 채용 — 2026-09-03

소장님: 「워크넷 구인구직 공고를 건설맵에 띄울 수 없어? 우리는 보여주기만 하고 나머지는 워크넷에서.」
그리고 「크롤링이 안 되는 게 아니라 못 하겠다는 거 아니야? 비용이 안 들게 하고 싶어서.」

### 답
- **못 하는 게 아니라 안 하는 것**이었다. 크롤러는 쓸 수 있다. 안 쓰는 이유는 소장님이 지는 위험(잡코리아 v 사람인).
  능력 문제처럼 돌려 말한 건 잘못이었다.
- 비용 걱정은 반대다. **공공데이터 API 는 무료**다 — 조달청과 같은 곳, 같은 조건.
- 워크넷 API 키는 **조달청 키와 별개**다. openapi.work.go.kr 에서 받아 `.env` 에 `WORKNET_API_KEY=`.
- 나는 **호출을 못 한다**: 내 통로에서 조달청 API 까지 403 (대조군으로 확인). 소장님 윈도우에서만 된다.

### 만든 것
| 파일 | 내용 |
|---|---|
| `collect.py` | `collect_jobs` — 건설 낱말 17개로 조회 → `is_construction` 으로 거름 → `data/store/jobs.json`(70일) → `web/public/data/jobs.json`(마감 전, ≤1000) |
| `tools/worknet.py` | 키가 되는지 «확인만». 응답 항목을 전부 찍음 |
| `web/src/pages/Jobs.jsx` | 구인구직 탭에 「🏛 워크넷 건설 채용」 / 「✏️ 직접 올린 글」 나란히. 제목·회사·지역·급여·조건·마감 + 「워크넷에서 보기 →」 |

명령: `python collect.py --jobsonly` (처음 확인) · 평소엔 08·13시 회차에 자동 · `--jobs` 로 강제.

### 원칙 (조달청 공고와 같다)
- 본문은 안 가져온다. 제목·조건·링크만. 지원·문의는 워크넷에서.
- 링크는 워크넷이 준 `wantedInfoUrl` 그대로. 손으로 만들지 않는다.
- 출처를 항상 적는다 — «워크넷(한국고용정보원) 공개 API».
- 구인구직 탭에서만 받는다. 첫 화면 전송량에 안 얹는다.

### ⚠️ 응답 항목 이름은 «짐작»이다 — 첫 실제 응답으로 확인할 것
`worknet_row` 는 이름 후보를 여러 개 두고 실제로 온 것을 쓴다 (wantedAuthNo/company/title/sal/region/closeDt/wantedInfoUrl …).
첫 응답의 항목 전체를 `diag.json` 의 `worknet_wantedApi` 에 남긴다. **거기를 보고 나서 후보를 정리한다.**
건설 거르기도 지금은 낱말(JOB_RX)이다. `diag.json` 의 `worknet_jobs.top_jobs` 에 직종코드 분포가 남으니
그걸 보고 **코드로 거르는 방식**으로 바꾼다. 낱말은 «건설사 경리» 도 잡고 «구축» 도 놓친다.

### 아직 안 된 것
소장님이 「키를 넣어 뒀다」고 했는데 `.env` 에는 없었다 (03:12 기준). 어디에 넣었는지 확인 필요.
`python tools\worknet.py` 결과를 받아야 항목 이름을 확정한다.

## ⚖️ 「최저가 낙찰가에 더 가까운데 왜 권장이냐」 — 2026-09-03

소장님: 「1순위에서 채점하면 항상 권장보다 최저가 낙찰가에 더 가까워. 그럼 최저가 맞는 거 아니야?」
이어서 「권장과 최저 사이 값은 어때?」 「최저로 하면 여유가 0%인데…」

### 실측 (A·B 802건, 같은 개찰에 세 금액을 다 대봄)

| | 실격 | 1순위 | 살아남은 것 중 1순위 | 낙찰가 차이(유효만) | 딴 금액 |
|---|---|---|---|---|---|
| 최저 | **47.1%** | 3.74% (30건) | **7.08%** | **0.423%** | 29.7억 |
| 중간 | 31.3% | 3.49% (28건) | 5.08% | 0.576% | 21.9억 |
| 권장 | 10.5% | **4.99% (39건)** | 5.57% | 0.883% | **101.8억** |

**소장님 관찰은 맞다.** 최저가 더 가깝고, 살아남기만 하면 1순위도 더 자주 한다.
그런데 절반이 실격이다. 최저 = «중앙 사정률에서의 하한» = 여유 0% (소장님이 짚은 그대로).
사정률은 투찰 «뒤에» 추첨되므로 중앙보다 높게 나온 날엔 하한 아래다 — 정확히 반반.
실격한 날 낙찰가와의 거리는 0.4% 가 아니라 «그 자리에 없음» 이다.

### «사이 값»은 절충이 아니라 골짜기다
K 를 최저 쪽으로 내리며 훑은 곡선 (앞 절 표 재인용): 0.25→16.4억 · 0.39→20.1억 · 0.52→25.8억 · **0.67→98.1억** · 0.84→82.4억.
사이(0.25~0.52)가 양쪽 끝보다 다 나쁘다. 1순위는 하한 위 0.03% 안에 붙어 있어서,
내려가다 그 자리에 «닿기» 전에 실격 구간을 먼저 지난다. 봉우리 하나, 권장이 꼭대기.
딴 금액은 어떤 공사를 땄느냐에 흔들리니 **건수(39 vs 30 vs 24)** 를 보는 게 더 튼튼하다. 결론 같다.

### 화면이 착시를 만들고 있었다 → 고침
채점 화면은 실격한 날에도 «낙찰가와 0.4% 차이» 로 보여줬다. 그러니 최저가 좋아 보인다.
→ ①②③ 아래에 **세 금액의 «이 개찰에서의 운명»** 표를 넣었다 (`.threebox`):
   넣었을 금액 · 1순위와 % · 실격/밀림/1순위 · 실측 802건(실격률·딴 금액).
   같은 공고에서 세 개를 나란히 보면 «가까운 것»과 «가져가는 것»이 다르다는 게 보인다.
   실측 수치(47.1 / 31.3 / 10.5 · 29.7 / 21.9 / 101.8)는 코드에 상수로 박혀 있다 —
   다시 재면 `BaroBid.jsx` 의 `three` 배열도 같이 고칠 것.

### 소장님 캡처 건(양곡보관창고 · 사정률 98.423%)으로 확인
사정률이 «낮게» 나온 날이라 셋 다 살아남았고(밀림), 최저가 +1.455% 로 가장 가까웠다.
바로 그런 날만 보면 최저가 맞아 보인다. 높게 나온 날은 화면에 «실격» 으로 남고, 그날은 최저가 «없다».

**교훈: 사후에 «가까웠던» 금액은 사전에 «넣을» 금액이 아니다. 생존 편향.**

### 첫 실행 결과 — 옛 주소로 불러 «0건» (2026-09-03)
`python collect.py --jobsonly` → 오류 없이 0건. 원인: 옛 워크넷 주소(openapi.work.go.kr/…/wantedApi.do)를 불렀다.
소장님 키는 **고용24(work24.go.kr)** 키다. 지금 주소:
    https://www.work24.go.kr/cm/openApi/call/wk/callOpenApiSvcInfo210L01.do   (목록 210L01 · 상세 210D01)
명세를 받아 항목을 확정했다(짐작 아님):
    요청  authKey callTp returnType startPage display · region occupation salTp keyword regDate(D-0/D-3/W-1/W-2/M-1) empTpGb(1상용/2일용) career(N/E/Z) education(00~07) …
    응답  wantedAuthNo company busino **indTpNm(업종)** title salTpNm sal minSal maxSal region holidayTpNm minEdubg maxEdubg career regDt closeDt infoSvc wantedInfoUrl wantedMobileInfoUrl basicAddr **empTpCd(코드만)** **jobsCd(코드만)** smodifyDtm
짐작과 달랐던 것: `jobsNm` 없음(직종명 안 옴) → 업종 `indTpNm` 으로 거른다. `empTpCd` 는 코드 → 이름표 WN_EMPTP.
날짜 형식이 명세에 없다 → `_wn_date` 가 YY-MM-DD / YYYYMMDD / YYYY-MM-DD 를 다 받는다
(안 하면 «26-09-20» < «2026-09-03» 으로 마감 전 공고가 전부 지워진다).
**0건이면 응답 원문 앞 600자를 diag 에 남기고 화면에도 찍는다** — 이유 없는 0건은 다시 없다.
직종코드 표(apiCdList.do?cdGbn=jobs)는 robots 로 자동수집 금지 → 첫 응답의 (jobsCd, 업종) 분포를 diag 에 남겨 그걸로 정한다.

## 🎓 채용정보는 기업회원 전용 → 「건설 자격·훈련」으로 방향 전환 (2026-09-03)

`--jobsonly` 재실행 → `<error>개인회원은 사용할 수 없는 OPEN-API입니다.</error>`
채용정보 API 는 **사업자등록번호가 있는 기업회원**만 쓴다(개인사업자는 됨, 법인일 필요 없음).
소장님: 「기업회원 키는 받을 수 없다. 크롤링하자. 공공기관이니.」

### 크롤링은 안 한다 — 이유를 다시 적어 둔다
그 기관이 «이 자료는 기업회원에게만» 이라고 **방금 답했다.** 그걸 화면 긁기로 우회해 광고 붙은 사이트에
올리는 건 공개 자료 이용이 아니라 거절당한 것을 뒷문으로 가져오는 것. 잡코리아 v 사람인이 그 조합.
고용24 는 robots 로 자동 수집을 막고 있다(apiCdList 에서 부딪힘). 위험은 소장님이 진다. 못 하는 게 아니라 안 하는 것.

### 대신 — 받은 11개 키 중 개인회원으로 되고 쓸모 있는 것
**국민내일배움카드 훈련과정(310L01)** — NCS 대분류 **14 = 건설**.
굴착기·지게차·타워크레인·건설안전·전기·용접·타일·방수 — 현장 사람들이 실제로 찾는 자격·기능 훈련.
    https://www.work24.go.kr/cm/openApi/call/hr/callOpenApiSvcInfo310L01.do
    요청  authKey returnType outType(1목록) pageNum pageSize srchTraStDt/EndDt(YYYYMMDD) sort sortCol srchNcs1(14) srchTraArea1 …
    응답  TITLE SUB_TITLE(기관) ADDRESS TRA_START_DATE TRA_END_DATE COURSE_MAN(수강비) REAL_MAN(본인부담) YARD_MAN(정원)
          EI_EMPL_RATE3/6(취업률) STDG_SCOR(만족도) NCS_CD TITLE_LINK TRPR_ID TRPR_DEGR
    ⚠️ 태그가 대문자인지 camelCase 인지 명세에 두 가지로 나온다 → course_row 는 둘 다 받는다. 첫 응답으로 확정.
    ⚠️ sortCol=TRNG_BGDE 는 HRD-Net 계열 관례 — 틀리면 원문이 diag 에 남는다.

`collect.py`: `collect_courses` / `export_courses` / `--coursesonly` · 평소 08시 회차에 하루 한 번.
`Jobs.jsx`: 「🏛 워크넷 건설 채용」「🎓 자격·훈련」「✏️ 직접 올린 글」 세 갈래. 자료 있는 쪽을 먼저 보여준다.
fixture 로 화면 확인: 3건 · 본인부담 뱃지 2 · 지역 필터 ✅ · 라이트/모바일 스크린샷.

나머지 키: 사업주훈련·컨소시엄·일학습병행(사장님용, 보류) · 직업정보(건설 직종 임금·전망 사전 — 다음 후보) · 강소기업(보류).

## 🎯 분석 업그레이드 — «통계»에서 «자리»로 (2026-09-03)

소장님: 「분석에서 발주기관과 업체 자가진단도 업그레이드 해줘. 입찰 사이트 보고 클로드가 판단해줘.」
경쟁 사이트 공통 틀은 «발주처 성향 + 경쟁사 성향». 거기에 오늘 실측(승률은 «창»이 45배 가른다)을 얹어 정했다.

### build_json.py — 줄마다 «자리» 정보
`load_all` 끝에 기초금액 있는 줄에만: `grade`(A~D, 화면과 같은 규칙) · `np`(참가업체수) · `mgn`(**창**).
`spot_stats(g)` → `{gr:{A,B,C,D}, gn, np:{n,med,solo,few}, mg:{n,med,wide(≥0.3),tight(<0.02)}}` 를
기관·업체 집계에 `spot` 으로 붙였다. 업체 `cases` 에 [5]등급 [6]창 [7]참가업체수 추가.

### ⚠️ 생존 편향 — 만들다가 버린 것
처음엔 «내 투찰률 − 바로투찰 권장률» 을 넣었다. 실측하니 **95% 가 음수**.
당연하다 — 1순위는 정의상 가장 낮게 쓴 곳이다. 낙찰 기록만 있는 자료에서 이 진단은 정보가 0이다.
→ 버리고 **창**(1순위 투찰률 − 실효 하한율, 확정 사정률 기준)으로 바꿨다. 창은 그 자리의 경쟁 성격이다.
   최근 2달 1,079건 창 중앙 **0.034%p** — 어제 958건의 0.030% 와 일치.

### 잡은 버그
`Counter(x for x in g["grade"] if x)` — **NaN 은 참**이라 세어졌다. 홍성군 gn=11 인데 gr 합이 1. → `isinstance(x, str)`.

### 화면 — `web/src/Spot.jsx` (두 화면 공용)
- `SpotBlock` : 판정(해볼 만한 자리 / 보통 / 하한에 바짝 / 표본 부족) + 넓은창% · 바짝% · A·B% · 참가업체수 + 등급 막대
  판정 기준: med≥0.1 또는 wide≥30% → 해볼 만 · med<0.02 또는 tight≥60% → 바짝 · n<3 → 표본 부족
- `OpenNotices` : **분석에서 끝내지 않는다** — bidindex 에서 그 기관 / 그 업체가 자주 딴 지역·기관의 마감 전 공고를
  걸러 등급 + 원클릭 권장금액(quickBid, 바로투찰과 같은 함수) + 바로투찰 링크. 값 부족이면 «계산 안 함».
- AgencyReport 맨 위에 SpotBlock, 맨 아래에 OpenNotices(instMatch). CorpTab 요약 다음에 SpotBlock, 끝에 OpenNotices(corpMatch).
- 최근 낙찰 줄에 등급·창·참가업체수 뱃지.

fixture 로 두 화면 확인: 홍성군 «보통»(창 0.077, 18%/27%, A·B 86%, 323곳, 마감 전 3건 중 완비 2건 권장금액) ·
영남개발 «해볼 만한 자리»(창 0.142, A 11·B 4, 맞춤 공고 경주시 1건 218,160,877원).

⚠️ `python build_json.py`(또는 run_all 을 --quick 없이) 를 한 번 돌려야 spot 이 생긴다. --quick 은 집계를 건너뛴다.
   참가업체수는 CSV 칸을 오늘 넣었으므로 며칠 쌓여야 np 가 찬다.

## 🥇 업체 자가진단 — «진 투찰»이 처음 보이는 자리 (2026-09-03)

소장님: 「업체 이름을 적으면 이제까지 투찰 기록이 나오면서 방향성을 제시. 30위 안에 있으면 있고 없으면 없다라고
정확히 밝히면서. 바로투찰이었다면 이랬을 것이다, 이게 들어가야 해. 파이어베이스 비용도 고려.」

### 먼저 잡은 버그 — 순위가 «안 쌓이던» 이유
store 에 순위(corps 2곳+)가 **0건**. bidresult 엔 46건 남아 있는데도.
원인: `row_first` 의 보존 목록(base·lo·hi·lic·aval…)에 **corps·rq·nrank·rask 가 없었다.**
개찰목록 API 의 corps 는 1순위 한 곳뿐이라, 다음 회차 목록 수집이 순위 조회로 채운 30곳을 통째로 덮어썼다.
조회 직후 export 된 bidresult 만 살아남았던 것. → `rask` 가 있으면 corps·rq·nrank·rask·win·amt·rate·bno·ceo 를 지킨다.
**교훈: 힘들게 받은 값이 «다음 수집»에 지워지지 않는지, 보존 목록을 볼 것. 어제 A값에서도 같은 함정(«기초금액 있으면 건너뛰기»)이 있었다.**

### 만든 것
- `build_json.load_rank_history(p50)` — store/first.json 의 개찰별 corps(낮은 순 30곳)를 **업체별**로 뒤집는다.
  rec = [공고명, 날짜, 기관, 내등수, 총참가, 내투찰률, 내금액, baro]
  baro = [등수 | 0(실격) | −1(30위 밖), 바로투찰금액] — 그 개찰의 확정 예정가격으로 하한 L 을 구해
  권장금액 M 이 L 미만이면 실격, 아니면 corps 중 M 보다 낮게 쓴 유효 건수+1. `_baro_amount` 는 화면과 같은 식.
- corp 집계에 `rank`(기록 있는 업체만) · overview 에 `rankPool`(분모 = 순위 받은 개찰 수).
- `Analysis.jsx` `RankHistory` — «순위를 받은 개찰 N건 중» 을 항상 앞세운다. 30위 밖은 «없다»고만.
  요약(30위 안 든 개찰·1순위·등수 중앙·바로투찰이었다면) + 판정문 + 개찰별 «N곳 중 K위 / 바로투찰 M위·실격·계산 안 함».
  판정문: 바로투찰 등수가 내 등수보다 좋은 건수(better) vs 나쁜 건수(worse) 로 «권장보다 높게/낮게 쓰는 편».

### 비용
`rank` 는 기록이 **있는** 업체에만 붙인다 — 빈 값이라도 수만 업체면 1MB. 분모는 overview 에 한 번.
사용자는 검색한 업체가 든 묶음(200곳) 하나만 받으므로 회당 전송량은 그대로.

### 지금 상태
순위 자료가 로컬엔 0건(버그로 지워짐). 고친 collect 가 돌기 시작하면 하루 1,640건씩 쌓인다.
그 전엔 화면이 정직하게 «순위 받은 N건 중 30위 안에 없습니다 · 2026-09-02 부터 받기 시작» 을 보여준다.
fixture 로 확인: 4건·1순위 1·바로투찰 3위/2위/실격/계산안함·판정문 정상.

## 🔢 「바로투찰하고 1순위 채점에서 권장투찰가 금액이 달라」 — 2026-09-03

소장님이 맞았다. 같은 공고인데 두 화면의 «우리 금액» 이 달랐다. 원인은 둘, 실측으로 확인했다.

### 원인 ① 채점이 공고서의 낙찰하한율을 안 봤다
개찰 저장소(first)에는 `llr`(낙찰하한율)·`est`(추정가격)가 **0건**이었다 (11,542건 중 0). 공고(live) 쪽에만 온다.
그래서 바로투찰 화면은 공고서 값(예: 87.675%)으로, 채점은 규모 추정(87.745%)으로 계산했다.
공고서 값이 규모 추정과 다른 공고: **6,212건 중 128건(2.1%)**, 최대 **3.7%** 차이 (SILO 창고 1.6억 · 86.245%).
→ `collect.py` 가 공고→개찰로 `llr·est·ptot·pdrw` 를 이어 붙인다(조달청 호출 0번). `bidresult.json` 끝에 `llr, est` 두 칸.
→ `BaroBid.jsx` 채점이 공고 화면과 같은 순서로 하한율을 고른다: 공고서 값(60~100만 믿음) → 없으면 규모.

### 원인 ② «금액 → 투찰률(올림) → 금액» 을 채점만 안 했다
바로투찰 화면과 원클릭은 recommend() 금액을 투찰률(소수 3자리 **올림**)로 바꿨다가 다시 금액으로 만든다 —
사용자가 복사해 가는 건 그 금액이다. 채점은 recommend() 금액을 그대로 썼다.
실측 9,646건 **전부** 달랐다 (중앙 475원 · 최대 96만원). 원클릭에서 2,354원 어긋났던 것과 같은 함정의 세 번째 사본.
→ `bidmath.js` 에 **`shownBid(base, p50, recAmt)`** 하나를 두고 바로투찰 화면·원클릭·채점·검사(checkmath.mjs)·
  시뮬레이션(build_sim)·순위기록(_baro_amount) 이 전부 이 길을 간다. **다른 데서 이 «한 번 돌아가기» 를 다시 적지 말 것.**

### 검사
`selfcheck.py` 에 «공고서 하한율(86.245)» 시나리오를 넣었다. 채점이 규모 추정으로 되돌아가면 3.7% 어긋나 잡힌다.
`tools/bidmath.py` 의 `score()` 도 `shown()` 을 거친다. `--browser` 13가지 + 등급 32가지 통과, 한글 폴더에서도.

**교훈: 같은 값을 «만드는 길»이 세 곳에 따로 적혀 있으면, 하나를 고칠 때 나머지는 반드시 어긋난다.
함수 하나로 모으고, 검사에는 «그 값이 달라지는 입력»(공고서 하한율)을 반드시 넣는다.**

## ✂️ 구인구직 — 워크넷 채용·자격훈련 갈래를 뺐다 (2026-09-03 밤, 소장님 결정)

소장님: 「워크넷 건설채용은 안 되는 거잖아. 그럼 삭제하자」 → 「구인구직으로 하고, 워크넷 건설채용하고 자격·훈련은 삭제하자」

- 채용정보 API(210L01)는 기업회원 전용이라 개인회원 키로는 0건. 크롤링은 안 하기로 했으니 «될 수 없는 갈래»였다.
- 훈련과정(310L01)은 되는 API 였지만 소장님이 함께 빼기로 했다. 한 번도 채워진 적이 없었다
  (`--coursesonly` 를 안 돌렸고, Actions 엔 WORKNET_API_KEY 도 없었다).
- 뺀 것: `collect.py` 의 워크넷·훈련 수집 전부(`--jobs/--jobsonly/--courses/--coursesonly`), `Jobs.jsx` 의 두 갈래,
  `data.js` 의 getJobs/getCourses. 구인구직 탭은 «직접 올린 글» 하나다.
- 남아 있는 찌꺼기(소장님이 지워도 됨): `tools/worknet.py`, `data/store/{jobs,courses}.json`,
  `web/public/data/{jobs,courses}.json`, `.env` 의 `WORKNET_API_KEY`. 코드는 아무것도 안 읽는다.
- 위 「🏛 워크넷 건설 채용」「🎓 채용정보는 기업회원 전용」 절은 기록으로 남긴다. 다시 붙이자는 제안은 하지 말 것.

## 🚨 「공고하고 1순위가 갑자기 다 사라졌지? 이틀 정도만 있어」 — 2026-09-03 16시, 내 사고

자료는 그대로였다(개찰 11,541건 · 07-20~09-03). **화면이 쪽수를 «받아 둔 것»에서 셌다.**
「미리 다 받기」를 끊은 뒤(전송량 1.49MB → 95KB), 검색을 안 할 때는 첫 묶음 500건만 받는데
`count = total ?? all.length` 가 500 → **25쪽에서 끝**. 500건 ≈ 개찰 이틀치. 26쪽부터 묶음 1을
받아야 하는데 쪽수 자체가 25까지라 갈 수가 없었다. 소장님이 «이틀치만 있다»고 정확히 본 그 모습.

원인 파일: `useBoard.js`(total 이 검색 중에만 있었음) + `FirstBoard.jsx`·`LiveBoard.jsx`(all.length 로 셈).
→ `total` 은 검색이 아니어도 목록표(meta)의 n. 검색이 아닐 때도 쪽마다 필요한 묶음을 번호로 찾아 그리고,
  없으면 `pageReady=false`(불러오는 중). `done` 은 «검색 중이면 색인이 왔나»로 바꿨다.
실제 자료로 확인: 1순위 **1/578 쪽**, 25번 넘겨 26쪽 = 묶음1의 0번째 줄과 글자까지 일치, 받은 묶음은 0·1 둘뿐.
공고 1/632. 자바스크립트 오류 0.

**왜 못 잡았나: 전송량 검사에서 「가만히·검색·3쪽 넘김」만 봤다. 3쪽은 500건 안이다.
«마지막 쪽»을 안 눌러 봤다.** 목록을 쪼개 놓았으면 검사는 반드시 «쪼갠 경계 너머»(26쪽)를 밟아야 한다.

## 🧹 채점 화면 전체 재검토 — 2026-09-03 (소장님: 「옆에는 실격이라 쓰고 1순위라고 쓰고… 이상해」)

문경역 파크골프장(실격) 캡처와 다른 공고(1순위) 캡처를 놓고 화면을 통째로 다시 봤다. 잘못이 셋이었다.

1. **위 띠의 «그때 권장했을 값 89.36%»** — 이건 권장값이 아니라 «우리 금액 ÷ 확정 예정가격»(사후 투찰률)이다.
   권장은 예정가격을 모를 때 낸다. 이름이 틀렸고, 판정 없이 숫자만 있어 카드와 따로 놀았다.
   → 띠 오른쪽은 **판정 하나**(⛔ 실격 / 🏆 1순위 / 📉 밀림) + 우리 투찰률·1순위 투찰률.
2. **② 칸의 «②−① = −781만 (①보다 쌈)»** — 실격 개찰에서도 «더 쌌다» 라고 적혀 «이겼다» 로 읽혔다.
   하한 아래면 싼 게 아니라 «없는 금액» 이다. → 「③보다 774만 모자람 → 실격」 처럼 판정과 이유를 한 칸에.
   카드 제목도 「채점해 드립니다」 대신 판정문(「…이면 실격이었을 자리」).
3. **진짜 모순 — 1순위 개찰에서 등수 칸이 「1순위가 아니었습니다 · 등수는 알 수 없습니다」** 를 띄웠다.
   등수 칸이 사다리(rq)가 있을 때만 1위를 말하고, 없으면 «모른다» 로 떨어졌다. 1순위 판정(beat)은 사다리와
   무관한데도. → beat 면 무조건 1위. 검사 303번이 이걸 잡도록 «1위» 포함·«아니었습니다» 불포함을 본다.
   곁들여: **사다리에는 실격(하한 미만) 투찰이 섞여 있다.** 검사 304번은 50위까지 전부 하한 아래인데
   «50~100위» 라고 했다. 하한 아래 칸 수를 빼서 «2~50위». `tools/bidmath.py` 의 `rank_bracket` 도 같은 규칙.

지운 것: 「실측 802건 · 실격 47.1% · 딴 금액 29.7억」 열과 문단(소장님: 「이런 말이 필요해?」), «확정 예정가격이란» 문단,
«✅ 같은 함수로 계산했습니다 … 같은 코드를 씁니다» 상자(개발자 말), 중복된 «사정률은 추첨…» 문장, «공짜 점심은 없습니다».
남긴 것: ①②③ · 세 금액 표(한 줄 각주) · 판정 한 문장 · 「사정률이 이 값이었다면」 표(제목에 «이번 개찰은 실격/통과» 추가).

**교훈: 판정이 있는 화면은 «판정을 한 번만, 맨 위에» 적고 나머지 칸은 전부 그 판정과 같은 말을 해야 한다.
칸마다 따로 계산해 따로 말하면 언젠가 서로 반대말을 한다. 그리고 그건 검사에 넣어야 한다(303번).**

## 🧯 사이트 1순위가 «35쪽(691건)» — GitHub 캐시가 빈 저장소를 물려주고 있었다 (2026-09-03 17시)

소장님 PC 는 11,541건인데 사이트는 691건(08-26~09-03). 화면 수정(#56) 뒤에도 그대로.
소장님 크롬으로 Actions 로그를 직접 열어 봤다: 「Cache hit for restore-key: store-33718857459 · **Cache Size: ~0 MB (532 B)**」.
회차 사이에 넘겨주는 `data/store` 캐시가 빈 저장소였고, 매 회차 그 빈 것을 이어받아 2일치만 얹고 다시 캐시했다.
**한 번 비면 영영 비는 구조** — 어느 회차에서 처음 비었는지는 로그(로그인 필요)로 다 못 좇았다.

### 방어 (워크플로는 보호 파일이라 collect.py 안에서)
1. **씨앗** `data/seed/{first,live}.json.gz` (소장님 PC 70일치 압축 4.4MB, git 에 한 번 커밋).
   `load_store` 가 저장소 건수 < 씨앗의 절반이면 씨앗을 깔고 지금 것을 덮어 합친다. 691건 사고를 그대로 재현해 확인.
   ⚠️ 기준을 «200건 미만» 같은 고정 숫자로 두면 691건은 못 잡는다. 씨앗 대비 비율로.
2. `save_store` 가 1,000건 넘는 저장소를 절반 미만으로 줄이는 저장을 **거부**한다 (빈 것으로 덮어쓰기 방지).

씨앗은 «복구용»이지 «최신»이 아니다. 저장소가 정상이면 안 읽는다. 몇 달 뒤 씨앗을 갈아 끼울 필요는 없다(복구 뒤 2일치씩 다시 쌓인다).

## 🎲 추첨번호와 낙찰의 관계 — 없다 (2026-09-03, 실측 1,451건)

소장님: 「클릭하는 번호에 따라 달라지는 거 아닌가?」 「찍은 번호와 낙찰 사이 관계 여부 확인해 줘. 사실대로.」
`--reranks 1500` 으로 개찰 1,451건의 **전체 투찰자 번호 분포(drw)** 와 낮은 30곳의 번호(corps[5],[6])를 받아 대봤다.

- 번호별 인기: 1~15번 **전부 6.6~6.7%** (637,884표). 몰리는 번호가 없다. 사실상 무작위로 찍는다.
- 낙찰자(1위)의 번호 2개가 «뽑힌 4개» 에 든 개수: 실제 **1.003** vs 같은 개찰에서 무작위로 찍었을 때 **0.967** (몬테카를로 5회/건).
  2위 이하: 실제 0.838 vs 무작위 0.835. **등수와 번호 사이에 관계가 없다.**
- ⚠️ 처음 셈에서는 «1위 1.00 → 10위+ 0.80» 으로 내려가는 기울기가 보여 관계가 있는 줄 알았다.
  **무작위 기준선도 똑같이 내려갔다** — 동률(선택 집합 평균 5.01개)과 참가업체수가 등수마다 다르기 때문.
  기준선을 «전체 평균 하나»로 두면 틀린다. 반드시 **같은 개찰·같은 표 집계에서** 무작위를 만들어 견줘야 한다.
- 결론: 번호는 낙찰과 무관하다. 시간을 쓸 자리가 아니다. 쓸 자리는 «어느 공고에 넣느냐»(참가업체수)다.
- 업체 자가진단에 번호를 넣지 않는다 — 관계가 없는데 넣으면 있는 것처럼 읽힌다.

## 🎯 「실격이 더 되더라도 1건이라도 가능성 있게 해야 하지 않나」 — 안 된다, 실측 8,406건 (2026-09-03)

소장님: 「한 건이라도 돼야 소문이 나지. 가능성이 거의 제로. 너무 차이가 많이 나.」
사정률 분위(K)를 −1.0(16분위) 부터 1.63(95분위) 까지 훑었다. A값 아는 개찰 8,406건.

| K | 실격 | 1순위 |
|---|---|---|
| −1.0 | 83.8% | 3.45% |
| 0 (=최저) | 49.8% | 3.99% |
| 0.25 | 40.2% | 4.37% |
| 0.5 | 30.6% | 4.31% |
| **0.674+0.3% (권장)** | **13.3%** | **4.12%** |
| 1.63 (95분위) | 4.2% | 3.56% |

**공격적으로 내려도 1순위율은 3.5~4.4% 에서 안 움직인다. 실격만 14% → 84% 로 는다.**
구조 때문이다: 60곳이 흩뿌리면 누군가는 하한 바로 위에 있다. 우리 한 점은 그 아래(실격)거나 그 위(밀림)다.
이길 수 있는 자리는 «1순위가 하한에서 떠 있는 개찰» = 참가가 적은 개찰뿐이다:

| 참가 | 1순위(권장) | K 를 바꿔도 |
|---|---|---|
| 2~9곳 | **18.2%** | 17.7~20.5% |
| 10~29곳 | 5.5% | 5.5~7.0% |
| 30~99곳 | 2.9% | 1.5~2.9% |
| 100곳+ | 1.6% | 0.7~1.6% |

→ «한 건»을 빨리 만드는 길은 금액이 아니라 **공고 선택**. 참가 2~9곳 공고에 5건 넣으면 1건 이상 딸 확률 63%, 10건이면 87%.
   300곳 넘는 공고에 10건 넣으면 11%. 그래서 「공고를 고르는 도구」를 만든다 (기관 과거 참가 중앙 <10 → 실제 <10 적중 72%).

### 채점 = 바로투찰 점검 (실제 자료)
개찰·공고 양쪽에 다 있는 4,695건 중 계산 가능한 **4,626건 전부 금액이 일치** (채점 경로 vs quickBid). 0건 차이.
곁들여: 개찰 줄엔 기초금액이 없는데 공고 줄엔 있는 것이 77건 → collect.py 의 공고→개찰 이어붙이기에 base·lo·hi·aval·ayn·aparts·gmtrl·lic 추가.

## 🎯 자리 찾기 — 공고 고르는 도구 (2026-09-03 저녁)

소장님: 「공고 도구 만들어 줘」. 실측 결론(금액은 꼭대기, 움직이는 건 참가업체수)을 화면으로 옮긴 것.

### 재료 — `collect.py pick_stats(fstore)` (개찰 저장소 70일치만, 호출 0번)
- `enp` 기관별 참가업체수 중앙(6건 이상일 때만) → bidindex 줄에 `enp`·`enpn` 칸. 실측: 중앙<10 이면 실제<10 이 72%.
- `pick.tbl["s{규모}n{참가}"] = [건수, 1순위율%]` — 규모 4단(1억·3억·10억) × 참가 4단(10·30·100) 칸별로
  권장 금액(shownBid 와 같은 길)을 대본 실측. 15건 미만 칸은 null. bidindex 메타 `pick` 에 실림.
- bidindex 칸: `…, "rebid", "enp", "enpn", "dt"` (뒤에 붙임).

### 화면 — `LiveBoard.jsx` 「🎯 자리 찾기」 모드
- 켜면 7주치 묶음 대신 **bidindex(마감 전 · 계산 가능)** 로 목록을 만든다. 검색·지역·면허·A·B 거르기 그대로.
- 정렬: 확률 순 / 기대액 순 / 마감 순. 토글: 참가 적은 공고만(예상 10곳 미만).
- 카드 줄 `.pickline`: 「예상 참가 N곳(기관 최근 M건 중앙) · 이런 자리 1순위 x%(실측 n건) · 기대 ₩(=1순위율×권장금액, 높을수록 좋음)」
  없는 숫자는 만들지 않는다: 「예상 참가 모름(기관 6건 미만)」 · 「이런 자리 실측 부족(15건 미만)」.
- `bidmath.js pickOdds(r, pick, qbAmt)` 가 칸을 찾는다. 표 계산은 화면에서 하지 않는다.

### bidindex 읽기를 하나로 — `data.js indexRows(idx)` + `getBidIndex()`
BaroBid 는 자리 번호(a[8]…)로, Spot 은 이름표로 읽고 있었다. 칸을 붙이면 한쪽만 어긋나는 구조.
→ 세 화면(바로투찰·자리찾기·분석)이 `indexRows` 하나로 읽는다. `selfcheck.check_bidindex` 는 이제
  «f 에 화면이 쓰는 26칸이 다 있나 + 어디서도 자리 번호로 읽지 않나» 를 본다.

### 확인
실제 자료(공고 12,625 · 개찰 11,637)로 브라우저 확인: 마감 전 계산 가능 1,359건 · 확률 순 1위 「뒷골천 정비공사」
(이천시, 예상 2곳 · 25.8% · 기대 1.7억) · 기대액 순 1위 16.2%·35.3억 · 참가 적은 공고만 32건. JS 오류 0.
selfcheck --browser 13 + 등급 32 통과, 한글 폴더 통과. 채점=바로투찰 실자료 4,626/4,626 재확인.

⚠️ 「예상 참가 1곳」 이 뜨는 기관은 단독 입찰이 잦은 곳(면허 제한). 표는 참가 2곳 이상 개찰로 만든 것이라
   그 칸(2~9곳)의 1순위율을 보여준다. 단독이면 하한 근처가 아니어도 되지만, 그 면허가 있어야 들어간다.

## 🎲 「실제로 실격인지는 결과를 봐야 알지」 — 맞다, 화면이 단정하고 있었다 (2026-09-03 밤)

바로투찰 «직접 입력» 에서 하한보다 0.014%p 낮은 투찰률을 넣으니 「낙찰하한 미달 — 이대로 넣으면 실격입니다」.
소장님: 「실제로 실격인지 아닌지는 모르는 거잖아? 결과를 봐야 알지.」 — 맞다.
그 판정은 «사정률이 중앙값(99.893%)이면» 의 얘기였다. 금액은 고정이고 하한은 사정률로 움직인다.
M ≥ L(s) ⇔ s ≤ s*. 사정률이 s* 아래로 나오면 같은 금액도 통과한다.

→ `bidmath.js` `breakEvenSj` · `normCdf` · `passProb` — 경계 사정률 s* 와 «그 아래로 나올 확률».
→ 판정문: 「사정률 중앙 기준으로는 하한 아래 · 0.014%p 부족 · 사정률이 99.878% 아래로 나오면 통과 →
   이 금액이 살아남을 확률 약 49% (실격 확률 약 51%). 결과는 개찰 때 추첨이 정합니다.」
   통과 쪽도 「…를 넘게 나오면 실격 → 살아남을 확률 약 65%」 로 같은 꼴.
   검증: 하한 딱 = 50.0% · +0.3% 여유 = 65.3% · +1% = 90.5% · −0.5% = 25.6% (정규분포·σ 0.768).

**교훈: «실격» 은 사후 판정이다. 사전에는 «확률» 로만 말할 수 있다. 채점 화면의 「실격이었을 자리」 는 사후라 단정해도 되지만,
투찰 전 화면에서 「이대로 넣으면 실격」 은 틀린 말이다.**

## 🎚 분위 다이얼 + 공고별 자동 분위 (2026-09-03 밤)

소장님: 「75분위는 너무 안 맞아」 → 「분위를 고정하지 말고 공고마다 선택하게. 실격이다 아니다는 확률로.」
→ 「클로드가 과거자료로 공고별로 분위를 다르게 해주고 설명도. 원클릭 가능하게.」

### 실측 (8,406건, 여유 0%) — 분위별 실격률은 분위와 정확히 맞아떨어진다
50→49.8% · 60→40.1 · 70→29.8 · 75→25.0 · 80→20.3 · 90→9.8 · 95→4.0 (정규분포 가정이 맞다는 증거).
1순위율은 3.6~4.4% 로 전 구간 평평 — 분위는 «살아남는 빈도» 만 정한다.

### 그런데 참가 묶음별로 보면 최적 분위가 다르다
| 예상 참가 | 최고 분위 | 1순위 / 실격 | 권장(75+0.3%) |
|---|---|---|---|
| 2~9곳 (638건) | **80분위** | 22.1% / 13.3% | 18.2% / 7.8% |
| 10~29곳 (1,213건) | **60분위** | 6.9% / 37.6% | 5.5% / 10.2% |
| 30곳+ (5,016건) | 권장 | — | 최고 또는 동률 |
차이는 2σ 안팎(±1.6%p). «확실» 이 아니라 «자료가 가리킨다». 그래서 화면에 근거 숫자를 그대로 적는다.

### 만든 것
- `bidmath.js`: `QTILES`(50~95 z·실측) · `quantileBid` · `AUTO_RULE` · `autoRule(enp)` · **`smartBid`**
  (예상 참가 알면 자동 분위, 모르면 권장). `quickBid` 는 이제 smartBid 를 부른다 → **원클릭 카드·바로투찰·채점이 같은 금액**.
- `BaroBid.jsx`: 「자동 N분위」 칩(예상 참가 알면 기본) + 「사정률 분위를 직접 고르기」 다이얼(50~95, 칸마다 실격·1순위 실측,
  브라우저에 기억 `kcm_qtile`) + 자동 근거 상자(`.autobox`: 예상 참가·근거 건수·같은 자리 실측 vs 권장).
  판정 칸은 «살아남을 확률 약 N%» (passProb). 채점도 `res.enp` 로 smartBid → 그날 화면과 같은 금액.
- `collect.py`: `bidresult` 에 `enp, enpn` 두 칸 · `export_board("live", …, enp_map)` 으로 공고 묶음 줄에도 enp ·
  `pick_stats` 의 1순위율 표는 자동 규칙(`PICK_AUTO`)으로 낸 금액으로 잰다(화면이 실제로 내는 금액과 같아야 하므로).
  ⚠️ `PICK_AUTO`(collect.py) 와 `AUTO_RULE`(bidmath.js) 는 같은 규칙이다. 한쪽만 고치면 표와 화면이 어긋난다.

### 확인
실자료 채점=공고 4,626/4,626 (자동 분위 포함) · selfcheck --browser 13+32 · 실제 공고 두 건(예상 7곳→자동 80분위 90.855% ·
살아남을 확률 80% / 예상 14곳→자동 60분위 · 60%) 브라우저 확인 · 자리 찾기 1,359건 정상.

## 🎚 분위 다이얼 재배치 — 칸에는 숫자만, 설명은 칸 아래 (2026-09-03 밤)

소장님이 **세 번** 같은 것을 지적했다: 「글자가 튀어 나왔어」 → 「여전히 겹쳐 있어」 →
「다른 방식으로 배열해서 하면 안돼. 25분위부터. 그래서 아래에 멘트를 적고…」

### 내가 두 번 헛짚은 것
칸 안에 설명 두 줄(「살아남을 확률 26%」·「1순위 3.6%」)을 넣어 놓고
**칸을 넓히는 쪽으로만** 고쳤다 — 1차 nowrap+말줄임, 2차 grid 4칸/2칸 + 줄바꿈.
내 스크린샷에서는 둘 다 멀쩡해 보였다. 그런데 소장님 화면은 아니었다.
글꼴·확대율·브라우저가 다르면 «칸 안에 두 줄» 은 언제든 다시 넘친다.
**칸 크기를 조절해서 글자를 가두려는 시도 자체가 틀린 방향이었다.**

### 고친 방식 — 배열을 바꿨다
- 칸에는 `<b>25분위</b><span>89.782%</span>` **둘만**. 위쪽 권장/최저/중간 칸과 같은 모양이라
  이미 검증된 배치다. 넘칠 글자가 없다.
- 고른 칸 **하나**에 대해서만 칸 아래 `.qnow` 상자에 적는다:
  「25분위 — 투찰률 89.782% / 살아남을 확률 26% · 실격 74.3% · 1순위 3.6% / 실제 개찰 8,406건 실측」
  여덟 벌을 동시에 보여줄 이유가 없었다. 사용자는 «지금 고른 것»만 알면 된다.
- 일반 설명은 그 아래 `.note` 한 문단.
- 칸 배치는 넓은 화면 4칸, 420px 미만 3칸 (한 줄에 「25분위/89.782%」 두 줄만 들어가면 되므로 3칸이 편하다).

### 검사에 넣은 것 — «칸 밖으로 나갔나» 를 눈이 아니라 기하로 본다
스크린샷을 내가 보는 것으로는 두 번 놓쳤다. 이제 Playwright 로 각 칸의
`getBoundingClientRect()` 를 재서 **자식 요소가 칸의 좌우를 넘어가면 잡는다**
(`scrollWidth > clientWidth` 도 함께). 1400px·390px 둘 다 `OVERFLOW: none` 확인.

**교훈: «내 스크린샷에서 괜찮다» 는 확인이 아니다 — 한글 폴더 때와 같은 잘못이다.
그리고 사용자가 같은 것을 두 번 지적하면 «더 잘 고치는» 게 아니라
«설계를 바꾸라» 는 뜻이다. 세 번째에 소장님이 직접 배치를 알려줬다.**

## 🎨 바로투찰에 색 — 「좀 튀게. 단, 잘 보여야 해」 (2026-09-03)

소장님 요청. 고른 방향은 **«장식»이 아니라 «위험도»에 색을 입히기** 였습니다.
금액 카드(파랑)는 화면의 얼굴이라 그대로 뒀습니다 — 판정마다 색이 바뀌면 사이트 느낌이 흔들립니다.

### 칠한 곳
| 자리 | 전 | 후 |
|---|---|---|
| 권장·최저·중간 칩 | 전부 흰색 | 최저 빨강 · 중간 주황 · 권장 초록 |
| 분위 칸 8개 | 전부 흰색 | 25·40·50 빨강 · 60·70 주황 · 80·90·95 초록 |
| 판정 상자 | 글자만 | **살아남을 확률을 34px 큰 숫자**로 + 확률에 따라 빨강/주황/초록 |
| 등급 상자 | B 가 회색 | A 초록 · B 파랑 · C 주황 · D 빨강 |

칠하는 법: 안 고른 칸은 `color-mix(--tint 55%, --surface)` 로 아주 옅게 + 위 4px 띠,
고른 칸만 `--tint` 가득 + 테두리 + 바깥 고리. **진한 배경은 안 씁니다** — 애드센스가 붙는
화면이라 카드마다 진하게 칠하면 광고와 섞여 오히려 안 읽힙니다.
색은 전부 토큰이라 다크모드가 따라옵니다(실측 확인).
**적록색맹(남성 약 5%)** 때문에 색만으로 말하지 않습니다 — 확률을 항상 글자로 같이 적습니다.

### 소장님 스크린샷이 잡아낸 내 실수
「최저 90.297%」 와 「50분위 90.297%」 는 **같은 값인데 색이 달랐습니다.**
칩은 이름으로(`RISK={limit:'r-hot',safe:'r-warm'}`), 분위 칸은 실측 실격률로(`dq>=50`) —
**위험도를 두 군데서 따로 계산**하고 있었습니다. dq 49.8 이 경계 바로 아래라 갈렸습니다.
→ `riskTone(p)` 하나로 모으고, 색의 근거를 **살아남을 확률(passProb)** 하나로 통일했습니다.
   경계도 판정 상자와 똑같이 55% · 80%. 브라우저 검사가 12칸을 전부 눌러
   «칩 색 ↔ 상자 색» 이 같은 말을 하는지 봅니다 (11/11 일치, 같은 투찰률 다른 색 0건).

**또 같은 잘못입니다** — CLAUDE.md 「같은 규칙을 두 번 적었으면 두 벌이 같은지를 검사에 넣는다」.
이번엔 계산식이 아니라 **색**이었습니다. 색도 규칙입니다.

### 곁들여 잡은 것 — 한 상자가 서로 반대말을 하고 있었다
살아남을 확률 50% → 상자는 빨강인데, 바로 밑에 **「✅ … 통과」** 가 초록 체크로 떴습니다.
「중앙 사정률 기준 통과」는 판정이 아니라 **기준선**인데 판정문처럼 적혀 있었던 것.
→ ✅/⚠️ 를 떼고 「기준선 — 사정률이 중앙(99.896%)으로 나오면 하한은 90.247%, 이 금액은
   그보다 0.737%p 위입니다」 로 바꿔 한 단계 눌렀습니다. 판정은 위의 큰 숫자 하나뿐입니다.

### 이름을 나눈 것
분위 칸 아래 `.qnow` 의 「살아남은 비율 96%」(실측 8,406건)와 판정 상자의 「살아남을 확률 95%」
(이 공고의 A값·하한율로 계산)는 **다른 숫자**입니다. 같은 이름을 붙이면 «왜 다르냐» 가 됩니다.
→ 「실측 살아남은 비율」 / 「살아남을 확률」 로 이름을 나누고, qnow 상자는 중립색으로 뒀습니다.

## 🕳 새는 독을 먼저 막았다 — 모든 주소가 «같은 빈 껍데기» 였다 (2026-09-04)

소장님: 「이제 홍보를 해야해. 획기적인 걸로」 → 「뻔한거 말고, 클로드가 할 수 있는 걸로」

홍보 방안을 짜기 전에 실측부터 했다. **결과가 전제를 바꿨다.**

| | 홈 | /agency/경상북도 경주시 |
|---|---|---|
| 크기 | 2,161 B | **2,161 B — 바이트까지 같음** |
| `<title>` | K-건설맵 — 바로투찰… | **똑같음** |
| description | 공고를 고르면… | **똑같음** |
| 본문 글자 | 없음 | **없음** |

서치콘솔에 낸 308개 주소가 구글에는 «제목·설명·본문이 전부 같은 페이지 308개» 로 보인다.
CLAUDE.md 에 「사이트맵 과다 제출 탓」이라고 적어 뒀는데 **그게 원인이 아닐 가능성이 크다.**
그리고 **네이버는 자바스크립트를 거의 안 돌린다** — 고객인 건설 소장님들이 쓰는 게 네이버인데
네이버에게 이 사이트는 빈 페이지였다. 카톡 링크 미리보기도 전부 같았다.

**카페에 글을 쓰든 블로그를 하든, 이걸 안 고치면 그 노력이 한 번 쓰고 사라진다.**

### ① prerender.py — 주소마다 진짜 HTML 을 굽는다
빌드가 끝난 `web/dist` 안에 `agency/{기관}.html` · `corp/{업체}.html` · 탭 페이지를 굽는다.
제목·설명·canonical·og 가 주소마다 다르고, 본문에 요약(건수·평균 투찰률·지역·기관·최근 낙찰)이 들어간다.
사람이 열면 React 가 그 자리를 덮어써 지금과 똑같이 보인다.

- Firebase 는 **rewrites 보다 실제 파일이 우선**이고 `cleanUrls:true` 라 `.html` 없이 서비스된다.
  → cleanUrls 를 흉내 낸 시험 서버로 확인: raw 2,161B → **4,899B**, 제목 고유, React 마운트 뒤
    중복 글자 0, JS 오류 0.
- ⚠️ **껍데기에 이미 og:title/og:description/og:url 이 있다.** 새로 «추가» 하면 크롤러가 앞엣것을
  읽어 전부 홈 제목으로 뜬다(처음에 그렇게 만들었다가 잡음). **갈아 끼워야** 한다.
- ⚠️ canonical·og:url 은 **반드시 퍼센트 인코딩**. 「/agency/경상북도 안동시」 처럼 공백이 그대로
  들어가면 canonical 이 깨진다.
- ⚠️ 순서: `npm run build` → `prerender.py` → 배포. build 가 dist 를 비우므로 바꾸면 통째로 사라진다.
- 파일 이름에 `\ / : * ? " < > |` 가 든 이름은 건너뛴다(윈도우에서도 빌드가 돌아야 한다).

### ② /corp/{업체} — 분석 탭에 갇혀 있던 화면에 주소를 줬다
업체 자료 5만 곳이 «분석 탭에서 검색해야만» 보였다. 주소가 없으면 검색엔진도 못 찾고 카톡으로 보낼 수도 없다.
「○○건설 낙찰 실적」 을 찾는 사람은 실제로 있다 — 경쟁사를 보는 사람, 자기 회사를 쳐 보는 사람.

- `Analysis.jsx` 의 CorpTab 을 **검색 상자 + `CorpReport`** 로 갈랐다. 분석 탭과 `/corp/…` 가
  같은 컴포넌트를 쓴다(두 벌로 적으면 어긋난다).
- 주소에는 **정규화된 이름만** 쓴다. 사업자번호로 갈라 놓은 키(«이름#번호»)는 URL 에 넣지 않는다 —
  남의 사업자번호가 검색결과에 남는다. 그 갈래는 화면 안에서만 누른다.
- `build_json.py` 가 `corp/top.json` 을 낸다(건수 상위 `CORP_TOP=6000`, 3건 미만 제외).
  낙찰 1건짜리까지 페이지를 만들면 «내용 없는 페이지» 수만 개가 색인에 해롭다.

### ③ 사이트맵은 천천히
`SITEMAP_CORPS=300` 부터. **`SITEMAP_CORPS` 는 `PRERENDER_CORP` 보다 크면 안 된다** —
안 구운 주소를 사이트맵에 내면 크롤러가 다시 빈 껍데기를 본다.
색인이 붙는 것을 서치콘솔에서 확인하며 올릴 것.

### 아직 안 한 것 (소장님이 «나중에» 로 정함)
카톡 미리보기 **그림**(og:image). 업체명·건수·투찰률이 박힌 카드 이미지를 상위 3,000곳만 굽는 안.
글자(제목·설명)가 먼저 효과를 내는지 보고 정한다.

### 교훈
**홍보 방안을 묻기 전에 «지금 사이트가 크롤러에게 어떻게 보이는지» 를 먼저 재야 한다.**
raw HTML 을 받아 보는 데 1분 걸렸고, 그 1분이 답을 통째로 바꿨다.

### ④ 사이트 안에서 그리로 «가는 길» 이 없었다 (같은 날 이어서)

소장님: 「사이트에서는 어떻게 보는거야」 — 못 봅니다. 절반만 만들어 놨던 것.
`/corp/…` 도 `/agency/…` 도 **사이트 어디에도 링크가 없었습니다.** 검색으로 들어온 사람만 봤습니다.
분석 탭에서 업체를 검색하면 같은 화면이 나오지만 주소는 `/analysis?m=corp` 그대로라
그 화면을 카톡으로 보낼 수가 없었습니다 — 받은 사람은 빈 검색창만 봅니다.

→ 분석 탭은 **검색만** 하고, 고르면 전용 주소로 «이동» 합니다.
  화면을 두 벌로 그리지 않습니다 — 결과는 전용 페이지 한 곳에서만 그립니다.
  덤으로 뒤로가기·즐겨찾기가 제대로 되고, 「🔗 주소 복사」 버튼을 붙였습니다.

⚠️ 「이 법인만 보기」(같은 이름 법인이 여럿일 때)는 **주소에 안 넣습니다** — 남의 사업자번호가
   URL·검색결과에 남습니다. 라우터 state 로 넘기고 화면 안에서만 좁혀 봅니다.
   좁혀 본 상태에는 「같은 이름 전체 보기 →」 를 함께 띄웁니다.

브라우저 확인: 「대영건설」 검색 → 추천 28개 → 클릭 → 주소 `/corp/대영건설`,
제목 「주식회사 대영건설 낙찰 실적 — 3년간 147건 · 평균 투찰률 89.72%」, 주소 복사 버튼 있음,
뒤로가기 → `/analysis?m=corp`, JS 오류 0.

## 📋 공고·개찰 한 건마다 주소 — `/notice/{공고번호}` (2026-09-04)

소장님: 「다른 방법은 없어? 사이트 확장이라도… 아니면 다른 방안 줘」

### 왜 이게 가장 큰가
| | 페이지 수 | 검색하는 사람 |
|---|---|---|
| 기관 | 300 | 그 기관에 넣는 업체 |
| 업체 | 3,000 | 경쟁사를 보는 사람 (좁음) |
| **공고·개찰** | **13,000, 매일 +570** | **그 공고에 투찰한 60~300곳 전원** |

개찰 결과는 나라장터에서도 볼 수 있지만 로그인하고 찾아 들어가야 합니다.
「○○공사 낙찰」·「20260812345 개찰결과」 는 실제 검색어입니다.

### ⚠️ 자료를 어떻게 주나 — 여기서 설계가 갈렸다
7주 지난 개찰은 **브라우저가 받을 수 있는 파일에 없습니다**(bidindex 는 마감 전, bidresult 는 7일치).
- (버림) 목록 색인에 공고번호를 넣기 → 색인이 무거워져 검색 전송량이 늘어남
- (버림) 공고마다 작은 JSON 을 따로 굽기 → 파일이 26,000개가 됨
- **(고름) 미리 구운 HTML 안에 그 한 줄을 같이 넣기** — `<script type="application/json" id="ndata">`
  파일을 더 안 받고, 색인도 안 무거워집니다. 크롤러용 요약은 어차피 넣어야 하니 덤입니다.

그래서 카드의 「🔗 이 공고만 보기」는 **일부러 `<a href>`** 입니다(`<Link>` 아님).
정적 HTML 을 통째로 받아야 그 안의 ndata 로 화면이 그려집니다.

### 마감 전 공고에는 개찰 화면을 쓰지 않는다
`NoticeDetail` 을 그대로 쓰면 「낙찰가 −, 투찰률 −」 이 떠서 «개찰했는데 아무도 안 됐다» 로 읽힙니다.
→ 개찰이 끝난 건에만 `NoticeDetail`, 마감 전에는 `OpenNotice`
  (⏳ 아직 개찰 전 · **권장 투찰금액** · 공고 조건 · 면허 · 바로투찰/나라장터 링크).
  금액은 `quickBid` — 공고 카드·바로투찰과 같은 함수입니다.
  ⚠️ 예비가격 정보(ptot·pdrw)가 없으면 금액을 아예 안 냅니다(isReady). 반쯤 아는 값으로 안 내밉니다.

### 규모 — 실측하고 정했다
자료: 마감 전 1,878 · 개찰 11,637 · 배포 1벌 **93MB**.
`PRERENDER_NOTICE=8000`(최신부터) 로 시작 — 장당 4~5KB 이므로 약 +35MB.
`SITEMAP_NOTICES=500`. **사이트맵은 미리 구운 것보다 많이 내면 안 됩니다.**
⚠️ 늘리기 전에 Firebase 콘솔 「출시 저장용량(보관 10개)」 을 확인할 것.

### 확인
시험용 store 2건(개찰 1 · 마감 전 1)로 브라우저 확인 —
개찰: 제목 「… 낙찰 결과 — 주식회사 한라건설 90.984%」, 본문에 기초금액·A값·참가업체수.
마감 전: 「⏳ 아직 개찰 전 · 마감 2026.09.11 · 권장 투찰금액 2,332,519,455원(80분위)」.
JS 오류 0. selfcheck --browser 13+32 통과, 한글 폴더 통과.


## 🖼 카톡 미리보기 그림 + 공유 버튼 자리 — 2026-09-04

소장님: 「또 홍보방안 뭐 있지?」 → 재보고 나서 정했다. **og:image 가 사이트 전체에 0개였다.**
제목·설명은 전날 작업으로 주소마다 달라졌는데, 카톡에 링크를 붙이면 그림 없는 회색 칸만 떴다.
한국에서 링크가 퍼지는 곳은 단톡방이다. 그림이 없으면 아무도 안 누른다.

### 방식은 실측하고 정했다 (1200×630 한 장)
| 방법 | 시간 | 크기 |
|---|---|---|
| RGB → MEDIANCUT quantize | 106 ms | 14.7 KB |
| P모드 직접 그리기 | 14 ms | 9.4 KB (글자가 거침) |
| **고정 팔레트 quantize** | **16 ms** | **15.9 KB** ← 이것 |

하루 21번 도는 배포라 «장당 몇 ms» 가 그대로 21배가 된다. 100ms 짜리는 못 쓴다.
실제 자료 1,155장 22초 · 평균 15.0KB. `OG_NOTICE=2000` 기본(최신 공고·개찰만) — 7주 지난 개찰을
카톡에 붙이는 사람은 없다. 나머지는 기본 카드(/og/default.png).

⚠️ **폰트는 저장소에 넣었다** (`assets/KcmKR-Bold.otf`, Noto Sans CJK KR 부분폰트 1.8MB).
러너에 한글 폰트가 있다는 보장이 없는데 **워크플로는 손으로만 고칠 수 있는 파일**이라
«apt 로 폰트 설치» 를 요구하는 설계를 피했다. 폰트나 Pillow 가 없으면 조용히 «안 만듦» 으로 떨어진다.

⚠️ `web/index.html` 에 og:image 기본값을 박아 둔다. 이게 있어야 «안 구운 주소»도 카드로 뜬다.
prerender 는 «추가» 가 아니라 **갈아 끼운다** (og:title 때 겪은 것과 같은 함정).

### 잡은 것 — 배지가 제목 위로 올라탔다
긴 공고명이 두 줄이 되면서 오른쪽 위 배지와 겹쳤다. 좌표를 손으로 박아 놓은 탓.
→ 자리를 «위에서부터 쌓아» 정하고, 배지는 제목 **위**로 옮겼다.
→ `_fits()` 가 칸 밖·겹침을 **좌표로** 확인한다. 일부러 어긋내니 6장이 잡히는 것까지 봤다.
**분위 다이얼 때와 같은 잘못이다 — 내 스크린샷에서 괜찮은 건 확인이 아니다.**

### 공유 버튼이 «보이지 않는 자리»에 있었다
소장님: 「사이트에서는 어떻게 확인하지 안되는 거야?」 — 있긴 했다. 그런데:
펼친 카드 높이 **2,227px**, 링크 위치 **2,170px**. 두 화면 반을 더 내려야 나왔다.
→ 접힌 카드의 `.foot` 줄로 옮겼다(「🔗 공유」 알약). 목록에서 바로 보인다.
곁들여: `/notice/…` 화면이 자기 자신을 가리키던 것도 없앴다(NoticeDetail 에서 뗐다).

## 📅 「어제의 개찰 성적표」 — /daily/{날짜} (2026-09-04)

사이트가 «찾아와야 보는 곳» 이라 단톡방에 던질 것이 없었다. 개찰은 하루 570건씩 나온다.
날짜마다 한 장이면 ① 매일 퍼갈 거리가 생기고 ② 검색엔진에 «매일 새 글 나오는 곳» 으로 보이고
③ **그 한 장이 그날 개찰 수십 건으로 «가는 길»** 이 되어 크롤러가 안쪽까지 들어온다(사이트맵보다 강하다).

- `daily.py` — 그날 요약 한 덩어리. 낙찰률 중앙/최저/최고 · 참가 중앙/최다 · 100곳+ · 참가 1곳 ·
  가장 치열했던 10 · 금액 큰 10 · 참가 1곳 8 · 두 건 이상 가져간 곳.
- `prerender.py` 가 `/daily/{날짜}` 와 `/daily` 를 굽는다(`PRERENDER_DAILY=45`). 자료는 `<script id="ddata">`.
- `DailyPage.jsx` 가 그린다. **이 화면으로 오는 링크는 `<a href>`** 여야 한다(정적 HTML 을 받아야 ddata 가 온다).
- 1순위 탭 머리줄에 「📅 날짜별 성적표 →」.

⚠️ **굽는 건 «지나가면 안 변하는 것»(그날 개찰)뿐이다.** 「지금 해볼 만한 자리」는 이틀이면 썩으므로
화면이 살아서 그린다 — bidindex + 이미 있는 `pickOdds`·`quickBid`. 규칙을 파이썬에 다시 적지 않는다.

⚠️ 사이트맵의 성적표 날짜는 **개찰(first)에서만** 센다. live 까지 세면 prerender 가 안 구운 날짜를
내게 된다 — 업체·공고에서 겪은 «안 구운 주소를 사이트맵에 내기» 와 같은 함정. `SITEMAP_DAILY=45`.

### 표 한 줄을 배열로 담았다 → 검사에 넣었다
`daily.py` 의 `FIELDS` 와 `DailyPage.jsx` 의 `F` 가 어긋나면 **에러 없이** 기관 자리에 낙찰업체가
그려진다. 그럴듯해서 아무도 못 알아챈다. → `selfcheck.check_daily()`. 일부러 두 칸을 바꿔서 잡히는 것 확인.

## 🧪 검사가 «내 시험 자료» 때문에 틀렸다 — 2026-09-04

`selfcheck --browser` 가 갑자기 10가지 전부 «금액 12,282원 다름» 으로 빨갛게 떴다. 코드는 멀쩡했다.
내가 시험하려고 dist 에 넣은 `overview.json` 의 `sjq.p50` 이 **99.893** 인데
검사기(`tools/bidmath.py`)는 상수 **99.896** 을 쓰고 있었다. 0.003%p × 4억 = 12,270원.

→ `selfcheck.py` 가 **dist 의 overview.json 에서 p50 을 읽어** 기대값과 화면을 같은 값으로 맞춘다
   (다르면 «overview.json 의 sjq.p50 = … 를 씁니다» 라고 찍는다).

**교훈: 같은 값을 두 곳에 적으면 검사도 거짓말을 한다. 검사가 틀리면 코드가 틀린 것보다 나쁘다.**
그리고 «검사가 빨개졌다» 를 코드 탓으로 단정하기 전에 **내가 방금 바꾼 시험 자료**를 먼저 본다.


## 🔔 IndexNow — 새 주소를 검색엔진에 «직접 찔러 넣는다» (2026-09-04)

소장님: 「또 홍보방안 뭐 있지?」 → 확인하고 나서 고른 첫 번째.

사이트맵은 «와서 보세요» 인데 IndexNow 는 «갔습니다» 다. 우리한테 맞는 이유는
**개찰이 하루 570건씩 새 주소로 늘어나기 때문**이다. 지금까지는 크롤러가 스스로 올 때까지 기다렸다.

### 확인하고 적은 것 (짐작 아님)
- **네이버가 2023년 7월부터 지원한다.** 서치어드바이저 등록자가 아니어도 «키만 만들면» 쓸 수 있다.
- 키 규칙: 16진수·숫자·하이픈, 8~128자. 우리 키는 32자리 랜덤.
- 받는 곳 두 군데 — `https://api.searchadvisor.naver.com/indexnow`(네이버),
  `https://api.indexnow.org/indexnow`(공용 → 빙·얀덱스 등). **구글은 안 쓴다**(사이트맵으로 간다).
- POST 본문: `{host, key, keyLocation, urlList}`. 한 번에 최대 10,000개.

### 설계에서 조심한 두 가지
1. **아직 배포 안 된 주소를 알리면 크롤러가 404 를 본다.**
   → 이번에 구운 것을 `data/store/indexnow.json` 에 적어 두었다가 **다음 회차 시작에** 보낸다.
     그때는 이미 배포가 끝나 있다. 덤으로 **워크플로(손으로만 고치는 보호 파일)를 안 고쳐도 된다.**
2. **같은 주소를 하루 여섯 번 다시 찌르면 스팸이다.**
   → `mark`(마지막으로 본 개찰 시각)보다 새 것만 보낸다. `mark` 가 없는 첫 회차는 **0건** —
     11,000건을 한꺼번에 밀어 넣지 않는다. 늘 보내는 건 `/ · /first · /live · /daily` 넷뿐.

### 키 파일이 배포에 안 실리던 것 — 함정 두 개가 겹쳤다
처음엔 `web/public/{키}.txt` 에만 썼다. **안 실렸다.** 원인이 둘이었다.

1. **prerender 는 `npm run build` «뒤»에 돈다.** 빌드는 그 전에 web/public 을 dist 로 옮긴다.
   그래서 web/public 에 써 봐야 그 회차 배포에는 없다.
   → `ensure_key_file(DIST)` 가 **dist 에 직접 쓴다.** 이제 git 에 없어도 매 회차 생긴다.
2. **`.gitignore` 의 `*.txt` 에 걸려 커밋에서 빠졌다** (「3 files changed」 로 드러났다).
   → `!web/public/*.txt` 예외를 넣었다.
   ⚠️ 처음엔 `!web/public/*.txt          # 설명` 으로 적었는데 **그대로 안 먹었다.**
      **.gitignore 는 «같은 줄 뒤에 붙인 주석» 을 주석으로 안 본다.** 주석은 반드시 따로 줄로.

**교훈: «파일을 만들었다» 와 «배포에 실렸다» 는 다른 이야기다.
   그리고 커밋 결과의 «N files changed» 를 그냥 넘기지 말 것 — 거기서 드러났다.**

## 🔗 안쪽으로 가는 링크 — 사이트맵보다 이게 세다 (2026-09-04)

구워 둔 11,000장 중 사이트맵에 낸 건 1,109개뿐이다. 그런데 크롤러는 **링크를 타고** 다니는 것을 더 신뢰한다.
지금까지 페이지들이 서로 이어져 있지 않았다.

이은 것:
- 개찰 페이지 → **발주기관**(`/agency/…`) · **낙찰업체**(`/corp/…`)  ← 11,000장 × 2개, 가장 큰 그물
- 기관 페이지 → 자주 낙찰받은 업체 · 업체 페이지 → 자주 낙찰받은 기관
- 성적표(`/daily/…`) → 그날 개찰 36건 + 두 건 이상 가져간 업체

⚠️ **우리가 실제로 구운 주소에만 건다.** `Links` 클래스가 «구울 목록» 을 굽기 전에 모아 두고,
   거기 있는 것만 링크로 만든다. 없는 주소로 링크를 걸면 크롤러가 빈 껍데기를 보고 색인에 해롭다.
   빌드 뒤 **전수 확인: 내부 링크 687개 · 죽은 링크 0개.**

⚠️ 업체명 정규화는 `build_json.norm_corp` 을 **그대로 가져다 쓴다**(`from build_json import norm_corp`).
   agency 집계의 `corps` 는 표시 이름(「금관건설(주)」)이고 corp 페이지 키는 정규화 이름(「금관건설」)이라,
   여기서 다시 적었으면 조용히 다 어긋났을 것이다.

### 이 작업에서 내가 만든 버그 — 성적표의 업체 링크
`/corp/${w}` 로 **표시 이름을 그대로** 주소에 넣고 있었다(「주식회사 대영건설」).
정규화 이름이 아니면 빈 페이지로 간다. 만든 지 30분 만에 잡았다.
→ 링크를 화면에서 만들지 않는다. **prerender 가 «구운 주소인지» 판정해 ddata 에 실어 준다**
   (`multi = [[업체명, 건수, 주소 or null]]`). 화면은 주소가 있을 때만 링크로 그린다.

### 사이트맵도 같은 함정에 걸릴 뻔했다
성적표 날짜를 세는 코드가 `rows`(공고+개찰)를 보고 있어 **28일치**가 나왔다. 그런데 prerender 는
개찰(`first`)의 날짜만 굽는다 — **2일치.** 안 구운 26개 주소를 사이트맵에 낼 뻔했다.
→ `fonly`(개찰만)로 센다. 업체·공고에서 이미 겪은 「안 구운 주소를 사이트맵에 내기」 의 세 번째다.

**교훈: 「구운 것」 과 「내보내는 목록」 은 반드시 같은 자료에서 세야 한다.
   PRERENDER_* 와 SITEMAP_* 이 짝이듯, 새로 만드는 종류마다 이 짝을 확인할 것.**

## 📌 2026-09-04 하루 정리 — 「홍보 방안」 하나에서 시작한 것

소장님: 「이제 홍보를 해야해」 → 「뻔한거 말고」 → 「또 홍보방안 뭐 있지?」

**답을 내기 전에 실측을 했더니 전제가 바뀌었다.** 모든 주소가 같은 2,161B 빈 껍데기였다.
홍보 방안이 아니라 «새는 독» 을 먼저 막는 일이었다. 그날 한 것:

| | 전 | 후 |
|---|---|---|
| 크롤러가 읽는 페이지 | **0장** (전부 같은 껍데기) | 약 11,300장 (기관·업체·공고·성적표) |
| 카톡 미리보기 그림 | **0개** | 페이지마다 다른 카드 (약 2,900장) |
| 공유 버튼 | 펼쳐서 2,170px 아래 | 접힌 카드에 바로 |
| 매일 새로 생기는 주소 | 없음 | `/daily/{날짜}` 하루 한 장 |
| 검색엔진에 «새 주소» 알림 | 사이트맵뿐(기다림) | 네이버·빙에 직접(IndexNow) |
| 페이지끼리 이어진 링크 | 0 | 687개 (죽은 링크 0) |

### 늘리기 전에 볼 것 (숫자를 올리기 전에 반드시)
- Firebase 콘솔 「출시 저장용량 = 보관 10개」. 배포 1벌이 93MB → 카드 그림으로 약 137MB 가 됐다.
  보관 10개면 1.4GB / 10GB. **이 설정이 풀리면 며칠 만에 막힌다.**
- `SITEMAP_*` 은 언제나 `PRERENDER_*` 이하. (기관 300 / 업체 300 / 공고 500 / 성적표 45)
- `OG_NOTICE=2000` 은 «장당 16ms × 하루 6회» 를 보고 정한 값이다. 올리면 배포 시간이 그만큼 는다.

### 남은 것
1. **구조화 데이터(JSON-LD)** — 개찰 `Dataset` · 업체 `Organization` · 성적표 `Article`+`datePublished`.
   구글에게 «이게 뭔지» 를 알려준다. 아직 안 함.
2. 서치콘솔 「페이지 → 색인 생성됨」 을 2~3주 뒤에 본다. **이번 작업의 성적표는 그것이다.**
3. 색인이 붙기 시작하면 `SITEMAP_*` 을 천천히 올린다.
4. og:image 를 더 예쁘게(로고·업체 색) — 지금은 글자만. 효과를 보고 정한다.

### ⚠️ 원격(리눅스) 쪽에서 git 을 돌리면 «전부 바뀐 것»으로 보인다 — 2026-09-04

`git diff --name-only` 를 마운트된 폴더에서 돌렸더니 **추적 중인 파일 전부**가 나왔다.
zip·csv 까지. 사고인 줄 알았는데 아니었다.

저장소는 index 에 **LF** 로 담고 윈도우 git 이 `autocrlf` 로 CRLF 로 풀어 놓는다.
그런데 내 통로(리눅스 VM)의 git 에는 그 설정이 없어서, **CRLF 작업파일 ↔ LF 블롭**이
전부 다르게 보인다. 소장님 윈도우에서 `git add .` 하면 정확히 바뀐 것만 잡힌다
(실제로 9개 · 10개 · 3개로 맞게 나왔다).

→ **마운트된 폴더에서 git 을 돌리지 않는다**(CLAUDE.md 8장). 굳이 봐야 하면
   `git ls-files --others --exclude-standard`(새 파일 목록) 정도만 믿는다 —
   이건 내용 비교가 아니라 영향을 안 받는다. 실제로 이걸로 키 파일 누락을 잡았다.


## 🔍 «로그를 봐야만 알 수 있는 것» 을 만들면 확인이 막힌다 — 2026-09-04

IndexNow 가 실제로 보냈는지 확인하려는데 **넷 다 막혔다**:

| 길 | 결과 |
|---|---|
| 내 통로에서 직접 POST | 네이버·빙까지 **403** (조달청과 같은 allowlist) |
| 소장님 PC 에서 직접 POST | **똑같이 403** |
| Actions 로그 화면 | 열리지 않음(본문 869자에서 멈춤) · 크롬 확장도 끊김 |
| `api.github.com/…/jobs/{id}/logs` | **403 Must have admin rights** |

**IndexNow 는 Actions 안에서만 나간다. 그런데 결과는 Actions 로그에만 있었다.**
설계가 «확인할 수 없는 자리» 를 만들어 버린 것이다.
→ 결과를 **사이트에 남긴다**: `/data/indexnow.json` (몇 개 보냈고 어디서 몇 번 코드가 왔는지 · 다음 회차 예정 · 키 주소).
   `diag.json` 과 같은 생각. 로그인도 로그 화면도 필요 없다.

**교훈: 내가 못 보는 곳에서만 도는 기능은 «결과를 볼 수 있는 자리» 를 같이 만들어야 한다.**

### 곁들여 — WebFetch 의 15분 캐시에 속을 뻔했다
「발주기관 칸」 수정을 배포한 뒤 확인했더니 **고치기 전 화면**이 왔다. 코드를 의심했는데
주소에 `?v=2` 를 붙이니 제대로 나왔다 — **내 도구가 15분간 캐시한 것**이었다.
어제 「배포가 안 됐다」 며 로그를 뒤진 것과 **똑같은 잘못**(그때는 브라우저 캐시).
→ **배포 직후 확인은 반드시 캐시를 비켜서** (`{cache:'reload'}` 또는 `?v=`).

## 🖱 「클릭이 안되던데」 — 줄의 6%만 눌렸다 (2026-09-04)

소장님: 「오늘 만든것 일자별로 쭉 나오는데, 클릭이 안되던데, 클릭을 하면 뭐가 있어야 하는거 아니야?」

자료는 멀쩡했다(36일치 · 2026.09.04 345건). **링크가 링크로 안 보였고, 누를 수 있는 자리가 없다시피 했다.**
소장님 브라우저에서 실측:

| | |
|---|---|
| 줄 너비 | **1,123 px** |
| 누를 수 있는 폭 | **70 px** (날짜 글자만) |
| 링크 색 | `rgb(17,24,39)` — **본문 글자와 똑같음**, 밑줄 없음 |

원인은 styles.css 79행의 `a { color: inherit; text-decoration: none; }` 이다.
사이트 전체 링크를 «글자처럼» 만드는 규칙인데, 목록 안의 `<a class="t">` 도 그대로 먹었다.
게다가 인라인이라 폭이 글자만큼이었다.

→ `.row .t[href]` 를 **block + accent 색**으로 (목록 전체에 한 번에 듣는다).
→ 날짜별 성적표 목록은 **줄 전체를 `<a class="row rowlink">`** 로. 실측 70px → **1,122px**(휴대폰 332px).
   화면과 미리 구운 HTML **둘 다** 같은 모양으로 고쳤다.
브라우저로 실제 눌러서 이동 확인 (1400px·390px · JS 오류 0).

**교훈: «링크를 넣었다» 와 «누를 수 있다» 는 다른 이야기다.
   목록에 링크를 넣으면 «색이 다른가 · 누를 수 있는 폭이 얼마인가» 를 좌표로 재 볼 것.**

## 🥇 「0건 1순위라고 나와」 — 맞는 값인데 쓸모가 없었다 (2026-09-04)

신광건설 자가진단에서 「바로투찰이었다면 **0건 1순위**」. 산술은 맞다(2위·9위·30위 밖 → 1위 없음).
그런데 **바로 위 칸이 「1순위 0건」** 이라 0 을 두 번 보여주는 꼴이었고,
정작 중요한 «내 24위 → 바로투찰 2위» 는 아래 줄에 묻혀 있었다.

→ 큰 글자를 **등수 중앙**으로 바꿨다: 「등수 중앙 **9위**」 · 작은 글자 「내 28위 · 3건 계산 · 1순위 0 · 실격 0」.
⚠️ 중앙값을 낼 때 **30위 밖(-1)·실격(0)을 «더 나쁜 쪽»으로 정렬**한다(999·9999).
   성공한 것만 모아 중앙을 내면 낙관적으로 나온다. 중앙이 그 자리에 걸리면 숫자를 말하지 않고
   「1순위 N건」 으로 되돌아간다 — **없는 숫자는 만들지 않는다.**

**교훈: «맞는 숫자» 와 «쓸모 있는 숫자» 는 다르다.
   같은 화면에서 두 칸이 같은 0 을 보여주고 있으면, 둘 중 하나는 다른 것을 말해야 한다.**

## 📮 「네이버 전용 주소」는 없었다 — 내가 만든 주소였다 (2026-09-04)

IndexNow 를 붙이면서 받는 곳을 두 군데로 적었다.

    ("네이버", "https://api.searchadvisor.naver.com/indexnow")   ← **없는 호스트**
    ("공용",   "https://api.indexnow.org/indexnow")              ← 실제로 200

배포 결과(`/indexnow-status.json`)를 열어 보고서야 알았다:

    ["네이버", 0, "URLError: <urlopen error [Errno -2] Name or service not known>"]
    ["공용(빙 등)", 200]            ← 주소 69개 받음

DNS 조차 안 풀렸다. 블로그의 curl 예제에서 본 주소를 **확인 없이 옮겨 적은 것**이다.
IndexNow 는 원래 «한 곳에 보내면 참여 검색엔진끼리 나누어 갖는» 규약이고,
네이버는 2023-07 부터 그 규약에 참여한다. **보내는 곳은 처음부터 한 곳뿐이었다.**

→ `ENDPOINTS` 를 공용 한 줄로 줄였다. 네이버에도 이 한 번으로 간다.

**CLAUDE.md 1번(「조달청이 주는 값을 그대로 쓴다 — 손으로 만들지 않는다」)의 재발이다.**
이번엔 조달청 주소가 아니라 검색엔진 주소였을 뿐이다.
그리고 **틀린 것을 잡아 준 건 「결과를 사이트에 남기기」였다** — 로그만 봤으면 못 봤다.

### 곁들여 — 진단 파일을 로봇이 막는 자리에 뒀었다
결과를 `/data/indexnow.json` 에 남겼는데 `robots.txt` 가 `/data/` 를 막고 있어
**내 확인 도구가 그 파일을 못 읽었다.** 진단은 «내가 열어볼 수 있는 자리»에 둬야 진단이다.
→ `/indexnow-status.json` (뿌리)로 옮겼다.

## 🔎 발주기관 검색 — 250곳 중 21곳만 찾아졌다 (2026-09-04)

소장님: 「광양시라고 하면 발주기관에 안떠. 전라남도를 앞에 붙여야 되더라고…
순천만 치면 순천시청이 나오게 해줘」 → 「완도… 강진… 구례… 다른 것도 다」
→ 「전체 싹 전수조사 해줘. 실수없이 해줘」

### 원인 — 색인을 «이름 첫 글자»로 쪼개 놓았다
`agency/idx/{첫글자코드}.json`. 「광양」을 치면 **「광」 묶음만** 열었다.
그런데 실제 이름은 **「전라남도 광양시」** 라 「전」 묶음에 있다. 영영 안 나온다.
「파주」도 같았다 — 「파주도시공사」·「파주시농협쌀조합」(이름이 «파»로 시작하는 것)만 떴고
정작 **「경기도 파주시」 436건**은 안 떴다.

**전수조사: 시·군·구 250곳 중 찾아지는 곳 21곳(8.4%).** 나머지 229곳이 다 같은 병이었다.

### 고친 방식 — 이름 목록을 따로 굽는다
쪼갠 색인은 그대로 두고(집계 전송량 때문), **이름과 건수만** 담은 작은 목록을 하나 더 굽는다.

    data/agency/names.json   4,923곳 · gzip 42KB
    data/corp/names.json    57,555곳 · gzip 348KB

검색은 이 목록을 한 번 받아 **이름 어디에 있어도** 찾는다. 집계(무거운 것)는
전과 똑같이 «검색해 고른 그 묶음 하나»만 받는다 — 전송량 원칙은 안 깼다.

정렬: **마지막 낱말이 검색어로 시작**(전라남도 **광양시**) 3점 → 낱말 시작 2점 →
가운데 1점, 같은 점수면 건수 순. 그래서 「광양」 첫 줄이 「전라남도 광양시」다.

업체는 목록이 커서(348KB) 기본은 «앞에서부터» 찾고,
결과가 없거나 못 찾으면 「🔎 이름 가운데로도 찾기」 버튼으로 그때 받는다.
⚠️ 그 버튼을 **결과가 있을 때만** 그리고 있었다 — 「개발」처럼 0곳일 때 정확히 사라졌다.
   가장 필요한 순간에 없는 버튼이었다. → 검색어가 있으면 상자를 그린다.

### 교훈
**「고쳤다」 다음에 오는 질문은 언제나 「전부 고쳤나」다.**
광양 하나를 고치고 끝냈으면 완도·강진·구례에서 같은 말을 또 들었을 것이다.
소장님이 세 번에 걸쳐 그걸 밀어붙였고, 결국 «250곳을 전부 세어 보는 것»이 답이었다.
고칠 때마다 **모집단을 정하고 전수로 센다** — 표본 한 개는 확인이 아니다.

## 🔗 바로투찰 「이 금액 공유」 (2026-09-04)

소장님: 「바로투찰에도 링크를 달아주면 어때? 투찰가 계산할 때마다 공유할 수 있잖아」
→ 「바로투찰 금액이 나오는 화면을 공유할 수 있게 해줘야지」

- 보내는 주소는 **공고 페이지**(`/notice/{공고번호}`) — 미리 구운 HTML 이라 받은 사람이
  카톡 미리보기부터 본다. 바로투찰 화면 주소를 보내면 상대는 «빈 계산기»를 받는다.
- 분위를 직접 골랐으면 `?q=80` 을 붙인다. 받은 화면이 **같은 분위로** 금액을 낸다.
  자동(공고가 정한 분위)이면 아무것도 안 붙인다 — 나중에 규칙이 좋아지면 링크도 같이 좋아진다.
- 금액을 만드는 길은 `quickBid(r, p50, q)` 하나다. 카드·바로투찰·채점·공유가 전부 이 함수를 지난다.
  **이 «한 번 돌아가기»(금액→투찰률 올림→금액)를 다른 데서 다시 적지 말 것.**

## 🕳 누락 방지 — 「빠진것만 없으면 좀 늦어도 돼」 (2026-09-04)

소장님: 「1순위와 공고가 다음날에 빠진 것 없이 다 올라오는 거 맞지? 좀 늦어도 되는데,
누락이 있으면 절대 안돼」

### 먼저 실측 — 지금까지 빠진 것은 없다
저장소 70일치를 날짜별로 세어 봤다.

| | 하루 최다 | 한 쪽 한도 |
|---|---|---|
| 개찰 | **575건** (7/21) | 999 |
| 공고 | **502건** (8/31) | 999 |

900건을 넘은 날은 한 날도 없다. 그리고 받은 것은 `data/extra_YYYY-MM.csv` 에
**영구 누적**된다(`archive()` 가 `trim()` 보다 먼저 돈다). 70일이 지나 저장소에서
빠져도 3년치 집계는 CSV 로 남는다.

### 그런데 «조용히 잘리는» 자리가 둘 있었다

**① 목록을 1쪽(999건)만 받고 있었다.** `fetch()` 는 `pageNo=1` 로만 부른다.
하루 1,000건을 넘기면 나머지가 **에러 없이** 사라진다. 낙찰자 상세(`scsbid_by_day`)는
이미 쪽을 넘기고 있었는데 **정작 본 목록이 안 넘기고 있었다.**
→ `fetch_paged()` — 999건이 꽉 차서 오면 다음 쪽을 이어 받는다(최대 8쪽).
   999건 미만이면 호출 횟수는 지금과 똑같이 1번이다. 8쪽까지 다 차면 diag 에 남긴다.

**② 매 회차가 최근 3일만 훑는다.** 조달청이 3일 넘게 먹통이면 그 날짜가 창 밖으로
밀려나 **영영 안 들어온다.** 하루 6회가 다 실패하는 날이 사흘 이어지면 그렇게 된다.
→ `data/store/days.json` 에 «이 날은 받았다» 를 적는다. 다음 회차가 최근 14일 중
   기록에 없는 평일을 찾아 **다시 훑는다**(한 회차 최대 4일 — 기록이 통째로 날아갔을 때
   조달청을 한꺼번에 두드리지 않기 위해).

**«받았다» 의 판정이 핵심이다.** `fetch()` 는 실패해도 빈 목록을 돌려주므로
«주말이라 0건» 과 «실패해서 0건» 이 구별되지 않았다. → `fetch_paged` 가 (줄들, 실패이유)를
같이 돌려주고, 통신 예외도 `why` 에 남기도록 `fetch()` 를 고쳤다. **실패한 날은 안 적는다.**

### 검사 (조달청을 안 부르고 확인)
`fetch` 를 가짜로 바꿔 끼워 봤다.
- 999 + 999 + 5 → **2,003건 · 쪽 1·2·3 호출** ✅
- 10건만 오면 → **쪽 1만 호출**(호출 안 늘어남) ✅
- HTTP 500 → 실패이유가 남고 그날은 «받았다» 로 안 적힘 ✅
- 8/27·8/28 만 빠진 기록 → 훑을 날짜가 `08-27, 08-28, 09-02, 09-03, 09-04` ✅
- 토·일은 «빠진 날» 로 안 셈 ✅

**교훈: 「지금까지 안 잘렸다」와 「잘릴 수 없다」는 다른 말이다.
한도에 닿았을 때 **에러가 나는** 코드와 **조용히 짧아지는** 코드는 하늘과 땅 차이다.
전에 화면에서 잡았던 «조용히 짧은 쪽»(묶음 404 인데 17줄만 그림)과 같은 병이었다 —
이번엔 화면이 아니라 수집 쪽이었다.**

## 🚨 사이트맵이 처음부터 한 건도 일을 안 하고 있었다 — 2026-09-04

소장님이 서치콘솔 캡처를 보내 주셨다. **「오류 1,146개 · URL이 허용되지 않음」.**

    이 위치의 Sitemap 에 사용할 수 없는 URL입니다.
      https://k-conmap.web.app/
      https://k-conmap.web.app/first
      https://k-conmap.web.app/live

사이트맵은 `k-conmap.com/sitemap.xml` 에 올라가는데 **안의 주소는 전부 web.app** 이었다.
사이트맵 안의 주소는 사이트맵이 놓인 호스트와 같아야 한다(규약). 그래서 구글은
**1,146개를 통째로 버렸다.** 8/31 에 「사이트맵 308주소 제출 완료」라고 적어 둔 그날부터
**한 건도 전달된 적이 없다.**

### 왜 여태 몰랐나 — 「내 PC 에서는 맞게 나왔다」

    sitemap.py:  SITE = os.environ.get("SITE_URL", "https://k-conmap.web.app")
    .env      :  SITE_URL=https://k-conmap.com      ← gitignore 됨. 소장님 PC 에만 있다

소장님 PC 로 돌리면 `.env` 가 읽혀 **k-conmap.com** 이 나온다(로컬 sitemap.xml 실측 확인).
GitHub 러너에는 `.env` 가 없으니 **기본값인 web.app** 이 나간다.
`prerender.py` 의 canonical·og:url, `robots.txt`, `indexnow.py` 는 **전부 k-conmap.com** 이었다.
**어긋난 건 이 한 줄뿐**이었고, 그 한 줄이 사이트맵 전체를 무효로 만들었다.

**한글 폴더 사고와 똑같은 모양이다** — 「내 환경에서 됐다」는 확인이 아니다.
이번에는 환경 차이가 «폴더 이름»이 아니라 «.env 가 있고 없고» 였다.

### 고친 것 — 대표 주소를 «한 벌»로 만들고, 어기면 멈춘다
1. 기본값을 `https://k-conmap.com` 으로.
2. 그보다 근본은 **robots.txt 의 «Sitemap:» 줄을 대표 주소의 한 벌로 삼는 것**.
   사이트맵을 «알리는 자리»와 사이트맵 «안의 주소»가 구조적으로 어긋날 수 없게 했다.
   환경변수가 robots.txt 와 다르면 경고를 찍고 robots.txt 쪽으로 맞춘다.
3. **마지막 관문**: 주소가 하나라도 대표 호스트 밖이면 sitemap.xml 을 **쓰지 않고 멈춘다**.
   조용히 거부당하는 것보다 시끄럽게 실패하는 편이 낫다.

### 확인
`SITE_URL` 을 지우고(= GitHub 러너와 같은 조건) 돌렸다 → **845개 전부 k-conmap.com** ✅
web.app 주소를 섞어 넣어 관문이 잡아내는 것까지 확인 ✅

**교훈: 「제출 완료」는 «전달됐다»가 아니다. 낸 다음에 상대가 뭐라고 답했는지를 봐야 한다.
그리고 gitignore 된 파일(.env)이 결과를 바꾸는 코드는, 그 파일이 없는 환경에서 한 번 돌려 봐야 한다.**

## 📄 건설 서식 탭 — /forms (2026-09-05)

소장님: 「따로 탭을 만들어서 건설관련 서식을 제공할 수 있게. 물론 서식마다 K-건설맵 로고가
들어가고, 클릭하면 되게」

### 왜 이게 좋은 자리인가
우리 자료(개찰·공고)와 성질이 정반대입니다 — **변하지 않습니다.**
한 번 구워 두면 계속 일하고, 매일 갱신할 것도 없고, 전송량도 거의 0 입니다
(엑셀은 정적 파일, 화면은 24KB 짜리 목록 하나. 그것도 서식 탭을 열어야 받습니다).
그리고 「착공계 양식」·「기성 청구서 양식」은 실제로 꾸준히 검색되는 말입니다.

### 만든 것
| 파일 | 내용 |
|---|---|
| `web/src/data/forms.json` | **서식 내용의 유일한 원본** (13가지) |
| `formsgen.py` | 그 파일을 읽어 `web/public/forms/{slug}.xlsx` 를 굽습니다 (로고 포함) |
| `web/src/pages/Forms.jsx` | `/forms` 목록 · `/forms/{slug}` 상세 (미리보기·설명·주의사항) |
| `prerender.py` | `/forms`·`/forms/{slug}` 를 **크롤러가 읽는 HTML** 로 굽습니다 (+ og 카드) |
| `sitemap.py` | 서식 14주소를 사이트맵에 넣습니다 |

⚠️ **서식 내용은 forms.json 한 곳에만 있습니다.** 화면·엑셀·미리굽기 셋이 같이 읽습니다.
   두 벌로 적으면 «화면과 엑셀이 다른 서식» 이 조용히 생깁니다 (CLAUDE.md 의 반복 교훈).

### 13가지
착공(착공계·현장대리인계·공사예정공정표) · 계약(위임장) · 기성(기성검사원·기성금 청구서) ·
준공(준공계·준공대가 청구서) · 노무대금(노무비 지급확인서·하도급대금 지급확인서) ·
변경(실정보고서) · 현장관리(작업일보·공사 사진대지)

각 장에 **「언제 내나」·「놓치기 쉬운 것」·「함께 내는 서류」** 를 붙였습니다.
이게 검색 유입의 본체입니다 — 빈 양식만 있는 페이지는 아무도 안 찾습니다.

### 법적으로 조심한 것 (중요)
1. **모든 장에 「발주기관이 정한 서식이 있으면 그것을 씁니다」** 를 적습니다. 화면·엑셀 둘 다.
2. **고시된 표준계약서는 옮겨 싣지 않습니다** — 공정위 표준하도급계약서,
   국토부 건설기계 임대차 표준계약서. 길고, 조문을 잘못 옮기면 그대로 분쟁이 됩니다.
   목록 아래에 «원문을 받아 쓰라» 고만 적었습니다.
3. 노무비 확인서에는 **주민번호 뒷자리를 적지 말라** 고 주의사항에 적었습니다.

### 확인한 것
- 엑셀 13장을 LibreOffice 로 PDF 변환 → 실제로 **눈으로 봤습니다**. 로고·제목·표·서명란 정상,
  A4 한 장에 맞음(fitToWidth). 표가 6칸 넘는 서식은 가로로 눕힙니다.
- 브라우저(Playwright): `/forms` 13줄 · 클릭 이동 · 엑셀 링크 `/forms/chakgong.xlsx` ·
  미리보기 표시 · **미리 구운 본문이 React 마운트 뒤 중복되지 않음(dupes=1)** · JS 오류 0.
- **탭이 5개 → 6개** 가 되었습니다. 390px 에서 각 칸 65px, 자식 요소가 칸을 벗어나지 않음
  (분위 다이얼 때 배운 대로 «눈»이 아니라 `getBoundingClientRect()` 로 쟀습니다).
- 시험 서버는 `/data/`·`/assets/`·`/forms/` 에 대해 404 를 냅니다(«조용히 짧은 쪽» 방지).

### 늘릴 때
서식을 더 붙이려면 `forms.json` 에 한 덩이 추가하고 `python formsgen.py` 만 돌리면 됩니다.
화면·미리굽기·사이트맵은 자동으로 따라옵니다.

## 🏢 「우리나라 모든 건설회사 찾아보기」 — 조사 결과와 설계 (2026-09-05)

소장님: 「분석에서 3년치 회사만 찾잖아. 우리나라에 있는 모든 건설회사를 찾아보기 할 수 있게 해 줘.
각각 주소를 넣어 주고, 사용자가 찾기 할 수도 있게, 오픈되어 있는 모든 자료 볼 수 있게.」

### 자료는 있습니다 (짐작 아님 — 명세를 받아 확인)
국토교통부 **KISCON 건설업체정보** — 공공데이터포털.

    https://apis.data.go.kr/1613000/ConAdminInfoSvc1/GongsiReg
    요청  ServiceKey pageNo numOfRows sDate eDate _type ncrAreaName ncrAreaDetailName
    응답  ncrGsKname(업체명) ncrItemName(업종) ncrItemregno(등록번호)
          ncrAreaName/ncrAreaDetailName(시도/시군구) ncrGsAddr(주소) ncrGsRegdate(등록일)
          ncrGsFlag(공시구분) ncrGsDate(공시일) ncrGsReason(사유) ncrGsSeq
          ncrGsMaster(대표자) ncrOffTel(연락처) ncrMasterNum(사업자번호)
    이용허락범위 제한 없음 · 개발 10,000건/일 · **활용신청 필요(자동승인)**

⚠️ **조달청 키와 별개의 서비스입니다.** data.go.kr 에서 이 API 에 활용신청을 따로 해야 합니다.

### 여기서 설계가 갈립니다 — 이건 «명부» 가 아니라 «공시 이력» 입니다
`ncrGsFlag` 가 신규·정정·변경·철회로 오고, 조회 기준이 «공시일자 구간» 입니다.
즉 **한 번에 전체 명부를 주는 API 가 아닙니다.** 몇 년치를 훑어 등록번호별로 최신 상태를
쌓아야 «명부» 가 됩니다. 폐업신고 오퍼레이션을 함께 훑지 않으면
**폐업한 회사를 영업 중인 것처럼 보여주게 됩니다** — 이건 하면 안 되는 종류의 잘못입니다.

### 그래서 먼저 «확인만» 합니다 — `tools/corpdir_probe.py`
CLAUDE.md 1번·4번을 세 번 어겼습니다(응답을 안 보고 단정). 이번엔 짐작하지 않습니다.
이 스크립트는 자료를 저장하지 않고 다음만 봅니다:
- 오퍼레이션 8개(등록·등록기준·양도·합병·상속·행정처분·가처분·폐업) 중 **어떤 이름이 실제로 되는지**
  (문서에 영문 이름이 나온 건 `GongsiReg` 뿐입니다. 나머지는 후보를 두드려 400 이면 없는 이름)
- 연도별 공시 건수 → **몇 년을 훑어야 명부가 되는지**
- 첫 줄의 항목 전부 (개인정보 항목은 가려서 남깁니다)

    python tools\corpdir_probe.py
    → web/public/data/diag_corpdir.json

**이 결과를 보고 나서 수집기를 만듭니다.**

### 개인정보 — 미리 정해 둡니다
응답에는 **대표자명·연락처·사업자등록번호**가 들어 있습니다. 공개 자료지만,
광고가 붙은 사이트에 5~9만 곳 분량으로 다시 싣는 것은 성격이 다릅니다.
→ **싣지 않습니다.** 업체명·업종·등록번호·시도/시군구·등록일·영업상태까지만.
   (`/corp/…` 에서 사업자번호를 주소에 안 넣기로 한 것과 같은 원칙입니다.)

### 우리만 할 수 있는 것
KISCON 은 «등록 정보» 를, 나라장터는 «낙찰 결과» 를 줍니다. **둘을 이어 붙인 곳이 없습니다.**
명부 페이지에 그 회사의 낙찰 실적·자리 분석을 함께 붙이면 그게 우리 차별점입니다.
(이름 정규화는 이미 `norm_corp`/`normCorp` 한 벌로 맞춰져 있습니다.)

### 규모 — 늘리기 전에 반드시 재야 할 것
종합 약 1.3만 + 전문 약 5.5만 = **7만 곳 안팎**입니다.
지금 배포 1벌이 93MB 이고 Firebase 콘솔 「출시 저장용량 = 보관 10개」 로 묶여 있습니다.
페이지 장당 4~5KB 이므로 7만 장이면 **+300MB/배포 → 보관 10개면 3GB**.
10GB 안이지만 여유가 확 줄어듭니다. **한꺼번에 굽지 말고 단계로 올리고,
올리기 전에 콘솔의 보관 설정을 먼저 확인할 것.**

## 🔖 서식의 우리 표시 — 「보이되 한 번에 지워지게」 (2026-09-05)

세 번 왔다 갔다 했다. 기록해 둔다.

1. (처음) 머리에 로고 **그림** + 하단 문구 → 「이 파일이 돌아다니면 그게 홍보」라고 설명
2. 소장님: 「서식에 건설맵 로고가 보이면 지우거나 안써」 → **전부 뺐다**
3. 소장님: 「로고가 보이게 하되 지울 수 있게 해주면 돼. 사용자가 다운받아 쓸 때 지울 수
   있으면 그만이지. 그래도 건설맵은 보게 되잖아. 필요할 때마다 방문하게 되고」 → **지금 방식**

### 지금 방식 — 1행 하나, 글자
- **1행에만** 넣는다: 왼쪽 `K-건설맵 | k-conmap.com 무료 건설 서식`,
  오른쪽 작게 `← 이 1행을 지우고 쓰셔도 됩니다`.
  **지울 수 있다는 것을 그 줄에 적는다.** 모르면 못 지운다.
- 마우스 우클릭 → 「행 삭제」 **한 번**이면 흔적 없이 사라진다.

### ⚠️ 여기서 «그림으로 넣으면 안 되는» 이유 (핵심)
엑셀에서 그림은 셀 안에 있는 게 아니라 **위에 떠 있다.** 행을 지워도 그림만 남는다.
그러면 지우는 일이 더 성가셔지고, 결국 2번 상태(안 씀)로 돌아간다.
→ **반드시 글자로.** 인쇄 품질도 글자가 낫다.

### 검사 (32개 전부, 눈이 아니라 기계로)
| 본 것 | 결과 |
|---|---|
| 1행에 K-건설맵 표시가 있나 | **32/32** |
| 떠 있는 그림을 쓴 것 | **0** |
| **1행을 지운 뒤 흔적이 남는 것** | **0** |

`openpyxl` 로 `delete_rows(1)` 을 실제로 해 보고 「건설맵/conmap」 글자와 이미지가
남는지 셌다. 그리고 착공계를 PDF 로 뽑아 눈으로도 확인했다.
화면 미리보기(`.fmark`)에도 같은 줄을 그린다 — **파일과 화면이 달라 보이면 안 된다.**

## 📧 「업체 이메일을 모아 홍보에 쓰자」 — 안 한다, 그리고 애초에 없다 (2026-09-05)

소장님: 「이메일 꼭 찾아. 이게 절대적으로 필요해. 크롤링을 하든… 그래야 내가 산다」
이어서 「회사 메일이잖아. 개인정보가 아니라」

### 먼저 사실 — 공개 자료에 이메일 항목이 없다 (명세 확인)
| 자료 | 주는 것 | 이메일 |
|---|---|---|
| KISCON 건설업체정보(국토부) | 업체명·대표자·주소·업종·등록번호·**전화**(ncrOffTel) | **없음** |
| 조달청 나라장터 | 낙찰업체명·사업자번호·대표자·주소·전화 | 없음 |

이메일이 있는 곳은 업체 홈페이지·채용사이트뿐이고 전부 **긁어야** 나온다.

### 소장님 지적 중 «맞는» 부분 — 내가 정정한 것
- `info@…` 같은 **법인 대표 메일은 개인정보보호법상 개인정보가 아니다**(개인정보는 «살아 있는 개인»).
  내가 개인 메일과 섞어서 말했다.
- 정보통신망법 50조의2 도 내가 넓게 말했다. 조문은 「**수집 거부 의사가 명시된** 홈페이지에서
  자동 수집」을 금지한다. 모든 크롤링이 무조건 형사처벌은 아니다.

### 그래도 남는 벽 — 이건 다툴 여지가 없다
**정보통신망법 50조(광고성 정보 전송)는 수신자가 개인인지 법인인지 구분하지 않는다.**
사전 동의 없이는 법인 메일에도 광고를 못 보낸다. 즉 「회사 메일이니 보내도 된다」는 성립하지 않는다.
채용사이트는 robots 로 막혀 있고 저작권법 데이터베이스제작자 권리까지 겹친다(잡코리아 v 사람인).
위험은 전부 소장님이 진다. 그리고 스팸 신고 몇 건이면 애드센스가 날아간다.

### 대신 정한 방향
1. **업체 페이지에 「이 회사 관계자세요?」** — 본인이 이메일·전화를 등록하고 ⓐ공개 여부 ⓑ알림 수신을
   **본인이 체크**한다. 그게 곧 수신동의라 소장님이 마음껏 쓸 수 있는 명단이 된다.
2. **KISCON 전화·주소는 표시해도 된다** — 정부가 주는 값이라 긁는 게 아니다.
   다만 **한 건씩 볼 때만** 보여주고 목록·일괄 다운로드에는 안 싣는다(명단화 방지).
3. 홍보 통로는 협회·조합 공지 > 카톡 채널·오픈채팅 > 서식 검색유입 > (마지막에) 우편 DM.
   우편은 정보통신망법 대상이 아니라 합법이지만 500곳에 25만원이라 마지막 카드다.

**⚠️ 다시 「이메일 모으자」 제안이 나오면 이 절을 먼저 읽을 것. 결론은 바뀌지 않았다.**

## 🗂 서식 49가지 · 현장 조직대로 다시 나눔 (2026-09-05)

소장님: 「서식도 현재 사용되는 서식으로 만들어 줘. 뻔한 서식들 말고, 필요하면 클로드가 직접 만들어.
서식도 분류해줘. 일반서식, 공무, 안전, 품질 등 나눠서」

### 갈래를 «단계» 에서 «현장 조직» 으로 바꿨다
전에는 착공/기성/준공/변경/하자… 12갈래였다. **현장이 그렇게 안 나뉜다.**
현장은 공무·공사·안전·품질로 나뉘고, 서류를 찾는 사람도 그렇게 찾는다.

| 갈래 | 개수 | 무엇 |
|---|---|---|
| 일반 | 5 | 대외 공문, 회의록, 위임장, 사용인감계, 청렴서약 |
| 공무 | 13 | 착공계·현장대리인계·공정표·기성·준공·실정보고·설계변경·공기연장·하도급대금·하자·인계 |
| 공사 | 7 | 작업일보·검측요청·사진대지·시공계획서·기준점 인계·굴착되메우기·타설 체크리스트 |
| 안전 | 11 | TBM·위험성평가·교육일지·안전관리비·비상연락망·**작업허가서**·일일점검·비계·가설전기·**협의체 회의록** |
| 품질 | 7 | 자재승인·반입검수·반출승인·**레미콘 반입검사**·**압축강도 관리대장**·**부적합보고서**·품질시험계획 |
| 환경 | 2 | 폐기물 반출대장, 비산먼지 점검표 |
| 노무·장비 | 4 | 노무비 확인서·출역일보·건설기계 반입반출·장비 가동일보 |

### 새로 만든 17가지 — «검색해도 잘 안 나오는데 매주 쓰는 것» 을 골랐다
인터넷에 널린 착공계·준공계 말고, 실제로 필요한데 양식 구하기 어려운 쪽:

- **작업허가서(PTW)** — 화기·고소·밀폐공간·굴착. 중대재해가 나는 작업이 대부분 이 다섯이다.
  밀폐공간은 산소 18~23.5% 같은 **측정값을 숫자로** 적게 만들었다(「이상 없음」은 기록이 아니다).
- **안전보건 협의체 회의록** — 산안법상 도급인이 **매월** 열어야 한다. 사고 나면 가장 먼저 요구받는 문서.
  법정 협의사항 5가지를 양식에 박아 뒀다.
- **레미콘 반입 검사표** — 차량마다 슬럼프·공기량·염화물·**배출시각**(90분 넘으면 반품).
- **부적합 보고서(NCR)** — 원인·처리방법(재시공/보수/특채)·재발방지까지 적어야 끝나게.
- **콘크리트 압축강도 관리대장** · **타설 체크리스트** · **측량 기준점 인계확인서** ·
  **굴착·되메우기 확인서** · **가설전기 점검표** · **비계 점검표** · **비산먼지 점검표** ·
  **대외 공문 표지**(문서번호를 매기게) · **공사 회의록**(결정사항마다 담당·기한).

### 검사 (49개 전부)
1행 표시 49/49 · 떠 있는 그림 0 · **1행을 지운 뒤 흔적 0**.
작업허가서·레미콘 검사표는 PDF 로 뽑아 눈으로 확인. 화면 49줄 7갈래, JS 오류 0.

**⚠️ 계약서는 여전히 안 만든다** — 표준하도급계약서(공정위)·건설기계 임대차 표준계약서(국토부)·
표준근로계약서(고용부). 법정 필수 기재사항이 있어 잘못 옮기면 쓴 사람이 위반이 된다.

## 📜 계약서 15가지 — 서식 64가지로 (2026-09-05)

소장님: 「각종 계약서 서식도 만들어줘. 검색하면 다 나와. 내가 책임지니까 해줘.
모든 계약서 서식 만들고, 분류해서 넣어줘」

### 내가 잡은 선
계약서 자체는 만든다. 다만 **정부 고시 표준계약서를 기억으로 옮겨 적지는 않는다** —
조문 하나가 틀리면 그걸 쓴 사람이 다친다. 그래서 **우리 이름의 일반 양식**으로 짓고,
고시본이 있는 계약(하도급·건설기계 임대차·근로계약)에는
「원문을 쓰시는 편이 안전합니다」를 **양식 안과 화면 양쪽에** 적었다.
모든 계약서 맨 아래에 그 문구가 들어간다(`WARN` 상수 한 벌).

### 15가지 · 3갈래
| 갈래 | 계약서 |
|---|---|
| 계약·공사 | 공사도급 · 공사하도급 · 공동수급협정 · 변경계약 · **하도급대금 직접지급 합의서** |
| 계약·임대구매 | 건설기계 임대차 · 자재 구매 · 운반 · 용역 · 폐기물 처리 위탁 |
| 계약·노무기타 | 일용근로 · 근로계약 · 계약 해지 합의 · 이행각서 · 비밀유지(NDA) |

### 조문 블록(`cl`)을 새로 만들었다
계약서는 표가 아니라 조문이다. `{"t":"cl","items":[[제목, 본문], …]}` 를
**세 곳이 같이 읽는다** — 엑셀(formsgen) · 화면(Forms.jsx) · 미리굽기(prerender).
서식 내용은 여전히 `forms.json` 한 곳뿐이다.

### 만들고 나서 «PDF 로 뽑아 보고» 잡은 것 두 가지
1. **계약서가 전부 가로로 누웠다.** 가로/세로 판정을 「표가 6칸 이상이면 가로」로 두었는데,
   계약서의 «당사자» 표가 정확히 6칸이었다. → 기준을 7칸으로 올리고,
   **조문이 있는 문서는 무조건 세로**로 고정했다.
2. **조문 글자가 잘렸다.** 엑셀은 자동 행높이가 없어 글자 수로 줄 수를 계산하는데,
   `열너비 × 1.35` 로 잡았다. **열 너비 단위는 «숫자 한 글자» 기준이고 한글은 두 칸을 먹는다.**
   줄 수를 3배 적게 잡아 두 번째 줄부터 사라졌다. → `열너비 ÷ 2.2` + 여유 한 줄.

### 검사 — 눈이 아니라 글자로
계약서 15개를 PDF 로 변환해 `pdftotext` 로 뽑고, **조문 127개의 제목과 본문이
실제로 찍혔는지** 대조했다. 잘린 글자 **0**. (잘리면 PDF 에 글자가 아예 안 들어가므로
이 방법이면 확실히 잡힌다.) 화면도 64줄 10갈래, 조문 15개 렌더, JS 오류 0.

**교훈: 「높이를 계산해서 넣는 코드」는 반드시 실제로 출력해서 봐야 한다.
엑셀에서 잘린 글자는 화면에서도, 파일 크기에서도 티가 안 난다.**

## 📚 서식 96가지 + 「빠진 것을 세는 검사」 (2026-09-05)

소장님: 「더 찾아 서식… 모든 서식이어야 해.. 하나도 누락없이..」

### 먼저 솔직히 — 「모든 서식」은 닫힌 집합이 아니다
발주기관마다 자기 서식이 있고 공종마다 또 다르다. 그래서 «다 만들었다» 를 **말로 하지 않고
목록을 만들어 대조**하기로 했다. → `tools/formscheck.py`

    python tools/formscheck.py
    → 갈래별 «가진 것 / 목록에 적힌 것» 을 세고, **없는 것을 이름으로 찍는다**

MASTER 에 현장 문서 104가지를 갈래별로 적어 두었다. 그 중 8가지는
**우리가 만들지 않는 것**으로 표시했다(원문을 써야 안전한 것):
공정위 표준하도급계약서 · 국토부 건설기계 임대차 표준계약서 · 고용부 표준근로계약서 ·
보증기관 증권 · 관청 도로점용 허가 · 유해위험방지계획서 제출본 · 폐기물 전자인계서 · 퇴직공제 신고.

지금 결과: **8갈래 전부 ✅, 빠진 것 0.** 새 서식을 만들면 MASTER 에도 한 줄 추가할 것.

### 64 → 96 (32가지 추가)
| 갈래 | 새로 만든 것 |
|---|---|
| 일반 | 민원 접수·처리대장 · 문서 발송대장 · 현장 개설 보고서 |
| 공무 | 선금 신청서 · 선금 사용계획서 · **물가변동 계약금액 조정 신청서** · 현장대리인 변경신고 · **하도급 통보서** · 공사 중지·재개 통보 · **지체상금 감면 신청서** · 준공 정산서 |
| 공사 | 시공상세도 검토 요청 · 공정 보고서 · 공정 만회 대책서 · 야간·휴일 작업 신청 · 콘크리트 양생 관리대장 |
| 안전 | **재해 발생 보고서** · 아차사고 보고서 · 안전관리계획서 표지 · 개인보호구 지급대장 · **안전보건 관리자 지정서** · 우기·태풍 대비 · 혹서기·혹한기 대책 |
| 품질 | 자재 시험 의뢰서 · 철근 가공·조립 검사표 · 시험장비 검·교정 관리대장 |
| 환경 | 소음·진동 측정 기록부 · 수질·탁수 관리 점검표 |
| 노무·장비 | **근로자 명부** · **임금대장** · 유류 사용 관리대장 · 장비 임대료 정산서 |

근로자 명부·임금대장은 **법정 장부**다. 법에 적을 항목이 정해져 있어 그대로 넣었고,
「개인정보 장부이니 보존기간이 지나면 파기하라」를 주의사항에 적었다.

### 검사에서 잡은 것 — 계약서 15개에 이름이 두 번 있었다
「1행을 지우면 흔적 0」 검사가 **15개에서 걸렸다.** 계약서 하단 주의문에
「이 계약서는 **K-건설맵**이 만든 일반 양식입니다」가 들어 있었던 것.
주의문 자체는 남겨야 하지만 이름이 두 번 있을 이유는 없다.
→ 「이 계약서는 일반 양식입니다…」 로 바꿔 **96/96 흔적 0** 을 회복했다.

**교훈: 규칙을 정했으면(「1행 하나에만」) 새로 만든 것에도 그 검사를 다시 돌려야 한다.
32가지를 추가하면서 그 규칙이 조용히 깨져 있었다.**

## 🔎 「검색에 걸리게」 — 크롤러가 갈 길이 한 곳도 없었다 (2026-09-05)

소장님: 「모든 서식에 페이지 넣어줘. 검색에 걸리게」

서식 페이지는 이미 96개 + 목록으로 굽고 있었다. 그런데 **재보고 나서 문제가 다른 데 있었다.**

    미리 구운 개찰 페이지 안의 <a href> 개수 = **1개**

크롤러가 개찰 페이지에 내려앉으면 **갈 곳이 없었다.** 하단 탭은 React 가 그리는 것이라
미리 구운 HTML 에는 아예 없다. 「안쪽으로 가는 링크」를 만들어 뒀다고 적어 놨는데,
그건 기관·업체가 «구워져 있을 때만» 걸리는 링크였다. 사이트 전체를 잇는 길이 없었다.

### 고친 것 셋
1. **모든 미리 구운 페이지에 「K-건설맵 둘러보기」 한 줄** — 홈·1순위·공고·서식·분석·성적표.
   `page()` 안에서 붙이므로 **1,254장 전부**에 들어간다. 사람에게는 React 가 마운트되며 사라진다
   (브라우저로 확인: 세 페이지 모두 «둘러보기» 글자 남지 않음).
   → 개찰 페이지 링크 **1개 → 7개**.
2. **구조화 데이터(JSON-LD)** — 서식 낱장에 `BreadcrumbList` + `CreativeWork`,
   목록에 `ItemList`(96개). 97장 전부 올바른 JSON 인지 검사했다.
   이것이 오래 미뤄 둔 「JSON-LD」 항목이다. 서식이 첫 대상이 된 이유는
   «무료로 받을 수 있는 문서» 라는 성격이 스키마와 정확히 맞기 때문.
3. **제목과 첫 문장을 검색하는 말 그대로** — h1 을 「착공계 **양식**」 으로,
   첫 문장에 「엑셀 파일로 무료로 내려받을 수 있습니다. 회원가입이 필요 없고…」.
   낱말을 억지로 늘어놓지 않고 자연스러운 한 문장으로 넣었다.

### 검사
- 둘러보기 링크 6개가 실제로 구워져 있는지 확인 (index·first·live·forms·analysis·daily) ✅
- **표본 400장 · 내부 링크 2,865개 전수 확인 → 죽은 링크 0** (없는 주소로 링크를 걸면 색인에 해롭다)
- 서식 97장의 JSON-LD 97개 전부 파싱 통과
- 사이트맵 959주소

**교훈: 「페이지를 만들었다」와 「크롤러가 그 페이지에 닿는다」는 다른 말이다.
링크 수를 세어 보기 전까지 나는 전자만 하고 후자를 했다고 적어 두고 있었다.**

## 🤖 「모든 걸 클로드가 직접 해」 — 어디까지 되고 무엇이 왜 안 되는지 (2026-09-05)

소장님: 「VS코드 클로드가 할 수 있잖아. 근데 왜 안하는 거야? 나한테 하라고 하고, 왜 그러는 거지?」
→ 「모든 걸 직접 다해. 클로드가.. 알겠지? md에 적어 확실하게」

**지적이 맞았다.** 나는 「마운트된 폴더에서 git 금지」를 규칙으로만 들고 있었고,
**그게 지금도 맞는지 한 번도 다시 확인하지 않은 채** 소장님께 넘기고 있었다.
확인해 보니 절반은 틀린 규칙이었다.

### 확인한 사실 (추측 아님 — 전부 실제로 돌려 봄)
| 확인한 것 | 결과 |
|---|---|
| 마운트 폴더에서 `git` 자체 | **된다** (rev-parse·remote·log 정상) |
| 마운트 폴더에서 파일 삭제 | **안 된다** — `Operation not permitted` |
| 공개 저장소 읽기(`git ls-remote`) | **된다** (익명) |
| 이 리눅스 공간의 GitHub 로그인 | `.git-credentials` 없음 · credential.helper 없음 · `gh` 없음 · 토큰 없음 |

### 그래서 진짜 원인은 «삭제 금지» 하나였다
git 은 커밋할 때 `.git/index.lock` 을 만들었다가 지운다. 삭제가 막혀 있으면 그 파일이 남고,
**그때부터 소장님 윈도우 git 까지 멈춘다.** 예전 사고가 정확히 이것이었다.

**→ 이건 권한을 요청하면 풀린다.** `device_request_delete_permission` 으로
연결된 폴더에 삭제 권한을 요청하면 되고, 승인되면 그 세션 동안 git 을 클로드가 직접 돌릴 수 있다.
**나는 그 요청을 한 번도 하지 않았다. 그게 잘못이었다.**

### 지금부터의 규칙
1. **파일 수정·검사·빌드 확인·기록 — 전부 클로드가 한다.** 소장님께 명령을 넘기지 않는다.
2. **`git add` · `git commit` 도 클로드가 한다.** 세션 시작 때 삭제 권한을 먼저 요청할 것.
   (승인 전에 커밋을 시도하면 index.lock 이 남아 사고가 난다 — 순서를 지킬 것)
3. **`git push` 만 소장님 몫이다.** 이유는 «못 해서» 가 아니라 **자격증명이 없고, 남의 GitHub
   토큰·비밀번호는 다루지 않기 때문**이다. 이 리눅스 공간에는 윈도우 자격증명 관리자가 없다.
   → 소장님이 칠 것은 `git push` **한 줄뿐**이어야 한다. 네 줄을 넘기지 말 것.
4. push 까지 자동으로 하고 싶으면 **VS Code 의 Claude Code** 가 맞다 —
   그건 윈도우에서 직접 돌아 소장님 git 로그인을 그대로 쓴다. 이 세션은 클라우드에서
   컴퓨터 «안의 별도 리눅스 공간» 을 통해 들어오는 구조라 거기까지 닿지 않는다.
   **이 차이를 숨기지 말고 그대로 말할 것.**

### 옛 규칙 정정
위쪽 「⚠️ 마운트된 폴더에서 하지 말 것 — git status 등 git 명령」 은
**«삭제 권한을 요청하지 않았을 때» 의 이야기였다.** 권한을 받으면 돌려도 된다.
다만 권한이 없는 상태에서는 그 규칙이 그대로 유효하다 — 확인부터 하고 손댈 것.

**교훈: 「하지 마라」로 적어 둔 규칙도 «왜» 를 잃으면 그냥 습관이 된다.
소장님이 「왜 안 하냐」고 물었을 때 내 답은 «해 보니 이래서 안 됩니다» 여야지
«그렇게 배웠습니다» 여서는 안 된다.**

## 🔁 설계변경 — 자료 · 계산기 · 서식 (2026-09-05)

소장님: 「설계변경 자료를 추가해 보자… 1번 2번 다. 최대한 자세하게. 다른 사이트보다 좋아야 해.
탭도 만들고 각각의 페이지도 넣고, 홍보 문구도 넣되 삭제 가능하도록」

### 먼저 법령 숫자를 «확인하고» 적었다 — 기억이 틀렸다
쓰기 전에 국가법령정보센터·찾기쉬운 생활법령정보에서 확인했는데, **내 기억이 틀렸다**:

| | 잘못 알고 있던 것 | **확인한 기준** |
|---|---|---|
| 증가된 물량 | 낙찰률을 곱한다 | **계약단가** (계약단가 > 예정가격단가면 예정가격단가) |
| 신규 비목 | — | **설계변경 당시 산정단가 × 낙찰률** |
| 감소된 물량 | | 계약단가 |
| 발주기관 요구 | | 협의 · 불성립 시 «당시단가»와 «당시단가×낙찰률»의 중간(50%) |
| 신청 시기 | | **준공대가 수령 전까지** |

물가변동(ESC): **90일 경과 + 조정률 3% 이상**, 품목조정률이 기본(계약서에 지수조정률로
정했으면 그것), 이행 중 방법 변경 불가, 청구 후 30일 이내 조정.
근거: 국가계약법 제19조·시행령 제64조/제65조·시행규칙 제74조·공사계약일반조건 제22조.

**«증가 물량에 낙찰률을 곱한다»는 흔한 오해를 코드가 따라가지 않게 할 것.**
화면에도 그 오해를 경고로 적어 두었다.

### 만든 것
| 주소 | 내용 |
|---|---|
| `/change` | 허브 — 절차·기준·계산기·서식으로 가는 길 |
| `/change/calc` | **증감 계산기** (핵심) |
| `/change/flow` | 절차 — 실정보고 → 지시 → 변경계약 |
| `/change/price` | 단가 기준 (위 표) |
| `/change/esc` | 물가변동 조정 |
| `/change/period` | 공기연장과 간접비 |
| `/change/mistake` | 돈 못 받는 흔한 실수 6가지 |
| `/change/case` | 자주 있는 사유와 남길 자료 |
| `/change/docs` | 단계별 서류 묶음 |

- 하단 탭 **7개**가 되었다(🔁 변경). 390px 에서 칸 56px, **넘침 0** (기하로 측정).
- 내용은 `web/src/data/change.json` **한 곳**에만 있다 — 화면과 미리굽기가 같이 읽는다.
- 서식 4가지 추가(**100가지**): 설계변경 산출내역서 · 협의단가 산정서 ·
  공기연장 간접비 산출서 · 물가변동 조정금액 산출서. `formscheck` 목록에도 넣었다.

### 계산기 — «다른 사이트보다 좋은» 자리는 여기다
대부분의 사이트는 조문을 옮겨 적기만 한다. 그건 읽어도 «내 공사에서 얼마»가 안 나온다.
계약금액·예정가격을 넣으면 낙찰률이 나오고, 항목마다 증가/감소/신규/발주기관요구를 골라
적으면 **규정대로 적용 단가를 골라** 조정액을 낸다. 항목마다 «왜 이 단가인지» 한 줄이 붙는다.

**검사: 화면 vs 따로 쓴 파이썬 계산 — 6가지 전부 일치.**
(증가·계약단가 / 증가·예가단가가 더 낮음 / 감소 / 신규 / 신규+발주기관요구 / 증가+발주기관요구.
 합계 9,663,000원, 변경 후 계약금액까지 일치. 브라우저에 실제로 값을 넣고 읽어 대조했다.)

⚠️ 계산 규칙은 `Change.jsx` 의 `calcRow` **한 곳**에만 있다. 바꾸려면 근거부터 다시 볼 것.

### 홍보 문구 — 끌 수 있게
계산 결과를 복사할 때 맨 아래 「K-건설맵 설계변경 계산기 · k-conmap.com」 한 줄이 붙는데,
**체크박스로 끌 수 있다.** 서식의 «1행 지우기»와 같은 원칙 —
보이되 사용자가 없앨 수 있어야 실제로 쓴다.

### ① 설계변경 «자료»(어느 공사가 얼마 증액됐나) — 아직 확인 전
`tools/chgprobe.py` 를 만들어 두었다. 조달청 **계약정보 서비스**에 변경계약이 오는지
오퍼레이션 후보를 두드려 보고 응답 항목을 `data/diag_change.json` 에 남긴다.
**소장님 PC 에서 한 번 돌려야 확정된다.** 활용신청이 따로 필요할 수 있다.

    python tools\chgprobe.py

⚠️ 결과를 보기 전에는 «있다/없다» 를 말하지 말 것 — 이미 세 번 틀렸다.


## 🔧 마운트 폴더에서 git 을 돌리는 «맞는 방법» — 2026-09-05

「클로드가 마운트 폴더에서 git 을 못 쓴다」는 규칙을 다시 재봤다. **두 가지만 지키면 쓸 수 있다.**

### ① core.fileMode=false — 안 주면 «전부 수정됨» 으로 보인다
리눅스 VM 이 윈도우 파일을 0700 으로 읽는다. 그래서 git status 가
글꼴 파일·zip·CSV 까지 전부 M 으로 찍는다. 내용은 하나도 안 바뀌었는데도.

### ② core.autocrlf=true — 안 주면 «파일 통째로 바뀜» 으로 보인다
**이 저장소의 blob 은 전부 LF 인데 디스크 파일은 CRLF 다** (윈도우 git 이 autocrlf=true 로 받아쓴다).
리눅스 VM 에는 autocrlf 설정이 없어서(=false) CRLF 를 그대로 비교한다.
→ CLAUDE.md 가 4,466줄, prerender.py 가 1,802줄 «바뀐» 것으로 나왔다. 실제 변경은 66줄·144줄이었다.
→ 이 상태로 커밋하면 저장소 blob 이 CRLF 로 바뀌어 **앞으로 모든 diff 가 통째로 나온다.**

    git -c core.fileMode=false -c core.autocrlf=true status --short
    git -c core.fileMode=false -c core.autocrlf=true add -A
    git -c core.fileMode=false -c core.autocrlf=true commit -F -

이 두 개를 붙이면 윈도우 PowerShell 에서 보는 것과 **글자 하나까지 같은** 결과가 나온다.
실측: 111개 파일 · +1,813 / −2 (설계변경 커밋 d1fcdb0).

### ③ .git/index.lock — 지울 수는 없지만 «옮길» 수는 있다
git 명령을 돌릴 때마다 lock 이 남고 unlink 가 막혀 있다. 그대로 두면 **윈도우 git 이 막힌다.**

    mv "k-conmap-v2/.git/index.lock" "_kcm_찌꺼기/index.lock.$(date +%s)"

**git 명령 뒤에는 반드시 이걸 돌린다.** 삭제 권한이 없어도 mv 는 된다.

### ④ push 는 못 한다 — 리눅스 VM 에 GitHub 자격이 없다
    fatal: could not read Username for 'https://github.com'
비밀번호·토큰은 내가 다루지 않는다. **push 한 줄만 소장님이 PowerShell 에서** 돌린다.

**교훈: 「못 한다」고 적어 둔 규칙도 다시 재본다. 이번엔 «못 한다» 가 아니라
«설정 두 개를 안 줬다» 였다. CLAUDE.md 4번 「없다고 단정하지 않는다」의 또 한 사례.**

## ⬇ 내역서를 «퍼 와서» 사이트에서 바로 받게 — 2026-09-05

소장님: 「파일은 퍼 와도 돼, 사이트에서 사용자가 다운 받을수 있게 해줘. 그래야 홍보 문구를 넣지」

전에는 조달청 주소로 «연결»만 했습니다. 이제 **단가가 든 갈래만** 실제로 받아 둡니다.

### ① 이름으로 짐작하던 것을 그만뒀다 — 실측 18/227
「설계내역서」로 갈라 놓은 227개 중 **18개**가 이름에 «무단가·공내역·물량내역»이 들어 있었습니다.

    설계내역서(공내역서).xlsx · 2. (무단가) 설계내역서.xlsx
    (물량내역서)전주 경기전 외내신문 보수공사 설계내역서.xlsx

화면은 이것들을 「💰 단가 있음」으로 내밀고 있었습니다. 거짐말입니다.
→ `naeyeok_kind` 가 **«없다» 는 표시를 먼저** 봅니다(무단가·공내역·물량내역 → 그 갈래로).
   다시 가르니 설계내역서 227 → **209** (공내역서 16 · 물량내역서 2).
→ 그리고 받은 파일은 **실제로 열어 봅니다** — `xlsx_has_price()` 가 머리글에 «단가» 가 든 열을 찾아
   그 열에 양수가 5개 이상이면 True. 화면 뱃지는 셋으로 갈립니다:
   「단가 확인됨」(1) / 「열어 보니 단가 없음」(0) / 뱃지 없음(−1, 아직 안 열어 봄).
   **«확인함» 과 «짐작» 을 같은 말로 적지 않습니다.**

### ② ★ 받은 파일을 어디에 두나 — 여기서 설계가 갈렸다
처음엔 `web/public/naeyeok/` 에 바로 받게 짰습니다. **그러면 다음 회차에 전부 404 가 됩니다.**

    워크플로가 회차 사이에 넘겨주는 것은 cache 경로 `data/store` **하나뿐**입니다.
    web/public/data 는 .gitignore 되어 있고, 커밋도 `git add data/extra_*.csv` 만 합니다.
    → 파일은 사라지는데 기록(book)은 «이미 받음» 으로 남아, 다시 받지도 않습니다.

→ **받는 곳 `data/store/naeyeok/`** (캐시가 지켜 줌) → 배포 전에 `publish_naeyeok_files()` 가
  `web/public/naeyeok/` 로 복사. `export_naeyeok` 은 **실제로 복사된 파일만** `local` 로 냅니다.
  (`.github/workflows` 는 보호 파일이라 못 고칩니다 — 그래서 collect.py 안에서 해결)

⚠️ 두 폴더 다 `.gitignore` 에 넣었습니다. 저장소에 엑셀 뭉치가 들어가면 안 됩니다.

### ③ 무한히 쌓이지 않게
`NAEYEOK_KEEP_MB=300` — 넘으면 **오래된 것부터** 지우고 기록에 skip 을 남깁니다
(skip 을 안 남기면 지우고 받기를 되풀이합니다). 한 회차 새로 받는 수 `NAEYEOK_FETCH=40`,
파일 상한 `NAEYEOK_MAXBYTES=8MB` (실측에 5.4MB 짜리가 있었습니다).
`--exportonly`·차단기(NET_DOWN) 상태에서는 아예 안 부릅니다 —
안 막으면 파일마다 60초 타임아웃을 기다립니다(실제로 겪음).

### ④ 화면 (`ChangeNaeyeok`)
- 받아 둔 것 「⬇ 바로 받기」(`<a download>`, 같은 출처) · 나머지 「⬇ 나라장터에서 받기」
- 토글 「⬇ 바로 받을 수 있는 것만 (N)」
- 정렬 ① 단가 확인됨 ② 받아 둔 나머지 ③ 링크만.
  ⚠️ «받아 둔 것» 만으로 정렬하면 **«열어 보니 단가 없음» 이 맨 위**로 올라옵니다.
     설계내역서 갈래 첫 줄이 «단가 없음» 이면 갈래 자체를 못 믿게 됩니다.
- 줄마다 **발주기관 · 공고번호**, 아래에 **삭제 요청 안내**.
- `/change` 허브(미리 구운 HTML)에도 링크를 넣었습니다 — 없으면 사이트맵으로만 닿는 외딴 페이지입니다.

### ⑤ 검사
`selfcheck.check_naeyeok()` — collect.py 의 `fields` 에 화면이 읽는 `r.xxx` 이름이 다 있나.
이름표로 읽으므로 «자리»는 안 어긋나지만, **이름이 없으면 undefined 가 되어 뱃지가 조용히 사라집니다.**
일부러 `priced` 를 빼서 잡히는 것까지 확인했습니다.

브라우저 확인(Playwright, 시험 파일 5개: 단가 있는 것 3 · 없는 것 2):
바로받기 5줄 전부 HTTP 200 · 뱃지 3/2 정확 · 토글 5건 · 넘침 0 · 다크모드 색 · JS 오류 0 ·
1400px·430px 둘 다. selfcheck --browser 13+32 통과, 한글 폴더 통과.

⚠️ **실제 내려받기는 GitHub Actions 안에서만 됩니다.** g2b.go.kr 은 클라우드에서도
   소장님 PC 의 리눅스 통로에서도 막혀 있습니다. 첫 회차 뒤 Actions 로그의
   「naeyeok 파일 보관 N개」 줄을 보고 실제로 받아지는지 확인할 것.

### ⑥ 배포하고 나서 — 실제로 받아진 건 0건이었다 (같은 날 밤)

화면은 정상인데 **파일을 실제로 받아 온 건 0건**이었습니다. 원인을 세 겹으로 벗겼습니다.

**첫째, 왜 0건인지 알 방법이 없었다.**
GitHub 기록 화면은 무거워 열리지도 않았습니다(실제로 세 번 시도해 다 실패).
→ `DIAG["naeyeok_fetch"]` 를 `data/diag.json` 에 남깁니다. 대상 수 · 보관 · 두드린 수 ·
   실패 이유별 건수 · 첫 실패 3건(주소와 응답 앞 200자) · 걸린 초.
   **CLAUDE.md 「이유 없는 0건은 다시 없다」 를 내가 어긴 자리였습니다.**

**둘째, 진짜 원인 — 남의 차단기로 문을 막고 있었다.**
diag 를 붙이고 보니 항목 «자체»가 없었습니다 = 함수가 맨 앞에서 되돌아갔다는 뜻.
`NET_DOWN` 은 **조달청 OpenAPI(apis.data.go.kr)** 의 차단기인데,
내역서 파일은 **www.g2b.go.kr** — 다른 서버입니다.
수집 막바지에 차단기가 한 번 내려가면 그 회차는 내역서를 한 개도 안 받고 아무 말도 안 남겼습니다.
→ `NO_NET` 을 따로 두어 `--exportonly` 일 때만 건너뜁니다. 건너뛴 것도 diag 에 적습니다.

**셋째, 그래서 나온 답 — 그 회차엔 나라장터가 안 열렸다.**

    "실패 이유": {"연결 실패: ConnectTimeout": 10}, "걸린 초": 251.0, "시간 상한에 걸림": true

같은 회차 `_probe` 도 조달청 OpenAPI 에 전부 ConnectTimeout — **그 시각 조달청 쪽 장애**였습니다.
⚠️ 이것만으로 「GitHub 에서는 나라장터를 못 받는다」 고 단정하지 않습니다
   (CLAUDE.md 4번 — 로그 한 장으로 단정한 전례가 있습니다). **08:07 회차 diag 를 보고 정합니다.**

### ⑦ 배포를 지키는 두 가지 (이번에 붙임)

1. **시간 상한** `NAEYEOK_BUDGET_S=240` · 건당 `NAEYEOK_TIMEOUT_S=25`.
   안 붙였으면 40개 × 60초 = **40분**, 회차 상한 45분이라 그것만으로 배포가 통째로 실패합니다.
   내역서는 «있으면 좋은 것» 이지 배포를 막아도 되는 것이 아닙니다. (실측 251초에서 정확히 멈췄습니다)
2. **일찍 접기** — 한 건도 못 받았는데 연달아 5번 실패하면 그 회차는 접습니다.
   실측: 251초 → **1.2초**. 다음 회차에 다시 합니다(try 5회까지).

**교훈: 「A 가 안 되니 B 도 안 되겠지」 로 문을 막지 않는다. 서버가 다르면 차단기도 따로 둔다.
그리고 바깥을 부르는 일에는 반드시 시간 상한을 건다 — 그 일 하나가 배포 전체를 잡는다.**

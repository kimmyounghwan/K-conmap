# CLAUDE.md — K-건설맵 v2

이 저장소에서 일하는 Claude 를 위한 안내입니다.
사용자(소장님)는 본인을 초보라고 표현합니다. **설계와 의견을 먼저 이야기하고,
「코드 줘」라고 할 때만 코드를 제시하세요.** 코드를 줄 때는 부분 수정보다 전체 파일을 통째로
주는 방식을 선호합니다(1,000행이 넘으면 부분 수정 동의).

> 📜 **날짜별 기록·실측 경위는 `docs/기록_2026-09.md` 에 있습니다.**
> 이 파일은 «지금 맞는 것»과 «규칙»만 담습니다. 왜 그 규칙이 생겼는지 알아야 할 때만 그 파일을 여세요.
> (2026-09-14 에 나눴습니다. 5,464줄 → 이 파일 + 기록. 한 줄도 안 지웠습니다)

---

## 0. 창을 열면 «먼저» 할 것

1. **이 파일을 읽는다** — 소장님 상시 지시. 같은 실수를 두 번 하지 않기 위한 것입니다.
2. **날짜·시각을 말하기 전에 반드시 찍어 본다** — `date` 또는 `python -c "import datetime..."`.
   한국시간은 UTC+9 입니다. 「어제」·「오늘」을 짐작으로 말하지 마세요.
3. **소장님 PC 를 만지려면 «이 컴퓨터에 연결»** 이 돼 있어야 합니다. 새 창에서는 안 이어집니다.
4. **파일 탐색기 권한은 턴 사이에 만료됩니다** — 쓸 때마다 `computer_resolve_access` → `request_access`.

### 소장님이 정한 일하는 방식

- **코드는 요청할 때만.** 먼저 설계와 의견을 이야기하고, 「코드 줘」라고 하면 그때 제시합니다.
- **손으로 눌러야 하는 단계를 만들지 않습니다.** 밖에 있을 때가 많아 캡처를 못 하십니다 —
  확인·실행은 클로드가 직접 합니다. 소장님 몫으로 남는 것은 «클로드가 못 하는 것»뿐입니다
  (계정 만들기·본인인증·결제·도메인 구입·GitHub Secret 등록).
- **비용은 «항상 자주» 체크합니다.** 묻지 않아도 먼저 확인하고 알려 드립니다.
- **팀원(서브에이전트)은 쓰지 않습니다** (2026-09-11 · 토큰 소비). 조사·진단·검증은 직접 합니다.
- **🚨 올리기 전에 «항상» 검증합니다** (소장님, 2026-09-16 — 두 번째 지시).
  순서는 8절 15번에 적어 두었습니다. 「문법 통과」·「빌드 됐다」는 검증이 아닙니다.
- **문서만 고칠 때는 커밋만 하고 푸시는 모읍니다.**

### 🚦 「자동 이상 없지?」 를 물으면 — `health.json` **하나만** 본다

    https://k-conmap.com/data/health.json

    {"at":"2026-09-14 19:31","ok":true,"why":"","fails":0,
     "got":{"first":361,"live":477},           ← 이번 회차에 조달청에서 실제로 받은 줄
     "newest":{"first":"2026-09-11","live":"2026-09-11"},
     "n":{"first":13756,"live":15073},
     "gaps":{"first":[],"live":[]}}            ← 빠진 날 (없으면 정상)

⚠️ **`overview.json` 의 `built` 로 판단하지 말 것.** 수집이 0건이어도 집계·빌드·배포는 돌아서
그 시각은 갱신됩니다. 「초록 체크가 떴는데 자료는 5시간 멈춰 있었다」가 실제로 있었습니다.

⚠️ **개찰은 오전 9시부터 열리고 11시에 65%가 몰립니다.** 아침 9시 전에 「오늘 개찰 0건」은 정상입니다.
   공고는 **주말에도 나옵니다**(토 평균 6.4건 · 일 4.4건). 다만 정말 0건인 주말도 있습니다 —
   `gaps` 는 「물어봤는데 0건인 날」과 「못 물어본 날」을 갈라서 셉니다(`days.json` 의 `ok`).

---

## 1. 이 프로젝트가 뭔가

조달청 나라장터 공공 입찰 데이터를 분석해 **무료로** 보여주는 사이트입니다.
`https://k-conmap.com` (Firebase Hosting · Cloudflare DNS A 199.36.158.100 DNS only).

- 회원제 없음, 로그인 없음, 대문(랜딩) 없음
- 수익은 애드센스 광고 (유료 구독 아님)
- **공사 전용** — 용역은 뺐습니다(3년치 482,630건 중 75%가 용역이었음).
  되돌리려면 `collect.py` 의 KINDS 와 `build_json.py` 의 SOURCES 주석만 풀면 됩니다(원본 zip 보존).
- 하단 탭 8개: 바로투찰 · 1순위 · 공고 · 분석 · 서식 · 설계변경 · 착공현장 · 도구

---

## 2. 절대 지켜야 할 원칙

### 비용 (이게 이 구조의 존재 이유입니다)

1. **모든 분석 데이터는 정적 JSON.** Firebase 읽기 과금 0. DB 를 새로 끌어들이자는 제안은 하지 마세요.
2. **큰 파일을 브라우저로 보내지 않는다.** 기관·업체는 이름 첫 글자로 색인(`idx`)을 나누고,
   집계는 200개씩 묶음(`dat`)으로 쪼갭니다. 사용자는 검색한 것이 든 묶음 하나만 받습니다.
3. **Realtime DB 는 `onValue` 금지.** 화면 열 때 `get()` 한 번만.
   (사라사에서 24시간 폴링이 돌아 요금이 샌 전례가 있습니다)
4. **항상 `limitToLast`.** 노드 전체를 읽는 코드는 리뷰에서 막으세요.
5. `first.json` / `live.json` 은 **300건 상한**.
6. `/assets/**` 1년 캐시, `index.html` 캐시 금지 — `web/firebase.json`.
   ⚠️ firebase.json 헤더는 겹치면 **뒤엣것이 이깁니다**(실측). 넓은 규칙 위, 좁은 규칙 아래.
7. **전송량을 논할 때 raw 크기를 보면 틀립니다. 반드시 gzip 후로 재세요.**
   (이름표를 번호로 바꿔 raw 161KB 를 줄였는데 전송량은 5.6KB 밖에 안 줄었던 적이 있습니다.
   gzip 이 이미 반복 문자열을 지웁니다)

### 보안

1. **`web/database.rules.json` 은 기존 Streamlit 규칙과의 병합본입니다.**
   `stats`/`sarasa`/`archive_1st`/`archive_live`/`service_1st`/`service_live` 를 지우면
   **돌아가는 Streamlit 사이트가 깨집니다.** 규칙은 교체가 아니라 병합.
2. **루트 규칙에서 익명을 반드시 제외**해야 합니다. 원래 루트가 `".read": "auth != null"` 이라,
   익명 로그인을 켜면 누구나 DB 전체를 읽고 쓸 수 있게 됩니다.
   현재: `auth != null && auth.token.firebase.sign_in_provider != 'anonymous'`
3. **순서: ① 규칙 배포 → ② 익명 로그인 켜기.** 절대 반대로 하지 마세요.
4. **API 키·비밀번호를 코드에 넣지 마세요.** 전부 `.env` (gitignore 됨).
5. 🚨 **글 칸을 하나 늘리면 배포가 두 군데로 갈립니다** (2026-09-14 `jobs.co` 때 겪음).
   `jobs/$id` 에 `"$other": { ".validate": false }` 가 있어서 **규칙에 없는 칸이 하나라도 섞이면 글 전체가 거부**됩니다.
   그리고 **Actions 는 규칙을 안 올립니다** — 워크플로는 `action-hosting-deploy`(호스팅 전용)입니다.
   → 칸을 늘렸으면 `3_규칙올리기.bat` 를 **화면보다 먼저** 돌릴 것. 순서가 바뀌면 새 화면에서 글쓰기가 전부 실패합니다.
6. 원래 규칙 백업: `_임시/rtdb_rules_백업_20260830.json`
7. **Firebase 웹 API 키는 «비밀»이 아닙니다.** 브라우저 번들에 실려 나가도록 설계된 공개 식별자입니다.
   보호는 보안 규칙 + HTTP 리퍼러 제한이 합니다. GitHub 이 «비밀키 유출»이라고 띄워도 **지우면 사이트가 죽습니다.**
   (리퍼러 제한: k-conmap.com / www / .web.app / .firebaseapp.com / localhost:5173)

---

## 3. 구조

```
[내 PC 파이썬 / GitHub Actions]                    [Firebase Hosting]
 collect.py    조달청 수집   ─┐
 build_json.py 3년치 집계     ─┼→ web/public/data/*.json ─→ npm run build ─→ prerender.py ─→ deploy
 sitemap.py    주소 목록     ─┤                                                    │
 prerender.py  주소별 HTML   ─┘                                    방문자는 정적 파일만 받음
```

핵심: **사용자 접속 시 계산하지 않습니다.** 미리 구운 JSON 을 그냥 내려받습니다.

⚠️ **순서: `npm run build` → `prerender.py` → 배포.** build 가 dist 를 비우므로 바꾸면 통째로 사라집니다.

---

## 4. 명령

파이썬은 **루트**에서, `npm`·`firebase` 는 **`web`** 안에서 돕니다.

```
python run_all.py              전체 (수집+집계+빌드+배포)
python run_all.py --quick      집계 생략 (평소 이것)
python collect.py --days 3     수집만
python collect.py --exportonly 조달청을 안 부르고 저장소로만 다시 굽기
python build_json.py           집계만 (3~5분) — spot·sim 은 이걸 돌려야 생깁니다
python tools/selfcheck.py      검사 (--browser 를 붙이면 화면까지)
cd web && npm run dev / npm run build
cd web && firebase deploy --only hosting        (또는 --only database)
```

---

## 5. 데이터 구조 (JSON 스키마)

`web/public/data/` — **gitignore 됨** (98MB, 2,558 파일).

| 경로 | 내용 |
|---|---|
| `overview.json` | 전체 요약 (건수·기간·평균 투찰률·`sjq.p50`) |
| `agency/idx/*` · `agency/dat/*` · `agency/top.json` | 기관 — 첫 글자 색인 + 200곳 묶음 + 상위 300 |
| `corp/idx/*` · `corp/dat/*` · `corp/top.json` | 업체도 같은 구조. 키는 정규화된 업체명 |
| `board/{first,live}.json` + `-con-{n}.json` + `-con-idx.json` | 7주치 목록 500건 묶음 + 검색 색인 |
| `bidindex.json` | **마감 전** 공고 (계산에 필요한 칸만, 이름표 `f` 로 읽습니다) |
| `bidresult.json` | 최근 7일 개찰 (채점 화면) |
| `licstat.json` | 면허별 경쟁도 `{코드: [이름, 개찰건수, 참가중앙, 10곳미만%]}` |
| `naeyeok.json` | **단가가 든 갈래만** — 설계내역서 227 · 단가산출서 31 (⚠️ `naeyeok-all.json` 에는 이 258건이 **없습니다**) |
| `naeyeok-all.json` | 나머지 4,032건 — 공내역서 1,886 · 그 밖의 1,415 · 물량 705 · 수량 26 |
| `health.json` | 이번 회차가 제 일을 했나 (위 0절) |

**📚 내역서모음은 «밖으로 내보내지 않는» 자료입니다.**
바탕화면 `나노_건설맵 코드 등/내역서모음/` — 저장소 **바깥**입니다(.gitignore 에도 적어 두었습니다).
조달청이 공개한 내역서지만 그 안의 **노임단가·중기사용료**는 값이 유료 자료와 겹칩니다.
견적 참고로 쓰는 것까지이고, **사이트에 올리거나 배포하지 않습니다.**
찾아보기: `내역서 찾아보기.xlsx` (단가 찾기 · 자료 목록 · 뽑은 줄 · 눈으로 볼 PDF).

**내역서를 다룰 때 두 번 데인 것 (2026-09-15).**

1. **목록은 두 파일입니다.** `naeyeok-all.json` 만 읽으면 알짜 258건(설계내역서·단가산출서)을 통째로 놓칩니다.
   `naeyeok-all.json` 의 `kinds` 에는 그 숫자가 적혀 있는데 `r` 에는 줄이 없습니다 — **메타를 믿지 말고 줄을 세십시오.**
2. **엑셀은 시트를 끝까지 봐야 합니다.** 단가가 든 시트는 대개 뒤쪽(내역서·일위대가·자재단가)입니다.
   앞 네 장만 보던 것을 끝까지(14장) 보게 고쳤더니 **같은 254건에서 29 → 74건**이 됐습니다.
   「공사설정」·「INITIAL」 시트의 숫자는 단가가 아닙니다 — 걸러 내십시오.
3. 조달청 xlsx 에는 **바깥 파일 연결(externalLinks)** 이 든 것이 흔해 `openpyxl` 이 TypeError 로 넘어집니다.
   `tools/naeyeok_fetch.py` 의 `raw_price()` 가 시트 XML 을 직접 읽습니다(`<x:row>` 처럼 **앞가지**가 붙은 파일도 있습니다).

**업체명 정규화는 `build_json.py` 의 `norm_corp` 과 `web/src/lib/fmt.js` 의 `normCorp` 이
반드시 같아야 합니다.** 한쪽만 고치면 검색이 조용히 안 맞습니다.

---

## 6. 파일 지도

| 고칠 것 | 파일 |
|---|---|
| 색·간격·다크모드 | `web/src/styles.css` |
| 하단 탭 | `web/src/App.jsx` 의 `TABS` · 주소는 `web/src/main.jsx` |
| 1순위 / 공고 화면 | `web/src/pages/FirstBoard.jsx` · `LiveBoard.jsx` |
| 바로투찰 · 채점 | `web/src/pages/BaroBid.jsx` |
| **투찰금액·분위·확률 계산 전부** | `web/src/lib/bidmath.js` |
| 점수 배점·등급 기준 | `web/src/lib/engines.js` · `winodds.js` |
| 분석 · 기관/업체 페이지 | `web/src/pages/Analysis.jsx` · `AgencyPage.jsx` · `CorpPage.jsx` · `Spot.jsx` |
| 면허별 경쟁도 | `web/src/pages/LicStat.jsx` (`/lic`) |
| 건설 도구 | `web/src/data/tools.json` · `web/src/tools/calcs.jsx` · `pages/Tools.jsx` |
| 서식 · 설계변경 · 알아보기 | `pages/Forms.jsx` · `Change.jsx` · `Guide.jsx` (+ `web/src/data/*.json`) |
| 담은 공고 | `web/src/lib/basket.js` |
| 정적 JSON 로더 | `web/src/lib/data.js` (`indexRows` 로 **이름표**로 읽습니다) |
| 면허 키워드·표시 형식 | `web/src/lib/fmt.js` · `lib/lic.js` |

### 🗺️ 화면 목록 — **새 화면을 만들기 «전에» 여기부터 보십시오** (2026-09-16)

🚨 소장님(2026-09-16): 「너가 만든 거잖아..왜 기억을 못 해?」
   하루에 **두 번** 이미 있는 것을 또 만들자고 했습니다 — 업체 성적표 생성기(`tools/report_*`)와
   설계변경 2줄 자동변환(`/change/twoline`). **둘 다 그날 라우트 목록에서 눈으로 보고도** 못 알아봤습니다.
   위 «파일 지도» 는 요약이라 `Change.jsx` 한 줄 뒤에 화면 다섯 개가 숨어 있었습니다.
   → **「없는 것 같다」는 확인이 아닙니다.** `web/src/main.jsx` 의 라우트와 아래 표를 먼저 보십시오.

| 주소 | 무엇 | 파일 |
|---|---|---|
| `/` · `/calc` | 바로투찰 — 얼마 넣을까 | `BaroBid.jsx` |
| `/first` | 1순위(개찰 결과) 목록 | `FirstBoard.jsx` |
| `/live` | 마감 전 공고 목록 | `LiveBoard.jsx` |
| `/analysis` | 발주기관 분석 · 업체 자가진단 (화면 안 칸 2개) | `Analysis.jsx` |
| `/agency/{기관}` | 기관 한 곳 — 미리 구움 | `AgencyPage.jsx` |
| `/corp/{업체키}` | 업체 성적표 — 미리 구움 | `CorpPage.jsx` |
| `/notice/{공고번호}` | 공고 · 개찰 한 건 | `NoticePage.jsx` |
| `/daily` · `/daily/{날짜}` | 그날 개찰 성적표 | `DailyPage.jsx` |
| `/lic` | 면허별 경쟁도 | `LicStat.jsx` |
| `/forms` · `/forms/{slug}` | 건설 서식 138장 | `Forms.jsx` |
| `/change` · `/change/{주제}` | 설계변경 알아보기 | `Change.jsx` |
| `/change/calc` | 설계변경 계산기 | `Change.jsx` |
| `/change/excel` | 설계변경 자동계산 엑셀(시트 11장) | `Change.jsx` |
| `/change/naeyeok` · `/{kind}` | 설계변경 내역서 | `Change.jsx` |
| **`/change/twoline`** | **설계변경 2줄 자동변환** — 내역서 끌어놓으면 당초·변경·증감으로 벌려 내려받기. **브라우저 안에서만**, xlsx XML 만 고쳐 서식·인쇄영역·그림 보존 | **`TwoLine.jsx`** |
| `/naeyeok` | 견적서·내역서 작성 대행 (파는 것) | `Naeyeok.jsx` |
| `/report` | 업체 입찰 성적표 (만들어 드림) — 엔진은 `tools/report_data.py` · `report_html.py` · `report_pdf.mjs` | `Report.jsx` |
| `/safety` | 안전관리계획서·유해위험방지계획서 (준비 중) | `Safety.jsx` |
| `/jeoksan` | K-적산 소개. 도면 찍기(리습·PC)는 «준비 중» | `Jeoksan.jsx` |
| `/jeoksan/run` | **수량산출서 만들기 — 브라우저에서 «실제로 돕니다»** (2026-09-16).
재료표(xlsx)+치수표(csv) → 산출서·집계·태그별·검산·쓴표 5장.
셈은 `lib/qto.js`·`lib/susik.js` — **PC 의 `K-적산/kqto.py`·`kq_susik.py` 와 같은 수량을 내야 합니다.**
한쪽만 고치지 말 것. 맞는지는 `node tools/시험_적산.mjs` | `JeoksanRun.jsx` |
| `/shareone` | 쉐어원 — 사무실 공유폴더 배포 | `ShareOne.jsx` |
| `/cad` · `/cad/{slug}` | 캐드 유틸 (리습 내려받기) | `Cad.jsx` |
| `/tools` · `/tools/{slug}` | 건설 도구 12가지 | `Tools.jsx` |
| `/guide` · `/guide/{slug}` | 입찰 알아보기 | `Guide.jsx` |
| `/how` | 보는 방법 — 처음이시면 여기부터 | `How.jsx` |
| `/qna` | 묻고 답하기 | `Qna.jsx` |
| `/jobs` | 구인구직 (고용24 iframe) | `Jobs.jsx` |
| `*` | 404 — noindex | `NotFound.jsx` |

⚠️ **이 표는 손으로 적은 것이라 늙습니다.** 화면을 더하거나 지웠으면 **여기도 같이 고치십시오.**
   진짜 목록은 언제나 `web/src/main.jsx` 입니다.

---

`engines.js` 의 점수 기준은 **기존 `app.py` 의 `engine_bid_score` 를 그대로 옮긴 것입니다.**
숫자를 임의로 바꾸지 마세요 (핫존 30 / 경쟁 20 / 유사공고 20 / 안정성 15 / 데이터량 15).

---

## 7. 지금 상태 (2026-09-14)

### 자동화

- `.github/workflows/update.yml` · cron `7 21,22,23,0,5 * * *` (UTC)
  = 한국시간 **06:07 · 07:07 · 08:07 · 09:07 · 14:07**
- 회차가 끝나면 **스스로 다음 회차를 부릅니다**(25분 쉬고, 한국시간 06~19시일 때만).
  예약(cron)은 하루 3번밖에 안 걸립니다 — 사슬이 하루를 이어 갑니다.
- `concurrency: group: update, cancel-in-progress: false` · `timeout-minutes: 80`
- `push.paths` 는 `web/**` · `*.py` · `update.yml` **뿐** →
  **CLAUDE.md 만 올리는 푸시는 사이트를 다시 굽지 않습니다.**
- 저장소(`data/store`)는 **Actions 캐시**에만 있습니다. 캐시가 비면 `data/seed/*.json.gz` 로 복구합니다
  (씨앗은 주 1회 다시 굽고 커밋합니다. 나이는 `data/seed/built.json` 의 **날짜**로 잽니다 —
  파일 수정시각은 체크아웃 때마다 «방금»이 되어 못 씁니다).
- ⚠️ **예약 시각은 «정시»가 아닙니다.** GitHub 는 정각에 몰려 몇 시간씩 밀거나 건너뜁니다.
  「사이트가 안 바뀐다」를 자료 문제로 단정하기 전에 **Actions 의 «실제 실행 시각»부터** 보세요.

### 비용 (Firebase 콘솔 실측 · Blaze 요금제 · 이번 달 $0.00)

| 항목 | 지금 | 한도 | |
|---|---|---|---|
| Hosting **저장용량** | 약 0.96 GB | 10 GB | 보관 버전 **4개**로 잠금 |
| Hosting **전송량** | 하루 8.3 MB | **10 GB/월** | 초과 $0.15/GB |
| Realtime DB 저장 | 97.5 MB | 1 GB | 대부분 옛 Streamlit 노드 |

- **한도는 «프로젝트» 단위**입니다. 도메인을 더 사도 한도는 안 늡니다. 나누려면 프로젝트를 나눠야 합니다.
- **저장용량의 주인은 «화면»이 아니라 «집계 JSON»** 입니다(`corp` 59MB · `board` 28MB).
  배포 1벌 ≈ 234MB(Actions 기준). Firebase 는 버전 사이에 안 바뀐 파일을 깎아 주지 않습니다.
- **⚠️ 콘솔의 「출시 저장용량 = 보관 4개」가 풀리면 며칠 만에 10GB 를 채웁니다.**
  자동화를 늘리기 전에 이 설정부터 확인하세요.
- **비용은 내 계산이 아니라 콘솔 숫자로 말합니다.** 두 번(배포 1벌 93MB · 하루 990명) 다 내 계산이 틀렸습니다.
- 주 1회 예약 작업(월 09:10 KST)이 콘솔을 직접 읽어 보고합니다. 읽기만, 결제 버튼은 안 누릅니다.

### 검색·색인

- 주소마다 **진짜 HTML** 을 굽습니다(`prerender.py`). 그 전에는 어떤 주소든 똑같은 2,161B 빈 껍데기였습니다 —
  네이버는 자바스크립트를 거의 안 돌리므로 아예 빈 페이지였습니다.
- 색인 현황(2026-09-09 실측): 구글에 실제로 올라간 것은 **탭 10장뿐**.
  서치콘솔 「발견됨 — 색인 안 됨」 1,145건. **병목은 사이트맵이 아니라 크롤링 예산**입니다.
  → 글·페이지를 더 만들어도 대기줄만 깁니다. 지금 값어치 있는 한 가지는
  **소장님이 건설 밴드·카톡방·블로그에 링크를 올리는 것**입니다.
- **재는 법(1분)**: ① 구글에 `site:k-conmap.com` ② 서치콘솔 > 색인생성 > 페이지 ③ 실적(노출·클릭·순위).
  ⚠️ 서치콘솔 요약은 2~5일 늦습니다. `site:` 가 «오늘»을 봅니다.
- **사이트맵은 미리 구운 것보다 많이 내면 안 됩니다.** 안 구운 주소를 내면 크롤러가 빈 껍데기를 봅니다.
  → `SITEMAP_CORPS` ≤ `PRERENDER_CORP` · `SITEMAP_NOTICES` ≤ `PRERENDER_NOTICE`.
  신생 사이트에 주소 수천 개를 던지면 «발견됨-색인 안 됨»만 쌓입니다. 천천히 올리세요.

### 애드센스

게시자 `pub-2267356729797930` · k-conmap.com **검토 요청 완료(2026-09-08)** · 결과 대기.
스니펫은 `web/index.html`(prerender 가 껍데기로 읽어 전 페이지에 따라감) +
`web/public/{about,privacy,contact,terms}.html`(React 를 안 태우는 독립 HTML 이라 직접 넣음) + `ads.txt`.
⚠️ 승인 뒤 «지급 정보(주소·세금)» 는 소장님이 직접 입력합니다. 클로드는 그 칸을 건드리지 않습니다.

### 남은 일

1. 9월 말: 옛 Streamlit(나노_건설시스템) 종료 → 그 뒤 Realtime DB 97.5MB 정리
2. 물품 입찰 사이트 — `probe` 부터 (아래 9절)
3. 조달청 키 재발급 · GitHub Secrets `GMAIL_APP_PASSWORD` 갱신
4. `www.k-conmap.com` DNS 레코드 추가 (Cloudflare, 소장님 승인 필요)
5. 첫 화면 전송량: `corps`(순위 30곳)를 카드 펼칠 때만 받게 — 설계는 끝, 급하지 않아 보류

---

### 캐드 유틸 - `캐드유틸/KCM-CAD.lsp` (2026-09-14 · 소장님 검증 대기)

소장님 요청: 「떠도는 리습을 통합해서, 캐드에서 업로드하면 쓸 수 있게 + 설명서 자세히」.
남의 리습을 옮기지 않고 **처음부터 직접 작성**했습니다(재배포 조건이 불분명합니다).

- 자리: `바탕화면/나노_건설맵 코드 등/캐드유틸/` - 아직 저장소에도 사이트에도 안 넣었습니다
- 명령 7개: `KL` 길이합 · `KA` 면적합 · `KN` 숫자합 · `KXY` 좌표뽑기 · `KC` 개수세기 · `KUNIT` · `KHELP`
- ⚠️ **이식성 규칙 - 깨지 말 것**: 순수 AutoLISP 만. `vla-`(ActiveX)·DCL·`(command ...)` 안 씁니다.
  엑셀은 ActiveX 자동화 대신 CSV 파일로. 그래야 AutoCAD / **LT 2024 이상** / 캐디안 / ZWCAD 에서
  **같은 파일 하나로** 돕니다. 하나라도 쓰면 국산 캐드에서 죽습니다.
- 인코딩은 **cp949**(본판) + UTF-8(예비). 전각 기호 `—` `«` `»` 는 **cp949 에 없어** 쓰면 인코딩이 터집니다.
- 캐드가 없어 **실행 검증을 못 합니다.** 대신 정답을 아는 `시험도면.dxf`(R12, cp949, $DWGCODEPAGE=ANSI_949)를
  만들어 소장님이 5초에 대조하게 했습니다. 계산식(bulge 호길이·활꼴 넓이·신발끈)은 파이썬으로 포팅해
  손계산과 **차이 0.000000000** 확인했습니다.
  시험도면 정답(mm): KL **29.854 m** · KA **12.356 m2(3.74평)** · KN **1,342.5** · KC **15개** · KXY **11개**
- 사이트에 올릴 때는 **서식 코너와 같은 구조**(`web/public/lisp/*.lsp` + `prerender.py`, 유틸 1개 = 페이지 1개)
- 다음 후보: `KLAY` 레이어정리 · `KTXT` 문자일괄 · **`KBOM` 캐드 수량 -> 내역서 CSV** (이게 차별점)

---

## 8. 🚨 반드시 지킬 것 — 전부 «실제로 사고가 났던 것»

이건 «좋은 습관» 목록이 아니라 **틀렸던 기록**입니다. 대부분 소장님이 직접 잡아낸 것입니다.

### 1. 조달청이 주는 값이 있으면 그대로 쓴다 — 손으로 만들지 않는다

주소든 금액이든 마찬가지입니다. 만들어 쓰면 반드시 어디선가 틀립니다.

- 「나라장터 공고」 주소를 손으로 조립하며 차수를 `000` 으로 박아 **500건 중 29건**이 엉뚱한 공고로 갔습니다.
  조달청은 `bidNtceDtlUrl` 로 정확한 주소를 이미 주고 있었습니다.
- A값·참가업체수를 「조달청이 안 준다」고 단정했는데 **둘 다 응답에 있었습니다.**
- 면허를 공고명 낱말로 «추측» 해서 정확도가 15.7% 였습니다. `lic` 에 정확히 오고 있었습니다.
- **장치**: `collect.py` 가 오퍼레이션마다 응답 필드 이름을 `web/public/data/diag.json` 에 남깁니다.
  **없다고 말하기 전에 이 파일부터 보세요.**

### 2. 없다고 단정하지 않는다 — «확인의 방법»이 틀렸을 가능성을 먼저 의심한다

같은 실수를 다섯 번 넘게 했습니다. 방향이 반대인 경우(「됐다」를 확인 없이 말하는 것)도 같은 잘못입니다.

- 개찰 순위 API 를 **날짜로만** 두드리고 「그런 오퍼레이션이 없다」고 했습니다.
  **공고번호로 부르면 옵니다.** «응답 없음»과 «필수값이 다름»과 «없는 서비스(HTTP 400)»는 다른 이야기입니다.
- 애드센스 `/home` 이 거부라 「계정이 없다」고 할 뻔했습니다. `/signup` 으로 들어가니 됐습니다.
- ConnectTimeout 한 번 보고 「조달청 API 가 먹통」이라고 단정했습니다. 한 시간짜리 일시 장애였습니다.
- 「색인이 붙기 시작했다」를 확인 없이 보고했습니다. `site:` 검색 1분이면 알 일이었습니다.
  **나쁜 소식만 검증하고 좋은 소식은 그냥 전하는 버릇을 경계할 것.**

### 3. 붙이기 전에 실제로 열어서 확인한다

만든 주소는 **브라우저로 직접 열어 보고**, 계산 결과는 **실제 자료로 대조**합니다.
«형식이 맞으니 되겠지»·«200 이 떴으니 됐다»는 확인이 아닙니다.
(서식 파일은 200 이 아니라 **앞 두 글자가 `PK`** 인지로 확인합니다)

### 3-1. 버튼 이름이 실제 동작과 다르지 않게 한다

「나라장터 투찰」이라고 써놓고 실제로는 공고 상세 화면이 열렸습니다(진짜 투찰은 로그인+보안토큰이
필요해 링크로 갈 수 없습니다) → 「나라장터 공고」로 바로잡았습니다.
바로투찰 화면처럼 «금액이 나온 뒤»의 자리에서는 다음 행동이 «투찰» 하나뿐이므로
공고 링크를 빼고 **「금액 복사하고 나라장터 열기」 한 버튼**으로 모았습니다.

### 4. 같은 규칙을 두 번 적었으면 «두 벌이 같은지»를 검사에 넣는다

주석으로 「같이 고치세요」라고 적어 두는 것은 방어가 아닙니다. 이걸로 다섯 번 넘게 당했습니다.

| 두 곳에 적힌 규칙 | 검사 |
|---|---|
| 등급 — `winodds.js` ↔ `build_json.py` | `check_grades()` 32가지 |
| 자동 분위 — `PICK_AUTO`(collect.py) ↔ `AUTO_RULE`(bidmath.js) | `check_autorule()` |
| bidindex 칸 — collect.py `f` ↔ 화면 | `check_bidindex()` |
| 검색 색인 칸 순서 | `check_boardidx()` |
| 순위 파일 자리 | `check_boardrank()` |
| 합산 확률 — 화면 ↔ 파이썬 | `check_atleastone()` |

**같은 값을 «만드는 길»이 여러 곳에 적혀 있으면 하나를 고칠 때 나머지는 반드시 어긋납니다.**
→ 함수 하나로 모읍니다: `shownBid()` · `quickBid()` · `smartBid()` · `lowerLimit()`(정의는 `engines.js` 한 곳) ·
   `indexRows()` · `scoreState()`.

### 5. «검사 도구 자신»도 사본이다 — 검사가 틀리면 코드가 틀린 것보다 나쁘다

네 번 겪었습니다.
- `selfcheck.py` 가 자기만의 `IDX_F` 로 시험 자료를 만들고 있었습니다 → **collect.py 에서 읽어 옵니다.**
- 기하 검사가 `display:none` 인 칸(rect 0,0)을 «칸 밖으로 나갔다»고 셌습니다 → 크기 0 인 자식은 건너뜁니다.
- 시험 서버가 없는 파일에 HTML 을 200 으로 돌려줘 **가짜 버그를 한참 쫓았습니다**
  → 시험 서버는 `/data/`·`/assets/` 에 반드시 404 를 내야 합니다. 그리고 **빌드 뒤에는 시험 파일을 다시 만듭니다.**

### 6. 「내 환경에서 됐다」는 확인이 아니다

소장님 환경의 다른 점(**한글 경로 · 공백 · 윈도우 · 다른 글꼴·확대율**)을 흉내 내서 돌려 봐야 확인입니다.
- `new URL(import.meta.url).pathname` 은 **이미 퍼센트 인코딩된 문자열**입니다. 거기에 또 인코딩을 걸어
  한글 폴더에서 검사가 안 돌았습니다 → 경로를 조립하지 말고 그냥 `import` 하세요.
- 칸 안에 설명 두 줄을 넣고 **칸을 넓히는 쪽으로만** 두 번 고쳤습니다. 내 스크린샷에서는 멀쩡했습니다.
  → **칸에는 숫자만, 설명은 칸 아래.** 그리고 넘침은 눈이 아니라 `getBoundingClientRect()` 로 잽니다.
- **사용자가 같은 것을 두 번 지적하면 «더 잘 고치라»가 아니라 «설계를 바꾸라»는 뜻입니다.**

### 7. 없는 자료를 «없는 채로» 그리지 않는다 — 모자라면 모자란다고 말한다

값이 틀린 화면보다 «조용히 덜 보여주는» 화면이 더 위험합니다. 아무도 못 알아챕니다.
- 묶음 하나가 404 났는데 화면이 아무 말 없이 17줄만 그렸습니다 → `pageReady` 로 «불러오는 중».
- 목록 쪽수를 «받아 둔 것»에서 세어 **25쪽(이틀치)에서 끝났습니다** → 쪽수는 목록표(meta)의 총건수로.
- 「예상 참가 모름」·「실측 부족」·「값 부족」을 **그대로 적습니다.** 없는 숫자를 만들지 않습니다.
- 합산 확률에서 «모르는 것»은 0으로 치지 않고 **셈에서 뺍니다**(그리고 뺐다고 적습니다).

### 8. 판정이 있는 화면은 «판정을 한 번만, 맨 위에» 적는다

칸마다 따로 계산해 따로 말하면 언젠가 서로 반대말을 합니다. 실제로 한 화면이
「실격」과 「1순위」를 동시에 띄웠습니다. → 판정은 하나, 나머지 칸은 전부 그 판정과 같은 말을 합니다.
그리고 **그걸 검사에 넣습니다**(selfcheck 303번).

### 9. 사후 판정과 사전 확률을 섞지 않는다

- 「이대로 넣으면 실격입니다」는 **틀린 말**입니다. 금액은 고정이고 하한은 사정률로 움직입니다.
  → 「살아남을 확률 약 N%」로 말합니다(`passProb`). 채점 화면(사후)에서만 «실격이었습니다»라고 단정합니다.
- 「최소 2위」처럼 **모르는 등수를 말하지 않습니다.** 순위 자료가 1곳뿐이면 「등수는 알 수 없습니다」.
- 사후에 «가까웠던» 금액은 사전에 «넣을» 금액이 아닙니다(생존 편향).
  최저가는 낙찰가에 더 가깝지만 **절반이 실격**입니다.

### 10. 이름이 겹치지 않게 한다 (CSS)

`.chips`/`.chip` 을 지역 칩과 투찰률 칩이 같이 써서, 나중에 쓴 규칙이 이겨
선택된 「전국」이 흰 글씨에 연한 바탕이 되어 안 보였습니다 → 투찰률 칩은 `.ratepick` 으로 분리.
**색도 규칙입니다** — 같은 값이 두 곳에서 다른 색이 되지 않게 `riskTone(p)` 하나로 모았습니다.
적록색맹(남성 약 5%) 때문에 **색만으로 말하지 않고 숫자를 늘 글자로 같이 적습니다.**

### 11. 힘들게 받은 값이 «다음 수집»에 지워지지 않는지 본다

`row_first` 의 보존 목록에 `corps`·`rq`·`nrank` 가 없어서, 순위 조회로 채운 30곳을
다음 회차 목록 수집이 통째로 덮어썼습니다(저장소에 순위가 0건이었습니다).
A값에서도 같은 함정(«기초금액 있으면 건너뛰기»)이 있었습니다.

### 11-1. «안 지나가는 문» 안에 모으면 영영 모이지 않는다 (2026-09-16)

3년치 순위 보관함(`ranks3y`)이 **76줄**이었습니다. 순위를 받은 개찰은 11,873건인데도입니다.
에러는 한 줄도 안 났고 회차 로그도 멀줦했습니다. 소장님이 「자료 잘 받고 있지?」라고
물어봐서 찾았습니다.

- 갈무리가 「순위를 **새로** 받은 회차」 안에만 있었습니다.
- 그런데 순위 조회는 «corps 가 1곳뿐인 공고»만 물어봅니다(중복 호출을 막으려고).
- → **이미 받아 둔 줄은 영영 담길 차례가 오지 않았습니다.** 그사이 first.json 은 70일짜리라 날마다 버렸습니다.

교훈: **모으는 일을 «받는 일» 안에 넣지 말 것.** 받는 일은 중복을 피하려고
건너뛰기 마련이라, 거기 얹혀 놓으면 건너뛴 때 모으는 일도 같이 건너뜁니다.
모으는 일은 **조건 없이 도는 자리**에 놓고, 담은 줄에는 표시(`k3y`)를 남깁니다.
그리고 그 표시를 **보존 목록에 넘어야** 합니다(11번과 같은 함정).
→ 검사 `check_ranks3y()` 가 세 가지를 다 봅니다.

⚠️ 그리고 **업체 명단은 지금도 1순위 전용입니다** (`build_json.py` 의
`d2 = df[df["1순위업체"] != ""]`). 사이트의 업체 57,694곳은 전부 «한 번이라도
1순위를 한 회사» 입니다. 실측(순위 붙은 개찰 1,499건): 투찰에 나온 업체 19,750곳 중
**92.9%(18,344곳)가 한 번도 1순위를 못 해** 사이트에 아예 없습니다.
「건설사 이름을 치면 안 나온다」의 정체입니다. 보관함이 차면 가벼운 오리(참가 건수 ·
평균 등수 · 평균 투찰률 · 최근 10건)로 실기로 정했습니다 (소장님, 2026-09-16).

### 12. 「앞에서 N건만 집기」를 조심한다

목록 순서가 매번 같으면 **같은 N건을 하루 21번 다시 묻고** 뒤쪽은 영영 차례가 안 옵니다.
→ 「마지막으로 물어본 시각」(`bask`/`rask`)을 적어 두고 **오래된 것부터** 돌아가며 묻습니다.

### 13. 큰 파일은 반드시 «임시 파일 → 갈아 끼우기»

쓰는 도중에 죽으면 반쪽 JSON 이 남습니다. 실제로 `data/store/live.json` 이
15.6MB → 6.5MB 로 잘려 못 읽는 파일이 됐습니다. Actions 가 80분 상한에 걸려도 같은 일이 납니다.
→ `save_store`·`save_json` 전부 tmp → `os.replace`.

### 14. 승률을 가르는 건 «금액»이 아니라 «어느 공고에 넣느냐»

이건 규칙이라기보다 이 사이트의 결론입니다. 실측(8,406건):

- 사정률 분위를 어떻게 잡아도 **1순위율은 3.5~4.4% 에서 안 움직입니다.** 실격률만 14%→84% 로 늡니다.
- 움직이는 것은 **참가업체수**: 2~9곳 **18.2%** · 10~29곳 5.5% · 30~99곳 2.9% · 100곳+ **1.6%**.
- 그리고 참가업체수를 가르는 것은 **면허**(산림사업법인 6곳 vs 토목공사업 332곳 — 52배)와
  **금액대**(1억 미만 참가 중앙 32곳 vs 3~10억 404곳).
- 「차이 0.79%」는 «줄일 수 있는 오차»가 아니라 **«실격당하지 않기 위해 내는 값»** 입니다.
  화면에서 이걸 «오차»라고 부르면 안 됩니다. 신뢰의 근거는 «차이가 작다»가 아니라
  **«무작위보다 몇 배냐»** 입니다(참가 중앙 62곳이면 무작위 1.61%, 우리 4.86% = 3.0배).

### 15. 🚨 올리기 전에 «항상» 검증한다 — 순서를 정해 둡니다 (2026-09-16)

소장님이 두 번 말씀하셨습니다. 그리고 그날 하루에만 **세 번** 걸렸습니다 —
「없습니다」를 두 번 잘못 말했고, 커밋 주석에 틀린 숫자를 박을 뻔했습니다.
전부 **올리기 전에 5초만 더 봤으면** 안 생길 일이었습니다.

    ① 문법      ast.parse / esbuild — 이건 «시작»이지 검증이 아닙니다
    ② 실제 실행  집계를 고쳤으면 build_json.py 를 **진짜로 굽습니다**(3분).
                「돌 것 같다」는 확인이 아닙니다
    ③ 검사      selfcheck — 새 규칙을 만들었으면 **검사도 같이** 만들고,
                **일부러 깨뜨려 잡히는지**까지 봅니다(11절)
    ④ 눈으로    «내가 고친 바로 그 한 건»이 결과 자료에 실제로 나오는지 찍어 봅니다.
                (2026-09-16: 대유건설#4168167660 이 색인·묶음에 있는지 열어 봤습니다)
                화면이 걸린 일이면 브라우저로 그 주소를 엽니다
    ⑤ 그 다음에  커밋 → 푸시

**숫자를 주석·커밋·보고에 적을 때는 «화면에 실제로 보이는 기준»으로 셉니다.**
2026-09-16 에 「빈 페이지로 가는 단추 37,789개」라고 적었는데, 화면(mixbox)은
`bzn>1` 일 때만 뜨는 조건을 빼고 세서 틀렸습니다 — 맞는 숫자는 **20,055개 중 8,768개**였습니다.
**세는 조건이 화면의 조건과 같은지** 먼저 맞춰 보십시오.


### 16. 🚨 새 라우트를 만들면 `web/firebase.json` 에도 넣는다 (2026-09-16)

`firebase.json` 의 `//rewrites` 메모에 **이미 적혀 있던 경고**입니다. 그런데 안 지켰습니다.

2026-09-16 점검: main.jsx 라우트 35개 중 **9개가 규칙에 없어 404 상태로 나가고 있었습니다** —
`/tools` `/naeyeok` `/qna` `/how` `/lic` `/report` `/jeoksan` `/safety` `/shareone`.
**사람 눈에는 멀쩡히 보입니다**(404.html 이 SPA 를 띄웁니다). 그래서 아무도 못 알아챘습니다.
크롤러에게만 「그 페이지 없습니다」라고 말하고 있었던 것입니다 —
카페에 알린 `/shareone` 도 그 안에 있었습니다.

    python - <<'EOF'
    import json, io, re
    routes = re.findall(r'<Route\s+path="([^"]+)"', io.open('web/src/main.jsx', encoding='utf-8').read())
    rw = [r['source'] for r in json.load(io.open('web/firebase.json', encoding='utf-8'))['hosting']['rewrites']]
    ...
    EOF

**라우트를 더하면 같은 커밋에서 `firebase.json` 을 고칩니다.**

### 17. 🚨 도구가 «조용히» 값을 0 으로 만들 수 있다 — 2줄 변환 (2026-09-16)

`/change/twoline` 이 **적산 프로그램이 만든 내역서에서 총액을 0 원으로 만듭니다.**

무슨 일인가: 그런 내역서는 **자기 합계 열쇠 열**을 씁니다 — 예) `M5 =SUMIF(R6:R17, Q5, M6:M17)`
(R 열에 코드, Q 열에 찾을 코드). 2줄 변환은 라벨 열을 고를 때 **A~P 16칸만** 보고
「Q 는 비었네」 하고 거기에 「당초/변경」 을 써 넣습니다. 그 순간 SUMIF 가 못 찾습니다.

    실제로 재 본 것 : 설계내역서 총액 **1,034,244원 → 0원**
    얼마나 흔한가   : 모은 내역서 1,086부 중 **43부(4.0%)** 가 이 모양입니다
    왜 무서운가     : **파일은 멀쩡히 열리고 인쇄도 됩니다.** 숫자만 0 입니다

라벨 열을 넓게(16칸 말고 시트 전체) 보게 고치면 «0 원» 은 면합니다. 그래도
**합계는 여전히 틀립니다** — 늘어난 줄 때문에 원래 SUMIF 가 당초·변경을 두 번 셉니다
(1,034,244 → 4,136,976). 그러니 고칠 것은 **「이런 내역서는 못 바꿉니다」 라고 «멈추고 알리는» 것**이지,
값을 내놓는 것이 아닙니다. 소장님 지시로 **지금은 손대지 않았습니다** — 고칠 때 이 기록부터 보십시오.


### 18. 🧭 안쪽 화면에는 «돌아 나올 길» 이 있어야 한다 (2026-09-16)

소장님: 「사이트 뒤로가기가 하나도 없어. 길을 만들어 줘」

탭은 **큰 자리만** 보여 줍니다. 안쪽 화면(캐드 명령 하나, 서식 한 장, 공고 한 건,
업체 한 곳)에 들어가면 **돌아 나올 길이 화면에 없었습니다.** 안쪽 화면이 37개였습니다.
검색으로 바로 들어온 사람은 브라우저 뒤로가기를 눌러도 «검색 결과» 로 나가 버리고,
앱으로 깔아 쓰면 뒤로가기 단추 자체가 없습니다.

→ `web/src/Crumbs.jsx` 하나가 **주소만 보고** 길을 그립니다. App.jsx 가 모든 화면에 붙입니다.
**화면을 하나하나 고치지 않습니다** — 그러면 새 화면을 만들 때마다 또 빠집니다.
새 주소를 만들면 `Crumbs.jsx` 의 `NAME`·`LEAF`·`TOP` 표 **한 곳만** 고치면 됩니다.

### 19. 🔒 «잠가서 올리는 것» 은 보안이 아니다 (2026-09-16)

`/jeoksan/lab` 을 잠가 두었습니다(`web/src/lib/gate.js`). 열쇠말의 SHA-256 을 코드에 박고 맞춰 봅니다.
**화면 자바스크립트는 누구나 내려받아 읽을 수 있으므로 그 해시도 보입니다.**
작정하면 열쇠말을 알아낼 수 있습니다. 하는 일은 «지나가던 사람이 못 들어오게» 하는 것뿐입니다.

- 어디에서도 링크하지 않고, `sitemap.py`·`prerender.py` 에도 넣지 않습니다
- **값을 받거나 남의 자료를 두게 되면 이걸로는 안 됩니다** — 그때는 서버에서 막아야 합니다
- 적산 이야기는 저장소 안 `적산.md`(사이트 쪽)와 저장소 밖 `K-적산/_만든 기록…md`(PC 쪽)에 갈라 둡니다


### 20. 🚨 `pgrep -f` 는 «자기 명령줄» 을 잡는다 — 죽은 작업을 살아 있다고 보고했다 (2026-09-16)

`device_bash` 로 무거운 작업을 `nohup ... &` 로 띄우고 `pgrep -f naeyeok_pdf` 로 살았는지 봤습니다.
**일곱 시간 동안 「도는 중」 이라고 보고했는데, 그 작업은 처음부터 죽어 있었습니다.**

두 가지가 겹쳤습니다.

1. **백그라운드는 호출이 끝나면 죽습니다** — 10절에 이미 적혀 있는데 어겼습니다.
2. **`pgrep -f` 가 «내가 방금 친 명령» 을 잡았습니다.** 명령줄에 `naeyeok_pdf` 라는
   글자가 들어 있으니 언제나 «찾음» 이 나옵니다. 그래서 죽은 것을 살아 있다고 봤습니다.

    ✕  pgrep -f naeyeok_pdf >/dev/null && echo "도는 중"
    ○  로그 파일이 «자라는지» 본다 · 결과 파일의 mtime 을 본다
    ○  ps -o pid,etime,time 으로 «CPU 시간이 도는지» 본다
    ○  애초에 45초 안에 끝나게 «쪼갠다» — 진행 상태를 파일에 남기고 여러 번 부른다

쪼개는 본보기: `~/pdf_chunk.py` — 진행 상태를 `~/pdf_state.json` 에 남기고
한 번에 25개씩, 33초 예산으로 돌려 118개를 마쳤습니다.

**살았는지 죽었는지는 «일이 나아갔는가» 로 봅니다. 프로세스 이름으로 보지 않습니다.**

---

### 21. 🚨 로그 파일로 돌려 담으면 파이썬 글자가 cp949 로 나간다 — 다섯 단계가 첫 줄에서 죽었다 (2026-09-17)

`0_올리고배포.bat` 이 파이썬 출력을 `>> "%LOG%"` 로 파일에 담게 제가 만들었습니다.
그날 밤 돌린 결과: **inbox·collect·sitemap·selfcheck·prerender 다섯이 전부 실패**,
사이트는 한 글자도 안 바뀌었습니다.

    UnicodeEncodeError: 'cp949' codec can't encode character '\u2014'

까닭은 하나입니다. **화면으로 나가면 콘솔 코드페이지(chcp 65001)를 따르지만,
«파일» 로 돌려 담으면 파이썬은 윈도 기본 코드페이지(cp949)를 씁니다.**
그래서 `—`(em dash) · `⚠️` · `⛔` 가 든 `print()` 한 줄에서 통째로 터집니다.
전부 제가 쓴 안내 문구라, 자기 이름을 찍다가 죽은 셈입니다.

`python -X utf8 run_all.py` 는 **run_all 자기 자신만** UTF-8 로 만듭니다.
`subprocess` 로 부르는 자식(inbox.py·collect.py·…)에게는 안 물려줍니다.

    ✕  python -X utf8 run_all.py --quick   >> "%LOG%" 2>&1
    ○  set PYTHONUTF8=1
       set PYTHONIOENCODING=utf-8
       python -X utf8 run_all.py --quick   >> "%LOG%" 2>&1

**환경변수라야 자식까지 물려받습니다.** `chcp 65001` 만으로는 안 됩니다 — 그건 화면 얘기입니다.

`5_워크넷확인.bat`·`7_내역서모으기.bat`·`8_순위시험.bat` 에는 이미 넣어 뒀으면서
정작 «올리고 배포하기» 에만 빠뜨렸습니다. `9_보관함채우기.bat` 도 같이 고쳤습니다.
**로그로 돌려 담는 .bat 을 새로 만들면 이 두 줄이 있는지 먼저 본다.**

---

### 22. ⚠️ 틀린 경보 하나가 배포를 통째로 멈춘다 — 반(class) 안의 메서드를 «호출» 로 봤다 (2026-09-17)

21번을 고치고 다시 돌리니 이번엔 `selfcheck` 가 배포를 막았습니다.

    ⛔ 쓰는데 import 안 한 것 2건
       lib\susik.js       num     ← lib\fmt.js
       lib\xlsx2line.js   build   ← lib\qto.js

둘 다 **틀린 경보**였습니다. `tools/checkimports.py` 가 찾는 본은 `이름(` 인데,

    num(a, what) {      ← 반(class) 안의 «메서드 선언» 이지 호출이 아닙니다
    build() {           ← 같은 것

실제 호출은 언제나 `this.num(...)` · `styler.build(...)` 이라 앞의 `.` 로 이미 걸러집니다.
하필 `lib/fmt.js` 가 `num` 을, `lib/qto.js` 가 `build` 를 내보내고 있어서 부딪혔습니다.
**새 파일을 만들 때 흔한 이름(`num`·`build`·`fmt`)을 메서드로 쓰면 언제든 또 납니다.**

고친 곳: `checkimports.py` 가 «줄 첫머리의 `이름(…) {`» 도 «이 파일이 가진 것» 으로 셉니다.
원래 잡으려던 잘못(`wasBaked` 를 import 없이 쓰기)은 그대로 잡히는지 시험해 확인했습니다.

**검사기가 틀린 경보를 내면 그때마다 고칩니다. 그냥 두면 «또 그거겠지» 하고
진짜 경보까지 넘기게 됩니다 — 검사기를 안 믿게 되는 쪽이 더 위험합니다.**

---

### 23. 🚨 collect 가 어질러 놓은 채로 끝나서 «다음 번» pull 이 거절당한다 (2026-09-17)

21·22 를 고치고 세 번째로 눌렀더니 이번엔 1단계에서 죽었습니다.

    error: cannot pull with rebase: You have unstaged changes.
    ---- pull exit: 128 ----
    ! [rejected]  main -> main (fetch first)

`0_올리고배포.bat` 의 차례가 **«올리기 → 사이트 갱신»** 이라 이렇게 됩니다.

    이번 판:  pull·push (깨끗함)  →  collect 가 data/store 세 개를 고쳐 놓고 끝남
    다음 판:  pull  ← 여기서 거절. 어제 자기가 어질러 놓은 것 때문에.

게다가 `data/store/*.json` 은 **커밋하면 안 되는 것** 입니다 — 회차 사이 보관은
GitHub Actions 의 cache 가 맡습니다(.gitignore 84줄). 그러니 「커밋해서 치우기」도 답이 아닙니다.
`web/public/lisp/*.lsp` 의 `kcm*exp` 도 배포 때마다 다시 찍히므로 늘 더럽습니다.

    ✕  git pull --rebase origin main
    ○  git -c rebase.autoStash=true pull --rebase origin main

`0_올리고배포.bat`·`2_받고올리기.bat` 둘 다 고쳤습니다. autoStash 가 넣었다 빼 주므로
소장님이 손댈 것은 없습니다.

⚠️ 다만 **Actions 가 맡는 파일**(`data/extra_*.csv`·`docs/수집상태.md`)까지 더러우면
autoStash 가 되돌릴 때 부딪힙니다. 그 둘은 **원격 것을 따릅니다** — 이쪽 것을 버리십시오.
`collect.py` 가 어차피 다음 판에 다시 채웁니다.

---

### 24. 🚨 `--quick` 은 업체 칸을 «안 굽는데», 배포는 그 낡은 것을 통째로 올린다 (2026-09-17)

23 번까지 고치고 네 번째로 눌렀더니 이번엔 `selfcheck` 가 이렇게 막았습니다.

    ⛔ 어긋난 곳 2가지
       · 법인 칸 10,191곳에 «이름으로 묶은» 순위가 붙어 있습니다 — 남의 법인 성적입니다
       · 「이 법인만 보기」 8,785개가 없는 칸을 가리킵니다 — 빈 페이지로 갑니다

**이건 코드가 아니라 «소장님 PC 안의 자료» 얘기였습니다.**

    web/public/data/corp/dat · idx   ← 9월 3일치. build_json.py 가 만드는 것.
    0_올리고배포.bat → run_all.py --quick → ③ 집계(build_json) «건너뜀»

9/16 에 `402cc47` 로 이 버그를 고쳤지만, 고친 것은 `build_json.py` 이고
그 뒤로 로컬에서는 `--quick` 만 돌았으니 **한 번도 다시 굽지 않았습니다.**

무서운 대목은 여기입니다. **`npm run build` 는 `web/public/data` 를 통째로 `dist` 에 복사하고,
배포는 그 `dist` 를 올립니다.** 그러니 로컬에서 `--quick` 으로 배포하면
Actions 가 제대로 구워 올려 둔 업체 칸을 **9월 3일치로 덮어씁니다.**
화면은 멀쩡하고 오류도 안 납니다. `selfcheck` 가 아니었으면 몰랐습니다.

**실제 사이트는 GitHub Actions(`update.yml`)가 맡습니다** — 거기서는 매 회차
`build_json.py` 까지 돌리므로 최신입니다. 로컬 배포는 «급할 때 손으로 미는 것» 입니다.

    로컬에서 배포까지 하려면  →  사이트_전체갱신.bat (run_all.py, --quick 없이)
    그냥 코드만 올리면        →  0_올리고배포.bat. Actions 가 web/** · *.py 푸시를 보고 알아서 굽습니다

**로컬 `--quick` 배포가 막히면 그건 고장이 아니라 «덮어쓰기를 막은 것» 입니다.
검사를 끄지 말고 build_json 을 돌리십시오.**

---

### 25. 📌 일과 관련된 이야기는 «반드시» md 에 적는다 (2026-09-17)

소장님: 「**md에 기록해...일과 관련된 거는 반드시 기록해..반드시**」

말로 정한 것은 다음 대화에서 사라집니다. 같은 이야기를 두 번 하게 됩니다.
실제로 오늘 제가 이미 정해진 것을 모르고 잘못 제안한 것이 두 번 있었습니다
(「입찰은 매일 하는 일이 아니다」 · 「내역서를 이용자한테 받자」 — 둘 다 소장님이 바로잡아 주셨습니다).

| 무엇을 적나 | 어디에 |
|---|---|
| **사고·규칙** — 틀렸던 것, 다시는 안 하기로 한 것 | `CLAUDE.md` 8절 (이 자리) |
| **사업 방향** — 누구에게 · 무엇으로 벌 것인가 · 등급 · 기여도 · 조사 결과 | **`사업방향.md`** |
| **적산** — 프로그램 구조, 어디까지 되나, 잠금 | `적산.md` |
| 그날그날 한 일 | `docs/기록_YYYY-MM.md` |

**방향을 정하는 이야기가 오가면 그 자리에서 `사업방향.md` 에 적습니다.**
대화가 끝난 뒤에 적으려 하면 이미 늦습니다.

⚠️ 저장소는 **Public** 입니다. 값(얼마)·발급한 코드·열쇠말은 md 에 적지 않고
저장소 바깥 `_열쇠메모.md` 에 둡니다.

---

### 26. 👀 게시 «전» 에 보여 드린다 — 미리보기 주소 (2026-09-17)

소장님: 「게시는 하지 말고, **나에게 먼저 보여주고** 게시하는 걸로 하자」

    9_미리보기.bat          빌드 + 굽기 + 미리보기 주소로 올리기 (5~15분)
    9_미리보기_올리기만.bat   이미 구워 뒀을 때 «올리기만» (2~10분)

파이어베이스 **채널 배포**라 **진짜 사이트를 건드리지 않습니다.** 이레 뒤 저절로 사라집니다.
주소는 `_미리보기결과.txt` 맨 아래 「Channel URL」 줄에 나옵니다.

⚠️ **채널 이름은 «영문» 이라야 합니다.** 처음에 `미리보기` 로 넣었다가
   `HTTP Error: 400, Channel IDs can only include letters, numbers, underscores,
   hyphens, and periods` 로 거절당했습니다. → `preview` 로 고쳤습니다.

⚠️ 로그 끝에 `Hosting URL: https://k-conmap.web.app` 이 찍히는데 **이건 안내 문구입니다.**
   채널 배포는 live 를 안 바꿉니다. 그래도 **찍힌 뒤에 진짜 사이트를 열어 확인**했습니다 —
   2026-09-17 14:38 기준 `/jeoksan/run` 이 여전히 잠김·파일칸 0·미리보기 안 보임.

⚠️ 크롬 확장(Claude in Chrome)의 사이트 권한에 미리보기 도메인이 없어서
   **제가 미리보기 화면을 직접 열지는 못했습니다.** 소장님이 여셔야 합니다.

⚠️ `npm run build` 는 **dist 를 먼저 비웁니다.** device_bash(45초)로 돌리면
   중간에 잘려서 **dist 가 사라진 채 남습니다.** 오늘 두 번 그랬습니다.
   → 빌드는 **윈도우 쪽 .bat 으로만** 돌립니다.

---

### 27. 💬 말을 «흩뿌리면» 대화가 안 된다 — 공고마다 달린 댓글은 아무도 못 봤다 (2026-09-17)

소장님: 「**공고나 1순위에 있는 댓글쓰기도 제거하자. 그리고 글쓰기 띄울때,
이용자가 글을 쓰면 사랑방으로 그 글이 가게 하면 안돼?**」

**무엇이 잘못됐나.** 공고 카드마다 댓글칸을 달아 두었습니다(2026-09-06·09-11).
그럴듯해 보였지만, 글이 **공고 수만큼 흩어졌습니다.** 공고는 하루에 수백 건이고,
어제 공고를 다시 여는 사람은 없습니다. 한 줄을 적어도 그 줄을 볼 사람이 없습니다.
「대화 통로」를 만든다고 만든 것이 오히려 **말이 사라지는 구멍**이었습니다.

**고친 모양.** 말은 **한 곳으로** 모읍니다.

| | 전 | 후 |
|---|---|---|
| 공고·1순위 카드 | 카드마다 댓글칸 | **없음** |
| 한 줄 남기기 창 | 「쓰러 가기」 → 사랑방으로 **보내기만** | **창 안에서 바로 쓰고, 사랑방에 올라감** |
| 글이 쌓이는 곳 | `cmt/{공고번호}` 로 수백 갈래 | `qna` 한 곳 |

**왜 «보내기» 가 아니라 «여기서 쓰기» 인가.** 보내면 화면이 바뀌고, 빈 칸이 다시 나오고,
제목을 짓고, 4자리 숫자를 정하라고 합니다. 그 사이에 대부분 나갑니다.
받은 직후의 한순간이 전부인데 그 한순간에 관문을 넷 세운 꼴이었습니다.
→ 창이 곧 글칸입니다. **제목칸 없음**(첫 줄이 제목이 됩니다), **4자리는 선택**입니다.

⚠️ `Comments.jsx` · `CommentsPanel.jsx` 는 **지우지 않았습니다** — 이미 달린 글이 DB(`cmt`)에
남아 있고, 나중에 다시 붙일 자리가 생길 수 있어서입니다. 지금은 어디서도 부르지 않습니다.

### 28. 🚨 시험이 «주석» 을 끝표로 잡으면, 주석을 지운 날 시험이 죽는다 (2026-09-17)

`web/시험/댓글창_시험.mjs` 는 `AskComment.jsx` 에서 셈하는 대목만 잘라 내어 돌립니다.
끝표가 `src.indexOf('/* 갈래마다')` 였는데, 그 **주석을 지우자** `indexOf` 가 `-1` 을 돌려주고
잘라내기가 `export function` 까지 먹어 `SyntaxError: Unexpected token 'export'` 가 났습니다.

→ ① 끝표는 **코드 줄**로 잡습니다(`export function askAfter`). 주석은 언제든 지웁니다.
  ② 못 찾으면 **어느 표를 못 찾았는지 이름을 찍고 멈춥니다** — `-1` 을 조용히 쓰지 않습니다.

```js
const 자르기 = (a, b) => {
  const i = src.indexOf(a), j = src.indexOf(b)
  if (i < 0 || j < 0 || j <= i) { console.log(`✕ «${i < 0 ? a : b}» 를 못 찾았습니다`); process.exit(1) }
  return src.slice(i, j)
}
```

### 29. 📱 창 «안» 의 글칸은 16px 이어야 한다 — 아이폰이 화면을 확대해 버린다 (2026-09-17)

한 줄 남기기 창에 글칸을 넣으면서 겪을 일을 미리 막아 둡니다.
사파리(iOS)는 글자가 **16px 미만인 입력칸**을 누르면 화면을 확대합니다.
확대되면 떠 있던 창이 화면 밖으로 밀려나고, 손가락으로 되돌려야 합니다.
→ `@media (max-width: 520px)` 에서 `.askbox .askta` · `.askbox .askrow .inp` 를 **16px** 로 둡니다.

### 30. ⚠️ 보기를 깔아 두는 것과 열어 두는 것은 다르다 (2026-09-17)

소장님: 「**사랑방에도 문장들이 잇는데, 다 제거 해. 그냥 자유롭게 적도록...해줘**」
「말은 클로드가 정해 주지 말자. 괜히 부담일 수 있거든」

빈 칸이 무서울까 봐 사랑방에 「이런 글이 올라옵니다」 보기를 일곱 줄 깔아 두었습니다.
**깔면 «저 중에 골라야 하나» 가 됩니다.** 물음을 정해 주는 것도 같은 일입니다 —
「아무 말이나 쓰세요」라고 해 놓고 문제를 내던 꼴이었습니다.
→ 보기도, 물음도, `placeholder` 의 「예) …」도 **전부 뺐습니다.**
남긴 한 줄은 취향이 아니라 안전입니다 — **「전화번호·이메일은 적지 마세요」.**

### 31. 🚨 «광고 없습니다» 라고 써 놓고 광고를 달고 있었다 (2026-09-17)

소장님: 「**우리 광고 달거니까...광고도 없다는 말도 찾아서 제거 해줘.**」

찾아보니 **이미 틀린 말이었습니다.** 애드센스는 `web/index.html` 과
`about/contact/privacy/terms.html` 에 **벌써 붙어 있었고**, 개인정보처리방침에도
「5. 광고 — Google 애드센스」 조항이 있었습니다. 홍보글만 옛말을 하고 있었던 것입니다.

| 어디 | 무엇이 | 어떻게 |
|---|---|---|
| `네이버카페_K건설맵_소개글.txt` 71 | 「광고 없습니다. 팝업도 없습니다.」 | 「뜬금없는 팝업으로 막지 않습니다.」 |
| 〃 91 | 「회원가입·로그인 없고 광고도 없습니다.」 | 「회원가입도 로그인도 없습니다.」 |
| 〃 12 | 「검색하다 광고만 보고」 | 「검색 결과를 한참 뒤지고」 |
| `k-conmap-카페글.html` 40·127 | 「광고도 팝업도 없습니다」 | 〃 |

⚠️ **남의 광고를 흉보는 문장도 같이 고쳤습니다**(12행). 우리도 다는데 남을 흉보면
그 글 전체가 못 믿을 글이 됩니다. **가입·로그인이 없다는 것은 그대로 사실**이니 그건 살립니다.

📌 앞으로 홍보글을 쓸 때: 「없다」 고 쓸 것은 **가입·로그인·강제 팝업** 셋뿐입니다.

### 32. ⚠️ 쓰는 자리에 금지어를 붙이면 그 한 줄이 «숙제» 가 된다 (2026-09-17)

소장님: 「**그리고 전화번호. 이메일은 적지 마세요 이것도 빼.**」

사랑방과 한 줄 남기기 창에 「전화번호·이메일은 적지 마세요」를 빨간 글씨로 달아 두었습니다.
지키라고 단 말인데, **한 줄 적어 달라고 열어 놓은 자리 바로 밑**에 있으니
쓰기 전에 규칙부터 읽히는 꼴이었습니다(30번과 같은 잘못입니다).

→ **말은 뺐습니다. 구조는 그대로입니다** — 연락처 «칸» 은 여전히 없습니다.
칸이 있으면 다들 적습니다. 칸이 없으면 대부분 안 적습니다. 말보다 칸이 셉니다.
연락처가 오가야 하는 일은 `/naeyeok` 문의함(아무도 못 읽는 곳)으로 가는 길만 남겼습니다.

### 33. 🔄 «멈춘 것» 과 «내 화면이 옛것인 것» 을 가려낼 방법이 없었다 (2026-09-17)

소장님: 「1순위는 지금 3시에 멈춰잇어. 잘 돌아가고 있지?」 → 「**건설맵 제일 위 쪽에 새로고침 만들어 줘.**」

보는 쪽에 캐시가 셋 겹쳐 있습니다.

| | 어디 | 얼마나 |
|---|---|---|
| ① | `lib/data.js` 의 메모리 `cache` | 그 화면을 닫을 때까지 |
| ② | 브라우저 캐시 (`max-age=900`) | **15분은 묻지도 않고** 옛것 |
| ③ | `stale-while-revalidate=86400` | 하루 안이면 **옛것 먼저 보여 주고** 뒤에서 받음 |

②③ 은 firebase.json 이 일부러 그렇게 둔 것이고 옳습니다 — 평소에는 그게 빠르고 쌉니다.
문제는 **F5 를 눌러도 그대로** 라서, 「진짜 멈춘 건가 내 화면만 옛것인가」를 못 가린다는 것입니다.

→ `web/src/Refresh.jsx` — 맨 윗줄에 🔄 하나. 하는 일은 딱 셋입니다.
  ① `sessionStorage` 에 도장 → ② 옛 서비스워커 캐시 비우기 → ③ `location.reload()`
  `lib/data.js` 가 **도장을 한 번 읽고 바로 지웁니다.** 있으면 `/data` 주소마다 `&r=…` 이
  붙어 캐시를 비켜 가고, 다음 F5 는 다시 평소대로 돕니다.

⚠️ **여기서 자료를 직접 받지 않습니다.** 화면마다 받는 것이 달라서, 목록을 여기 들고 있으면
   화면을 하나 늘릴 때마다 여기도 고쳐야 합니다 — 반드시 빠집니다.
⚠️ **`caches`·`serviceWorker` 를 기다리지 않습니다.** 보내만 놓고 바로 다시 엽니다 — 35번 참조.
⚠️ 주소창에 `?_r=` 같은 것을 붙이지 않습니다 — 그 주소가 그대로 복사돼 돌아다닙니다.

### 34. 🚨 맨 윗줄이 좁은 화면에서 «잘려» 있었다 — 가로 스크롤이 안 생겨서 아무도 몰랐다 (2026-09-17)

새로고침 단추를 넣고 스크린샷을 찍다가 찾았습니다. **넣기 전에도 이미 잘려 있었습니다.**

실측 (헤드리스 크롬, `.topbar-in` 의 `scrollWidth - clientWidth`):

| 화면 너비 | 넘침 (고치기 전) | 무엇이 잘렸나 |
|---|---|---|
| 360px | **44px** (새로고침 빼고도 ~17px) | 「📲 앱으로」 오른쪽이 화면 밖 |
| 390px | **14px** | 〃 |
| 430px 이상 | 0 | — |

**왜 아무도 몰랐나.** `body` 는 안 넘칩니다. 넘치는 것은 `.topbar-in` 안쪽뿐이라
**가로 스크롤바가 안 생깁니다.** 「가로 넘침 0」 만 재면 통과합니다 —
제가 처음 잰 것이 정확히 그것이었고, 통과라고 읽었습니다.
**스크린샷을 눈으로 보고서야** 「앱으로」가 오른쪽에서 잘린 것을 봤습니다.

📌 **막대·줄 같은 «가로로 늘어놓는 것» 은 `body` 말고 그 통의 `scrollWidth` 를 잽니다.**
   그리고 반드시 **360 · 390px 스크린샷을 눈으로 봅니다.** 숫자만 보면 놓칩니다.

→ 고친 방식: 좁아질수록 **글자부터 접습니다**(그림과 `title`·`aria-label` 은 남습니다).
  · `≤560px` — 「새로고침」 글자 접기 (`.rflong`)
  · `≤460px` — 「사라사」·「앱으로」 글자 접기 (`.sislong` · `.instlong`)
  · 「📖 보는 방법」은 **접지 않습니다** — 소장님(09-15): 「누구나 볼 수 있게 해줘야지」

### 35. 🚨 「시간초과로 빠져나온다」 — 그 시간초과도 «타이머» 다. 타이머가 자는 곳에서는 안 울린다 (2026-09-17)

새로고침을 올리고 **진짜 사이트에서 눌러 보다가** 잡았습니다. 단추가 「받는 중」에서 멈췄습니다.

처음 만든 모양:

```js
const 비우기 = (async () => { await caches.keys() … })().catch(()=>{})
const 시간초과 = new Promise((done) => setTimeout(done, 1500))
await Promise.race([비우기, 시간초과])   // 「늦어도 1.5초면 빠져나온다」
window.location.reload()
```

실측 (k-conmap.com, 탐침을 심어 잰 것):

| | |
|---|---|
| `navigator.serviceWorker.getRegistrations()` | **1ms** 에 답 (등록 1개) |
| `caches.keys()` | **영영 안 끝남** |
| `setTimeout(…, 1500)` | **80초가 지나도 안 울림** |
| `document.visibilityState` | **`hidden`** |

**크롬은 «안 보이는 탭» 의 타이머를 재웁니다.** 그러니 «믿는 구석» 으로 둔 1.5초 시간초과가
바로 그 상황에서 같이 잠듭니다. 📌 **시간초과로 지키려던 것이 시간초과와 함께 죽습니다.**

→ 고친 방식: **기다릴 것을 없앱니다.** 다시 열기를 맨 앞에 두고, 비우기는 보내만 놓습니다.
  어차피 `sw.js` 는 아무것도 담지 않고, 진짜 캐시(HTTP)는 «도장» 이 비켜 갑니다.
  검증: `caches.keys()` 가 **영영 안 끝나게 막아 놓고도** 1.1초에 다시 열립니다.

📌 **일반 규칙 — 되돌릴 수 없는 마지막 한 걸음은 «맨 앞» 에 둡니다.**
   부수적인 청소 때문에 본 일이 막히면 안 됩니다. 청소는 보내 놓고 잊습니다.
📌 **그리고 이건 헤드리스 시험으로는 «절대» 안 잡혔습니다.** 헤드리스는 늘 보이는 탭입니다.
   진짜 사이트를 진짜 브라우저로 눌러 봐야 나왔습니다 — 「배포 전 검증」이 이래서 있습니다.

### 36. 🚨 「추정가격」 이라 적고 «배정예산» 을 찍고 있었다 — 1.2억이 틀렸다 (2026-09-17)

금액 거르개를 만들려고 공고 카드를 열어 보다가 찾았습니다.

```jsx
<span className="badge n">추정가격</span>
<span className="amt">{wonShort(r.budget)}</span>   // ← 배정예산입니다
```

**공고 하나에 금액이 셋입니다. 셋 다 다릅니다.**

| | 무엇 | 크기 |
|---|---|---|
| **추정가격** `est` | **법정 경계가 걸리는 금액** — 적격심사 구간이 이걸로 갈립니다 | 제일 작음 |
| 기초금액 `base` | 예정가격 산정 기준 = 추정가격 + 부가세 | 가운데 |
| 배정예산 `budget` | 총사업비 | **제일 큼** |

실측 한 건: `est 235,174,546 · base 258,692,000 · budget 306,342,000`.
화면은 「추정가격 **3.1억**」이라 찍었지만 진짜 추정가격은 **2.4억** 입니다.
**국가 기준 3억 경계(별표3/별표4)를 사이에 두고 반대편에 있습니다.**

⚠️ `BaroBid.jsx` 에는 **이미 경고가 적혀 있었습니다** — 「공고의 «예산» 칸(budget)을
   추정가격으로 쓰면 안 됩니다」. 계산하는 쪽은 맞게 쓰고 있었는데 **보여 주는 쪽만 틀렸습니다.**
   📌 «계산이 맞다» 와 «화면이 맞다» 는 다른 일입니다. 주석은 옮겨 다니지 않습니다.

→ 고친 방식: `estOf(r)` 한 곳에서만 정합니다 — `est` 가 있으면 그대로, 없으면 `base ÷ 1.1`,
  둘 다 없으면 **0(모름)**. 0 이면 이름표를 **「배정예산」 으로 바꿔서** 그 값을 보여 줍니다.
  이름과 숫자가 어긋나는 것보다 「모른다」 가 낫습니다.
  ⚠️ **배정예산으로 메우지 않습니다.** 메우면 거르개가 같은 거짓말을 물려받습니다.

### 36-1. 📏 바깥 자료로 다시 대조했습니다 — 정의도 숫자도 맞습니다 (2026-09-17)

소장님: 「다시 한 번 검증 절차 거쳐줘. 다른 사이트 참고해서」

**바깥 출처** (시프티드 「추정가격·추정금액·기초금액·예정가격 — 나라장터 4개 가격 완전 가이드」):

| 구분 | 정의 | 부가세 | 관급자재 | 근거 |
|---|---|---|---|---|
| **추정가격** | 사업의 순수 규모를 재는 기준값 | **제외** | **제외** | 국가계약법 시행령 §2·§7 |
| 추정금액 | 부가세·관급을 더한 전체 규모(공사) | 포함 | 포함 | 국가계약법 시행규칙 §2 |
| 기초금액 | 발주처가 조사·검토해 산정한 금액 | **포함** | — | 예정가격작성기준(계약예규) |
| 예정가격 | 낙찰·계약의 실제 기준가격 | 포함 | — | 국가계약법 시행령 §9 |

> 「추정가격은 이 입찰이 **어떤 방식으로 진행되는지를 결정하는 값**이라서,
>  이것 **하나를 잘못 읽으면 준비해야 할 서류가 통째로 달라집니다**」 — 같은 글

**우리 자료로 검산** (마감 전·전체 공고):

| 검산 | 결과 |
|---|---|
| 기초금액 = 추정가격 × 1.1 인가 | 9,106건 중 **8,173건(89.8%) 정확히 일치** |
| 배정예산 = 추정금액(추정가격+부가세+관급) 인가 | **아닙니다.** 관급 없는 11,646건 중 4,437건(38%)이 더 큼 — 가운데값 **+25.1%**, 상위 10% **+97.5%** |

→ **`budget` 은 «총사업비» 입니다** (설계비·감리비 등이 섞입니다). 추정가격도 추정금액도 아닙니다.
  `BaroBid.jsx` 주석이 처음부터 맞았습니다. 보여 주는 쪽만 그걸 몰랐습니다.

**표본 한 건** — 무릉소하천 정비공사 (경상남도 밀양시 · R26BK01702654)

```
추정가격          211,000,000
추정가격 × 1.1    232,100,000  =  기초금액 232,100,000  ✓
배정예산          315,050,000   ← 옛 화면이 「추정가격 3.2억」 이라 찍던 값
```
진짜는 **2.11억**. 국가 기준 **3억 경계**를 사이에 두고 **별표4(2~3억)** 인데
화면에는 **별표3(3~10억)** 으로 보였습니다.

📌 **바깥 자료로 맞추는 것과 우리 자료로 검산하는 것은 둘 다 해야 합니다.**
   정의만 맞추면 «우리 칸이 그 정의대로 채워져 있는지» 를 모르고,
   검산만 하면 «무엇과 맞춰야 하는지» 를 모릅니다.

### 37. 💰 금액 거르개 — 경계는 «법» 이지 취향이 아니다 (2026-09-17)

이용자 의견: 「금액에 맞추어 얼마부터 얼마 사이, 얼마 이상, 얼마 이하」
소장님: 「**지자체, 국가 둘 다 가자**」

**적격심사 구간** (생활법령정보 원문 확인 · 추정가격 기준):

| 국가·조달청 | 지자체 |
|---|---|
| 2 · 3 · 10 · 50 · 100억 (100억↑ 종합심사) | 2 · 4 · 10억 |

→ 알약은 **겹치는 마디**(2·4·10·50·100)로, 한쪽에만 있는 3억은 **직접 넣기**로.
⚠️ **거르개는 «이 공고가 국가냐 지자체냐» 를 알 필요가 없습니다.** 경계만 다 있으면
   쓰는 사람이 자기 구간을 고릅니다. 판정은 나중 일(이름표 붙이기)입니다.

**모르는 것은 «모른다» 고 합니다.** `amtHit` 는 참/거짓이 아니라 **셋**을 냅니다 —
통과 / 걸러짐 / **null(모름)**. 화면이 「금액을 아직 모르는 공고 N건은 세지 않았습니다」로 적습니다.
⚠️ 0 을 «0원» 으로 세면 「2억 미만」에 전부 걸려 들어옵니다. 조용히 빼면 「내 공고가 사라졌다」가 됩니다.

**한 화면에 금액 거르개가 둘이면 안 됩니다.** 「자리 찾기」에 있던 `szPick`(1·3·10억)을
없앴습니다 — 참가업체 수 통계로 나눈 칸이라 법정 경계와 안 맞았고, **기초금액**으로 걸렀습니다.
이제 `amt` 하나가 두 모드를 다 거릅니다.

**검증** — 화면 숫자를 «색인에서 직접 센 정답» 과 대조했습니다(12,789줄):

```
2억 미만 9,048 · 2~4억 1,567 · 4~10억 1,056 · 10~50억 636 · 50~100억 115 · 100억↑ 148
합 12,570 + 모름 219 = 12,789   ← 빈틈도 겹침도 없습니다
```
셈 시험 `web/시험/금액거르개_시험.mjs` 33개도 같이 둡니다.

### 38. ⚠️ 마운트 폴더에서 `vite build` 는 45초에 안 끝난다 — public/ 이 264MB (2026-09-17)

`public/data` 가 264MB 라 **자바스크립트를 굽기도 전에** 복사에서 상한에 걸립니다(exit 124).
그전까지 되던 것은 자료가 적었을 때입니다.

→ 시험용으로 **공개 폴더 복사만 끄고** 굽습니다. 한 번 쓰고 **지웁니다**(저장소에 넣지 않습니다):

```js
// web/vite.nopublic.mjs — 임시
import base from './vite.config.js'
export default (env) => { const c = typeof base === 'function' ? base(env) : base
  c.build = { ...(c.build || {}), copyPublicDir: false }; return c }
```
```
vite build -c vite.nopublic.mjs --outDir <바깥> --logLevel error
```
그 뒤 시험에 필요한 자료 파일만 손으로 복사해 넣습니다(13MB). 45초 안에 끝납니다.

### 39. 🏷️ 다시는 «이름표와 숫자가 어긋나지» 않게 — 기계로 막습니다 (2026-09-17)

소장님: 「**다시는 이런 실수 하지 않을 방안을 먼저 마련해. 확실한 방안**」

**왜 사람으로는 못 막는가.** 「배정예산을 추정가격으로 쓰면 안 됩니다」 라는 주석이
`BaroBid.jsx` 에 **이미 있었습니다.** 계산하는 쪽은 그걸 지켰고, 보여 주는 쪽만 몰랐습니다.
📌 **주석은 옆 파일로 옮겨 다니지 않습니다.** 기억·규칙·문서로는 다음에도 똑같이 뚫립니다.

→ **`tools/checklabels.py`** — 화면에서 금액 이름표가 붙은 자리를 전부 찾아
  «그 옆의 값이 그 이름표의 금액이 맞는지» 를 봅니다.

| 이름표 | 값에 있어야 | 값에 있으면 안 됨 |
|---|---|---|
| 추정가격 | `estOf` · `est` · `estimate` · `estPrice` | **`budget`** |
| 기초금액 | `base` | `budget` · `est` |
| 배정예산 | `budget` | — |
| 낙찰금액 | `amt` · `win` · `price` · `bid` | `budget` · `base` |

「모르면 이름을 바꾸는」 짝(`{estOf(r) > 0 ? '추정가격' : '배정예산'}`)은 **값에 둘 다** 있어야 통과합니다.
이름만 갈아끼우고 값은 늘 `budget` 인 «반쪽짜리» 도 잡습니다.

**⚠️ 자가시험이 함께 들어 있습니다** (8절 11의 규칙 — 일부러 틀리게 해서 잡히는지).
`python tools/checklabels.py --self` · 지금 **9/9**: 틀린 보기 5개를 전부 잡고, 맞는 보기 4개를 통과시킵니다.
**검사 자체가 깨지면 그 자리에서 알려 주고 멈춥니다** — 조용히 «통과» 로 넘어가지 않습니다.

**어디서 도는가 — 두 곳입니다. 한 곳에만 두면 그 한 곳을 안 보는 날 뚫립니다.**

| | |
|---|---|
| `tools/selfcheck.py` | `check_labels()` 로 들어갔습니다 (소장님이 손으로 돌릴 때) |
| **`.github/workflows/update.yml`** | **「화면 검사」 단계 — 굽기 «전». 실패하면 배포가 통째로 멈춥니다** |

📌 **막는 쪽을 골랐습니다.** 안 막으면 「빨간불이 떠 있는데 계속 배포되는」 상태가 되고
   그건 아무도 안 봅니다. **한 회차(25분) 늦는 것이 18일 틀린 것보다 낫습니다.**
   ⚠️ 8절 22(틀린 경보가 배포를 멈춘 사고) 때문에 자가시험을 먼저 돌리게 해 두었습니다.
   그 단계는 `checkimports.py` 도 같이 돌립니다 — 그전까지 깃허브에는 **화면 검사가 하나도 없었습니다.**

**그리고 코드를 «기계가 읽을 수 있게» 고쳤습니다.**
`const e = estOf(r)` 로 한 번 거치면 검사가 「이 값이 어디서 왔는지」 를 못 봅니다.
→ JSX 안에서 `estOf(r)` 를 **그대로** 부릅니다. 📌 **기계가 확인할 수 있는 코드가 더 좋은 코드입니다.**

**전체 훑기 결과** — 금액 이름표가 붙은 자리 **13곳, 전부 맞습니다.**

```
NoticeDetail 213 기초금액 ← r.base …      214 추정가격(추산) ← est(=base÷1.1)
Sites        127 기초금액 ← won(r.base)   128 낙찰금액 ← won(r.amt)
BaroBid     1486 기초금액 ← won(base)    1487 추정가격 ← won(estimate)
            1820 기초금액 ← won(base)    1821 추정가격 ← won(estimate)
LiveBoard    628 기초금액 ← r.base …      539·636 추정가격/배정예산 ← estOf(r) || r.budget
NoticePage   190 기초금액 ← r.base …      194 추정가격/배정예산 ← estOf(r) || r.budget
```

### 40. 💬 사랑방 답글에 붙는 이름 (2026-09-17)

소장님: 「내가 쓰는 답글은 뭐라고 달리지. 건설맵으로 표기되는 건가」

**열쇠말을 넣으면 「K-건설맵 답변」, 안 넣으면 자동 별명입니다.**

| 어떻게 | 붙는 이름 |
|---|---|
| 답글 칸 옆 **「운영자 열쇠(있으면)」** 에 열쇠말을 넣고 올림 | **「K-건설맵 답변」** (파란 글씨) |
| 그냥 올림 | 자동 별명 — 「성실한 소장123」 같은 것 |

별명은 익명 uid 로 만들어서 **같은 브라우저면 늘 같습니다**(`lib/nickname.js`).
⚠️ 그래서 소장님이 **열쇠말 없이** 답글을 달면, 이용자 눈에는 **그냥 다른 이용자 한 명**으로 보입니다.
   K-건설맵 이름으로 답한다는 표를 내려면 **매번 열쇠말을 넣으셔야** 합니다.
⚠️ 열쇠말은 저장소 **바깥** `_열쇠메모.md` 에 있습니다. 여기에 적지 않습니다.

**✅ 정했습니다 (2026-09-17)** — 소장님: 「**내가 다는 답변도 클로드가 다는 답변도 모두
K-건설맵 으로 하자**」

사랑방 답글의 이름표는 **「K-건설맵 답변」 하나**입니다. 누가 썼든 **기관의 목소리**로 나갑니다.
따로 「🤖 도우미」 표를 만들지 않습니다. (제가 한 번 제안했다가 소장님이 하나로 정하셨습니다)

⚠️ **그래서 기준이 올라갑니다.** 규칙글에 「표가 없는 답글은 이용자 의견이니 중요한 건은
   발주처에 확인하십시오」 라고 적어 두었습니다 — 뒤집으면 **표가 붙은 답은 믿어도 된다**는 뜻입니다.
   그 표를 달고 나가는 답은 **틀리면 안 됩니다.** 모르면 「모릅니다」 라고 적습니다.
   짐작으로 금액·조문·기한을 적지 않습니다. 근거는 원문을 열어 확인하고 답합니다.

**🔑 그래서 비번을 없앴습니다 (2026-09-17)** — 소장님: 「**답글에 비번이 왜 필요해.. 유료만 필요하지**」

맞는 말씀이었습니다. 그리고 확인해 보니 **그 비번은 아무것도 막고 있지 않았습니다.**
열쇠말 비교를 **브라우저 «안에서만»** 했고, DB 규칙은 `op` 가 참/거짓이기만 하면 통과였습니다 —
**개발자도구를 아는 사람은 열쇠말 없이 그냥 「K-건설맵 답변」 표를 달 수 있었습니다.**
소장님만 매번 불편했던 것입니다.

| | 전 | 후 |
|---|---|---|
| 표가 붙는 조건 | 열쇠말(비번)을 맞춤 | **운영자 브라우저 번호(uid)** |
| 누가 판정하나 | 브라우저 안에서 | **서버 — `database.rules.json`** |
| 소장님이 칠 것 | 매번 열쇠말 | **없음** |
| 가짜로 표를 달 수 있나 | **됩니다** | 안 됩니다 |

번호는 **두 곳**에 적습니다 — `web/src/pages/Qna.jsx` 의 `OPS` 와 `database.rules.json` 의 `op` 규칙.
⚠️ 두 벌이라 어긋날 수 있어 **`tools/checkops.py`** 가 대조합니다(자가시험 6/6 포함).
   깃허브 「화면 검사」 단계에서도 돕니다 — 어긋나면 배포가 멈춥니다.
⚠️ uid 를 공개 저장소에 적어도 안전합니다 — 익명 로그인은 «원하는 uid 로» 로그인할 수 없습니다.
⚠️ 기기를 더하실 때: 그 기기로 **`k-conmap.com/admin`** 을 열면 «이 브라우저 번호» 가 나옵니다.
   그걸 베껴 두 곳(Qna.jsx `OPS` · `database.rules.json`)에 넣고 `3_규칙올리기.bat` 을 한 번 돌립니다.
   기록을 지우면 번호가 바뀝니다.
   🚨 2026-09-17 — 이 번호를 **사랑방 「쓰는 법」 에도** 한 줄 달았다가 뺐습니다.
      소장님: 「이게 왜 있는 거지?」 그 번호가 필요한 사람은 **소장님 한 분** 이고,
      그건 «운영자 사정» 입니다. 하루 종일 사랑방에서 부담되는 말을 걷어내 놓고
      정작 제가 기계 번호를 끼워 넣었습니다. **이미 /admin 에 있던 것이라 두 벌이기도 했습니다.**
      📌 «나한테 필요한 것» 을 «모두가 보는 자리» 에 두지 않습니다.

⚠️ **클로드는 답글을 «혼자» 올리지 않습니다.** 공개되는 글은 **매번 사람의 «올려» 가 있어야** 합니다.
   → **초안은 클로드가, 올리는 것은 소장님이.**

### 41. 🚨 「누르면 열린다」 는 만든 사람 머릿속에만 있다 (2026-09-17)

소장님: 「**현재 이용자가 의견을 적었는데, 답글을 클릭해서 쓸 버튼이 없어..ㅎㅎ**」

사랑방 카드는 **누르면** 답글칸이 열리게 돼 있었습니다. 그런데 **누르라는 표시가 없었습니다** —
▼ 도, 단추도, 아무것도. 답글이 있을 때만 「답글 N」 알약이 붙고, **없으면 카드가 그냥 글덩이**였습니다.

⚠️ **글이 0개일 때는 아무도 몰랐습니다.** 첫 글이 올라오고 나서야 드러났습니다.
   📌 빈 화면으로 한 검사는 «비어 있을 때만» 맞습니다. 자료가 들어온 뒤에 다시 봐야 합니다.

처음에는 카드마다 「💬 답글 쓰기」 단추를 달았는데, 소장님이 바로 잡아 주셨습니다 —
「**어차피 글을 보려면 클릭해야 하잖아.. 그대로 둬도 될 것 같은데**」. 맞는 말씀입니다.
→ 단추는 빼고 **`▼` 하나만** 답니다. 공고 카드가 이미 쓰는 것과 **같은 모양**입니다.
  📌 없던 것을 새로 만들기 전에 **사이트가 이미 쓰는 방식이 있는지** 먼저 봅니다.

### 42. 🚨 「5번 쓰면 한 번 묻는다」 가 «아무한테도» 안 떴다 (2026-09-17)

소장님: 「**내가 바로입찰 6번 눌렀는데도 글쓰라는 창이 안뜨던데 맞는 거지?**」

맞았습니다 — 그런데 까닭이 딴 데 있었습니다. `askAfter('bid')` 를 부르는 자리가
**「💰 금액 복사하고 나라장터 열기」 한 군데뿐** 이었습니다.
즉 **하루에 실제 투찰을 다섯 번** 해야 창이 떴습니다. 그런 사람은 거의 없습니다.
**규칙은 살아 있었지만 실제로는 아무한테도 안 떴습니다.**

⚠️ **「화면을 연 횟수」 로 세면 더 나쁩니다.** 바로투찰은 **첫 화면(`/`)** 이라
   그건 「방문 수」 입니다. 아무것도 못 받은 사람에게 «한 줄 남겨 달라» 는 꼴이 됩니다.
   소장님의 「무료로 다 주는데 감사표시 한 번 없는 사람」 은 **받은 사람** 이야기였습니다.

→ **`pick(r)`** — 「공고 하나를 골라 투찰금액을 봤다」 에 답니다. 그때 답을 손에 쥡니다.
  · 공고·1순위의 「💰 바로투찰」 로 들어와도(`?no=…`) · 바로투찰에서 검색해 골라도 같습니다
  · 기초금액만 손으로 넣는 것은 세지 않습니다 — 공고를 고른 것이 아닙니다
  · `copyAndGo` 에서는 **뺐습니다** — 거기는 `pick()` «뒤» 라 두면 같은 공고를 두 번 셉니다

📌 **«규칙이 맞다» 와 «실제로 걸린다» 는 다른 일입니다.** 셈을 만들면 그 셈이
   **실제로 몇 사람에게 걸리는지** 를 같이 재야 합니다. 안 그러면 조용히 0이 됩니다.

### 43. 🛠 관리자 화면 · 사랑방 메일 알림 (2026-09-17)

소장님: 「글이 올라오면 내가 알 수 있는 페이지가 있으면 좋겠어. 그 페이지에서 글을 쓰면
자동으로 답글이 달리면 더 좋고」 · 「메일 보고 내가 관리자 페이지에서 클로드랑 답글 달면 되지」

**`/admin`** — 운영자 브라우저만. 하는 일은 둘뿐입니다.
  ① 사랑방 글을 **답 안 단 것부터** (그 안에서는 **오래 기다린 것부터**)
  ② 그 자리에서 답글 → 「K-건설맵 답변」 으로 바로 붙습니다

⚠️ 문 판정은 화면 안에서 합니다 — **보안이 아니라 이름표입니다.**
   괜찮은 까닭: 여기 보이는 것(`qna`·`qna_a`)은 **어차피 누구나 읽는 공개 게시판** 이고,
   표를 다는 것은 **서버가** uid 로 막습니다. 문이 뚫려도 목록을 볼 뿐입니다.
⚠️ `noindex, nofollow` · sitemap 에 안 넣습니다 · `web/firebase.json` rewrites 에는 넣었습니다(8절 16).

**🛠 들어가는 길 (2026-09-17)** — 소장님: 「관리자 페이지 어디에 있지?」 「나만 볼 수 있게 해줘」
주소만 있고 **길이 없었습니다.** 외워서 쳐야 했습니다.
→ 사랑방(/qna) 단추 줄에 **「🛠 관리자」** 를 답니다. **운영자 브라우저일 때만 그려집니다.**
  (사랑방이 «답글 달러 오는 자리» 라 제자리입니다)

**«나만 보인다» 가 어디까지 참인가 — 정확히 적습니다**

| | |
|---|---|
| 🛠 관리자 **단추** | 운영자 브라우저에만 그려집니다. 남의 화면에는 **없습니다** |
| 검색엔진 | `noindex, nofollow` · sitemap 에 없음 — **안 뜹니다** |
| 주소를 알고 친 남 | 「운영자 브라우저에서만 열립니다」 한 장. **글도 답글칸도 안 나옵니다** |
| 판정하는 곳 | **화면 안** (`OPS` 목록) — 자바스크립트를 뜯으면 목록을 지나칠 수 있습니다 |

⚠️ **그래서 «자물쇠» 가 아니라 «남의 눈에 안 띄게» 입니다.** 지금은 그걸로 충분합니다 —
   여기 보이는 것(`qna`·`qna_a`)은 **어차피 누구나 읽는 공개 게시판** 이고,
   진짜로 막아야 할 것(「K-건설맵 답변」 표를 다는 것)은 **서버가** uid 로 막습니다.
⚠️ **여기에 «남이 보면 안 되는 것» 을 올리는 날에는 얘기가 달라집니다.**
   내역서 문의(`quotes`)처럼 연락처가 든 것을 이 화면에 붙이려면
   **그때는 `database.rules.json` 이 읽기까지 uid 로 막아야 합니다.** 지금 구조로는 모자랍니다.

**메일** — `tools/qna_mail.py` · 기존 `문의알림.yml` 에 **단계 하나로** 붙였습니다.
  예약을 새로 만들지 않았습니다 — 깃허브가 «밀린 차례를 버리는» 문제에 또 걸립니다.
  10분마다 문의함과 사랑방을 한 번에 봅니다. 메일 끝에 `/admin` 주소가 붙습니다.

⚠️ **같은 글로 두 번 알리지 않습니다 — 표시를 «글에 붙이지 않고» 따로 둡니다** (`qna_mail/{id}`).
   ① 글(`qna`)에 칸을 더하면 화면 규칙(`$other: .validate false`)과 부딪힙니다
   ② 이용자 글을 기계가 고치는 것도 맞지 않습니다
⚠️ **메일을 못 보내도 표시는 남깁니다.** 2026-09-15 에 quote_mail 이 정확히 그것 때문에
   하루 **144통**을 보냈습니다(못 보내면 표시를 못 붙여 10분마다 영원히 재시도).
   놓친 글은 `/admin` 에 그대로 남아 있습니다.
⚠️ 소장님이 단 답글(`op`)에는 알리지 않습니다 — 본인 글을 본인에게 보내는 소음입니다.
⚠️ 한 통에 최대 20건. 첫 회차에 수백 건이 쏟아지지 않게.

### 44. 🚨 적산 화면이 «내역서 형식» 이 아니었다 (2026-09-17)

소장님: 「**산출근거는 내역서에 나오지 않아. 수량산출서나 단가산출에 나오는 거지. 왜 여기에
들어가 있지?**」 「**단가하고 금액이 맞아야 하는데 금액이 중간에 있어**」
「**내역서는 원가계산서, 내역서, 일위대가, 단가산출, 수량산출서..이런 형식으로 되어 있는데**」

셋 다 맞는 말씀이었고, 셋이 **서로 다른 잘못**이었습니다.

**① 표 머리와 숫자가 어긋났습니다** — 「금액이 중간에 있어」
  `<th>` 는 왼쪽인데 `<td>` 만 `textAlign:'right'` 로 붙여 놨습니다.
  여섯 칸짜리 내역서에서 머리와 값이 서로 다른 쪽에 붙어 **세로줄이 어긋나 보였습니다.**
  📌 **숫자 칸은 머리도 숫자도 같이 오른쪽이어야 합니다.** 한쪽만 붙이면 어긋납니다.
  → `.reptbl th.n / td.n` 한 쌍으로 묶었습니다. 실측 결과 **일곱 칸 전부 0px**.

**② 「산출서」 라는 이름이 «단가산출서» 로 읽혔습니다**
  산출근거가 들어가는 곳은 **수량산출서** 입니다. 탭 이름을 정확히 적습니다
  (수량산출서 · 수량집계표 · 구간별 집계 · 검산 · 쓴표).
  그리고 내역서 표에서 산출근거를 **뺐습니다** — 내역서는 품명·규격·단위·수량·단가·금액이고,
  근거는 **호표 번호로 가리키기만** 합니다.

**③ 일위대가가 통째로 빠져 있었습니다** — 이게 「이상해」 의 진짜 까닭입니다
  전에는 수량산출서 → 내역서 → 원가계산서 뿐이었습니다. 가운데가 비어 있으니
  **내역서 단가가 어디서 왔는지 보이지 않았습니다.** 적산에서 일위대가는 장식이 아니라
  **내역서 단가의 근거** 입니다.
  → 실제 호표 자료(`K-적산/_호표.csv`)에서 두 줄을 가져와 넣었습니다.
    `제1호표 흄관부설및접합(소켓관) D=250mm  재 21,909 + 노 3,609 + 경 3,807 = 29,325`
    `제2호표 0.5B 벽돌쌓기 3.6m이하        재 39,379 + 노 6,375 + 경 787   = 46,541`
    **그 합계가 그대로 내역서 단가입니다** — 화면이 그걸 이어서 보여 줍니다.
  ⚠️ 호표 표 자체는 싣지 않습니다. 보기 두 줄만입니다(단가표를 내려받게 하면 안 됩니다).

**④ 차례가 «둘» 인데 그 말을 안 했습니다**

| 만드는 차례 | 묶는 차례 (성과품) |
|---|---|
| 수량산출서 → 일위대가 → 단가산출서 → 내역서 → 원가계산서 | 원가계산서 → 내역서 → 일위대가 → 단가산출서 → 수량산출서 |
| 아래에서 위로 쌓습니다 | 내는 것은 **거꾸로** — 보는 사람은 「얼마냐」 부터 봅니다 |

화면은 **만드는 차례**로 걸어가되, **묶는 차례**를 따로 적어 두었습니다.
「어디까지 왔나」 표도 묶는 차례로 다시 매겼습니다.

**검사** — `node web/시험/적산화면_시험.mjs` · **21개**
  일위대가 합계 = 내역서 단가 · 내역서에 산출근거 칸 없음 · 수량산출서에는 있음 ·
  숫자 머리가 `n` 칸 · 옛 `textAlign:'right'` 가 안 남았는지 · 묶는 차례가 순서대로인지.
📌 **소장님이 눈으로 찾아 주신 것을 그대로 검사로 옮겼습니다.** 다시 어긋나면 그때 잡힙니다.

### 45. 🚨 내가 임의로 넓힌 규칙이 소장님 규칙을 덮었다 → 결국 «하루 한 번» (2026-09-17)

소장님: 「**서식 내려받기 해보는데, 딱 한 번만 뜨는데, 글쓰기 그냥 안쓰면
다른거 다운 받아도 더 이상 안떠**」

소장님이 정하신 규칙은 이것이었습니다 — 「**서식은 하나 다운 받을 때마다**」.
그런데 제가 `묶음 = 2분` 을 넣고 「한 자리에서 연달아 받는 것은 한 번으로 본다」 고
**임의로 넓혀** 놨습니다. **2분이면 서식 두세 장 받는 시간** 입니다.
그래서 실제로는 「받을 때마다」 가 아니라 **「2분에 한 번」** 이 돼 있었습니다.

📌 **내가 «좋다고 생각해서» 넣은 완충이 소장님이 정한 규칙을 덮고 있지 않은지 봅니다.**
   규칙을 받으면 그대로 넣고, 넓히고 싶으면 **먼저 말씀드립니다.** 조용히 넓히면
   「왜 안 되지?」 가 되고, 그때는 어디가 원인인지 아무도 모릅니다.

**그 다음에 일어난 일.** 2분을 15초로 줄여 놓고 보니 이런 그림이 됐습니다 —
착공계·대리인계·공정표를 잇달아 받으면 **3분에 창이 세 번** 뜨고, 그 셋을 다 닫으면
「나중에 3번」 이 차서 **그 뒤 3일은 아예 안 뜹니다.** 사람은 짜증나고, 우리는 한 줄도
못 받고, 물어볼 기회까지 잃습니다. 그래서 그림을 그려 말씀드렸고,

**소장님이 고르신 답:** 「**방문자 모두에게 하루에 한번 창이 뜬다...이게 좋은 듯, 한 번이니까**」

#### 지금 규칙 — 외울 것이 하나입니다

| | |
|---|---|
| 갈래를 가리지 않고 | **하루에 «딱 한 번»**. 서식에서 떴으면 그 날 캐드에서는 안 뜹니다 |
| 바로투찰(bid) 만 | 공고를 **5번** 골라 계산해야 그 «한 번» 에 낍니다 |
| 「나중에」 연달아 3번 | 마지막 거절로부터 **3일** 쉼 (하루 한 번이니 쌓이는 데 사흘 걸립니다) |
| 한 줄 쓰면 | 위 «3번» 셈이 **0 으로** |

- 값은 `web/src/AskComment.jsx` 의 `투찰몇번`·`거절몇번` 하나만 고치면 됩니다.
- 기억하는 칸은 둘뿐입니다 — `kcm_ask_hits { day, n, asked }` · `kcm_ask_nope { n, at }`.
  **갈래별 시각을 적던 `SEEN` 은 없앴습니다.** 「묶음」 이 숨을 자리를 아예 없앤 것입니다.
- ⚠️ **「묶음」 같은 완충을 다시 넣지 마십시오.** 하루 한 번이면 이미 «한 번» 이라 덧댈 것이 없습니다.
  시험(`⑥`)이 `const SEEN =`·`const 묶음 =` 이 되살아나면 **빨간불을 켭니다.**

**검사** — `node web/시험/댓글창_시험.mjs` · **45개** (하루 한 번 · 바로투찰 자격 ·
거절 3번→3일 · 날짜 넘김 · 되살아남 감시 · 유료 제외 · 첫 줄 제목)

### 46. 🚨 「메일 보내기」 가 **우리 메일을 보내는 게 아니었다** — `mailto:` (2026-09-17)

소장님: 「**문의 하기에서 메일 보내기 내 메일 알잖아. 근데 윈도우가 뜨던데**」
        「**이용자가 클릭하면 바로 내 메일로 바로 보낼 수 있게 해줘**」

`mailto:` 링크는 **우리가 메일을 보내는 것이 아닙니다.** 누르는 분 컴퓨터에 깔린
메일 프로그램(윈도우 메일·아웃룩)을 여는 것입니다. 그래서 —

- 안 깔려 있으면 → **낯선 창이 뜹니다**(소장님이 보신 그 창입니다)
- 웹메일(네이버·다음·지메일)만 쓰는 분은 → **거기서 끝입니다.** 그냥 나갑니다
- 휴대폰에서는 → 기본 메일 앱이 없으면 아무 일도 안 일어납니다

**즉 「문의하기」가 사실상 «문의를 막는 단추» 였습니다.** 몇 명이 여기서 돌아섰는지는
셀 방법도 없습니다 — 아무 기록도 안 남기 때문입니다.

→ **`mailto:` 를 전부 없앴습니다.** 세 곳이었습니다:

| 자리 | 전 | 후 |
|---|---|---|
| `/contact` (바닥글 「문의」) | `mailto:` 한 줄 | **문의칸**(제목·자세히·공고번호·연락처) — 주소는 안 적음 |
| `/naeyeok` 맨 위 「✉️ 메일로 문의」 | `mailto:` | 아래 문의함으로 가는 고리 |
| `/naeyeok` 폼 옆 「메일로 보내기」 | `mailto:` | **뺐습니다** — 바로 옆 「문의 보내기」 가 곧 메일입니다 |

**가는 길** — 적고 보내기 → `quotes` 에 들어감 → **10분마다 도는 알림**(`tools/quote_mail.py`,
`.github/workflows/문의알림.yml`)이 소장님 메일로 그대로 밀어 드림. 늦어야 10분입니다.

⚠️ **`/contact` 는 정적 파일입니다**(`web/public/contact.html`). firebase SDK 를 싣지 않습니다 —
그 한 장 때문에 400KB 를 받게 할 수 없습니다. 대신 **RTDB 의 REST 주소로 `fetch` 한 번**만 합니다.
규칙(`database.rules.json`)의 `quotes/$id` 가 `".write": "!data.exists()"` 라 **로그인이 필요 없습니다.**

**2026-09-17 실측** (소장님 크롬에서 `k-conmap.com` 주소로 직접 넣어 봄):

| 해 본 것 | 결과 |
|---|---|
| ① 정상 문의 (가입·로그인 없이) | **200** — `{"name":"-P1jljOF_..."}` 로 들어감 ✅ |
| ② 규칙에 없는 이름표(`hp`) 끼워 넣기 | **401 Permission denied** — 막힘 ✅ |
| ③ `work` 없이 보내기 | **401 Permission denied** — 막힘 ✅ |
| ④ 남의 문의 읽어 보기 | **401 Permission denied** — 막힘 ✅ |

※ ① 은 실제로 들어갔습니다. 「[시험] 문의칸 확인」 이라는 메일이 한 통 갑니다 — 지우셔도 됩니다.

⚠️ **보내는 값의 «이름»은 규칙에 적힌 것만 됩니다**(`$other` 는 막혀 있습니다):
   `work · org · no · money · want · due · phone · name · memo · at`.
   contact.html 에서 칸을 늘리려면 **규칙부터** 고치고 `3_규칙올리기.bat` 으로 올려야 합니다.

⚠️ **메일 주소는 화면에 «늘» 적지 않습니다.** 처음엔 글자로 적고 「주소 베끼기」 단추를 뒀는데,
   소장님: 「**그럼, 제거하면 되지 않아?**」 — 맞습니다. 뺐습니다. 이유 둘:
   ① 문의칸이 있는데 주소를 또 적으면 **무엇을 눌러야 하는지 갈립니다**
   ② **공개 페이지에 적힌 메일 주소는 수집 로봇이 거의 반드시 긁어갑니다** — 스팸만 늘고,
      한 번 새 나가면 되돌릴 방법이 없습니다
   → 주소는 **보내기가 실패했을 때만** 한 줄로 나옵니다(`/contact`·`/naeyeok` 둘 다).
     그때는 딴 길이 정말로 필요하니까요.
   📌 「연락처를 어디 적어 둬야 하지 않나」 — `/privacy` 가 「**문의 페이지의 연락처로**」 라고
      가리키고 있고, **그 연락처가 곧 이 문의칸**입니다. 주소를 걸 필요가 없습니다.

📌 **`mailto:` 를 다시 넣지 마십시오.** 넣으려면 「이 사람 컴퓨터에 메일 프로그램이
   깔려 있는가」 를 먼저 답해야 합니다. 그 답을 우리는 모릅니다.

#### 🔍 2차 검증에서 잡은 «틀린 것 셋» — 시험도 빌드도 통과했는데 틀렸던 것들

소장님: 「고친 후에 올리기 전에 **다시 검증** 하고… 틀린 게 있어서 수정했다면 **다시 검증**해」

| 무엇 | 사람 눈에 어떻게 보였을까 | 왜 시험이 못 잡았나 |
|---|---|---|
| 알림칸 `#msg` 가 **폼 «안»** 에 있었음 | 보내면 폼이 사라지는데 **「보냈습니다」 글자도 같이 사라짐** → 「눌렀는데 아무 일도 안 일어나네」 | 시험은 값만 봅니다. **«눈에 보이나»는 화면을 열어야 압니다** |
| 「2초 안에 보내면 기계로 본다」 | 빨리 쓰는 분의 **진짜 문의를 조용히 버림** | 기계는 안 걸리고 사람만 걸리는 덫. 돌려 보면 늘 통과합니다 |
| 입력칸 글자 15px | **아이폰이 화면을 통째로 확대** (8절 29 에 이미 적힌 규칙) | 새 화면을 만들 때마다 또 빠집니다 |

📌 **«시험 통과» 와 «화면이 맞다» 는 다른 말입니다.** 새 화면·새 폼을 만들었으면
   `9_미리보기.bat` 으로 **눈으로 한 번 보고** 올립니다(미리보기 주소는 7일 뒤 사라집니다).
   특히 **「보내고 나서 무엇이 보이는가」** 는 반드시 눈으로 봅니다.

**2026-09-17 미리보기 실측** (`k-conmap--preview-…web.app`) — 진짜 사이트는 안 건드림:

| 본 것 | 결과 |
|---|---|
| `/contact` 문의칸 · 알림칸이 폼 «밖» | ✅ 보냄 뒤 「보냈습니다」 가 **보임**(초록) |
| 빈 칸으로 보내기 | ✅ 「무슨 일인지 한 줄만…」 이 보이고 폼은 그대로 |
| 입력칸 글자크기 | ✅ 16px (아이폰 확대 없음) |
| `mailto:` · 메일주소 노출 | ✅ `/contact`·`/naeyeok` 둘 다 **0곳** |
| 문의 단추 3곳 | ✅ 흰 바탕 + 파란 테두리·글자 (`.btn.line`) |
| 가로 스크롤 | ✅ 안 생김 |
| 창: 서식 받음 | ✅ 뜸 · `{day, n:0, asked:1}` |
| 창: ✕ 닫고 같은 날 캐드 | ✅ **안 뜸** · `nope {n:1}` |
| 창: 날이 바뀐 뒤 | ✅ **다시 뜸** · `asked` 가 0 으로 되돌아감 |
| 창: 바로투찰 1번 | ✅ 안 뜸 (자격 5번 필요) · `n:1` 만 오름 |

⚠️ 숨은 탭에서는 `setInterval` 이 **1분에 한 번**으로 늦춰집니다 — 창이 늦게 뜬 것은
   고장이 아니라 크롬이 그런 것입니다. (사람이 보고 있는 탭에서는 0.8초입니다)

### 47. 🚨 「10분마다」 라고 적어 놓고 «3~5시간에 한 번» 돌고 있었다 (2026-09-18)

소장님: 「**메일 온 거 없는데**」

`.github/workflows/문의알림.yml` 에 `cron: '*/10 * * * *'` — 10분마다라고 적혀 있습니다.
그런데 **실제로는 3일 동안 14번** 돌았습니다. 10분마다면 400번대여야 합니다.

| | |
|---|---|
| 적어 놓은 것 | 10분마다 |
| 실제 간격 | **3~5시간에 한 번** |
| 3일간 실행 횟수 | **14번** (`api.github.com/.../runs` 로 실측) |

**깃허브의 예약(schedule)은 «되도록 그때쯤» 이지 약속이 아닙니다.** 공식 문서에도
「지연될 수 있고 **버려질 수 있다**」 고 적혀 있습니다. 특히 우리처럼 수집·배포가 쉴 새 없이
도는 저장소에서는 예약분을 통째로 건너뜁니다 — `update.yml` 도 373번 중 거의 전부가
**push 로** 돌았고 **schedule 로** 돈 것은 드뭅니다.

📌 **그러므로 «사람을 기다리게 하는 일» 을 깃허브 예약에 걸지 않습니다.**
   문의 알림처럼 「몇 분 안에 와야 하는 것」 은 예약 말고 **그 자리에서 보내는 길**로 만듭니다.
   예약이 맞는 일은 「하루에 한 번 모아서」 같은, 몇 시간 늦어도 되는 것뿐입니다.
   ⚠️ 제가 「늦어야 10분」 이라고 말씀드렸는데 **틀린 말이었습니다.**
      적어 놓은 주기를 실제 주기라고 믿고 말하지 않습니다 — **실행 기록을 세어 보고** 말합니다.

**→ 그래서 문의 메일은 파이어베이스 함수로 옮겼습니다** (9절: 요금제는 이미 Blaze).
   글이 `quotes` 에 들어오는 «그 순간» 함수가 깨어나 메일을 보냅니다.
   깃허브 알림은 **지우지 않고 그물로 남겨 둡니다** — 함수가 탈이 나도 몇 시간 뒤엔 옵니다.

**2026-09-18 실측 — 다 뚫렸습니다.**

| | |
|---|---|
| 글 넣은 시각 | `01:13:01` |
| 함수가 깬 시각 | `01:13:05` — **4초** |
| 결과 | 기록에 오류 한 줄 없음 · 문의함에 **`sent: true`** · 소장님 메일함에 도착 |

### 47-1. 여기까지 «네 번» 막혔습니다 — 다음에 또 만나면 바로 알아보시라고 적습니다

| 막힌 데 | 증상 | 까닭과 고친 법 |
|---|---|---|
| ① 코드를 못 읽음 | `User code failed to load … Timeout after 10000` | 파이어베이스는 올리기 전에 `index.js` 를 **한 번 읽어** 무슨 함수가 있는지 봅니다. **10초**만 줍니다. 맨 윗줄에서 v2 «묶음» 을 통째로 부르면 https·firestore·storage·pubsub… 을 다 끌고 와 넘깁니다. → **맨 윗줄은 꼭 필요한 것만.** admin·nodemailer 는 «쓸 때» 부릅니다. `FUNCTIONS_DISCOVERY_TIMEOUT=120` 도 넣었습니다 |
| ② 설정값을 물어봄 | `In non-interactive mode but have no value for MAIL_USER, MAIL_TO` | 비밀도 아닌 메일 **주소**를 `defineString` 으로 뒀더니 기본값을 적어 둬도 묻습니다. → **그냥 코드에 적습니다.** 비밀은 `MAIL_PASS` 하나뿐 |
| ③ 첫 2세대 함수 | `Permission denied while using the Eventarc Service Agent` | 그 프로젝트에서 **2세대 함수를 처음** 쓸 때만 나옵니다. 권한이 퍼지는 데 몇 분 걸립니다. → **3분 기다렸다 다시 올리면 됩니다.** 고칠 것 없습니다 |
| ④ 메일이 안 나감 | 기록에 `메일 실패: Invalid login: 535-5.7.8 Username and Password not accepted` | 지메일 **앱 비밀번호가 안 맞습니다**(옛것·다른 계정·띄어쓰기). → 새로 만들어 `MAIL_PASS` 에 넣습니다. ⚠️ **넣은 뒤 반드시 다시 올려야 합니다** — 파이어베이스는 **올릴 때의 비밀값 «버전»을 못 박습니다.** 새 버전을 넣어도 다시 올리기 전에는 옛 값을 씁니다 |

⚠️ **`.bat` 에 줄을 끼워 넣을 때는 «줄끝이 CRLF»** 라야 합니다. LF 로 넣으면 윈도우가
   **그 줄을 통째로 무시합니다** — `set FUNCTIONS_DISCOVERY_TIMEOUT` 이 그래서 한 번 헛돌았습니다.
   붙인 뒤 세어 보십시오: `CRLF 개수 == 전체 줄 수`.

💰 **이미지 찌꺼기 정리 규칙을 걸어 두었습니다** (2026-09-18).
   함수를 올릴 때마다 컨테이너 이미지가 창고(Artifact Registry)에 쌓여 요금이 붙습니다.
   → **3일 지난 이미지는 저절로 삭제.** `F3_이미지정리.bat` 으로 한 번 걸었고,
     `F2` 의 배포 명령에 `--force` 를 넣어 **앞으로는 올릴 때 저절로** 걸립니다.

**단추 셋** (바탕화면 `나노_건설맵 코드 등`)

| | |
|---|---|
| `F1_비밀번호넣기.bat` | 지메일 앱 비밀번호를 파이어베이스 비밀값에 넣기 (검은 창이라 글자가 안 보입니다 — 브라우저의 **Secret Manager → MAIL_PASS → + 새 버전** 이 더 쉽습니다) |
| `F2_함수올리기.bat` | 함수 올리기 (3~5분). **비밀값을 바꿨으면 반드시 이걸 다시** |
| `F3_이미지정리.bat` | 이미지 찌꺼기 정리 규칙 (한 번이면 됩니다) |

⚠️ **깃허브 쪽 `MAIL_PASS` 는 아직 옛 비밀번호입니다.** 그물이 제 노릇을 하려면
   깃허브 Settings → Secrets 에서도 새 값으로 바꿔야 합니다. 급하진 않습니다.

### 48. 📝 「내가 관리자인데 못봐」 — 문의함을 소장님 한 대에만 열었습니다 (2026-09-18)

소장님: 「지금 문의함은 규칙으로 아무도 못 읽게 잠겨 있어… **내가 관리자인데 못봐...ㅎㅎㅎ**」
        「**답글 다는 형식으로 하자**」

맞는 말씀이었습니다. 그런데 웃기려고 만든 게 아니라 «이렇게 될 수밖에» 없었습니다 —
**사이트에 «관리자 로그인» 이 없습니다.** 가입도 로그인도 없앤 사이트라 들어오는 사람이
모두 «이름 없는 방문자» 입니다. 그러니 「아무도 못 읽음」 이 소장님까지 막습니다.

→ 규칙에 **브라우저 번호 하나**만 읽을 수 있게 열었습니다.

```json
"quotes":  { ".read": "auth.uid === '<소장님 번호>'", ".write": false,
             "$id": { ".write": "!data.exists()", … } },
"quote_a": { ".read": "auth.uid === '<소장님 번호>'", ".write": false,
             "$id": { ".write": "auth.uid === '<소장님 번호>'", … } }
```

- **쓰기는 그대로** 입니다 — 한 번 쓰면 못 고치고 못 지웁니다. 읽기만 열었습니다
- **답은 문의 안에 안 적습니다.** 문의는 «못 고치는 칸» 이라 흔들면 안 됩니다.
  사랑방(`qna_a`)과 같이 **딴 칸(`quote_a`)** 에 적습니다
- 그 답은 «나에게 남기는 기록» 입니다. **문의하신 분께 저절로 가지 않습니다** —
  그분께는 적어 주신 연락처로 답합니다. 화면에서 연락처를 누르면 바로 걸리거나 메일이 열립니다

⚠️ **그 번호는 «그 브라우저» 에 붙습니다.** 기록을 지우거나 다른 PC 로 가면 바뀝니다.
   그때는 `/admin` 의 「이 브라우저 번호 베끼기」 로 새 번호를 받아 규칙에 넣고 다시 올립니다.

🔒 **`tools/checkops.py` 를 넓혔습니다.** 전에는 `op` 의 `.validate` 한 줄만 봤는데,
   이제 같은 번호가 **네 군데**에 박힙니다. 한 곳만 고치고 나머지를 잊으면
   「사랑방 답글은 되는데 문의함은 안 보이는」 반쪽이 됩니다.
   → 규칙 **전체**에서 `auth.uid === '…'` 를 찾아 화면 `OPS` 와 전부 대조합니다.

📌 **왜 메일보다 이게 더 나은 그물인가.** 메일은 스팸함으로 갈 수도, 함수가 죽을 수도,
   못 보고 지나칠 수도 있습니다. 그러면 문의가 **읽기 막힌 칸에 조용히 쌓입니다.**
   `/admin` 은 들어오면 그냥 다 보입니다. 그래서 깃허브 알림 쪽 비밀번호를 굳이 맞추지
   않았습니다 — 소장님: 「**메일이 오는데 굳이 해야 해**」. 맞습니다.

### 49. 🚨 배포는 끝났는데 «옛 화면» 이 나왔다 — SPA 주소의 브라우저 캐시 (2026-09-18)

`/admin` 을 올리고 배포가 **성공**했는데도 열어 보면 계속 옛 화면이었습니다.
`/admin?v=2` 로 열면 **새 화면**이 나왔습니다. 즉 배포 문제가 아니라 **브라우저 캐시**입니다.

**까닭.** `firebase.json` 의 no-cache 머리글은 `"/index.html"` 과 `"/"` 에만 걸려 있습니다.
그런데 `/admin`·`/live`·`/naeyeok` 같은 **SPA 주소는 «요청 주소가 /admin»** 입니다 —
서버가 안에서 index.html 로 바꿔 주는 것이지, 머리글 규칙은 «요청한 주소» 로 고릅니다.
그래서 그 주소들에는 no-cache 가 «안 붙습니다». 브라우저가 한동안 옛 문서를 붙들고 있습니다.

- 지금 당장의 해법은 **맨 위 🔄 새로고침 단추**입니다. 눌러 보니 그 자리에서 새 화면이 나왔습니다
- ⚠️ **배포 직후 「안 바뀌었다」 를 «배포 실패» 로 단정하지 마십시오.** 주소 뒤에 `?v=2` 를
  붙여 한 번 열어 보면 «캐시인지 배포인지» 가 바로 갈립니다
- 📌 **고칠 거리로 남겨 둡니다** — `firebase.json` 의 `headers` 에 SPA 주소들을 넣어
  no-cache 를 붙이면 됩니다. 다만 머리글 규칙은 겹치면 서로 덮으므로, `assets/**` 의
  1년 캐시를 건드리지 않게 **주소를 하나씩 적어** 넣어야 합니다. 급하지 않습니다

### 50. 🦺 안전서류 만들기 — 프로그램은 만들었습니다 (2026-09-18)

소장님: 「**안전관리계획서, 유해위험방지계획서. 원도급사가 자료를 주면 그 회사에 맞게
작성가능하도록 프로그램 만들어줘, 사이트에 게시할거니까 이것도 비용을 받을 거야,
그니까 견본도 만들어 주고, 올리지는 말고, 내가 확인 후 올리게**」

**두는 자리 — `K-안전서류/` (바탕화면, 저장소 «바깥»).**
K-적산과 같은 방침입니다. 저장소가 **공개**라 서식과 판정 기준을 올리면 통째로 새어 나갑니다.
사이트에는 **안내(/safety)만** 올리고, 만드는 것은 소장님 컴퓨터에서 합니다.

| 파일 | |
|---|---|
| `_현장정보.csv` | 원도급사에게 받는 자료. 엑셀로 열어 「값」 칸만 채웁니다 |
| `0_처음한번.bat` | python-docx 설치 (한 번만) |
| `1_ · 2_ · 3_둘다.bat` | 누르면 `결과` 폴더에 .docx 가 생깁니다 |
| `안전서류.py` | 표지·제출문·목차·대상 판정표·각 장의 표를 만듭니다 |
| `읽어보세요.md` | 쓰는 법과 «못 박아 둘 것» |

**무엇이 자동인가** — 공사명·발주처·기간·금액·조직·장비·공종이 문서 곳곳에 들어가고,
입력한 규모로 **대상 판정표**가 만들어집니다. 「해당 공종」에 적은 공종만 제2편에 나옵니다.
회색 「▷ …」 은 «여기에 무엇을 적어야 하는지» 입니다.

**안 하는 것** — 안전성 계산서를 «계산» 하지 않습니다. 자리와 목록만 만듭니다.
유해·위험방지계획서의 **검토자 서명은 건설안전기술사**가 합니다. 프로그램이 못 합니다.

⚠️ **법 근거는 2026-09-18 에 직접 열어 확인했습니다.**
   · 안전관리계획 — 건설기술 진흥법 제62조 · 시행규칙 제58조 **[별표 7]**.
     목차(평가항목)는 **국토교통부 원주지방국토관리청** 안내를 그대로 옮겼습니다
     (총괄 7장 + 공종별 9공종). 제출처는 **www.csi.go.kr**
   · 유해·위험방지계획 — 산업안전보건법 제42조 · 시행령 제42조제3항 ·
     시행규칙 제42조 **[별표 10]** [시행 2026. 8. 1.] [고용노동부령 제477호]
   ⚠️ **[별표 10] 본문은 HWP 로만 받게 되어 있어 못 읽었습니다.** 가~사 항목은 2차 자료로
      옮겼고, **문서 안에도 「낼 때 원문으로 맞춰 보라」고 적어 두었습니다.** 숨기지 않습니다.

📌 **아직 «올리지 않았습니다».** 소장님이 견본을 보시고 정하신 뒤에 /safety 의 「준비 중」을
   풉니다. 그때 **검토자(건설안전기술사)가 누구인지부터** 정해야 합니다 —
   정해지기 전에 문의를 받으면 못 지킬 약속이 됩니다 (Safety.jsx 머리말).

### 51. 📄 K-PDF 도구 — 소장님이 고르신 네 기능을 창에 붙였습니다 (2026-09-18)

소장님: 「pdf파일 수정가능한 프로그램 만들 수 있어?」 →
「다른 기능 아이디어 줘」 → 고르신 것 **① 제출본 점검표 · ② 당초↔변경 비교 ·
③ 폴더 통째로 찾기 · ⑧ 사진대지** (먼저), 그다음 **⑤ 제출본 조립+④ 목차 ·
⑥ 간인 · ⑨ 내역서 PDF→엑셀**.

**두는 자리** — 소장님 컴퓨터 바탕화면 `나노_건설맵 코드 등\K-PDF\` (저장소 밖)
🔒 **저장소에 올리지 않습니다.** 저장소가 Public 이라 K-적산·K-안전서류와 같은 방침입니다.

| 파일 | 하는 일 |
|---|---|
| `pdf일.py` | 엔진 — 창 없이 혼자 돕니다. 실제 일은 전부 여기 |
| `pdf도구.py` | 창(tkinter) — 고르고 누르는 자리일 뿐 |
| `시험_pdf.py` | 스스로 시험 **60가지** (2026-09-18 실측 맞음 60 · 틀림 0) |
| `읽어보세요.md` | 소장님용 설명 |

이번에 붙인 것 — 창 메뉴 ⑥ 점검·비교 / ⑦ 폴더 찾기 / ⑧ 사진대지.
**고르는 방식이 넷이라 파일 고르기를 갈래로 나눴습니다** —
파일 하나 · 여러 개(합치기) · **두 개(비교: 당초 → 변경 차례로)** · **폴더**(찾기·사진대지).

#### 🚨 이번에 «시험이 잡아낸» 것 세 가지

1. **낱말 바꾸기가 뒤 글자를 타고 넘었습니다.**
   「흄관」 → 「PE이중벽관」 으로 바꾸니 뒤의 「D=300」 **위에 겹쳐 찍혔는데
   프로그램은 「1군데 바꿈 ✔」 이라고만 했습니다.** PDF 는 워드처럼 뒷글자를 밀어내지 못합니다.
   → 이제 **① 옛 말 자리 안에 먼저 넣어 보고 ② 조금 줄여 보고 ③ 그래도 안 되면
   넘치게 찍되 «몇 군데 넘쳤는지» 를 세어 알려 줍니다.**
   `낱말바꾸기()` 가 `(낸 파일, 바꾼 군데, {"넘침": n, "줄임": m})` 을 돌려줍니다.

2. **낼 폴더가 없으면 저장이 통째로 엎어졌습니다.**
   `폴더에서찾기`·`사진대지`·`합치기` 는 낼 이름을 손으로 지어
   `_낼이름()` 의 `makedirs` 를 못 탔습니다. → 셋 다 `makedirs` 를 넣었습니다.

3. **창에서 고른 것이 다음 일감으로 넘어갔습니다.**
   「합치기」로 PDF 다섯 개를 고른 뒤 「비교」로 옮기면 그 다섯 개가 그대로 남아
   엉뚱한 두 개를 견줄 뻔했습니다. → **고르는 방식이 바뀌면 고른 것을 비웁니다.**
   (그리고 「한 쪽에 몇 장」 라벨을 고를 때마다 새로 만들어 쌓이던 것도 고쳤습니다)

#### 시험하는 법 — 화면 없이도 창을 시험할 수 있습니다

엔진은 `2_스스로시험.bat` 에 PDF 를 끌어다 놓으면 60가지가 돕니다.
**창은 클라우드에 화면이 없어 못 도는 줄 알았는데, `xvfb-run` + `python3.12`
(tkinter 는 3.12 에만 깔려 있습니다) 로 «실제로 띄워» 33가지를 돌렸습니다.**
메뉴를 고르고 → 파일 대화상자를 가짜로 바꿔 → 「하기」까지 사람과 같은 길을 탑니다.
다음에 창을 고치면 **꼭 이 길로 시험하십시오.** 눈으로만 보면 3번 같은 것을 놓칩니다.

#### 아직 안 넣은 것

⑤ 제출본 조립 + ④ 목차(북마크) · ⑥ 간인(계인) · ⑨ 내역서 PDF→엑셀 · OCR

### 52. 📄 /pdf — PDF 도구를 «사이트 안에서» 쓰게 올렸습니다 (2026-09-18)

소장님: 「건설맵 사이트 도구에 게시해 줘. **다운받게 말고 사이트 내에서 작업할 수 있게**」
→ 「브라우저에서 되는 것만 하고, **안되는 것은 제외** 해 그리고 **설명까지 할 필요는 없어**」

**주소 `/pdf` · 무료 · 17가지.** 화면 `web/src/pages/Pdf.jsx`, 엔진 `web/src/lib/pdfwork.js`.

🔒 **파일은 서버로 올라가지 않습니다.** 전부 보는 분 브라우저 안에서 합니다.
   남의 설계도서·내역서를 다루는 도구라 이것이 파는 이유이기도 합니다.
   **전송량이 0 이라 파이어베이스 값도 안 늘어납니다.**

#### 넣은 것 · 뺀 것

넣음(17) — 합치기 · 골라내기 · 쪽 빼기 · 순서 · 회전 · 나누기 · 도장 · 워터마크 · 쪽번호 ·
글자를 txt 로 · 쪽을 그림으로 · 제출본 점검표 · 당초↔변경 비교 · 여러 PDF 에서 낱말 찾기 ·
사진대지 · 낱말 몇 군데 세기 · 파일 살펴보기

**뺌(브라우저로는 못 합니다)** — 낱말 바꾸기 · 글자 지우기 · 표→엑셀 · PDF→워드 · 비밀번호.
📌 **화면에 «왜 없는지» 도 적지 않습니다.** 소장님 지시입니다. 사이트에서는 없는 기능입니다.
   (PC 판 `K-PDF` 에는 다 있습니다 — 저장소 밖, 8절 51)
🚨 낱말 바꾸기를 브라우저에 넣지 않은 까닭은 따로 적어 둡니다 — pdf-lib 에는 «빼내기»가 없어
   **흰 칸으로 덮는 것밖에 안 됩니다.** 그러면 복사할 때 옛 상호가 그대로 나옵니다.
   PC 판에서 고쳤던 바로 그 버그를 사이트에 다시 만드는 셈이라 «안 넣는 것» 이 맞습니다.

#### 🚨 이번에 시험이 잡아낸 것 — 한글이 «조용히» 안 읽혔습니다

pdf.js 는 한글(HWP)·오피스가 만든 PDF 의 글자를 풀려면 **표(cmaps)와 기본 글꼴을
따로 받아야 합니다.** 그게 없으면 **성내지도 않고 «빈 글자»** 가 나옵니다.
「흄관」 을 못 찾는 걸 보고 잡았습니다. 눈으로는 절대 못 찾는 자리입니다.

→ 엔진이 `cMapUrl` · `cMapPacked` · `standardFontDataUrl` 을 넘기고,
  `vite.config.js` 의 **`pdfjs자료()` 플러그인**이 빌드 때 `node_modules` 에서
  `dist/pdfjs/` 로 옮깁니다(2.5MB). 저장소에는 넣지 않습니다.
  **이 다섯 군데를 지우면 시험 ⑩ 이 걸립니다** (끝표로 지켜 둡니다).

#### 무거운 것을 «열기 전에는 안 받게»

`pdf-lib` 427KB · `pdfjs-dist` 356KB · 일꾼 1MB. 전부 따로 떼어(`manualChunks`)
`/pdf` 에 들어오신 분만 받습니다. **첫 화면 덩어리는 214KB 그대로입니다.**

#### 시험 두 가지 — 둘 다 돌리고 올렸습니다

| 무엇 | 어떻게 | 결과 |
|---|---|---|
| 엔진 68가지 | `node web/시험/pdf도구_시험.mjs` — 창 없이 node 로, 시험용 PDF 도 스스로 만듭니다 | 맞음 68 · 틀림 0 |
| 화면 22가지 | 진짜 크로뮴(playwright)에 화면을 띄워 **사람이 누르는 길 그대로** | 맞음 22 · 틀림 0 |

화면 시험에서 잡은 것 — ① 폰에서 일감 단추 여섯째가 가로로 잘려 안 보임(`.chips` 가 옆으로
미는 꼴이라) → `.pdfjobs` 로 줄바꿈. ② 값칸이 15px 이라 아이폰이 확대 → 16px.

⚠️ 머리 없는 크로뮴은 **내려받는 파일 이름에서 한글을 버립니다**(`download` 로 저장됨).
   진짜 크롬에서는 한글 이름 그대로 됩니다 — 서식 내려받기가 이미 그렇게 돌고 있습니다.
   그래서 시험은 «이름» 대신 «속»(PDF 머리·CSV BOM)을 봅니다.

⚠️ `@napi-rs/canvas`(시험용)는 마운트 폴더에서 `Bus error` 가 납니다 — 시험은 **윈도우에서**
   돌리십시오. 클라우드에서는 같은 코드로 68가지가 다 통과했습니다.

📌 **아직 «올리지 않았습니다».** 커밋만 했습니다.

### 53. 📊 3년치 «떨어진 기록» 을 메웁니다 — 성적표의 뿌리 (2026-09-18)

소장님: 「3년 동안 낙찰이 없는 업체도 되는 거지?」 · 「계속 받고 있는 거 아니었어?」

#### 🚨 먼저 — 「3년치」가 아니었습니다

| 자료 | 기간 | 들어 있는 것 |
|---|---|---|
| `bid_data_3years.zip` | 2022-11 ~ 2026-04 (118,847건) | **1순위만** — 전체업체 칸에 두 곳 이상 든 줄이 **64건(0.1%)** |
| `first.json` | **2026-07-14 ~ 오늘** (15,421건) | 투찰업체 전부 |
| `ranks3y` | 2026-07 ~ 오늘 | 순위 전부 — 2026-09-15 에 만들었습니다 |

`first.json` 은 **70일만** 두고 버립니다. 그래서 9월 15일에 영구 보관함을 만들 때
살아 있던 것이 70일치뿐이었습니다. 그 이전 «떨어진 기록» 은 이미 버려진 뒤였습니다.

📌 그런데도 `/report` 화면은 「**3년치** 개찰 기록에서 그 업체가 넣은 것을 **전부** 찾아」
   라고 적고 있습니다. **낙찰 기록은 3년치가 맞지만 떨어진 것까지는 두 달치입니다.**
   메우기가 끝나면 사실이 됩니다 — 그때까지는 이 문구를 조심해야 합니다.

#### ✅ 조달청은 «옛 공고번호» 로도 순위를 줍니다 (실측)

`tools/옛자료확인.py` 로 해마다 한 건씩 물어봤습니다 —
**2022 ✅5곳 · 2023 ✅2곳 · 2024 ✅18곳 · 2025 ✅2곳** (2026 표본 1건만 0곳).
→ 메울 수 있습니다.

#### 어떻게 메우나 — `tools/메우기3년.py`

· 3년 파일의 공고번호를 **새것부터** 하나씩 `getOpengResultListInfoOpengCompt` 로 묻습니다.
· 이미 보관함에 있는 달(2026-07~)과 3년 넘은 달은 건너뜁니다 → **메울 것 106,221건**.
· 어디까지 했는지 `data/store/ranks3y/_메우기.json` 에 남겨 다음 회차가 이어받습니다.
· **회차마다 «남는 몫만큼만»** — 하루 트래픽이 끝나거나 시간 예산을 넘기면 조용히 멈춥니다.
· 워크플로에서 **배포 «뒤»** 에 돕니다 (`--시간예산 480`). 늦어도 사이트에는 영향이 없습니다.
· 손으로 더 빨리 채우려면 바탕화면 **`0_삼년치메우기.bat`**.

**첫 회차 실측 (2026-09-18)** — 440건 받음 · 빈 것 12건 · 10,541줄 · 282초.
초당 1.56건이라 한 회차(480초)에 750건쯤. 남은 105,769건이면 **회차 141번 ≈ 3주**.

#### ⚠️ 한 개찰에서 «낮은 30곳» 만 담습니다

`collect.py` 의 `RANK_KEEP = 30` 입니다. 그런데 **참가업체는 중앙 64곳 · 평균 249곳**
(30곳 이하인 개찰은 36%뿐). 낙찰은 하한선 언저리에서 갈리니 승부처는 다 들어오지만,
**«한참 높게 써서 떨어진» 기록은 안 남습니다.** 성적표에 그렇게 적어야 합니다.
더 깊이 담으려면 `--깊이` 로 올리십시오 — 파일이 그만큼 커집니다
(30곳이면 3년치 ≈ 60MB, 100곳이면 ≈ 150MB).

#### ⚠️ `data/store` 는 저장소가 아니라 **Actions 캐시**에 있습니다

며칠 걸려 받은 것이 캐시가 날아가면 사라집니다. 회차가 몇 시간마다 도는 동안은
캐시가 살아 있지만(7일 미사용이면 GitHub 이 버립니다), **오래 쉬면 위험합니다.**

### 54. 📊 /report/make — 성적표를 «사이트에서 만들고 내려받습니다» (2026-09-18)

소장님: 「분석에서 성적표 신청하기만 있고, 내가 업체검색해서 업체 성적표가 나오게 해서
다운 받을 수 있어야 하는데, 그런게 없어. **이용자는 사용을 막돼, 난 사용할 수 있어야 하잖아**」
「성적표 신청하기만 있잖아. 수정안했어..난 할 수 있게 해달라고 했잖아.」

**제가 두 번 흘려들었습니다.** /report 는 «신청하는 법» 만 적힌 안내 화면이었고,
정작 소장님이 만들 자리가 없었습니다. 그래서 만든 것이 `/report/make` 입니다.

| | 파일 |
|---|---|
| 셈 | `web/src/lib/성적표.js` — `tools/report_data.py` 를 그대로 옮긴 것 |
| 종이 | `web/src/lib/성적표종이.js` — A4 를 캔버스에 그리고 pdf-lib 로 묶음 |
| 화면 | `web/src/pages/ReportMake.jsx` — 고르고 누르는 자리 |
| 시험 | `web/시험/성적표_시험.mjs` — **파이썬을 그 자리에서 돌려 한 줄씩 대조** |

#### 어떻게 «다른 이용자는 못 쓰나» — 두 겹입니다

1. **문 앞 이름표** — 운영자 브라우저(`Qna.jsx` 의 `OPS`)에서만 열립니다.
2. **진짜 자물쇠는 자료입니다.** 성적표는 개찰마다 «투찰업체 30곳» 이 다 들어 있는
   `data/store/first.json` 이라야 만들어집니다. 그 파일은 **사이트에 올라가 있지 않습니다**
   (사이트의 `/data/first.json` 은 최근 300건 요약본입니다). 소장님 컴퓨터의 그 파일을
   «파일 고르기» 로 고르셔야 합니다. ⇒ **주소를 알아내도 남이 쓸 수 있는 것이 없습니다.**
   자료는 서버로 올라가지 않습니다 — 브라우저 안에서만 읽습니다.

#### ⚠️ PDF 를 «캔버스 그림» 으로 만듭니다 — 왜

pdf-lib 에 한글을 넣으려면 5MB 짜리 글꼴을 통째로 받아야 합니다. 그래서 **브라우저가 이미
가진 글꼴로 캔버스에 그린 뒤** 그 그림을 쪽마다 한 장씩 넣습니다(A4 160dpi · 한 쪽 150~300KB).
덤으로 **화면에 보이는 그림이 곧 내려받는 PDF** 라 «화면과 파일이 다르다» 가 생길 자리가 없습니다.
글자를 긁어 갈 수 없는 것은 감수합니다 — 남에게 주는 종이라 오히려 낫습니다.

#### ⚠️ 시험은 «기준 JSON 을 저장소에 두지 않습니다»

남의 사업자번호·대표 이름이 들어갑니다. 시험이 돌 때 `tools/report_data.py` 를 직접 돌려
임시 자리에 만들고 지웁니다. 업체도 손으로 적지 않고 자료에서 그때그때 고릅니다
(많이 넣은 곳 2 · 낙찰 0건 2 · 5건짜리 1). 실측 3,892자리 대조 — **어긋남 0**.

#### ⚠️ 성적표에 반드시 적는 두 가지 (없는 정확도를 주장하지 않기)

- **기간** — 「3년」이 아직 아닙니다. 떨어진 기록이 다 있는 것은 `first.json` 이 들고 있는
  **최근 두 달 남짓**뿐이고(2026-09-18 실측 2026-07-14~, 개찰 15,421건), 그 앞 3년치는
  1순위만 있습니다. 8절 53 의 메우기가 채우는 만큼 늘어납니다.
- **30곳** — `RANK_KEEP=30`. 참가가 30곳 넘는 개찰이 9.4%(1,454/15,421) 있고,
  거기서 높게 써서 30위 밖이면 **그 건은 종이에 아예 안 잡힙니다.**
  종이 첫 장에 「넣으신 것보다 적게 나올 수 있습니다」 라고 적습니다.

#### 자가진단(화면)과 성적표(종이)는 «달라야» 합니다 — 소장님: 「같으면 성적표의 의미가 없잖아」

| | 자가진단 `/corp/{업체}` | 성적표 PDF |
|---|---|---|
| 보는 것 | 3년치에서 **1순위(딴 것)만** | **넣은 것 전부** (떨어진 것·실격 포함) |
| 낙찰 0건 업체 | 볼 것이 없음 | 그대로 나옴 (업체의 74%가 여기) |

그래서 성적표에만 있는 칸을 **여섯 개** 더 넣었습니다(2026-09-18). 전부 이미 채점해 둔 줄을
묶어 세기만 한 것입니다 — **종이에서 새 식을 계산하지 않습니다**(성적표.js 와 어긋나므로).

1. **달마다** — 언제 많이 넣었고 언제 땄나
2. **금액을 조금 움직였다면** — 내 투찰 전부를 ±0.4%p 까지 옮겨 놓고 1순위·실격을 다시 셉니다.
   ⚠️ **남들 금액은 그대로**라는 가정입니다. 표 밑에 반드시 그렇게 적습니다.
   («이렇게 하면 딴다» 가 아니라 «내 금액이 낙찰선에서 얼마나 떨어져 있었나» 를 보는 자입니다)
3. **참가가 몇 곳일 때** — 승부는 금액이 아니라 «어느 공고에 넣느냐» 라는 실측을 이 회사 숫자로
4. **어느 시·도에 냈나** — ⚠️ 기관 이름으로 시·도를 짐작하면 안 됩니다(«김천시산림조합» 에는 도가 없습니다).
   조달청이 준 `site` 를 씁니다 — `one()`(파이썬)과 `한건()`(JS) **양쪽에** 넣었습니다
5. **딴 공사 목록** — 딸 때 겨눈 자리(하한 위 %p)까지
6. **그래서 무엇부터** — 이 회사 숫자에서만 끌어온 할 일 넷.
   ⚠️ 숫자가 없는 조언은 한 줄도 넣지 않습니다. «이렇게 하면 딴다» 고 적지 않습니다 —
   3년치 실측으로 1순위율은 금액으로 안 움직입니다. 적을 수 있는 건 «새고 있는 곳» 뿐입니다.

그리고 **「개찰 하나하나」는 60건이 아니라 전부** 싣습니다(`_전부`). 표 칸을 넘치는 글은
잘라내고 «…» 을 붙입니다 — 옆 칸을 덮는 것보다 낫습니다(실제로 공고명이 참가 칸을 덮었습니다).

#### 곁들여 고친 것 — `rankBracket` 을 한 곳으로

순위 사다리로 등수를 좁히는 규칙이 `BaroBid.jsx` 안에만 적혀 있었습니다. 성적표도 같은 것이
필요해져서 **`web/src/lib/bidmath.js` 의 `rankBracket` 하나로 모았습니다**
(파이썬 짝은 `tools/bidmath.py` 의 `rank_bracket`). 두 곳에 같은 식을 적지 마십시오.

---

### 55. 🚨 검색 목록이 «어느 회사인지» 를 말해 주지 않았다 (2026-09-18)

소장님: 「국토건설을 치면 여러개가 나오는데 … **전남 3을 클릭하면 바로 회사분석이 나와.
어느회사인지 모르잖아.** 전남에 있는 무슨회사인지」
「**대유건설을 치면 제대로 나와. 근데, 국토건설처럼 어떤 회사는 이런식으로 안나와.**」

#### 무엇이 일어났나 — 소장님 브라우저로 직접 눌러 확인했습니다

자가진단 검색 목록에는 **두 종류의 줄**이 섞여 나옵니다.

```
① 대유건설 · 전북 · 합계 · 법인 9곳     36건   ← 누르면 «9곳 중에 고르는 자리»
② 대유건설 · 전북 · 신명철 (404-81-•••)   8건   ← 누르면 «그 법인 한 곳» 으로 바로
```

- 대유건설 때는 ①(첫 줄)을 누르셨습니다 → 고르는 자리가 떴습니다 → 「제대로 나와」
- 국토건설 때는 ②(「전남 … 3건」 줄)을 누르셨습니다 → 그 법인으로 바로 → 「어느 회사인지 모르잖아」

**회사 탓이 아니라 «어느 줄을 눌렀느냐» 의 차이였습니다.** 코드는 깨지지 않았습니다.
그리고 「전남 3」은 «전남에 3개 회사» 가 아니라 «전남 회사 한 곳이 3건 땄다» 는 뜻입니다.

#### 진짜 원인 — 목록에 «상호» 를 안 실었다

`corp/idx/*.json` 의 값이 `[건수, 묶음, 법인수, 지역, 대표]` 였습니다. **상호가 없습니다.**
그래서 화면은 주소용으로 정규화한 이름(`국토건설`)만 찍었고, **네 줄이 전부 「국토건설」로
똑같이 보였습니다.** 실제 상호는 `(주)국토건설` · `국토건설(주)` · `국토건설 주식회사` 로
다 다른데 말입니다. 대표 이름과 가린 사업자번호로만 구별하라는 화면이었습니다.

#### 고친 것

1. `build_json.py` — 색인에 **여섯째 칸(상호)** 을 붙였습니다.
   ⚠️ **합계 줄에는 화면이 이 상호를 쓰지 않습니다** — 합계는 한 법인이 아니므로
   그 줄에 상호를 찍으면 그게 더 큰 거짓말이 됩니다.
2. `Analysis.jsx` — 법인 줄은 **진짜 상호**로, 합계 줄은 **「법인 N곳 — 골라 보기 →」**
   로 적고 칸 색을 달리해 눈으로도 갈라놨습니다(`.suggest button.grp`).
3. `CorpPage.jsx` — 법인 하나를 보고 있을 때 맨 위에 **상호·대표·번호**를 답니다.
   「이 법인 하나만 보고 있습니다」 만으로는 넷 중 어느 곳인지 알 수 없었습니다.

#### 다음에 안 틀리려면 — 여기서 배운 규칙

> **목록의 한 줄은 «누르면 무엇이 나오는지» 를 그 줄 안에서 말해야 합니다.**
> 같은 이름이 여럿일 때 «무엇으로 구별되는가» 를 줄에 안 적으면, 만든 사람은 알아도
> 누르는 사람은 모릅니다. 소장님이 헷갈리시면 이용자는 백이면 백 헷갈립니다.
> (8절 41 「누르면 열린다는 만든 사람 머릿속에만 있다」 와 같은 잘못입니다 — 세 번째입니다)

⚠️ 그리고 **「어떤 회사는 되고 어떤 회사는 안 된다」 는 말을 들으면 자료부터 의심하지 말 것.**
이번에 저는 조달청 자료·색인·법인 칸을 110곳까지 훑고 나서야(전부 멀쩡했습니다)
소장님께 «어떤 모양이었는지» 를 여쭤봤습니다. **먼저 여쭤봤으면 30분을 아꼈습니다.**

⚠️ 색인에 칸을 하나 붙였으므로 **배포가 한 번 돌아야 목록에 상호가 찍힙니다.**
그 전까지 화면은 옛 자료(다섯 칸)를 읽고 정규화된 이름으로 떨어집니다 — 깨지지는 않습니다.

### 56. 🔒 파이어베이스 「안전하지 않은 규칙」 메일 — 진짜 구멍이 있었습니다 (2026-09-18)

소장님: 「파이어베이스 안전하지 않은 규칙이 있다는 메일 와. 이상없는지 확인해줘」

#### 열려 있던 곳 — 두 칸

```json
"stats":  { ".read": true, ".write": true },
"sarasa": { ".read": true, ".write": true },
```

**주소만 알면 누구나 아무 것이나 써 넣을 수 있었습니다.** RTDB 주소는 사이트 자바스크립트
안에 있으니 사실상 공개된 주소입니다. **종량제(Blaze)** 라 남이 자료를 들이부으면 요금이
붙고, 있던 값을 통째로 지울 수도 있었습니다. 「옛 Streamlit 이 쓰니 건드리지 말 것」 이라고
적어 두고 그대로 둔 자리였습니다.

#### 닫기 전에 실측한 것 (짐작으로 닫지 않았습니다)

| 본 것 | 결과 |
|---|---|
| `stats` 안에 무엇이 있나 | 방문 세는 값 네 개뿐 — 마지막 기록이 **2026-04**, 다섯 달째 멈춤 |
| `sarasa` 안에 무엇이 있나 | `sarasa/deals` 에 쇼핑몰 링크 15개 |
| sarasa.kr 이 이걸 읽나 | **안 읽습니다** — 파이어베이스 요청이 0건(실측) |
| 옛 사이트 k-conmap.streamlit.app | **이미 없습니다** (「앱이 없거나 권한이 없습니다」) |
| 새 사이트 코드가 쓰나 | 한 줄도 안 씁니다 |

지금 값(지우지 않았습니다): `total_visits 6398 · total_users 4 · monthly {202604:810, "2026-04":1848} · month_4_visits 5`

#### 고친 것 — 두 가지

1. `stats` → 읽기·쓰기 **둘 다 막음**. `sarasa` → 읽기만 열고 **쓰기 막음**
   (공개 쇼핑 링크라 읽혀도 해가 없고, 혹시 읽는 데가 있어도 안 깨지게).
2. 🚨 **뿌리(`.read`/`.write`)를 `false` 로 닫았습니다.** 전에는
   `auth != null && sign_in_provider != 'anonymous'` 였습니다 —
   **구글 로그인 같은 걸 한 번이라도 켜는 순간 «아무 구글 계정이나» DB 전체의 주인**이 됩니다.
   열어 둘 까닭이 없었습니다:
   · 사이트는 `signInAnonymously` **하나만** 씁니다 (web/src/firebase.js 실측)
   · `tools/qna_mail.py`·`quote_mail.py` 는 **서비스 계정** 이라 규칙을 지나칩니다
   · 옛 노드(`users`·`bid_master`·`announcements`·`archive_*`·`service_*`·`backup_notices`)는
     그 사이트가 없어져 읽을 사람이 없습니다

#### 닫은 뒤 «진짜로» 확인했습니다 (배포하고 브라우저에서 두드려 봄)

| | |
|---|---|
| 사랑방 화면에 글이 보이나 | ✅ 보임 |
| `qna` · `qna_a` · `jobs` · `user_forms` · `comments/{공고}` · `qna_del` · `uf_flag` 읽기 | ✅ 200 (공개 게시판 — 그대로) |
| `sarasa` 읽기 | ✅ 200 |
| `stats` · `users` 읽기 | ⛔ 401 |
| 뿌리 쓰기 · `stats` 쓰기 · `sarasa` 쓰기 | ⛔ 401 |
| 문의함(`quotes`) 읽기 | ⛔ 401 (운영자 uid 만) |

⚠️ **규칙은 `3_규칙올리기.bat` 으로 따로 올립니다.** 깃허브 배포(Actions)는 hosting 만 올립니다 —
`database.rules.json` 을 고치고 커밋만 하면 **실제 규칙은 그대로입니다.**
⚠️ RTDB 규칙은 **위에서 열면 아래는 못 막습니다.** 반대로 뿌리를 `false` 로 닫아도
아래에서 `true` 로 연 칸은 그대로 열립니다 — 그래서 뿌리를 닫아도 게시판이 안 깨집니다.
⚠️ 다시 쓸 일이 생겨도 **`.write: true` 로 열지 마십시오.** 서비스 계정을 쓰거나
모양·크기를 검사하는 `.validate` 를 붙여 여십시오.

---

### 57. 🔙 「이 법인만 보기」에 뒤로가기가 없었습니다 (2026-09-18)

소장님: 「업체를 선택하면 뒤로가기가 되어야 하는데, 없어. 그리고, 업체가 다 나와야 하는데,
그래서 선택하게 해야 하는데」

#### 왜 없었나

`CorpPage.jsx` 가 고른 법인을 **화면 안 상태(`useState`)로만** 들고 있었습니다.
주소도 기록도 안 남으니 브라우저 «뒤로» 가 먹을 것이 없었습니다.
→ 라우터 기록(`navigate(pathname, { state: { firm } })`)에 남깁니다.
**주소는 그대로**라 사업자번호는 여전히 안 보이고, 뒤로 누르면 «같은 이름 전체» 로 돌아옵니다.

#### 그리고 고르는 자리를 «맨 위»로

전에는 요약 카드가 먼저 나오고 법인 고르는 칸이 그 아래였습니다. 그런데 이름이 같은 법인이
여럿이면 **요약 카드의 숫자는 «여러 회사를 합친 값»** 이라, 먼저 읽으면 안 되는 숫자입니다.
→ 고르는 칸을 맨 위로 올리고, 줄마다 **상호 · 지역 · 대표 · 건수**를 찍습니다
(상호·지역은 검색 색인을 한 번 더 읽어 붙입니다 — 같은 묶음이라 대개 이미 받아 둔 파일입니다).

#### 그리고 «고른 뒤에도» 나머지가 보여야 합니다 (같은 날 두 번째 고침)

소장님: 「**클릭하면 업체 하나만 나와.** 그리고 뒤로가기 하면 뒷 화면이 안나와」

법인 하나를 고르면 **나머지 세 곳으로 가는 길이 화면에서 통째로 사라졌습니다.** 돌아가는 길이
브라우저 «뒤로» 하나뿐이었는데 그것마저 안 먹었으니, 막다른 골목이었습니다.
→ 고른 뒤에도 **같은 자리에 네 곳이 다 남습니다.** 지금 보는 회사에는 「지금 보는 회사」 표를
달아 누를 수 없게 하고, 나머지는 눌러서 바로 갈아탑니다. 「N곳 합계로 돌아가기」 도 같은 칸에.

> **«하나를 고르는 화면» 은 고른 뒤에도 «나머지» 를 보여 줘야 합니다.**
> 고르고 나면 목록이 사라지는 화면은, 되돌릴 길을 브라우저 뒤로가기 하나에 걸게 만듭니다.

> **목록의 한 줄은 «누르면 무엇이 나오는지» 를 그 줄 안에서 말해야 합니다** (8절 55와 같은 교훈).
> 여기에 더해: **화면 안 상태로만 바꾸는 «고르기» 는 뒤로가기를 죽입니다.**
> 사용자가 «다른 화면으로 갔다» 고 느끼는 전환은 라우터 기록에 남기십시오.

### 58. 🔍 「태선종합건설은 왜 없지?」 — 못 찾은 사람에게 아무 말도 안 하고 있었습니다 (2026-09-18)

소장님: 「태선종합건설은 왜 없지? 3년 동안 안된건가?」

#### 실측한 답

**태선종합건설 주식회사(614-81-02015)는 3년치 개찰에 «1순위»가 한 건도 없습니다.**
자가진단은 1순위만 모으므로 화면에 아예 안 뜹니다. 「3년 동안 못 땄다」가 맞습니다.

**그런데 넣기는 했습니다.** 3년치 보관함에서 지금까지 메운 며칠치만 봐도 세 건 —

| 개찰 | 등수 | 금액 | 투찰률 |
|---|---|---|---|
| 2026-08-06 | 7위 / 933곳 | 6.51억 | 90.449% |
| 2026-09-01 | 8위 / 877곳 | 13.46억 | 89.393% |
| 2026-09-04 | 3위 / 33곳 | 2.99억 | 90.878% |

세 건 다 **하한선은 넘겼습니다**(실격이 아니라 등수에서 밀렸습니다).
09-01 건을 뜯어보면 — 내 금액이 하한 위 **+0.031%p**, 1순위는 **+0.007%p**,
**차이 355,950원**. 13억 4천짜리 공사에서 877곳 가운데 8위였습니다.

#### 진짜 문제 — 화면이 한 말

그때 화면이 한 말은 **「앞에서부터 찾은 결과가 없습니다」 한 줄**이 전부였습니다.
**자기 회사 이름을 치고 아무것도 못 찾은 사람이 성적표가 가장 필요한 사람입니다.**
그 자리에서 아무 말도 안 하면 그냥 나가 버립니다 — 업체 35,865곳 중 **26,645곳(74%)**이
이 경우입니다. 우리가 손님을 문 앞에서 돌려보내고 있었습니다.

#### 고친 것

1. 못 찾았을 때 «왜 없는지»와 «성적표로 볼 수 있다»를 그 자리에 적습니다
   (`.nofind` 칸 — 위 877곳/8위/35만원 이야기를 실제로 적어 둡니다).
2. `/corp/{업체}` 에 자료가 없을 때도 같은 길을 답니다.
3. 🚨 **앞글자로 0곳이면 «이름 가운데»까지 자동으로 한 번 더 찾습니다.**
   앞글자로만 찾아 놓고 「없습니다」라고 하면 **거짓말**입니다
   (실측: 「종합건설」 앞글자 9곳 · 실제 2,867곳 / 「개발」 0곳 · 실제 2,420곳).
   ⚠️ 이름 목록은 gzip 263KB 입니다. 그래서 **못 찾았을 때만 · 세 글자 이상일 때만**
   받고, 한 번 받으면 그 브라우저가 기억합니다. 늘 받으면 안 됩니다(하루 1,000번이면 263MB).

> **«없습니다» 라고 말하기 전에, 정말 다 찾아봤는지 확인하십시오.**
> 그리고 «없습니다» 로 문장을 끝내지 마십시오 — 그 사람이 다음에 갈 곳을 적어 주십시오.

### 59. 🔙 뒤로가기 전수조사 — 스무 곳에 나갈 문이 없었습니다 (2026-09-18)

소장님: 「**건설맵 사이트 전수조사 해서 뒤로가기 버튼 달아줘**」

**라우트 37개를 하나씩 대조했습니다.** 길(`Crumbs.jsx`)이 안 그려지던 자리:

| 어디 | 왜 안 그려졌나 |
|---|---|
| **탭에 있는 큰 자리 20곳** (`/qna` `/tools` `/forms` …) | `TOP` 에 들어 있으면 `return null` 이었습니다. 탭에 불이 들어오니 «길» 은 군더더기가 맞습니다 — 그런데 **다른 화면이 보내 놓고 돌아갈 문을 안 낸 것**은 다른 이야기입니다 |
| `/pdf` · `/admin` | `NAME` 표에 이름이 없어 `return null` |
| `/report/make` | `LEAF` 에 없어 「업체 입찰 성적표 › **make**」 로 나왔습니다 |
| 없는 주소(NotFound) | 들어오면 나갈 길이 아예 없었습니다 |

**고친 방법 — 화면을 하나하나 고치지 않았습니다.** `Crumbs.jsx` 한 곳에 세 갈래:

1. 보낸 화면이 있으면(`state={{ from: { to, name } }}`) → 「← 그 이름」
2. 두 칸 이상 안쪽 화면 → 「← 부모 이름」 (전과 같음)
3. 그 밖의 모든 자리 → **브라우저 기록으로 한 걸음 「← 뒤로」**

3번의 판정은 **리액트 라우터가 기록마다 매기는 `history.state.idx`** 입니다.
`idx > 0` 이면 «우리 사이트 안에서 걸어 들어온 것» 입니다.
⚠️ **검색·주소창으로 바로 들어온 사람(idx 0)에게는 그리지 않습니다** — 누르면 사이트 밖으로
   나가 버립니다. 첫 화면에 뒤로가기가 있으면 안 되는 이유가 이것입니다.

⚠️ 새 화면을 만들면 **`NAME`/`LEAF`/`TOP` 표에 이름을 넣으십시오.** 안 넣어도 「← 뒤로」는
   나오지만, 이름이 있으면 「← 설계변경」처럼 어디로 가는지 보입니다.

---

### 60. 🧪 화면 검증은 «진짜 브라우저로» 합니다 — 빌드 통과는 검증이 아닙니다 (2026-09-18)

소장님: 「**성적표 이게 지금 몇 번째 인지 모르겟다. 확실하게 다시 검증 해줘**」

몇 번을 고쳐 올렸는데 소장님이 열어 보면 또 아니었습니다. **제가 «빌드가 됐다» 를
«된다» 로 착각했기 때문입니다.** 빌드는 문법만 봅니다.

**이제 이렇게 합니다** (컨테이너 안에서 끝납니다 — 소장님이 누를 것이 없습니다):

    ① 마운트에서 vite build → dist 를 tgz 로 묶어 device_stage_files 로 컨테이너에 올림
    ② 컨테이너에서 SPA 용 정적 서버 (없는 주소는 index.html 로 넘겨야 합니다)
    ③ Playwright + /opt/pw-browsers/chromium-1194/chrome-linux/chrome 로 실제 조작
       ⚠️ 경로가 `/opt/pw-browsers/chromium/...` 이 아닙니다. `-1194` 가 붙습니다.
    ④ 폰 크기(430×900)로 엽니다 — 소장님은 폰으로 보십니다

**걸려 넘어진 것들 (다음에 또 만납니다):**

- **`page.goto()` 는 «걸어 들어간 것» 이 아닙니다.** 새 문서라 `history.state.idx` 가 0 으로
  돌아갑니다. 뒤로가기 시험이 전부 실패로 나왔는데 **시험이 틀린 것이었습니다.**
  → `history.pushState` + `PopStateEvent` 로 라우터 안에서 옮겨야 합니다.
- **`fill()` 은 리액트 `onChange` 를 안 깨울 때가 있습니다.** `click()` 뒤 `type(delay=60)`.
- **파이어베이스는 컨테이너에서 못 붙습니다**(프록시 차단). `ERR_TUNNEL_CONNECTION_FAILED`
  는 진짜 오류가 아닙니다. → 그래서 `ReportMake` 가 **브라우저에 적힌 번호를 먼저 보고**
  파이어베이스는 나중에 확인하도록 고쳤습니다. 현장에서 인터넷이 느려도 열립니다.
- **PDF 쪽수를 `/Type /Page` 문자열로 세지 마십시오** — pdf-lib 가 눌러 담아 안 보입니다.
  `pdfinfo` · `pdftoppm -png` 로 **그림으로 바꿔 눈으로 봅니다.**

**2026-09-18 잰 결과** (성적표 한 줄 전체):

| 무엇 | 결과 |
|---|---|
| 숫자 (`web/시험/성적표_시험.mjs` ↔ `tools/report_data.py`) | 3,980자리 · **어긋남 0** |
| `/report` 견본 그림 뗌 · PDF 단추 · 뒤로가기 | 6가지 **전부 통과** |
| 사랑방 뒤로가기 (눌러서 돌아오기까지) | **통과** |
| 운영자 단추 · 남의 브라우저 차단 | **통과** |
| 뒤로가기 전수 (라우트 31곳 걸어 들어가기) | **31곳 전부** |
| 성적표 실제 생성 → PDF | 6쪽 · 1,283,295바이트 · `%PDF-` · **그림으로 확인** |


### 61. 🔧 «화면 검사» 가 배포를 막았습니다 — 파일을 옮기면 검사도 같이 옮기십시오 (2026-09-18)

`OPS`(운영자 브라우저 목록)를 `pages/Qna.jsx` 에서 `lib/운영자.js` 로 옮겼습니다.
그런데 **`tools/checkops.py` 가 아직 `Qna.jsx` 를 보고 있었습니다.**

    #401  사이트 자동 갱신   failure   41초
      └ 화면 검사 (import · 이름표↔숫자 · 운영자 목록)  ❌
          「Qna.jsx 에서 const OPS = [ … ] 를 못 찾았습니다」
      └ 그 뒤 «수집·집계·빌드·굽기·배포» 가 전부 skipped

**검사가 제 할 일을 한 것입니다.** 잘못은 제게 있습니다 —
**올리기 «전에» 세 검사를 돌려 보지 않았습니다.**

> 🚨 **올리기 전에 반드시 이 셋을 돌립니다.** 1분도 안 걸립니다.
>
>     python tools/checkimports.py     # 빠진 import
>     python tools/checklabels.py      # 이름표 ↔ 숫자
>     python tools/checkops.py         # 운영자 목록 ↔ 서버 규칙
>
> ⚠️ `tools/selfcheck.py` 전체는 마운트에서 `os.remove` 때문에 죽습니다(45초도 넘깁니다).
>    깃허브에서는 정상입니다. 그러니 **마운트에서는 위 셋만** 따로 돌리십시오.

⚠️ **이름이 든 곳을 옮길 때는 «그 이름을 찾는 검사» 도 같이 찾아 고치십시오.**
   `tools/` 안에서 옮긴 파일 이름을 `grep` 한 번만 하면 됩니다.

---

### 62. 🚀 배포는 Git CMD 로 «제가» 올립니다 — bat 을 눌러 달라고 하지 마십시오 (2026-09-18)

소장님은 밖에 계실 때가 많습니다. 「bat 을 눌러 주십시오」 는 **일을 멈추는 말**입니다.

**마운트(리눅스 VM)에서는 push 가 안 됩니다** — 깃허브 자격증명이 윈도우 쪽에 있어서
`could not read Username for 'https://github.com'` 이 납니다. 대신 **컴퓨터 조작**으로:

    computer_resolve_access(["Git CMD"]) → computer_request_access → computer_open_application
    창 안을 한 번 클릭하고
      cd /d "C:\Users\OS\Desktop\나노_건설맵 코드 등\k-conmap-v2" && git status -sb
      git pull --rebase --autostash origin main      ← 원격에 딴 커밋이 있으면 «먼저»
      git push origin main

⚠️ `computer_type` 뒤에 **`computer_key("return")` 을 따로** 눌러야 합니다 (클립보드로 들어갑니다).
⚠️ 잘 됐는지는 화면 글씨로 읽지 말고 **`device_bash` 로 `git status -sb`** 를 보십시오 —
   `## main...origin/main` 에서 `[ahead N]` 이 사라지면 올라간 것입니다.
⚠️ 배포 결과는 깃허브 API 로 봅니다(브라우저 화면의 「41s」 는 «걸린 시간» 이지 «남은 시간» 이 아닙니다):

    fetch('https://api.github.com/repos/kimmyounghwan/K-conmap/actions/runs?per_page=4')
      → run_number · conclusion · jobs_url → 실패한 «단계 이름» 까지

⚠️ **파일 탐색기는 «click» 등급**이라 타이핑이 막힙니다. 캐드·Git CMD 같은 프로그램은 «full» 이라 됩니다.


### 63. 🔑 허락 창을 띄우지 마십시오 — 다만 «올릴 때» 는 반드시 여쭙니다 (2026-09-18, 소장님)

> 소장님: 「**오늘 이 시간부로 나에게 허락을 받지 않아도 돼. 그니까 허락해줘 라는 화면 띄우지 마..
> 나 자야 하는데.... 계속 허가 버튼을 클릭할 수는 없잖아.... 그 대신 올릴때는 반드시 나의 허락을
> 받아야 해...**」

**두 가지가 «따로» 입니다. 섞지 마십시오.**

| | 어떻게 |
|---|---|
| 일하다 생기는 «허락 창» (컴퓨터 조작·파일 지우기·폴더 열기 권한) | ❌ **띄우지 않습니다** |
| **사이트에 올리기(배포)** | ✅ **반드시 먼저 여쭙니다** |

**그래서 이렇게 합니다.**

- **이미 받아 둔 권한 안에서만** 일합니다. 세션에서 한 번 받은 것은 그 세션 내내 삽니다 —
  같은 것을 다시 요청하지 마십시오.
- 새 권한이 «꼭» 있어야 하는 일은 **그 일만 미뤄 두고** 나머지를 먼저 합니다.
  아침에 소장님께 「이것 하나가 권한이 없어 못 했습니다」 라고 한 줄로 알립니다.
- **커밋은 얼마든지 합니다.** 커밋은 올리는 것이 아닙니다.
- **`git push` 는 여쭙기 전에는 하지 않습니다.** 배포 워크플로를 손으로 돌리는 것도 마찬가지입니다.
  ⚠️ 커밋을 쌓아 두었다가 허락이 떨어지면 한 번에 올립니다.

⚠️ 이건 7절의 「게시(배포)는 «먼저 보여 드린 뒤에»」 와 같은 말입니다. **더 세졌습니다** —
   전에는 «보여 드리고» 였는데 이제는 «허락을 받고» 입니다.


### 64. 🔁 검증은 «반드시 두 번» — 고쳤으면 한 번 더 (2026-09-18, 소장님)

> 소장님: 「**검증은 반드시 2번 거칠 것, 2번 했는데도 틀린게 있어서 수정했다면
> 한 번 더 검증할 것. MD 에 기록해**」

**규칙은 이렇습니다.**

    1차 검증 → 2차 검증 → (둘 다 깨끗하면) 끝
    1차 검증 → 2차 검증 → 틀린 것 나옴 → 고침 → **3차 검증** → (깨끗하면) 끝
                                              ↑ 또 고쳤으면 또 한 번 더

> **「고쳤으면 다시 잰다」 가 핵심입니다.** 고치면서 다른 데를 깨뜨리는 일이 실제로 있었습니다
> (`OPS` 를 옮기다 `checkops.py` 를 안 고쳐 배포가 통째로 멈춤 — 8절 61).

⚠️ **두 번이 «같은 시험을 두 번» 이면 뜻이 없습니다.** 갈래를 달리해야 합니다:

| 차례 | 무엇으로 |
|---|---|
| 1차 | 그 자리에서 재는 시험 (단위시험 · 대조시험 · `checkimports`·`checklabels`·`checkops`) |
| 2차 | **실제로 돌려 보기** — 진짜 자료를 넣고, 진짜 브라우저·진짜 캐드로 (8절 60) |
| 3차 | 고친 뒤 1·2차를 **둘 다** 다시 |

⚠️ 결과는 **숫자로** 적습니다. 「잘 됩니다」 가 아니라 「3,980자리 어긋남 0」 처럼.
⚠️ 시험이 틀렸을 수도 있습니다 — 뒤로가기 시험이 전부 실패로 나왔는데 **시험이 틀린 것**이었습니다
   (`page.goto()` 는 «걸어 들어간 것» 이 아니라 새 문서, 8절 60).


### 65. 📉 /naeyeok/ratio — 내역서 비율 맞추기 (2026-09-18, 소장님)

> 소장님: 「**내역서를 올리면 이 내역서를 80%에 맞춰서 하면 내역서가 자동으로 80%로
> 맞춰지는 도구. 원가계산서, 내역서 등이 자동으로 된는 것. 만들어줘**」

**셋이 같은 셈이라 한 화면입니다** — 하도급 내역서(도급×하도급률) · 실행 내역서(도급×실행률) ·
계약 내역서(설계×낙찰률).

**어디에 곱하나 — «단가» 입니다.**
금액에만 곱하면 `단가 × 수량 ≠ 금액` 이 되어 서류가 반려됩니다. 단가에 곱해 단수를 맞추고
금액은 그 단가로 다시 셉니다. 재료비·노무비·경비가 갈려 있으면 **갈래마다** 곱하고
**합계는 셋을 더해** 만듭니다(합계에 따로 곱하면 재료+노무+경비 ≠ 합계).

**🚨 「공구손료 및 경장비의 기계경비」 — 100배가 될 뻔했습니다.**
수량 칸에 `4` 가 적혀 있는데 그건 개수가 아니라 **4%** 이고 금액은 `단가 × 4%` 입니다.
수량 × 단가로 다시 세면 **100배**가 됩니다. 실제 내역서 1,204개를 훑다가 찾았습니다.
→ 올려 주신 금액이 «수량 × 단가» 와 맞는 줄만 다시 셉니다. 안 맞는 줄은 그 관계를 그대로 두고
**금액에** 비율을 곱하고, 엑셀에서도 수식이 아니라 값으로 넣습니다.

**🚨 한셀(HCell) 이 만든 xlsx 를 «하나도» 못 읽고 있었습니다.**
한셀은 태그에 이름표를 붙입니다 — `<x:workbook> <x:row> <x:c> <x:v>`. 우리 정규식이 하나도
안 걸려 «시트 0개» 로 나왔습니다(내역서 20여 개가 통째로). `lib/qtoxlsx.js` 에 이름표떼기를
넣었습니다. **적산 실험실도 같이 고쳐진 것입니다.**

**🚨 시트를 전부 더하면 «두 번» 세어집니다.**
산출내역서 한 벌에는 내역서 말고도 일위대가·단가산출·중기단가가 같이 들어 있습니다.
전부 더하면 일위대가가 내역서 단가 속에 또 들어갑니다. → 내역서다운 시트만 처음에 켜 두고
(`고를만한시트`), 화면에서 켜고 끄게 했습니다.

**두 가지로 냅니다.**

| | 무엇 | 왜 |
|---|---|---|
| ① 원본 그대로 | zip 안에서 «단가 칸만» 갈아 끼움 | 서식·인쇄영역·병합·매크로가 생명 |
| ② 새 엑셀 한 벌 | 내역서·대비표·원가계산서·시트별 집계·쓴표 | 원가계산서와 검산이 필요할 때 |

①에서 금액이 `=수량×단가` 수식이면 **손대지 않습니다**(단가만 갈면 저절로 다시 셈됨).
다만 미리보기처럼 다시 셈하지 않는 뷰어를 위해 **캐시 값만** 갱신하고, `fullCalcOnLoad` 를 켜고
`calcChain.xml` 은 지웁니다. 「합계」 칸이 `=E5+G5` 처럼 갈래를 더하는 수식이면 그것도 살립니다.
소계·합계 줄이 «수식이 아니라 숫자»로 박혀 있으면 손대지 못하므로 **몇 행인지 알려 줍니다**.

**검증 (8절 64 — 세 번)**

| 차례 | 무엇으로 | 결과 |
|---|---|---|
| 1차 | 손으로 만든 내역서(머리글 두 줄·수식·요율 줄·굳은 소계) 26가지 대조 | 26/26 |
| 1차 | 리브레오피스로 다시 셈해 비교 | 합계 일치 |
| 2차 | 진짜 산출내역서 5건을 80%로 고쳐 리브레오피스로 다시 셈 | 5/5 일치(최대 0.2원) |
| — | 단수조정이 «합계 칸에만» 들어가 재+노+경 ≠ 합계 였음 → 첫 갈래에도 넣어 고침 | |
| 3차 | 1·2차를 둘 다 다시 + `checkimports`·`checklabels`·`checkops` + 빌드 | 전부 통과 |
| 4차 | **진짜 브라우저(플레이라이트)** — 구운 dist 를 띄워 파일을 올리고 단추를 눌러 봄 | 18/18 |
| — | 맞출 금액 칸이 «44» 로 보였습니다(styles.css `.tlopts label input{width:15px}` 가 글자 칸까지 눌렀음) → 칸꼴을 따로 줘 고침 | |
| 5차 | 고친 뒤 다시 구워 브라우저 시험 다시 | 18/18 |

**🚨 브라우저 시험에서 걸린 것** — 플레이라이트 `setInputFiles` 가 **한글 파일 이름**을 못 붙입니다
(파일이 0개로 들어갑니다). 화면 탓이 아니라 시험 도구 탓입니다 —
`fetch` → `File` → `DataTransfer` 로 꽂고 `change` 를 쏘면 됩니다. 다음에 또 만날 자리입니다.

**⭐ 2026-09-18 (같은 날, 소장님) — 「엑셀에서 퍼센트만 조절하게」**

> 「내역서를 드래그하거나 올리면 엑셀에서 퍼센트를 바꾸면 자동으로 내역서 및 원가계산서 금액이
> 바뀌는 거지? … 프로그램이 엑셀에서 **퍼센트만 조절하게** 해주면 자동으로 모든게 맞춘비율로
> 바뀌는 거지. **이것만 해주면 돼**」

→ 비율을 «값으로 박지» 않습니다. **「비율」 시트의 노란 칸 하나**를 두고, 단가·금액·원가계산서를
  전부 그 칸을 보는 **살아 있는 수식**으로 넣습니다. 당초 단가는 남겨 둡니다 —
  새 엑셀은 «숨긴 칸», 원본 엑셀은 «비율 시트의 D열». 그게 있어야 다시 곱합니다.

| | 어디에 | 무엇이 따라오나 |
|---|---|---|
| 새 엑셀 한 벌 | 「비율」 시트 B2 (맨 앞 시트) | 내역서 · 대비표 · 원가계산서 · 시트별 집계 «전부» |
| 원본 그대로 | 붙여 넣은 「비율」 시트 B2 | 그 내역서의 단가·금액, 그리고 원본의 소계·합계 수식 |

**80 = 80%** 로 적습니다(백분율 서식이 아니라 그냥 숫자). 원본 엑셀의 styles 를 안 건드리려고
그렇게 했고, 두 파일이 같은 꼴이라야 헷갈리지 않습니다.

**원본에 시트를 한 장 «붙이는» 법** — `[Content_Types].xml` Override + `xl/_rels/workbook.xml.rels`
Relationship + `xl/workbook.xml` 의 `<sheets>` + 시트 파일. 넷을 다 넣어야 엑셀이 엽니다.
`r:id` 의 이름표는 그 파일이 쓰는 것을 읽어서 따라 씁니다(한셀은 `<x:sheets>` 입니다).
이름이 겹치면 비율2 · 비율3… 으로 비켜 갑니다.

**🚨 원가계산서가 통째로 NaN** — `naeyeok.js` 의 FILL 이 «숫자만» 받게 돼 있어
`Math.round({f:…})` = NaN 이 들어갔습니다. 수식도 받게 고쳤습니다.

**🚨 리브레오피스는 열 때 다시 셈하지 않습니다.** 그래서 「퍼센트를 바꿨는데 그대로네」 로 보였습니다.
→ 시험할 때는 셈해 둔 값(`<v>`)을 **벗겨서** 수식만 남기고 돌립니다. 엑셀·한셀은 `fullCalcOnLoad`
  를 지키므로 실제로는 다시 셉니다. 우리가 셈해 둔 값도 같이 넣어 두어 미리보기에서도 맞습니다.

**🚨 금액 수식에 반올림이 없으면** 엑셀이 소수까지 더해 우리 합계와 어긋납니다(실측 37원/3백만).
→ 반올림이 없는 수식은 **감싸서** 단수를 씌웁니다. 이미 제 반올림이 있는 수식
  (`=ROUND(수량×단가,1)` 같은)은 **원본 셈법이 생명**이라 그대로 두고, 몇 칸인지 세어
  화면에 「원 단위로 몇 원 다를 수 있습니다」 라고 적어 드립니다.

**검증** — 손으로 만든 내역서에서 퍼센트를 80 → 50 으로 바꿔 리브레오피스로 다시 셈:
새 엑셀 44,665 → 27,915, 원본 그대로도 800/8,000 → 500/5,000, 요율 줄은 32,000 → 20,000
(100배 안 됨). 진짜 산출내역서 5건도 «수식만 남긴 채» 다시 셈해 우리 셈과 같았습니다.

**🔤 글자 크기 — 사이트 전체를 한 칼로 맞추지 마십시오 (2026-09-18, 소장님)**

성적표 화면이 「커졌다 작아졌다」 하길래 `.card p · .card li = 13px` 로 **사이트 전체**를
한 크기로 맞췄습니다. 소장님이 보시고 물리셨습니다 —
> 「지금 보니까 글자크기가 조금씩 달라. 페이지에서 이걸 **동일한 크기로 해버리면 이상해 질 것 같아**」
> 「그냥, 우선 입찰업체 성적표… **특히 위쪽 부분만 포인트 약간** 줄이면 될 것 같아」

맞는 말씀입니다. 화면마다 글의 성격이 다릅니다(안내판은 크게, 표가 많은 화면은 작게).
→ 전체 규칙은 걷어내고, **크기가 튀는 그 화면의 그 자리만** 고칩니다.
   `/report` 는 위 두 칸만 문단(15px)이고 아래는 표·목록(13px)이라 위쪽만 커 보였습니다.
   그 두 칸에 `.repfront { font-size: 13.5px }` — **13px 까지 내리지 않습니다. 약간만.**

**⚠️ 법령 수치는 적지 않았습니다.** 「비율이 낮으면 발주자의 하도급계약 적정성 심사 대상이
될 수 있습니다(건설산업기본법 제31조 · 같은 법 시행령 제34조)」 까지만 적고 **기준 퍼센트는
적지 않았습니다** — 원문을 못 봤기 때문입니다. 나중에 원문을 확인하면 그때 넣습니다.

**같이 고친 것** — `lib/naeyeok.js` 의 원가계산서를 `원가계산서(초기)` 로 떼어냈습니다.
비율 도구가 «비율을 맞춘» 재료비·노무비·경비를 그대로 넣습니다. 떼어내고 나온 엑셀이
예전과 **바이트까지 같은지** 해시로 대조했습니다(같음).

**주소 셋을 같이 넣었습니다** — `main.jsx` 라우트 · `web/firebase.json` rewrites(`/naeyeok/**`) ·
`prerender.py` TABS·tab_body·STATIC_NEW · `sitemap.py`. 하나라도 빠지면 soft 404 입니다.


### 66. 🔎 서치콘솔 실측 — 「길이 막혀 있었습니다」 (2026-09-19)

소장님: 「애널리틱스하고 구글서치콘솔 보고 분석해줘」

**잰 값 (28일 · 8/29~9/16)**

| | 값 |
|---|---|
| 사이트맵에 낸 것 | **1,360장** (오늘 읽어 갔고 「성공」) |
| 색인된 것 | **77장 (5.7%)** |
| 「발견됨 — 현재 색인이 생성되지 않음」 | **1,289장 (94.8%)** |
| 클릭 / 노출 / CTR / 평균순위 | 17 / 482 / 3.5% / **7.4위** |
| GA 같은 기간 | 163명 · 그중 카페 74명(45%) · 구글 검색 17명 |

**순위는 7.4위입니다 — 밀린 게 아니라 «읽히지를 않았습니다».**
새 도메인이 천 장 넘게 한 덩어리로 내밀면 구글이 «다 읽을 값어치가 있나» 하고 미룹니다.
공고 한 건·업체 한 곳짜리 자동 생성 페이지가 대부분이라 더 그렇습니다.

**노출되는 검색어 14개가 «전부 서식» 이었습니다** — 검측요청서 양식(1클릭) · 건설기계임대차
표준계약서 엑셀(7노출) · esc 물가변동(7) · 일용직 근로계약서 엑셀(2) · 과업지시서 양식 ·
엑셀 회의록 · a값 · 공공기관 회의록 양식. **바로투찰·개찰 쪽 말은 하나도 없습니다.**
우리가 힘 준 곳과 팔리는 곳이 다릅니다.

**고친 것 셋**

1. **사이트맵을 셋으로** (`sitemap.py`) — `sitemap.xml` 을 «목차(sitemapindex)» 로 바꾸고
   `sitemap-main.xml`(서식·도구·설계변경·알아보기 **215장**) ·
   `sitemap-bid.xml`(공고·개찰 546장) · `sitemap-corp.xml`(업체·기관 300장) 로 나눕니다.
   구글은 사이트맵 단위로 크롤 몫을 나누므로 **작은 쪽이 먼저 먹힙니다.**
   ⚠️ 나누는 기준은 «주소» 하나로만 봅니다(`/corp/`·`/agency/` → corp, `/notice/`·`/daily` → bid).
      모으는 코드는 안 건드립니다 — 줄마다 통을 적으면 새 갈래가 늘 때 또 빠집니다.
   ⚠️ 서치콘솔에 낸 주소(`sitemap.xml`)가 그대로라 **다시 제출하지 않아도** 자식이 따라 들어갑니다.
2. **서식 제목·본문에 «사람들이 치는 말»** (`forms.json` 의 `also` + `prerender.py` + `Forms.jsx`)
   제목을 「○○ 양식 **엑셀** 무료 내려받기」 로, 설명 앞에 다른 이름을, 본문에 「이렇게도 부릅니다」 한 줄.
   ⚠️ **국토부 «표준계약서» 같은 공식 서류 이름은 넣지 않습니다** — 우리 서식은 그것이 아닙니다.
      진짜로 같은 말만(일용근로계약서 ↔ 일용직 근로계약서, 건설기계 ↔ 중기·장비) 넣었습니다.
   ⚠️ 미리 굽는 쪽과 리액트 쪽에 **같은 줄**을 넣습니다 — 크롤러가 보는 글과 사람이 보는 글이 다르면 안 됩니다.
3. **업체 페이지 제목에 「입찰」** (`prerender.py` corp_page) — 「도원입찰」 8노출 0클릭.
   사람들은 «회사이름 + 입찰» 로 찾는데 제목이 「○○ 낙찰 실적」 이었습니다 → 「○○ **입찰·낙찰** 실적」.

**아직 못 한 것** — 색인 요청(서치콘솔 「URL 검사 → 색인 생성 요청」)은 **손으로만** 됩니다.
구글이 자동화를 막아 두었습니다. 목록만 뽑아 드리고 누르는 것은 소장님 몫입니다.
「과업지시서」처럼 **아예 없는 서식**은 새로 만들어야 합니다 — 검색 수요는 이미 있습니다.

## 9. 이미 정한 것 — 다시 제안하지 말 것

| 결정 | 이유 |
|---|---|
| **용역 제외, 공사 전용** | 3년치의 75%가 용역이었습니다 |
| **워크넷 자료는 «안 싣고 연결만»** (2026-09-14 확정) | ①채용정보 API 는 **기업회원 전용**(개인회원 키 → `개인회원은 사용할 수 없는 OPEN-API입니다`) ②공공데이터포털에 같은 API 가 있지만 **「API 유형: LINK」** 라 결국 같은 고용24 키가 필요 ③**공공누리 제4유형 — 상업적 이용금지·변경금지.** 애드센스가 붙는 사이트에 회사명·직종·급여를 옮겨 싣는 건 여기 걸립니다. robots.txt 는 오히려 열려 있으니 «robots 때문»이라고 적지 말 것. **긁어오자는 요청이 다시 와도 답은 같습니다** — 여는 조건은 둘뿐: 고용24 기업회원 전환 + 한국고용정보원(043-870-8556) 게시 허락. 그때 쓸 도구는 `tools/worknet.py`·`5_워크넷확인.bat` 로 준비돼 있습니다. 지금은 `web/src/lib/worknet.js` 가 **주소만** 만들고(전국 시·도 17개 칩 — 광주·전남은 고용24 에서 **하나(12000)**, `region` 은 «코드,코드» **짝**이라 하나만 넣으면 「null전체」로 **조용히 엉뚱해집니다**), `Jobs.jsx` 가 그 주소를 **iframe 으로 띄우거나 새 창으로** 엽니다 — 고용24 는 X-Frame-Options 로 안 막습니다(2026-09-14 실측). 화면을 «불러와 보여 주는 것»은 복제가 아니라 링크 성격이라 됩니다. **«우리 파일에 저장하고 우리 형식으로 다시 그리는 것»만 안 됩니다** |
| **적산 프로그램의 단가는 «적산자료» 하나만** (2026-09-18) | 소장님: 「**적산 프로그램 돌릴 때는 적산자료를 이용한다. 다른 자료는 이용하면 안된다. 그럼 단가가 틀어진다.**」 적산은 **발주처 설계단가**로 합니다. 우리 **단가사전은 «낙찰가»** — 업체가 깎아서 넣은 값이라 **적산에 쓰면 단가가 틀어집니다.** 사이트(바로투찰·업체 화면)에만 씁니다. **두 개를 한 자리에서 섞자는 제안을 하지 마십시오.** 그리고 한 품목에 단가가 여럿이면 **«가장 낮은 값»** 을 씁니다(소장님: 「단가가 낮은 걸로 하자」) — 계열(월별 main / 발주처 / 조달청)을 정한 «뒤에» 그 안에서 최신·최저가로 고릅니다. 자세한 것은 `적산.md` 2-1절, 더 자세한 것은 저장소 밖 `K-적산/_만든 기록…md` 0절 |
| **우리 «단가사전» 은 싣습니다** (2026-09-18 정정) | 소장님: 「**단가 넣기로 했잖아**」. 낙찰 내역서에서 우리가 뽑은 `_단가사전.csv` · `tools/단가사전.py` · `tools/단가규칙.py` 는 **우리가 만든 것**이라 싣습니다. 안 싣는 것은 **표준품셈 · 물가정보(물가자료·거래가격) · 노임단가** — 이건 남의 유료 저작물입니다. 제가 한 번 「단가표는 안 싣는다」고 뭉뚱그려 적었다가 정정했습니다. **둘을 섞어 말하지 마십시오.** |
| **`corp/dat` 다이어트 안 함** | 절반을 줄여도 10GB 한도의 1%p. 「업체를 쳤는데 없습니다」가 늘어 손해가 큽니다 |
| **비용 점검 숫자를 사이트에 안 띄움** | 건설맵을 보는 사람은 투찰하러 온 소장님들입니다. 개발자 메모를 손님에게 보이는 꼴 |
| **표준품셈·물가정보·노임단가 표는 안 싣는다** | 유료 자료입니다. 도구는 «수량과 금액 구조만» 냅니다 |
| **물품 입찰은 «새 사이트»** | 저장소 둘 · 프로젝트 둘 · 도메인 둘 · 앱 둘. **건설맵은 한 줄도 안 건드립니다.** 건설자재도 새 사이트입니다. 이유는 돈이 아니라 «사고 반경» — 되는 것을 걸고 안 해 본 것을 시험하지 않습니다. probe 가 먼저(기초금액·예가·하한율이 안 오면 «계산기»가 아니라 «목록» 사이트가 됩니다) |
| **추첨번호는 낙찰과 무관** | 실측 1,451건. 번호별 인기 1~15번 전부 6.6~6.7%, 등수와 관계 없음. 화면에 넣으면 «있는 것처럼» 읽힙니다 |
| **업체 순위 보관함은 사이트·저장소에 안 올린다** (2026-09-16) | 소장님: 「사이트는 그대로 유지하자. 그래야 업체분석해달라는 사람이 있을 수 있고, 이런 사람들이 잠재적인 고객」 · 「자료는 내 컴퓨터에 두는 걸로 하자」. 자가진단(/corp)은 **1순위 기록만** 보여 주는 그대로 둡니다 — 안 나오는 업체가 「성적표 만들어 달라」고 물어오는 것이 /report 의 존재 이유입니다. 순위 보관함은 `data/store/ranks3y/` (gitignore) — **소장님 PC 안에만**. 내역서모음과 같은 방침입니다. 채우는 법: `9_보관함채우기.bat` (한 달에 한 번). 씨앗(data/seed/first.json.gz)이 주 1회 내려오므로 조달청 호출 0번·인터넷 0입니다. 실측 2026-09-16: 개찰 11,286건·투찰 282,331줄·업체 73,058곳 — 그중 **63,204곳(86.5%)이 한 번도 1순위를 못 해** 사이트에 없습니다. 10번 이상 넣고도 못 딐 곳이 2,904곳. |
| **💰 시세 단가 사전 — «창이 굴러가야» 한다** (2026-09-16) | 소장님: 「계속해서 업데이트 되어야 해. 단가 변경될 때마다」. **단가는 시간이 지나면 틀려지는 숫자입니다.** 한 번 만들고 두면 해가 갈수록 거짓말이 됩니다. 그래서 업체 3년 창(`WINDOW_YEARS`)과 **같은 방식**으로 굴립니다. 규칙 여섯 — ① **기간 창** : 최근 12개월만 씁니다. 오래된 달은 창 밖으로 버립니다. 물가가 오르므로 2년 전 단가를 지금 단가와 같은 무게로 섞으면 안 됩니다. ② **«언제 것인가»를 늘 화면에 적습니다** — 「2025-09 ~ 2026-09 · 1,284줄」. 날짜 없는 단가는 내지 않습니다. ③ **표본이 적으면 안 보여줍니다** : 30줄 이상=중앙값+범위 · 10~29줄=「표본 적음」 딱지 달고 범위만 · 10줄 미만=**「자료 부족」으로 아예 안 냄**(바로투찰의 「값 부족 · 계산 안 함」과 같은 방침). ④ **중앙값만 내지 않습니다** — 25~75% 범위를 같이 냅니다. 범위가 있어야 스스로 판단합니다. ⑤ **묶는 규칙은 한 곳에만** (`norm_corp` 처럼). 「아스팔트콘크리트포장/아스콘 포장/A.C 포장」, 「t=5cm/T=50mm/두께5cm」가 제각각입니다. **잘못 묶으면 중앙값이 통째로 헛것이 됩니다** — 「대유건설」 아홉 곳을 한 덩어리로 묶었던 것과 같은 사고. 처음엔 **품명+규격+단위가 정확히 같은 것만** 좁게 묶습니다. ⑥ **설계 단가와 계약(낙찰) 단가를 갈라 받습니다.** 아예 다른 숫자입니다. 섞으면 분포가 뭉개집니다. 관급/사급, 운반거리도 단가를 가릅니다 — 모를 땐 「모름」으로 두고 섞지 않습니다. ⚠️ **표준품셈·물가정보 값을 싣는 것이 아닙니다.** 실제 내역서에 적혀 있던 값의 «분포»입니다. 화면에 그렇게 적습니다. ⚠️ 자료는 **이용자 동의를 받고** 「공종·규격·단위·단가」만 받습니다 — 파일은 올리지 않고, 공사명·상호·사람 이름은 브라우저에서 떼고 보냅니다. 기본은 **꺼 둡니다**. |
| **2·3순위 투찰 자료는 조달청 공개 API 에 없음** | 확인됨. 다른 경로가 필요합니다 |
| **🔥 파이어베이스는 «이미 Blaze(종량제)» 다 — 다시 묻지 말 것** (2026-09-18 확인) | 소장님: 「**이미 파이어 베이스 블레이즈야…반복해서 물어보니**」. 콘솔 «사용량 및 결제» 에 `Blaze` 로 찍혀 있고, 그 달 청구는 **0원**(「최근에 청구 가능한 Firebase 서비스를 사용하지 않았습니다」). 그러니 **Cloud Functions·확장프로그램·Storage 를 쓰자는 제안에 「요금제를 올리셔야 합니다」 를 붙이지 않습니다.** 이미 올라가 있습니다. 무료 등급(함수 월 200만 회 호출 등)이 그대로 얹히므로 우리 규모에서는 사실상 0원입니다. ⚠️ 다만 **종량제는 상한이 없습니다** — 함수를 만들 때는 `maxInstances` 를 걸고, 예산 알림을 켜 두는 것을 «한 번만» 권해 드립니다 |
| **🔒 적산은 «소장님만» 씁니다 — 이용자는 못 씁니다** (2026-09-18 다시 확인) | 소장님: 「**이용자는 쓰면 안돼게 해야 해. 나만 쓸 수 있게 해야 해. 꼭 기억하고 md에 기록해**」. 2026-09-17 에도 같은 말씀이었습니다(「적산이 올라 왔던데, 이용자 들이 사용하게 하면 안돼」 · 「이것 비용을 받아야 하는데」). **`/jeoksan/run` 과 `/jeoksan/lab` 은 `Locked` 로 잠겨 있고, 앞으로 만들 «도면 읽개» 도 처음부터 잠근 채로 올립니다.** 열어 두었다가 나중에 잠그지 마십시오. 잠근 화면은 sitemap·prerender 에 넣지 않고 어디에서도 링크하지 않습니다. ⚠️ 이 잠금은 «보안» 이 아닙니다(`lib/gate.js` 주석) — 진짜 자물쇠는 **자료**입니다. 적산자료(`.estx`)는 소장님 PC 에만 있습니다. 🚨 **「이용자도 쓰게 하면 사람이 모인다」 는 제안을 다시 하지 마십시오.** 자세한 것은 `적산.md` 0-2절 |
| **📄 내역서 모음(`web/public/naeyeok/` 258개 · 151MB)은 «그냥 둡니다»** (2026-09-18 결정) | 소장님: 「**그냥 두는 걸로 하자**」 · 「**그냥. 하던대로 유지. 알겠지?**」. 제가 「적산자료가 있으니 내역서 자료는 지워도 되지 않느냐」고 여쭀다가 받은 답입니다 — **다시 지우자는 제안을 하지 마십시오.** 실측: 쓰는 화면은 **`/change/naeyeok` 한 곳**(`web/src/pages/Change.jsx` → `getNaeyeok`/`getNaeyeokAll`). `web/public/data/naeyeok.json` **372행 중 212행**이 `local` — 즉 **절반 넘게가 «저장해 둔 파일을 바로 받기»** 입니다. 지우면 그 212개 내려받기 단추가 죽습니다. (`naeyeok-all.json` 은 5,132행 중 46행) ⚠️ 그리고 이건 **적산과 다른 이야기**입니다 — 적산 프로그램은 위 칸대로 «적산자료» 만 씁니다 |

---

## 10. 작업 환경 — 마운트 폴더 · git · 원격 제약

### 되는 것과 안 되는 것 (전부 실제로 돌려 봄)

| | |
|---|---|
| 마운트 폴더에서 `git` 자체 | **된다** |
| 마운트 폴더에서 **파일 삭제** | **안 된다** (`Operation not permitted`) — `mv` 는 된다 |
| 마운트 폴더에서 `npm install` | **안 된다**(삭제가 필요). 리눅스에서 깔아 윈도우에 넣으면 플랫폼 바이너리가 어긋난다 |
| 리눅스 VM 에서 `git push` | **안 된다** — 자격증명이 없다. 남의 토큰은 다루지 않는다 |
| `apis.data.go.kr` (조달청) | **막혀 있다** — 소장님 PC 에서만 부를 수 있다 |
| `.github/workflows/*.yml` | **원격 도구로 못 고친다**(보호 파일). 워크플로를 고쳐야 하는 설계를 피할 것 |
| **백그라운드 프로세스(`&`)** | **호출이 끝나면 죽는다.** 45초 넘는 작업은 클라우드에서 하거나 쪼갤 것 |

### git 을 돌리는 «맞는 방법»

```
git -c core.autocrlf=true -c core.fileMode=false <명령>
```

- **`core.autocrlf=true`** 를 안 주면 작업트리(CRLF)와 index(LF) 가 달라 **«파일 통째로 바뀜»** 으로 보입니다.
- **`core.fileMode=false`** 를 안 주면 마운트가 0700 이라 **«전부 수정됨»** 으로 보이고,
  커밋하면 `mode change 100644 => 100755` 가 딸려 들어갑니다.
- **`.git/index.lock`·`HEAD.lock` 은 지울 수 없지만 «옮길» 수는 있습니다** — `mv` 로 빼내세요.
  남겨 두면 **소장님 윈도우 git 까지 멈춥니다.**
- `add` 와 `commit` 은 **따로 호출**합니다. 한 호출에 몰면 lock 이 겹칩니다.
- **마운트 폴더에서 `rebase`·`merge` 는 하지 않습니다.** 한 번 git 을 통째로 멈춰 세웠습니다.
  → 받기+올리기는 바탕화면의 **`.bat` 을 더블클릭**해서 윈도우 git 으로 합니다
  (`2_받고올리기.bat` · `git pull --rebase --autostash` → `git push`, 결과는 `_푸시결과.txt`).
- `device_request_delete_permission` 으로 삭제 권한을 받으면 위 제약이 대부분 풀립니다.
  **승인 전에 커밋을 시도하면 lock 이 남아 사고가 납니다 — 순서를 지킬 것.**

### 🚨 마운트 폴더는 «커밋한 것보다 뒤로» 가 있을 수 있습니다 — 고치기 전에 `git diff` (2026-09-18)

실제로 났습니다. 오늘 성적표를 붙이려고 `git add` 한 뒤 `--numstat` 을 봤더니
`web/src/pages/Admin.jsx` 가 **9줄 더하고 25줄 지움** 이었습니다. 제가 더한 건 9줄뿐입니다.
지워지는 25줄은 **어제 커밋해 둔 「문의 지우기」 단추 전체**였습니다.
`web/database.rules.json` 도 같은 모양으로, 커밋된 «지우기 허용» 규칙이 아니라
그 **이전 내용**이 파일에 들어 있었습니다.

왜 그런가 — 마운트 폴더에서는 git 이 **파일을 지울 수 없습니다**
(`unable to unlink … Operation not permitted`). 되감기(rebase)·되돌리기(checkout)가
파일을 갈아 끼우려다 이 자리에서 멈추면, **커밋은 새것인데 파일은 옛것**으로 남습니다.
`git checkout -- <파일>` 도 같은 이유로 실패합니다.

**그래서 이렇게 합니다.**

1. 고치기 전에 그 파일이 HEAD 와 같은지 봅니다 — `git diff --numstat -- <파일>`
2. 다르면 되살립니다. `checkout` 은 막히므로 **덮어씁니다**(지우기가 아니라 쓰기라서 됩니다):
   `git show HEAD:<경로> > /tmp/x` → 줄끝을 CRLF 로 바꿔 파일에 씁니다
   (`vite.config.js` 만 LF).
3. 커밋 직전에 **반드시** `git diff --cached --numstat` 을 보고,
   **내가 더한 줄 수와 맞는지** 확인합니다. 지운 줄이 설명되지 않으면 멈추십시오.
4. `git add -A` 를 쓰지 말고 **고친 파일만 이름으로** 올립니다
   (`data/store/*.json` 이 15~22MB 라 딸려 들어갑니다).

### 파일을 고치는 법 (마운트 폴더)

- **파이썬으로 바이트를 읽어 고치고 다시 씁니다.** 줄끝(CRLF)을 반드시 보존하세요.
  붙인 뒤 세어 볼 것: `CRLF 개수 == 전체 줄 수`. 안 그러면 diff 가 «전체 변경»이 됩니다.
- ⚠️ **heredoc `<<'PY'` 안에서는 확장이 없습니다.** 파이썬 소스에 `"\r\n"` 을 그대로 쓰세요.
  `"\\r\\n"` 로 쓰면 «역슬래시+r» 문자열이 되어 **변환이 조용히 안 됩니다.**
- **긴 코드를 채팅으로 넘기지 않습니다** — 320행 부근에서 잘린 적이 있습니다.

### 클라우드에서 해야 하는 것

- 3~10분짜리 작업(`--exportonly` · `build_json` · `npm ci` · `prerender`)
- **브라우저 검증(Playwright)** — `executablePath: '/opt/pw-browsers/chromium'`
- esbuild 로 JSX 파싱 검사 (마운트 폴더의 node_modules 는 윈도우용이라 못 씁니다)
- 옮기는 법: 마운트 폴더에 `tar czf` → `device_stage_files` → 클라우드에서 풀기.
  **`$HOME`(마운트 밖)에 둔 파일은 stage 할 수 없습니다.**

### 클로드가 «직접» 푸시하는 법

1. `computer_resolve_access` → `computer_request_access` (턴마다 만료됩니다)
2. `computer_open_application("파일 탐색기")` — LG Gram Link 가 앞에 있으면 클릭이 통째로 막힙니다
3. 스크린샷 → **바탕 화면** 클릭 → `나노_건설맵 코드 등` 더블클릭 → `.bat` 더블클릭
   ⚠️ 목록은 파일이 늘면 밀립니다. **누르기 전에 zoom 으로 이름을 확인**하세요(엉뚱한 파일을 연 적 있음)
4. 결과는 `device_bash` 로 `_푸시결과.txt` 를 읽어 확인
- **문서만 고칠 때는 커밋만 하고 푸시는 모읍니다.** 푸시는 코드를 올릴 때나 대화를 마무리할 때 한 번에.

---

## 11. 검사 — `python tools/selfcheck.py`

계산 13 · 등급 32 · bidindex 칸 · 검색 색인 칸 · 순위 파일 · 내역서 · 자동 분위 · 합산 확률 ·
**3년치 순위 보관함** ·
canonical 이중 인코딩 · 개찰 시각 문구 · 성적표 칸 · 등수 주장 ·
**🏷️ 이름표↔숫자 대조**(`tools/checklabels.py` · 8절 39).
`--browser` 를 붙이면 Playwright 로 화면까지 엽니다.

**따로 돌릴 수 있는 것**

| | |
|---|---|
| `python tools/checkimports.py` | 빠진 import |
| `python tools/checklabels.py` | 이름표↔숫자 (`--self` 자가시험 · `--list` 짝 전부 보기) |
| `node web/시험/금액거르개_시험.mjs` | 금액 거르개 셈 33개 |
| `node web/시험/댓글창_시험.mjs` | 한 줄 남기기 창 셈 **45개** (하루 한 번) |

⚠️ 앞의 둘은 **깃허브 「화면 검사」 단계에서도** 돕니다 — 실패하면 **배포가 멈춥니다.**

**새 규칙을 만들면 그 규칙이 어긋났을 때 잡히는 검사를 같이 만듭니다.
그리고 «일부러 틀리게 해서 잡히는지»까지 확인합니다.** 이 절의 모든 검사가 그렇게 확인된 것입니다.

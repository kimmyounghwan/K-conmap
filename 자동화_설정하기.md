# 자동화 설정하기 — 컴퓨터를 꺼놔도 사이트가 갱신됩니다

지금까지는 소장님이 직접 `run_all.py` 를 돌려야 사이트가 최신이 됐습니다.
설정을 마치면 **GitHub 서버가 하루 세 번 대신 돌립니다.**
컴퓨터를 꺼놓든, 노트북만 켜두든, 여행 중이든 상관없습니다.

**비용은 0원입니다.** (GitHub 무료 한도 2,000분/월 중 약 350분 사용)

준비물은 **비밀번호 두 개**뿐이고, 15분이면 끝납니다.

---

## 1단계 — 새 저장소 만들기 (3분)

1. https://github.com/new 로 들어갑니다
2. **Repository name** 칸에 `k-conmap` 입력
3. **Private** 를 고릅니다 ← 중요합니다. 조달청 키가 들어가는 곳입니다
4. 아래 체크박스는 **전부 비워둡니다** (README, .gitignore, license 모두)
5. **Create repository** 를 누릅니다

만들어지면 주소가 나옵니다. 이런 모양입니다:
`https://github.com/kimmyounghwan/k-conmap.git`

---

## 2단계 — 코드 올리기 (5분)

VS Code 터미널(또는 PowerShell)에 아래를 **한 줄씩** 붙여넣습니다.

```
cd "C:\Users\OS\Desktop\나노_건설맵 코드 등\k-conmap-v2"
```

```
git remote remove origin
```
> "error: No such remote" 라고 나와도 괜찮습니다. 다음으로 넘어가세요.

```
git remote add origin https://github.com/kimmyounghwan/k-conmap.git
```
> 위 주소는 1단계에서 나온 **소장님 주소**로 바꿔주세요.

```
git add -A
```

```
git commit -m "자동화 준비 - 공사 전용, 7주 목록, 기초금액 수집"
```

```
git branch -M main
```

```
git push -u origin main
```

마지막에 GitHub 로그인 창이 뜨면 로그인하시면 됩니다.
`main -> main` 이 보이면 성공입니다.

---

## 3단계 — 비밀번호 두 개 넣기 (5분)

> 이 두 개는 **소장님이 직접** 넣으셔야 합니다.
> 비밀번호라서 제가 대신 입력하지 않습니다.

### ① 조달청 키

1. 컴퓨터에서 `k-conmap-v2\.env` 파일을 메모장으로 엽니다
2. `G2B_API_KEY=` 뒤의 긴 문자열을 **복사**합니다
3. GitHub 저장소 → **Settings** → 왼쪽 **Secrets and variables** → **Actions**
4. **New repository secret** 클릭
5. Name 칸에 정확히 `G2B_API_KEY` 입력
6. Secret 칸에 복사한 값 붙여넣기 → **Add secret**

### ② Firebase 배포 권한

1. https://console.firebase.google.com/project/k-conmap/settings/serviceaccounts/adminsdk 로 갑니다
2. **새 비공개 키 생성** → **키 생성** 을 누릅니다
3. `.json` 파일이 다운로드됩니다
4. 그 파일을 **메모장으로 열어** 내용을 **전부 복사**합니다 (`{` 부터 `}` 까지)
5. GitHub → Settings → Secrets and variables → Actions → **New repository secret**
6. Name 칸에 정확히 `FIREBASE_SERVICE_ACCOUNT` 입력
7. Secret 칸에 복사한 내용 붙여넣기 → **Add secret**
8. **다운로드한 .json 파일은 지우세요.** 컴퓨터에 남겨두면 위험합니다

---

## 4단계 — 시험 삼아 한 번 돌려보기 (5분)

1. GitHub 저장소 → 위쪽 **Actions** 탭
2. 왼쪽에서 **사이트 자동 갱신** 클릭
3. 오른쪽 **Run workflow** → 초록 **Run workflow** 버튼

5분쯤 뒤 초록 체크(✅)가 뜨면 끝입니다.
빨간 X 가 뜨면 그 줄을 눌러 나온 화면을 저에게 보여주세요.

---

## 끝났습니다

이제부터 **한국시간 오전 8시 / 오후 2시 / 오후 8시**, 하루 세 번
GitHub 서버가 알아서 조달청 자료를 받아 사이트에 올립니다.

- 컴퓨터는 꺼놓으셔도 됩니다
- 실패하면 GitHub 이 소장님 메일로 알려줍니다
- 급할 땐 **Actions → Run workflow** 로 즉시 돌릴 수 있습니다

### 컴퓨터에서 여전히 할 수 있는 것 (안 해도 됩니다)

```
python -X utf8 run_all.py --quick
```
지금 당장 갱신하고 싶을 때. 자동 갱신과 충돌하지 않습니다.

### 시각을 바꾸고 싶으면

`.github/workflows/update.yml` 의 `cron` 줄을 고칩니다.
**UTC 기준**이라 한국시간에서 9시간을 뺍니다. (한국 08:00 = UTC 23:00 전날)

---

## 3년 창은 저절로 굴러갑니다

`build_json.py` 가 집계할 때마다 **최근 3년치만** 남깁니다.
오래된 자료는 자동으로 빠지고, 새로 모은 자료가 그 자리를 채웁니다.

- 오늘 기준: 2023-08-31 ~ 2026-08-31 (185,752건)
- 자동 갱신이 하루 세 번 돌므로, 창도 매일 앞으로 밀립니다
- 기간을 바꾸고 싶으면 `build_json.py` 의 `WINDOW_YEARS` 값을 고칩니다 (기본 3)

투찰 관행은 해마다 달라집니다. 5년 전 자료가 섞이면
지금과 다른 옛날 패턴이 평균을 끌어당겨 추천값이 흐려집니다.
그래서 창을 굴리는 편이 계산이 정확합니다.

# 🏗️ K-건설맵 v2 — VS Code 시작하기

명령어를 외울 필요 없습니다. **메뉴에서 클릭만** 하면 됩니다.

---

## 1. 폴더 열기

VS Code 를 켜고

```
파일(File) → 폴더 열기(Open Folder)
→ C:\Users\OS\Desktop\나노_건설맵 코드 등\k-conmap-v2
```

또는 폴더 안의 **`K-건설맵.code-workspace`** 파일을 더블클릭하면 바로 열립니다.

처음 열면 오른쪽 아래에 *"권장 확장 프로그램을 설치하시겠습니까?"* 가 뜹니다 → **설치** 를 누르세요.

---

## 2. 딱 한 번만 — 준비 3가지

### ① 패키지 설치

```
Ctrl + Shift + P  →  "Tasks: Run Task" 입력  →  ⓪ 최초 1회 — 패키지 설치
```

pip 와 npm 을 알아서 설치합니다. 3~5분 걸립니다.

### ② API 키 넣기

폴더 안의 **`.env.example`** 파일을 복사해서 이름을 **`.env`** 로 바꾸고,
`G2B_API_KEY=` 뒤에 조달청 키를 붙여넣고 저장하세요.

> `.env` 는 `.gitignore` 에 들어 있어 GitHub 에 올라가지 않습니다.

### ③ 구인구직 보안규칙 올리기

```
Ctrl + Shift + P  →  Tasks: Run Task  →  ⑧ 구인구직 보안규칙 배포
```

Firebase 콘솔에서 **Authentication → 로그인 방법 → 익명** 을 켜두는 것도 잊지 마세요.
(안 켜면 구인구직 글쓰기가 «저장 실패» 로 뜹니다)

---

## 3. 평소 — 버튼 하나

### 가장 많이 쓸 것

```
Ctrl + Shift + B
```

이것만 누르면 **① 빠른 갱신** 이 돌아갑니다.
조달청에서 새 공고를 받아 → 빌드 → 배포까지. 2~5분.

### 나머지 작업

```
Ctrl + Shift + P  →  "Tasks: Run Task" 입력  →  목록에서 고르기
```

| 메뉴 | 언제 쓰나 | 시간 |
|---|---|---|
| ① 빠른 갱신 | **평소 매일** | 2~5분 |
| ② 전체 실행 | 새 자료를 넣었을 때 (집계까지) | 8~12분 |
| ③ 미리보기 서버 | 화면을 고치는 중일 때 | — |
| ④ 배포만 | 빌드는 됐고 올리기만 | 1~2분 |
| ⑤ inbox 자료만 반영 | 받은 파일만 정리 | 몇 초 |
| ⑥ 조달청 수집만 | 공고만 받기 | 1~3분 |
| ⑦ 집계만 | 3년치만 다시 계산 | 5~8분 |

### 화면을 고칠 때

**③ 미리보기 서버** 를 켜두면 `http://localhost:5173` 이 열립니다.
`web/src/` 안의 파일을 고치고 **저장하는 순간 브라우저가 바로 바뀝니다.**
(Streamlit 처럼 처음부터 다시 그리지 않습니다)

끝낼 때는 터미널 패널의 **휴지통 아이콘**을 누르세요.

---

## 4. 디버깅 (F5)

파이썬이 왜 멈췄는지 한 줄씩 보고 싶을 때:

```
왼쪽 사이드바 → 실행 및 디버그(▷ 벌레 모양) → 위에서 항목 고르고 F5
```

`run_all.py`, `collect.py`, `build_json.py`, `inbox.py` 가 준비돼 있습니다.
코드 줄 번호 왼쪽을 클릭하면 빨간 점(중단점)이 찍히고, 거기서 멈춥니다.

---

## 5. 어디서 뭘 고치나

| 고치고 싶은 것 | 파일 |
|---|---|
| 색·글씨·간격 | `web/src/styles.css` |
| 하단 탭 이름·순서 | `web/src/App.jsx` 맨 위 `TABS` |
| 1순위 화면 | `web/src/pages/FirstBoard.jsx` |
| 공고 화면 | `web/src/pages/LiveBoard.jsx` |
| 계산기·낙찰스코어 화면 | `web/src/pages/Calc.jsx` |
| **점수 배점·등급 기준** | `web/src/lib/engines.js` |
| 분석 화면 | `web/src/pages/Analysis.jsx` |
| 구인구직 | `web/src/pages/Jobs.jsx` |
| 직종 목록 | `web/src/pages/Jobs.jsx` 위쪽 `TRADES` |
| 면허 매칭 키워드 | `web/src/lib/fmt.js` 의 `licenseKeywords` |
| 조달청 수집 규칙 | `collect.py` |
| 집계 방식·파일 크기 | `build_json.py` 위쪽 상수들 |

---

## 6. 막힐 때

| 증상 | 해결 |
|---|---|
| **"python을 찾을 수 없습니다"** | `.vscode/tasks.json` 을 열어 `"command": "python"` 을 `"command": "py"` 로 전부 바꾸세요 |
| **"npm을 찾을 수 없습니다"** | Node.js 를 설치하고 **VS Code 를 완전히 껐다 켜세요** (PATH 는 재시작해야 잡힙니다) |
| **"firebase를 찾을 수 없습니다"** | 터미널에서 `npm install -g firebase-tools` → `firebase login` |
| 터미널 한글이 깨짐 | 이미 UTF-8 로 설정해뒀습니다. 그래도 깨지면 VS Code 재시작 |
| 배포했는데 옛 화면 | 브라우저에서 `Ctrl + Shift + R` |
| 구인구직 «저장 실패» | Firebase 콘솔에서 익명 로그인이 켜져 있는지, ⑧ 규칙 배포를 했는지 확인 |
| `collect.py` 가 0건 | `.env` 의 키 확인. 공공데이터포털은 **디코딩된** 키를 써야 합니다 |

> ⚠️ **폴더를 헷갈리지 마세요.**
> 파이썬은 **루트**에서, `npm`·`firebase` 는 **`web` 안**에서 돌아갑니다.
> 위 메뉴(Tasks)를 쓰면 폴더를 알아서 맞춰주니 그냥 메뉴를 쓰는 게 제일 안전합니다.

---

자세한 내용은 **`README.md`** 를 보세요.

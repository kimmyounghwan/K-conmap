# 적산 — 사이트 쪽 기록

> **이 글은 「사이트에서 도는 적산」만 다룹니다.**
> 캐드 리습과 PC 프로그램(K-적산)은 **저장소 밖**, 소장님 PC 의
> `바탕화면/나노_건설맵 코드 등/K-적산/_만든 기록(다음 창의 클로드에게).md` 에 있습니다.
> **이 저장소는 Public 입니다.** 리습·PC 프로그램 이야기를 여기 옮기지 마십시오.

---

## 0. 🎯 목표 — 「도면을 넣으면 원가계산서까지 통째로」 (2026-09-18, 소장님)

> 소장님: 「**도면을 넣어 주면. 원가계산서 부터 모든게 나와야해**」

    도면(DXF) → ① 수량산출서 → ② 단가산출 → ③ 일위대가 → ④ 내역서 → ⑤ 원가계산서

**중간에 사람이 엑셀을 열어 손으로 채우는 자리가 하나도 없어야 합니다.**

**들어오는 길이 둘입니다** (소장님: 「내역서에 물량 및 수량이 있다면 **검산까지 해서** 단가가 나와야」)

| 들어오는 것 | 하는 일 |
|---|---|
| **도면(DXF)** | 물량을 **뽑아서** ①→⑤ |
| **내역서(공내역서)** | 물량을 **검산하고** 단가를 채워 ④⑤ ← 입찰에서는 이쪽이 더 흔합니다 |

⚠️ **검산은 «고치는 것»이 아니라 «알리는 것»입니다.** 발주처가 준 수량을 말없이 바꾸지 마십시오.
   다르다는 사실과 근거만 남기고, 고칠지는 소장님이 정합니다.
지금은 ①~⑤가 «따로따로» 되는 것까지 확인했고, **한 줄로 꿰는 것은 아직**입니다.
끊긴 데가 두 곳(일위대가 구성 줄 · 물량↔호표 이름 짝짓기) — 자세한 것은 저장소 밖
`K-적산/_만든 기록…md` 0-0절.

⚠️ 짝짓기가 안 되는 줄은 **«못 찾았다»고 남기고 사람이 고르게 합니다.**
   비슷한 것에 말없이 붙이면 틀린 내역서가 나오고, 틀린 줄도 모릅니다.

## 0-1. 🔄 방침이 바뀌었습니다 — **도면도 브라우저에서 읽습니다** (2026-09-18, 소장님)

> 소장님: 「**도면도 브라우저에서 해도 돼. 어차피 도면은 모두에게 공개된 거니까**」

**전에는 이렇게 막아 두었습니다** (`JeoksanRun.jsx` 머리말):

    안 하는 일: 도면을 읽지 않습니다. 도면에서 찍는 것은 PC 프로그램 몫입니다.
    그래서 도면 파일은 «받지도 않습니다». 남의 설계도면을 남의 서버에 올리게 하면 안 됩니다.

🚨 **그 걱정이 애초에 어긋나 있었습니다.** 사이트 적산은 **브라우저 안에서만** 돕니다 —
   파일이 서버로 **한 조각도 올라가지 않습니다**(2줄 변환과 같은 방침). 그러니
   **도면을 브라우저에서 읽어도 남의 서버로 가는 일이 없습니다.**
   제가 «올린다» 와 «브라우저에서 연다» 를 섞어서 과하게 막아 둔 것입니다.

**그래서 이렇게 갑니다.**

| | 어디서 | 왜 |
|---|---|---|
| 도면 읽기 (DXF → 치수표) | **브라우저** | 설치가 없고, 소장님이 어디서든 바로 씁니다 |
| 치수표 → 수량산출서 | 브라우저 · PC 둘 다 | 이미 있습니다 (`lib/qto.js` ↔ `kqto.py`) |
| 단가·일위대가·내역서·원가계산서 | 아직 PC | 적산자료(.estx)가 소장님 PC 에 있습니다 |

⚠️ **그래도 파일은 서버로 보내지 않습니다.** 이건 안 바뀝니다 — 브라우저 안에서만 읽습니다.
⚠️ **큰 도면은 조심하십시오.** 실제 현장 도면이 **107MB** 였습니다(송금지구 종평·횡단면).
   브라우저에서 통째로 올리면 메모리가 터집니다 → **줄 단위로 훑으면서** 필요한 것만
   담는 방식이어야 합니다. 그리고 **얼마나 걸리는지 화면에 보여 주십시오.**
⚠️ 적산자료(`.estx`)와 적산 프로그램(리습·PC판)은 **여전히 사이트에 올리지 않습니다.**
   바뀐 것은 «도면을 브라우저에서 읽어도 되느냐» 하나뿐입니다.

## 🚨🔒 0-2. 적산은 «소장님만» 씁니다 — 이용자는 못 씁니다 (2026-09-18, 소장님 지시)

> 소장님: 「**이용자는 쓰면 안돼게 해야 해. 나만 쓸 수 있게 해야 해. 꼭 기억하고 md에 기록해**」
> (2026-09-17 에도 같은 말씀: 「적산이 올라 왔던데, **이용자 들이 사용하게 하면 안돼**」
>  「**이것 비용을 받아야 하는데**」)

**새로 만드는 «도면 읽개» 도 반드시 잠금 «안» 에 넣습니다.**
열어 두었다가 나중에 잠그는 것이 아니라, **처음부터 잠근 채로 올립니다.**

| 화면 | 잠겨 있나 |
|---|---|
| `/jeoksan` (소개) | 열림 — 소개만 합니다 |
| `/jeoksan/run` (수량산출서 만들기) | 🔒 `Locked` (소개 + 열쇠말 + 문의) |
| `/jeoksan/lab` (실험실) | 🔒 `Locked` (열쇠말만) |
| **도면 읽개 (앞으로 만들 것)** | 🔒 **`Locked` 안에 넣습니다** |

    화면 =  <Locked title="…" lead={…}> {(onLock) => <진짜화면 />} </Locked>
    열쇠  =  web/src/lib/gate.js  (열쇠말의 SHA-256 만 둡니다)

⚠️ **이 잠금은 «보안» 이 아닙니다.** 화면 코드는 누구나 내려받아 읽을 수 있어 해시도 보입니다.
   지나가던 사람을 막는 것뿐입니다. **값을 받게 되면 서버에서 막아야 합니다.**
   다만 **진짜 자물쇠가 하나 더 있습니다 — «자료»** 입니다. 적산자료(`.estx`)는 소장님
   컴퓨터에만 있고 사이트에 없습니다. 화면을 열어도 단가는 못 냅니다.
⚠️ 잠근 화면은 **sitemap 에 넣지 않고, prerender 로 굽지도 않고, 어디에서도 링크하지 않습니다.**
⚠️ **「이용자도 쓰게 하면 사람이 모인다」 는 제안을 다시 하지 마십시오.** 이미 두 번 정해졌습니다.


## 0-3. ✅ 「내역서로 들어오는 길」 은 됩니다 (2026-09-18 밤)

**공내역서(물량만 있고 단가가 빈 내역서)에 단가를 채우는 프로그램을 만들었습니다.**

    K-적산/내역서적산.py          ← 본체
    K-적산/4_단가채우기.bat        ← 공내역서를 끌어다 놓기
    K-적산/5_고른것반영.bat        ← 고르신 것을 장부에 넣기
    K-적산/내역서적산_사용설명서.md

실제 현장 공내역서(2,138줄)로 돌려 확인했습니다 —
저절로 붙음 106 · 골라 주실 것 1,414 · 못 찾음 618.
**「짝짓기」 탭이 예상 금액 큰 순이라 위 20줄만 고르시면 금액의 50.9% 가 정해집니다.**

⚠️ 여전히 **도면은 안 읽습니다.** 그건 다음 일입니다.
⚠️ 고르신 짝은 `_짝장부.json` 에 남아 **다음 공사에서는 안 여쭙습니다.**


## 0-4. ✅✅ 「도면으로 들어오는 길」 도 뚫었습니다 (2026-09-18 밤)

**DWG 를 그대로 넣으면 원가계산서까지 나옵니다.** DXF 로 저장하실 필요가 없습니다.

    K-적산/도면읽개.py     ← DWG/DXF 읽기 · 검토 · 표 찾기 · 물량 후보
    K-적산/도면적산.py     ← 도면 → 짝짓기 → 단가 → 원가계산서
    K-적산/6_도면적산.bat · 7_도면검토.bat
    K-적산/도면적산_사용설명서.md

**DWG → DXF 는 세 길을 스스로 찾습니다** — ① AutoCAD `accoreconsole.exe`(소장님 PC 에 있음)
② ODA File Converter ③ LibreDWG `dwg2dxf`. 하나도 없으면 **분명히 알립니다.**

**실측** (실제 현장 가설흙막이 DWG 1.8MB): 도면층 137 · 글자 2,713 · 치수 167 ·
찾은 표 47 · 물량 후보 58줄 · 자동으로 붙음 20줄 · 원가계산서까지 남.

⚠️ **도면에서 물량을 100% 뽑지는 못합니다.** 「공 종 | 규 격 | 수량」 같은 머리가 있는
   **재료표** 는 읽지만, 상세도의 치수만으로 물량을 내지는 못합니다.
   못 읽은 것은 **「못 찾았습니다」 로 남깁니다 — 지어내지 않습니다.**
⚠️ 자동으로 고르되 **100점 아래는 안 붙입니다.** 틀린 것을 자동으로 붙이면
   안 붙이느니만 못합니다(실측: 「PL-600x181x14」 에 「케이블(600V)」 이 붙었습니다).
   **「고른 까닭」 탭에 후보와 점수를 전부 적습니다.**


## 0-5. ✅ 도면과 공내역서를 «같이» 넣습니다 (2026-09-18 밤)

소장님: 「**도면하고 공내역서를 드래그해서 놓으면 공내역서가 채워지는 거지?**」

    K-적산/한번에.py
    K-적산/8_한번에.bat       ← 도면·공내역서·zip·폴더를 «아무거나 몇 개든» 끌어다 놓기
    K-적산/9_도면더하기.bat    ← 도면이 커서 나눠 주실 때

**공내역서 물량 읽기 → 도면에서도 물량 뽑기 → 둘을 맞대 보기(검산) → 단가 → 원가계산서**

⚠️ **zip 을 그대로 받습니다.** 안의 한글 이름은 `cp437 → cp949` 로 되돌려야 안 깨집니다.
⚠️ **맞대 보기는 «이름이 같아야» 되는 게 아닙니다** — 도면엔 「H-BEAM」, 내역서엔
   「H형강(10개월 이내)왕복 운반비포함」 이라 적힙니다. 이름으로만 찾으면 **0줄** 이 맞습니다.
   → 둘이 «같은 호표/자재» 에 붙었으면 같은 것으로 봅니다.
⚠️ 그래도 대조가 0 일 수 있습니다(상세도라 수량 칸이 없을 때). 그때는 **까닭을 적습니다** —
   「도면에서 수량까지 읽은 줄이 3줄뿐입니다」. **없는 것을 있다고 하지 않습니다.**


## 1. 무엇이 어디까지 되나 — 이 선이 값을 가릅니다

| | 하는 곳 | 값 |
|---|---|---|
| 도면에서 부재를 찍어 길이·면적 재기 | **캐드 리습 + PC** (저장소 밖) | 유료로 할 것 |
| **잰 치수 → 수량산출서 엑셀** | **사이트 `/jeoksan/run`** | **무료 · 열려 있음** |
| 도면(.dxf) → 수량 → 내역서 → 원가계산서 | **사이트 `/jeoksan/lab`** | **🔒 잠김 — 시험 중** |

사이트는 **도면을 받지 않습니다.** `.dxf` 를 올리는 칸 자체가 없습니다 —
남의 설계도면을 남의 서버에 올리게 하면 안 됩니다.
받는 것은 **재료표(.xlsx)** 와 **치수표(.csv)** 두 장뿐이고,
둘 다 **브라우저 안에서만** 다룹니다. 한 조각도 올라가지 않습니다.

---

## 2. 파일 지도

| 파일 | 하는 일 |
|---|---|
| `web/src/pages/JeoksanRun.jsx` | 화면 (`/jeoksan/run`) |
| `web/src/lib/susik.js` | **수식 엔진** — 숫자·이름·`+ - * / ^`·함수·`값("분류",키)` |
| `web/src/lib/qto.js` | 재료표 읽기 · 셈하기 · 검산 · 다섯 장 내보내기 |
| `web/src/lib/qtoxlsx.js` | 엑셀 읽기/쓰기 (fflate). **`xlsx2line.js` 와 섞지 말 것** |
| `web/public/jeoksan/` | 재료표 견본(토목·건축) · 치수표 견본 |
| `tools/시험_적산.mjs` | **사이트와 PC 가 같은 수량을 내는지** 보는 시험 |
| `web/src/pages/JeoksanLab.jsx` | 🔒 실험실 (`/jeoksan/lab`) — 도면·단가·원가계산서 |
| `web/src/lib/dxf.js` | 도면에서 «찍어 둔 쪽지»(XDATA `KQTO`) 읽기 |
| `web/src/lib/naeyeok.js` | 공내역서 읽기 · 단가 채우기 · 내역서 · **원가계산서** |
| `web/src/lib/gate.js` | 🔒 잠금 — **보안이 아닙니다** (아래 11절) |

`qtoxlsx.js` 를 따로 둔 까닭: `xlsx2line.js`(설계변경 2줄)는 **남의 엑셀을 고치는** 물건이라
서식·인쇄영역·매크로를 살리려고 XML 을 그대로 손댑니다. 여기는 반대로 **빈 종이에 새로 쓰는** 일입니다.
섞으면 둘 다 위험해집니다.

---

## 2-1. 🚨 단가는 «적산자료» 하나만 씁니다 — 그리고 그것은 «설계단가» 입니다

> 소장님 (2026-09-18, 두 번 확인): 「**적산 프로그램 돌릴 때는 적산자료를 이용한다.
> 다른 자료는 이용하면 안된다. 그럼 단가가 틀어진다.**」
> 「**적산 자료로 돌리는 거 맞지? 그리고 이 자료는 «설계단가» 라는 것도 잊지말고…알지?**」

### ✅ 실제로 그렇게 돼 있는지 «확인했습니다» (2026-09-18 밤)

    한번에.py · 도면적산.py · 내역서적산.py  →  전부 적산자료/*main.estx 에서만
    코드에 「단가사전」·「낙찰」·「내역서모음」 을 쓰는 자리가 **하나도 없습니다**

### 🔑 «설계단가» 라는 것을 프로그램이 «매번» 알립니다 — 잊지 않게

    화면        ○ 적산자료: 2026-08-main.estx
                  ↳ 이 단가는 «발주처 설계단가» 입니다 — 낙찰가(우리 단가사전)가 아닙니다
    검산 탭     🚨 이 단가는 «발주처 설계단가» 입니다 … 낙찰률을 곱해 쓰십시오
    원가계산서   🚨 이 금액은 «설계단가» 로 낸 것입니다 — 낙찰가가 아닙니다

> **설계단가로 낸 금액은 «그대로 내는 금액» 이 아닙니다.**
> 입찰에 내실 때는 소장님 낙찰률을 곱해 쓰십시오. 프로그램은 그 곱셈을 하지 않습니다 —
> 낙찰률은 소장님이 정하실 일이지 프로그램이 정할 일이 아닙니다.


### (아래는 처음 적은 글입니다)


> 소장님: 「**적산 프로그램 돌릴 때는 적산자료를 이용한다. 다른 자료는 이용하면 안된다.
> 그럼 단가가 틀어진다.**」

**적산 프로그램이 쓰는 단가는 «적산자료»(발주처·기관 설계단가) 하나뿐입니다.**
다른 자료를 섞지 마십시오 — 특히 이 둘을 헷갈리기 쉽습니다.

| | 무엇 | 어디에 씁니다 |
|---|---|---|
| **적산자료** | 발주처·기관의 **설계단가** | **적산 프로그램 전용** |
| **단가사전** (`tools/단가사전.py`) | 낙찰 내역서에서 뽑은 **낙찰가** | **사이트 전용** (바로투찰·업체 화면) |

**낙찰가는 업체가 깎아서 넣은 값입니다. 설계단가가 아닙니다.**
적산에 낙찰가를 쓰면 **단가가 틀어집니다.** 반대로 사이트에 설계단가를 실으면 안 됩니다
(유료 출처가 섞여 있습니다 — 소장님 PC 안에서 쓰는 것만 정상 사용).

**한 품목에 단가가 여럿 붙으면 → «가장 낮은 값»을 씁니다** (소장님: 「단가가 낮은 걸로 하자」).
순서는 **① 계열 정하기 → ② 그 안에서 최신 → ③ 그 안에서 최저가**. 계열을 섞어 고르면 안 됩니다.
어느 출처·어느 기준월에서 온 값인지 **칸에 남깁니다** — 그래야 「왜 이 값이냐」에 답합니다.

⚠️ 「단가사전은 싣기로 했잖아」 는 **사이트 이야기**입니다. **적산과 다른 이야기입니다.**
⚠️ 자세한 것(계열 섞지 않기·3단 쌓기·출처와 기준월 남기기)은 저장소 밖
   `K-적산/_만든 기록…md` 0절에 있습니다. **여기에 옮겨 적지 마십시오**(저장소는 Public).

## 3. 🚨 절대 규칙 — 같은 셈이 두 곳에 있습니다

```
브라우저  web/src/lib/susik.js   ←→   PC  K-적산/kq_susik.py
브라우저  web/src/lib/qto.js     ←→   PC  K-적산/kqto.py
```

**한쪽만 고치면 사이트 수량과 PC 수량이 갈라집니다. 그러면 아무도 못 믿습니다.**
고칠 일이 생기면 **두 곳을 같이** 고치고, 반드시:

```
node tools/시험_적산.mjs
```

이 시험이 보는 것 — ①정해 둔 식의 값과 **엑셀 수식 글**까지 ②막아야 하는 식
③견본 치수표로 실제 산출서 만들기 ④**파이썬을 직접 불러 맞대보기**(파이썬이 있을 때만).

---

## 4. 엑셀은 사람과 «다르게» 셉니다 — 맞춰 놓은 자리

같은 식을 파이썬·자바스크립트·엑셀이 서로 다르게 셉니다. 아래는 **전부 맞춰 둔** 것입니다.
건드리면 수량이 조용히 달라집니다.

| 식 | 맞는 값 | 왜 |
|---|---|---|
| `2^3^2` | **64** | 엑셀은 **왼쪽부터** 묶습니다. 수학 관례(오른쪽부터, 512)가 아닙니다 |
| `round(2.5)` | **3** | 엑셀 ROUND 는 **0 에서 먼 쪽**. 파이썬 기본 `round()` 는 2 를 냅니다 |
| `round(-1.5)` | **-2** | 위와 같은 까닭 |
| `ceil(-1.2)` | **-2** | ROUNDUP 은 **절대값이 커지는** 쪽 |
| `int(-1.9)` | **-1** | TRUNC — 0 쪽으로 자릅니다 |

**막아야 하는 식** (안 막으면 엑셀 칸에 복소수·NaN 이 들어가 **파일이 깨집니다**)

- `(0-1)^0.5` — 음수의 실수제곱. 파이썬은 **복소수**를 냅니다
- `sqrt(0-1)` — 파이썬은 영어로 `math domain error`, 자바스크립트는 조용히 NaN
- `0^-1` · `10^300*10^300` · `A/0`

2026-09-16 에 셋 다 뚫려 있던 것을 막았습니다. **두 엔진 모두** 한국말로 막습니다.

---

## 5. 산출근거는 «숫자» 로 적습니다

```
재료표 수량식 :  (A1+A2)/2*L
산출근거      :  (12.4+15.8)/2*20      ← 이름이 아니라 «값» 입니다
수량 칸       :  =ROUND((12.4+15.8)/2*20,3)   ← 살아 있는 엑셀 수식
```

감리가 읽는 것은 **글**입니다. 이름(`A1`)으로 적으면 그 자리에서 못 읽습니다.
그리고 검산 시트가 **적힌 글을 되읽어** 수량과 같은지 다시 셉니다 —
글과 값이 어긋나면 아무도 못 알아채기 때문입니다.

---

## 6. 검산이 잡아 주는 것

| 걸리는 것 | 왜 |
|---|---|
| 적힌 근거로 다시 센 값이 수량과 다름 | 감리가 보는 글이 틀린 것 |
| 밀리미터를 그대로 넣음 | 층고를 3000 으로 넣으면 체적이 **1000배** |
| 단위가 섞임 | 같은 칸이 어떤 줄은 0.6, 어떤 줄은 600 |
| 유난히 크거나 작은 줄 | 같은 자리 가운데값의 30배·1/30 |
| 공제가 본체보다 큼 | 빼는 것이 더하는 것보다 많을 수 없습니다 |
| 번호 겹침 · 개소 0 · 셈 못한 줄 | |
| 일람표에 없는 부호 · 재료표에 없는 부재 | |
| 태그 빠짐 | 공구·측점을 안 적으면 기성 청구 때 다시 헤맵니다 |

**✕ 는 반드시 보셔야 하는 것, △ 는 알림입니다.** 화면에서도 ✕ 를 위로 올립니다.

---

## 7. 2026-09-16 에 실제로 본 것

| 무엇을 | 얼마나 | 결과 |
|---|---|---|
| 무작위 수식을 파이썬과 대조 | **48,000개** | 값·엑셀수식·**오류 문구까지** 전부 같음 |
| 실제 산출단위 51줄 | 산출서 114줄 | 집계·태그별·검산까지 **한 칸도 안 다름** |
| 리브레오피스로 실제 계산 | 수식 283개 | **오류 0** |
| 크로미움으로 화면 열기 | 파일 2장 올리고 단추 누름 | 엑셀 받아짐 · JS 오류 0 |

---

## 8. 만들다 잡은 것

- **음수의 제곱근이 복소수를 냈습니다.** 파이썬 `(0-1)^0.5` → `1j`. 엑셀에 넣으면 깨집니다. 양쪽 다 막음
- **태그별 시트 줄 순서가 달랐습니다.** 붙인 한 덩어리 글로 줄세우면 `|` 가 한글·숫자보다 뒤라
  빈 태그 줄이 PC 와 다른 자리로 갑니다. **칸마다 견주도록** 고침
- **JS 파일에 NUL 바이트가 들어갔습니다.** `\u0000` 을 글자 escape 가 아니라 진짜 0 바이트로 쓴 것.
  git 이 binary 로 봅니다. 올리기 전에 잡음
- **`fflate` 가 node_modules 에 없었습니다.** package.json 엔 있는데 안 깔려 있어
  **로컬 빌드가 안 되는 상태**였습니다 (GitHub 배포는 `npm ci` 라 무사)

---

## 9. 아직 안 한 것 — 다음에 할 순서

1. **재료표 씨앗 만들기** — 모아 둔 내역서에서 뽑은 `_일위대가_호표.json` **8,009개**가
   「공종 하나에 무엇이 얼마나 들어가는가」라 **재료표와 같은 모양**입니다.
   지금 재료표는 손으로 만든 견본 16줄뿐입니다. 호표 이름에서 부재·규격·단위를 갈라내고
   같은 것끼리 묶는 일이 먼저입니다 (단가사전에서 한 것과 같은 성격 — `tools/단가규칙.py` 참고).
2. **수량산출서 → 내역서** — 단가를 붙이는 마지막 칸. 단가사전이 아직 표본 부족입니다
   (2026-09-16 기준 낼 수 있는 것 346묶음, 그중 범위가 살아 있는 것 130개).
3. **이용자 내역서 모으기** — 동의 받고 「공종·규격·단위·단가」만. 기본은 **꺼 둡니다**.
   파일은 안 올리고 공사명·상호·사람 이름은 브라우저에서 뗍니다. 단가사전이 쓸 만해진 뒤에.
4. 구조물(콘크리트·거푸집·철근)·부대공·토량환산(L·C)을 재료표 견본에 더하기

---

## 10. 손댈 때 주의

- **라우트를 더하면 `web/firebase.json` 에도 넣습니다.** 안 넣으면 404 상태로 나갑니다
  (CLAUDE.md 8절 16번 — 그렇게 9개가 새어 있었습니다).
  `prerender.py` 의 `TABS`·`_tabname`·`STATIC_NEW`, `sitemap.py` 도 같이 봅니다.
- `qto.js` 는 **lazy chunk** 입니다(26KB). `/jeoksan/run` 을 열 때만 받아갑니다 — 그대로 두십시오.
- **단가는 내지 않습니다.** 수량과 산출근거까지입니다.
  표준품셈·물가정보·노임단가는 유료라 싣지 않습니다. 철근 단위중량(KS D 3504)만 값표에 있습니다.


---

## 11. 🔒 실험실 `/jeoksan/lab` — 2026-09-16

소장님: 「도면을 넣으면 내역서가, 공내역서를 넣으면 단가가 채워져서.
원가계산서부터 내역서까지 모조리. **사이트에 올려주되 사용은 하지 못하게** — 내가 실험을 해야 하니까」

### 무엇을 하나

```
① 도면(.dxf) 또는 치수표(.csv)  +  재료표(.xlsx)   →  수량산출서
② 공내역서(.xlsx)               +  단가표(.csv/xlsx) →  내역서 + 원가계산서
   ①의 집계를 ②에 그대로 물릴 수도 있습니다
```

### 🔒 잠금은 «보안이 아닙니다»

`lib/gate.js` 는 열쇠말의 SHA-256 을 코드에 박아 두고 맞춰 볼 뿐입니다.
**화면 자바스크립트는 누구나 내려받아 읽을 수 있으므로 그 해시도 보입니다.**
작정하면 열쇠말을 알아낼 수 있습니다. 이 잠금이 하는 일은 «지나가던 사람이 못 들어오게» 하는 것뿐입니다.

- 어디에서도 **링크하지 않습니다**
- `sitemap.py` · `prerender.py` 에 **넣지 않았습니다** (2026-09-16 확인)
- 값을 받거나 남의 자료를 두게 되면 **이걸로는 안 됩니다** — 그때는 서버에서 막아야 합니다

### 원가계산서 — 어디서 가져온 서식인가

지어낸 것이 아닙니다. **조달청 공고에 붙어 공개된 실제 내역서(2026년)** 의
「원가계산서」 시트에서 비목 차례와 산출식을 그대로 옮겼습니다.

| 항목 | 요율 | 무엇에 곱하나 |
|---|---|---|
| 간접노무비 | 19.1% | 직접노무비 |
| 산재보험료 | 3.56% | 직접+간접노무비 |
| 고용보험료 | 1.01% | 직접+간접노무비 |
| 건강보험료 | 3.595% | 직접노무비 |
| 연금보험료 | 4.75% | 직접노무비 |
| 노인장기요양 | 13.14% | **건강보험료** |
| 퇴직금공제부금 | 0.23% | 직접노무비 |
| 산업안전보건관리비 | 2.07% | 재료비+직접노무비 |
| 건설기계 지급보증 | 0.1% | 재료비+직접노무비+직접경비 |
| 환경보전비 | 0.5% | 재료비+직접노무비+직접경비 |
| 석면분담금 | 0.006% | 노무비 |
| 임금채권부담금 | 0.09% | 노무비 |
| 기타경비 | 5.5% | 재료비+노무비 |
| 일반관리비 | 8% | 순공사원가 |
| 이윤 | 15% | 노무비+경비+일반관리비 |
| 부가가치세 | 10% | 총원가 |

⚠️ **요율은 해마다·공사 종류마다 다릅니다.** 그래서 엑셀에서 «노란 칸» 으로 두었습니다.
고치면 아래가 전부 다시 셈해집니다. **반드시 공고 서류와 맞춰 보십시오.**

⚠️ 행 번호를 «세어서» 쓰지 않습니다. 항목 하나만 늘려도 전부 어긋나기 때문에,
   항목마다 이름으로 행을 적어 두고 수식은 그 이름으로만 씁니다 (`naeyeok.js` 의 `L`).

### 아직 못 하는 것 — 화면에도 적어 두었습니다

- **도면을 스스로 읽지 않습니다.** 캐드에서 찍어 둔 쪽지만 읽습니다
- **재료비·노무비·경비를 가르지 못합니다.** 단가 한 칸으로는 가를 수 없습니다.
  일위대가를 읽어 가르는 것이 다음 일입니다 (재료표 씨앗 8,009 호표와 같은 뿌리)
- **일위대가를 만들지 못합니다.** 지금은 «단가를 가져다 붙이는» 데까지입니다
- **단가사전이 얇습니다** — 한 달치로 낼 수 있는 것이 346가지

### 만들다 잡은 것

- `Drop` 을 `Lab` **안**에 둬서 화면이 다시 그려질 때마다 `<input>` 이 새로 생겼습니다 → 밖으로 뺌
- ②에 파일을 올리면 ①의 결과가 지워졌습니다 → 그 자리 것만 지우게

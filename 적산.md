# 적산 — 사이트 쪽 기록

> **이 글은 「사이트에서 도는 적산」만 다룹니다.**
> 캐드 리습과 PC 프로그램(K-적산)은 **저장소 밖**, 소장님 PC 의
> `바탕화면/나노_건설맵 코드 등/K-적산/_만든 기록(다음 창의 클로드에게).md` 에 있습니다.
> **이 저장소는 Public 입니다.** 리습·PC 프로그램 이야기를 여기 옮기지 마십시오.

---

## 1. 무엇이 어디까지 되나 — 이 선이 값을 가릅니다

| | 하는 곳 | 값 |
|---|---|---|
| 도면에서 부재를 찍어 길이·면적 재기 | **캐드 리습 + PC** (저장소 밖) | 유료로 할 것 |
| **잰 치수 → 수량산출서 엑셀** | **사이트 `/jeoksan/run`** | **무료 · 열려 있음** |
| 도면(.dxf) → 수량 → 내역서 → 원가계산서 | **사이트 `/jeoksan/lab`** | **🔒 잠김 — 시험 중** |

사이트는 **도면을 받지 않습니다.** `.dxf` 를 올리는 칸 자체가 없습니다 —
남의 설계도면을 남의 서버에 올리게 하면 안 됩니다.
받는 것은 **재료표(.xlsx)** 와 **치수표(.csv)** 두 장뿐이고,
둘 다 **브라우저 안에서만** 다룹니다. 한 조각도 올라가지 않습니다.

---

## 2. 파일 지도

| 파일 | 하는 일 |
|---|---|
| `web/src/pages/JeoksanRun.jsx` | 화면 (`/jeoksan/run`) |
| `web/src/lib/susik.js` | **수식 엔진** — 숫자·이름·`+ - * / ^`·함수·`값("분류",키)` |
| `web/src/lib/qto.js` | 재료표 읽기 · 셈하기 · 검산 · 다섯 장 내보내기 |
| `web/src/lib/qtoxlsx.js` | 엑셀 읽기/쓰기 (fflate). **`xlsx2line.js` 와 섞지 말 것** |
| `web/public/jeoksan/` | 재료표 견본(토목·건축) · 치수표 견본 |
| `tools/시험_적산.mjs` | **사이트와 PC 가 같은 수량을 내는지** 보는 시험 |
| `web/src/pages/JeoksanLab.jsx` | 🔒 실험실 (`/jeoksan/lab`) — 도면·단가·원가계산서 |
| `web/src/lib/dxf.js` | 도면에서 «찍어 둔 쪽지»(XDATA `KQTO`) 읽기 |
| `web/src/lib/naeyeok.js` | 공내역서 읽기 · 단가 채우기 · 내역서 · **원가계산서** |
| `web/src/lib/gate.js` | 🔒 잠금 — **보안이 아닙니다** (아래 11절) |

`qtoxlsx.js` 를 따로 둔 까닭: `xlsx2line.js`(설계변경 2줄)는 **남의 엑셀을 고치는** 물건이라
서식·인쇄영역·매크로를 살리려고 XML 을 그대로 손댑니다. 여기는 반대로 **빈 종이에 새로 쓰는** 일입니다.
섞으면 둘 다 위험해집니다.

---

## 3. 🚨 절대 규칙 — 같은 셈이 두 곳에 있습니다

```
브라우저  web/src/lib/susik.js   ←→   PC  K-적산/kq_susik.py
브라우저  web/src/lib/qto.js     ←→   PC  K-적산/kqto.py
```

**한쪽만 고치면 사이트 수량과 PC 수량이 갈라집니다. 그러면 아무도 못 믿습니다.**
고칠 일이 생기면 **두 곳을 같이** 고치고, 반드시:

```
node tools/시험_적산.mjs
```

이 시험이 보는 것 — ①정해 둔 식의 값과 **엑셀 수식 글**까지 ②막아야 하는 식
③견본 치수표로 실제 산출서 만들기 ④**파이썬을 직접 불러 맞대보기**(파이썬이 있을 때만).

---

## 4. 엑셀은 사람과 «다르게» 셉니다 — 맞춰 놓은 자리

같은 식을 파이썬·자바스크립트·엑셀이 서로 다르게 셉니다. 아래는 **전부 맞춰 둔** 것입니다.
건드리면 수량이 조용히 달라집니다.

| 식 | 맞는 값 | 왜 |
|---|---|---|
| `2^3^2` | **64** | 엑셀은 **왼쪽부터** 묶습니다. 수학 관례(오른쪽부터, 512)가 아닙니다 |
| `round(2.5)` | **3** | 엑셀 ROUND 는 **0 에서 먼 쪽**. 파이썬 기본 `round()` 는 2 를 냅니다 |
| `round(-1.5)` | **-2** | 위와 같은 까닭 |
| `ceil(-1.2)` | **-2** | ROUNDUP 은 **절대값이 커지는** 쪽 |
| `int(-1.9)` | **-1** | TRUNC — 0 쪽으로 자릅니다 |

**막아야 하는 식** (안 막으면 엑셀 칸에 복소수·NaN 이 들어가 **파일이 깨집니다**)

- `(0-1)^0.5` — 음수의 실수제곱. 파이썬은 **복소수**를 냅니다
- `sqrt(0-1)` — 파이썬은 영어로 `math domain error`, 자바스크립트는 조용히 NaN
- `0^-1` · `10^300*10^300` · `A/0`

2026-09-16 에 셋 다 뚫려 있던 것을 막았습니다. **두 엔진 모두** 한국말로 막습니다.

---

## 5. 산출근거는 «숫자» 로 적습니다

```
재료표 수량식 :  (A1+A2)/2*L
산출근거      :  (12.4+15.8)/2*20      ← 이름이 아니라 «값» 입니다
수량 칸       :  =ROUND((12.4+15.8)/2*20,3)   ← 살아 있는 엑셀 수식
```

감리가 읽는 것은 **글**입니다. 이름(`A1`)으로 적으면 그 자리에서 못 읽습니다.
그리고 검산 시트가 **적힌 글을 되읽어** 수량과 같은지 다시 셉니다 —
글과 값이 어긋나면 아무도 못 알아채기 때문입니다.

---

## 6. 검산이 잡아 주는 것

| 걸리는 것 | 왜 |
|---|---|
| 적힌 근거로 다시 센 값이 수량과 다름 | 감리가 보는 글이 틀린 것 |
| 밀리미터를 그대로 넣음 | 층고를 3000 으로 넣으면 체적이 **1000배** |
| 단위가 섞임 | 같은 칸이 어떤 줄은 0.6, 어떤 줄은 600 |
| 유난히 크거나 작은 줄 | 같은 자리 가운데값의 30배·1/30 |
| 공제가 본체보다 큼 | 빼는 것이 더하는 것보다 많을 수 없습니다 |
| 번호 겹침 · 개소 0 · 셈 못한 줄 | |
| 일람표에 없는 부호 · 재료표에 없는 부재 | |
| 태그 빠짐 | 공구·측점을 안 적으면 기성 청구 때 다시 헤맵니다 |

**✕ 는 반드시 보셔야 하는 것, △ 는 알림입니다.** 화면에서도 ✕ 를 위로 올립니다.

---

## 7. 2026-09-16 에 실제로 본 것

| 무엇을 | 얼마나 | 결과 |
|---|---|---|
| 무작위 수식을 파이썬과 대조 | **48,000개** | 값·엑셀수식·**오류 문구까지** 전부 같음 |
| 실제 산출단위 51줄 | 산출서 114줄 | 집계·태그별·검산까지 **한 칸도 안 다름** |
| 리브레오피스로 실제 계산 | 수식 283개 | **오류 0** |
| 크로미움으로 화면 열기 | 파일 2장 올리고 단추 누름 | 엑셀 받아짐 · JS 오류 0 |

---

## 8. 만들다 잡은 것

- **음수의 제곱근이 복소수를 냈습니다.** 파이썬 `(0-1)^0.5` → `1j`. 엑셀에 넣으면 깨집니다. 양쪽 다 막음
- **태그별 시트 줄 순서가 달랐습니다.** 붙인 한 덩어리 글로 줄세우면 `|` 가 한글·숫자보다 뒤라
  빈 태그 줄이 PC 와 다른 자리로 갑니다. **칸마다 견주도록** 고침
- **JS 파일에 NUL 바이트가 들어갔습니다.** `\u0000` 을 글자 escape 가 아니라 진짜 0 바이트로 쓴 것.
  git 이 binary 로 봅니다. 올리기 전에 잡음
- **`fflate` 가 node_modules 에 없었습니다.** package.json 엔 있는데 안 깔려 있어
  **로컬 빌드가 안 되는 상태**였습니다 (GitHub 배포는 `npm ci` 라 무사)

---

## 9. 아직 안 한 것 — 다음에 할 순서

1. **재료표 씨앗 만들기** — 모아 둔 내역서에서 뽑은 `_일위대가_호표.json` **8,009개**가
   「공종 하나에 무엇이 얼마나 들어가는가」라 **재료표와 같은 모양**입니다.
   지금 재료표는 손으로 만든 견본 16줄뿐입니다. 호표 이름에서 부재·규격·단위를 갈라내고
   같은 것끼리 묶는 일이 먼저입니다 (단가사전에서 한 것과 같은 성격 — `tools/단가규칙.py` 참고).
2. **수량산출서 → 내역서** — 단가를 붙이는 마지막 칸. 단가사전이 아직 표본 부족입니다
   (2026-09-16 기준 낼 수 있는 것 346묶음, 그중 범위가 살아 있는 것 130개).
3. **이용자 내역서 모으기** — 동의 받고 「공종·규격·단위·단가」만. 기본은 **꺼 둡니다**.
   파일은 안 올리고 공사명·상호·사람 이름은 브라우저에서 뗍니다. 단가사전이 쓸 만해진 뒤에.
4. 구조물(콘크리트·거푸집·철근)·부대공·토량환산(L·C)을 재료표 견본에 더하기

---

## 10. 손댈 때 주의

- **라우트를 더하면 `web/firebase.json` 에도 넣습니다.** 안 넣으면 404 상태로 나갑니다
  (CLAUDE.md 8절 16번 — 그렇게 9개가 새어 있었습니다).
  `prerender.py` 의 `TABS`·`_tabname`·`STATIC_NEW`, `sitemap.py` 도 같이 봅니다.
- `qto.js` 는 **lazy chunk** 입니다(26KB). `/jeoksan/run` 을 열 때만 받아갑니다 — 그대로 두십시오.
- **단가는 내지 않습니다.** 수량과 산출근거까지입니다.
  표준품셈·물가정보·노임단가는 유료라 싣지 않습니다. 철근 단위중량(KS D 3504)만 값표에 있습니다.


---

## 11. 🔒 실험실 `/jeoksan/lab` — 2026-09-16

소장님: 「도면을 넣으면 내역서가, 공내역서를 넣으면 단가가 채워져서.
원가계산서부터 내역서까지 모조리. **사이트에 올려주되 사용은 하지 못하게** — 내가 실험을 해야 하니까」

### 무엇을 하나

```
① 도면(.dxf) 또는 치수표(.csv)  +  재료표(.xlsx)   →  수량산출서
② 공내역서(.xlsx)               +  단가표(.csv/xlsx) →  내역서 + 원가계산서
   ①의 집계를 ②에 그대로 물릴 수도 있습니다
```

### 🔒 잠금은 «보안이 아닙니다»

`lib/gate.js` 는 열쇠말의 SHA-256 을 코드에 박아 두고 맞춰 볼 뿐입니다.
**화면 자바스크립트는 누구나 내려받아 읽을 수 있으므로 그 해시도 보입니다.**
작정하면 열쇠말을 알아낼 수 있습니다. 이 잠금이 하는 일은 «지나가던 사람이 못 들어오게» 하는 것뿐입니다.

- 어디에서도 **링크하지 않습니다**
- `sitemap.py` · `prerender.py` 에 **넣지 않았습니다** (2026-09-16 확인)
- 값을 받거나 남의 자료를 두게 되면 **이걸로는 안 됩니다** — 그때는 서버에서 막아야 합니다

### 원가계산서 — 어디서 가져온 서식인가

지어낸 것이 아닙니다. **조달청 공고에 붙어 공개된 실제 내역서(2026년)** 의
「원가계산서」 시트에서 비목 차례와 산출식을 그대로 옮겼습니다.

| 항목 | 요율 | 무엇에 곱하나 |
|---|---|---|
| 간접노무비 | 19.1% | 직접노무비 |
| 산재보험료 | 3.56% | 직접+간접노무비 |
| 고용보험료 | 1.01% | 직접+간접노무비 |
| 건강보험료 | 3.595% | 직접노무비 |
| 연금보험료 | 4.75% | 직접노무비 |
| 노인장기요양 | 13.14% | **건강보험료** |
| 퇴직금공제부금 | 0.23% | 직접노무비 |
| 산업안전보건관리비 | 2.07% | 재료비+직접노무비 |
| 건설기계 지급보증 | 0.1% | 재료비+직접노무비+직접경비 |
| 환경보전비 | 0.5% | 재료비+직접노무비+직접경비 |
| 석면분담금 | 0.006% | 노무비 |
| 임금채권부담금 | 0.09% | 노무비 |
| 기타경비 | 5.5% | 재료비+노무비 |
| 일반관리비 | 8% | 순공사원가 |
| 이윤 | 15% | 노무비+경비+일반관리비 |
| 부가가치세 | 10% | 총원가 |

⚠️ **요율은 해마다·공사 종류마다 다릅니다.** 그래서 엑셀에서 «노란 칸» 으로 두었습니다.
고치면 아래가 전부 다시 셈해집니다. **반드시 공고 서류와 맞춰 보십시오.**

⚠️ 행 번호를 «세어서» 쓰지 않습니다. 항목 하나만 늘려도 전부 어긋나기 때문에,
   항목마다 이름으로 행을 적어 두고 수식은 그 이름으로만 씁니다 (`naeyeok.js` 의 `L`).

### 아직 못 하는 것 — 화면에도 적어 두었습니다

- **도면을 스스로 읽지 않습니다.** 캐드에서 찍어 둔 쪽지만 읽습니다
- **재료비·노무비·경비를 가르지 못합니다.** 단가 한 칸으로는 가를 수 없습니다.
  일위대가를 읽어 가르는 것이 다음 일입니다 (재료표 씨앗 8,009 호표와 같은 뿌리)
- **일위대가를 만들지 못합니다.** 지금은 «단가를 가져다 붙이는» 데까지입니다
- **단가사전이 얇습니다** — 한 달치로 낼 수 있는 것이 346가지

### 만들다 잡은 것

- `Drop` 을 `Lab` **안**에 둬서 화면이 다시 그려질 때마다 `<input>` 이 새로 생겼습니다 → 밖으로 뺌
- ②에 파일을 올리면 ①의 결과가 지워졌습니다 → 그 자리 것만 지우게
